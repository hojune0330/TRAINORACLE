# Native Concurrency Execution: 2026-09-08

Status: VERIFIED_LOCAL_SYNTHETIC_ONLY. No production application or release.

Base branch: `codex/account-canonical-storage-foundation`.
Base HEAD: `2509a173e564b7924d07903d138ff3603c150956`.
Other workers' concurrent runtime/app/config edits were preserved, not owned here.
No commit, push, package download, installed service change or user-file deletion.

## Runtime And Actual Execution

PATH did not contain PostgreSQL or Docker. Read-only discovery found installed
`C:\Program Files\PostgreSQL\17\bin\{postgres,psql,initdb,pg_ctl}.exe`.
`postgres --version` and `initdb --version` reported PostgreSQL 17.6.
Native tests used a new synthetic cluster, not the installed service/data directory.

From the repository root, this command finished exit 0:

```powershell
node supabase/tests/concurrency/account-plan-concurrency.test.mjs --run --pg-bin "C:\Program Files\PostgreSQL\17\bin"
```

- PostgreSQL 17.6, x86_64-windows, msvc-19.44.35213, 64-bit.
- Generated endpoint: `127.0.0.1:59525/trainoracle_concurrency_test_1de01b16a9a17557`.
- Observer PID: 30020. Independent writer PIDs: 6244 and 29828.
- Both writer PIDs were observed waiting on observer 30020's advisory lock for the
  initial CAS, existing-row CAS, and identical-operation races.
- Writer 29828 was observed waiting on writer 6244 in both legacy ordering tests.
- Tests 5, pass 5, fail 0, skipped 0; reported duration 9920.3025 ms.
- The account-B write finished before account A committed its open transaction.
- `pg_ctl stop -m fast -w` succeeded; the harness printed STOPPED.

Coverage: one CAS winner/no losing receipt/retained parts; identical-operation exact
receipt without duplicate revision; changed-operation rejection and late replay;
legacy-first source-CAS invalidation; collection-first legacy rejection with source
preservation; account-scoped locks, reads, attestation and operation UUIDs.

## Guard And Existing Suite

`node --test supabase/tests/concurrency/local-postgres-guard.test.mjs`: exit 0,
3/3 pass. `node supabase/tests/concurrency/account-plan-concurrency.test.mjs`
without opt-in: 0 passed, 5 explicitly skipped, no database provisioned.

`npm test` in `supabase/tests/local-postgres`: exit 0, tests 152, pass 152,
fail 0, skipped 0, duration 13451.4867 ms. The command now includes
`../account-plan-collection-handler.test.mjs` and
`../concurrency/local-postgres-guard.test.mjs`. The collection handler includes
the coordinating worker's gateway-configuration regression test.
This count includes PGlite/handler/unit coverage; it is NOT 152 native races.

## Named Defect Detection

All five README mutation commands were actually executed against separate native
clusters. Each finished exit 1 with exactly its selected test failing and four
unselected cases skipped. These are expected detector failures, not product passes.

| Selected case | In-memory SQL defect | Observed failure |
| --- | --- | --- |
| cas | owner-lock | Backend 28580 did not wait on observer 27184 advisory lock |
| idempotency | replay | Concurrent results differ: committed receipt versus conflict |
| legacy-first | owner-lock | Backend 21016 did not wait on writer 1140 advisory lock |
| collection-first | owner-lock | Backend 29468 did not wait on writer 25488 advisory lock |
| isolation | owner-read | A can read B's receipt where null was required |

Each mutation run printed STOPPED. Migration source files were never edited.

## Setup Failures And Retained Files

Initial harness bring-up attempts were failures, not successful database tests:
Windows restricted-token error 87 in sandbox; missing minimal Windows subprocess
environment; Windows single-quote handling for the empty socket-directory option;
an inherited control-process pipe delay; strict identity assertion comparing inet
CIDR text with host text. The harness fixes are confined to the new test files.
Tool-approved execution was used for native provisioning; no global permission
changes were made. One isolation mutation attempt hit Windows TCP bind permission
denied before tests; a new generated cluster rerun produced the named defect failure.

All task-created directories remain under
`C:\Users\admin\AppData\Local\Temp\trainoracle-pg-concurrency-<suffix>`:

`eTThU9`, `VldRuB`, `d6OKjx`, `RZhi3W`, `Vzppru`, `4H6db2`, `aLjSVp`,
`jiE5V7`, `tp3SpE`, `BsRbyT`, `VvY1wt`, `RQ9xXE`.

Read-only final inventory found no `data/postmaster.pid` in any of these 12
directories. All successfully started clusters were stopped, including failure
paths. No background test process is intentionally left running. Files were not
deleted. Normal success evidence is in the `4H6db2` cluster; mutation clusters
contain intentionally broken SQL and must never be reused for product evidence.

## Remaining Boundaries

Real Supabase JWTs/accounts, PostgREST, separate devices, production DB, recovery
keys/backups, deployment and public activation were NOT executed or verified here.
Native connection concurrency is now executed, not blocked on runtime availability.
Local trust auth is appropriate only for these short-lived synthetic clusters;
it is not a production-authentication configuration.
