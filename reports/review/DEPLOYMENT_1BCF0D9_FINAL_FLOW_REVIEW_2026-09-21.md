# DEPLOYMENT_1BCF0D9_FINAL_FLOW_REVIEW_2026-09-21.md

## 판정

REQUEST_CHANGES_FOR_FLOW_COMPLETION. 실제 배포는 확인했다. 전체 서비스 중단이나 데이터 손실을 주장하지 않지만, 오라클에서 기록을 남기고 원래 결과로 돌아오는 핵심 왕복은 완료로 판정할 수 없다.

이번 작업은 검토 및 재현 검사만 수행했다. 제품 코드, 운영 기록, 계정, 설정을 수정하지 않았고 재배포하지 않았다. 기존 별도 worktree의 미커밋 계획 화면 간소화와 이전 36인 페르소나 보고서를 보존했다.

## 정확한 버전

- main/source: `1bcf0d914b5538b267ecf96d78668713ca5719d4`.
- implementation: `ceda8f2b181996db44951347b898b0961a4b81ae`.
- gh-pages: `72c74f4a5b74a682a94cd0c8ace76411aae9decb`.
- 공개 배포 영수증: https://hojune0330.github.io/TRAINORACLE/trainoracle-deploy-receipt.json
- 영수증과 GitHub API를 현재 턴에 읽었다. 공개 앱에서 로드된 주 번들은 `assets/index-CDC0QQL3.js`, gh-pages index.html에도 동일 이름이 있다. 전체 자산 바이트 해시 대조를 수행했다는 뜻은 아니다.
- 영수증의 배포 시각: `2026-09-20T23:28:08.132Z`, 한국 시간 9월 21일 08:28.
- 수동 배포이며 영수증의 CI 상태는 `not-run-owner-requested-deploy-only`. CI 통과 배포라고 표시하지 않는다.
- 1bcf0d9 자체는 변경 파일 없는 release marker이며 실제 기능 변경은 직전 두 커밋이다. 5cc89a7 대비 변경은 48개 파일이며 오라클 탐색/개인 결과/재방문 기능 중심이다.
- `git diff --quiet 5cc89a7 1bcf0d9 -- app/src/screens/log-entry app/src/domain/intensity-assessment.ts` 결과 0. 지난 리뷰의 기록 입력 코드와 동일하다. 운동별 분기/저장 전 글 추가 기능이 이번 배포에 들어갔다고 간주하지 않는다.

## F1. P1: 정상 계정 저장 메시지가 검토 경고로 처리되어 결과 복귀를 막음

근거:

- `app/src/screens/log-entry/PostSessionForm.tsx:223`: 정상 ACCOUNT 저장도 `일지를 계정에 저장했어요.` 문자열을 만든다.
- `PostSessionForm.tsx:229`: 안전 검토 메시지와 일반 저장 메시지를 하나의 세 번째 인자로 합친다.
- `app/src/AppShell.tsx:271`: 이 인자가 undefined일 때만 원래 오라클 결과를 연다.

재현: synthetic LogEntry 저장 콜백으로 같은 entry를 전달했다. 메시지 undefined인 대조군은 compare 결과로 복귀했다. 일반 계정 저장 문구만 전달한 경우 홈에 남아 기대 heading을 찾지 못했다. 운영 계정에 실제 기록을 쓰지는 않았다.

영향: 정상 저장에 성공한 회원도 분석을 보려고 입력했는데 결과로 돌아오지 못한다. 테스트 DOM에는 일반 성공 문구가 `분석 결과를 확인해야 해요.` 뒤에 붙어 검토 안내로 표시됐다. 새 복귀 기능의 결함이며 저장 자체 실패/데이터 손실의 증거는 아니다.

권장: 문자열 존재 여부를 위험 판정으로 쓰지 말고 storage receipt와 review-required 상태를 분리한다. 일반 ACCOUNT 성공은 원래 주제 복귀를 허용하고, 실제 통증/검토 안내는 기존 우선 노출을 유지한다. 메시지 전체를 무시하는 수정은 금지한다.

## F2. P2: 뒤로가기의 주제는 복원하지만 내 기록/예시 선택은 복원하지 않음

근거: `app/src/screens/OracleExplore.tsx:29`, `:64`의 선택은 화면 local state에만 있고, `app/src/AppShell.tsx:698`의 topic key 변경으로 화면이 재생성된다. 사용자가 고른 mode가 history marker에 반영되지 않는다.

공개 앱 재현:

1. 홈에서 훈련 비교를 연다.
2. `내 기록`을 누른다.
3. `이어서 살펴보기`로 훈련 후 변화에 간다.
4. 브라우저 뒤로가기를 누른다.
5. 훈련 비교는 복원되지만 `예시`가 선택된다.

동일 경로를 임시 컴포넌트 검사에서도 재현했다. aria-pressed 기대 true, 실제 false로 실패했다.

권장: 주제, 결과 종류, 필요한 스크롤 맥락을 같은 탐색 상태에 둔다. 계정 전환 시 타 계정 상태를 복원하지 않는 기존 조건은 보존한다.

## F3. P2: 오라클에서 입력을 취소하면 원래 결과 대신 일지 선택으로 감

근거: `app/src/AppShell.tsx:619` 이후 LogEntry onBack은 일반 상세 기록을 entryType=choose로 되돌린다. oracleInputRef의 진입 원점을 이 취소 경로에 사용하지 않는다.

공개 앱에서 `훈련 비교 -> 내 기록 -> 훈련 기록 남기기 -> 뒤로`를 실제 클릭했다. `새 일지 / 어떤 일지를 쓰세요?`가 표시됐다. 숫자/메모 입력과 저장은 하지 않았다.

권장: 오라클에서 시작한 입력의 취소는 초안 보호 확인 후 원래 주제/모드로 복귀한다. 일반 일지 작성의 뒤로가기는 기존 종류 선택 경로를 유지한다. 입력 취소와 다른 종류 선택을 같은 동작으로 섞지 않는다.

## F4. P2: 예시가 약속하는 분석과 실제 개인 결과의 단위가 다름

- `app/src/domain/oracle-exploration.ts:72` 이후 훈련 비교 예시는 같은 5km 두 세션의 시간 비교다.
- `app/src/domain/oracle-personal-result.ts:316` 이후 개인 훈련 비교는 월별 한 번 훈련 거리의 중앙값이다. 같은 거리 세션 쌍을 고르는 기능은 아니다.
- 강점/보완점의 예시는 km 구간 페이스이고 실제 개인 결과는 계획에 연결된 RPE 비교다. 구현 보고서도 이 차이를 명시한다.

판정: 개인 결과의 숫자를 위조한 버그는 아니다. 그러나 `내 기록으로 확인하기`는 방금 본 예시와 같은 분석을 제공할 것이라는 기대를 만든다. 설명을 접어 놓은 현재 화면에서는 차이가 더 늦게 드러난다.

권장: 현재 가능한 결과에 맞춘 예시를 먼저 보여주거나, 진입 버튼 옆에 `현재 제공: 월별 거리 비교`처럼 짧게 범위를 명시한다. 세션 비교/구간 분석이 실제 구현되면 그때 예시와 연결한다. 기능을 과장하려고 데이터가 없는 구간 페이스를 만들어서는 안 된다.

## 이전 기록 흐름 리뷰의 유지/변경

유지되는 개선 필요:

- `없어요`/휴식 답변이 즉시 저장을 시작하고 글 추가는 저장 후에 있다.
- objective editor의 종류 변경이 아직 반영하지 않은 필드를 비운다.
- 분석용 엄격한 구성 schema를 모든 운동 기록의 형식으로 사용하면 kg/세트별 값/점프 단위/불균등 반복이 빠진다.
- 같은 세션의 운동 추가와 별도 오전/오후 기록은 구분해야 한다.

추가로 확인한 연결 문제:

- 오라클의 `훈련 기록 남기기`는 긴 post-session 폼으로 직접 간다 (`AppShell.tsx`의 `viewForTab(action, action === "log" ? "post-session" : undefined)`). 공개 화면에서도 강도 시스템 7종, 거리/시간/페이스, RPE, 예상 RPE, 객관 구성, 메모가 한꺼번에 나온다.
- 따라서 새 기록 입력을 홈에서만 간소화하면 안 된다. 홈/계획/오라클/지난 일지 수정이 같은 초안과 같은 확장 입력을 쓰되 진입 원점만 달리 보존해야 한다.
- 단순 완료 체크만 하고 돌아온 경우 분석 숫자가 생겼다고 약속하지 않는다. 오라클이 요구하는 구조화 근거를 적지 않았다면 그 한계만 짧게 보여준다.

## 실제 검사와 한계

기존 집중 검사:

- AppShell.oracle-exploration.contract.test.tsx
- domain/oracle-personal-result.contract.test.ts
- domain/oracle-return-state.test.ts
- 위 3개 파일 총 33개 통과.

임시 결함 재현 검사:

- 정상 메시지 없는 저장 후 복귀: 통과.
- 정상 계정 저장 문구 있는 저장 후 복귀: 실패, F1 재현.
- 내 기록 -> 다음 주제 -> 뒤로의 mode 복원: 실패, F2 재현.
- 합계 4파일 36개 중 34통과/2실패. 실패를 테스트 환경 문제로 제외하지 않았다.
- 검사 소스는 [재현 fixture](fixtures/OracleFinalReview.repro.test.tsx)에 보존했다. 실행 위치는 app/src/OracleFinalReview.repro.test.tsx였고, 실행 후 app/src에서 제거해 제품 변경과 분리했다. fixture 상단의 복원 위치를 따라 집중 실행할 수 있다.

공개 화면: 배포 영수증, 주 번들명, 375x667 오라클 비교 화면, 내 기록/예시 전환, 주제 이동/뒤로, 입력 진입/취소를 확인했다. 사용자 기록은 작성/수정/삭제하지 않았다. 운영 계정 저장, 전체 320px/200%/스크린리더, 모든 실제 기기, 전체 E2E/CI를 검증했다고 주장하지 않는다.

## 최종 구현 순서

1. F1 정상 저장/안전 검토 결과 분리. 로그인/게스트 성공, 대기, 충돌, 통증 경고 각각 복귀를 검사.
2. F2/F3 주제/모드/입력 원점 복원. 기존 계정 격리와 초안 이탈 보호 유지.
3. F4 예시와 실제 결과의 범위를 맞추고 사용자 문구 정리.
4. 이전 승인 방향의 작은 기록 요약 + 저장 전 운동/글 추가를 공통 진입에 연결.
5. 복합 운동 실제 기록 모델과 분석 적격성 분리, 서버/내보내기 호환성을 함께 검증.

## 보조 독립 검토

Luna max 1명에게 새 개인 결과/재방문 저장소의 근거·계정 경계만 별도 검토 요청했다. 제한된 대기 및 마무리 요청 후에도 최종 회신을 받지 못해 종료했다. 이 작업을 독립 검수 완료나 승인으로 계산하지 않는다. 위 판정은 주 작업자가 직접 수행한 배포 대조·공개 화면·코드 검토·재현 검사에만 근거한다. 하위 작업자의 미완성 추론이나 미확인 주장은 보고서에 포함하지 않았다.

[DRAFT_COMPLETE]
