# 세션 기록·오라클 최종 리뷰 후속 구현 보고

## 범위와 기준

- 기준 소스: `1bcf0d914b5538b267ecf96d78668713ca5719d4`.
- 승인: 미완료 작업과 권장 수정·개선 진행. 기존 다른 작업트리의 미커밋 변경은 포함하지 않는다.
- `DEPLOYMENT_1BCF0D9_FINAL_FLOW_REVIEW_2026-09-21.md`의 F1~F4와 세션 기록 페르소나 검토의 저장 전 추가·복합 운동 기록 요구를 구현했다.
- 새 계약: [SESSION_RECORDING_COMPOSER_CONTRACT.md](../../specs/active/SESSION_RECORDING_COMPOSER_CONTRACT.md). 문서 정본 승격이나 전체 제품 완료를 의미하지 않는다.

## 사용자가 겪는 변화

1. 운동 결과 → 시간대 → 체감강도·몸 상태 → 마지막 확인 → 저장. 마지막 답변만 누른 상태에서는 저장하지 않는다.
2. 간단히 끝내려면 마지막 확인에서 `이대로 저장`. 운동을 둘 이상 했다면 `운동 추가·수정`, 글을 남기려면 `글 추가`.
3. 달리기·반복 달리기·근력·점프·대체 운동·기타를 같은 일지에 보존한다. 종류만 남겨도 된다.
4. 필요할 때만 거리·초·반복·세트·반복 회복·세트 회복·중량·접지 수·좌우를 적는다. 빈 수치를 자동 채우지 않는다.
5. 운동과 글을 직접 오가며 작성할 수 있다. 작성 중 종류 변경, 복제, 순서 이동, 삭제 취소를 지원한다.
6. 저장 후 자세히 쓰기는 같은 일지 ID와 계획 연결을 유지한다. 오전·오후 별도 훈련은 별도 일지로 남기는 기존 방식을 유지한다.
7. 오라클에서 시작한 기록은 원래 주제와 보기 맥락으로 복귀한다. 정상 계정 저장 메시지를 통증 경고로 오해하던 흐름을 분리했다.
8. 예시 오라클은 실제 구현된 RPE·거리 중앙값·명시적 에너지 기록 범위에 맞췄다. 구현하지 않은 페이스 비교를 예시로 약속하지 않는다.

## 권한·데이터 경계

- `exerciseLog`는 사용자가 적은 수행 내용이다. 저장·본인 조회·백업은 지원하되 분석 적격성이나 훈련 처방 근거를 자동 부여하지 않는다.
- 거리·시간 이중 합산, 중량의 %1RM 환산, 운동 이름만으로 에너지 시스템 확정은 하지 않는다.
- 반복/세트의 마지막 회복 포함 여부를 받지 않으므로 총 소요 시간을 자동 계산하지 않는다.
- 메모의 기존 용도 구분·암호화·계정 격리·외부 모델 전송 금지는 유지했다. 로그에 메모 원문을 추가하지 않았다.
- 신형 클라이언트는 `supportsExerciseLogV1: true`를 보낸다. 새 구성 필드가 있는 일지를 구버전이 읽거나 덮어쓰려 하면 426으로 중단한다. 조용히 필드를 삭제하거나 기록을 목록에서 숨기지 않는다.
- 운영 DB 마이그레이션, 키 변경, 다른 계정의 기록 수정은 수행하지 않았다.

## 검수 범위별 판정

| 관점 | 판정 | 실제 범위 / 남은 한계 |
|---|---|---|
| 저장 전 추가 선택 | 구현·실행 확인 | 완료/휴식/건너뜀도 명시적 최종 저장. 조기 저장 없음 |
| 여러 운동 | 구현·실행 확인 | 반복 달리기 + 근력을 한 일지에 저장, 상세 편집 후 동일 ID·구조 보존 |
| 선택·글 오가기 | 구현·실행 확인 | 직접 이동하며 미반영 거리·회복 값 보존. 커서 위치 복원은 별도 미구현 |
| 구성 편집 | 구현·단위 확인 | 종류별 입력 보존, 수정, 복제 새 ID, 정렬, 삭제 취소, 한도에서 편집 지속 |
| 미기록 의미 | 구현·단위 확인 | 빈 값, 명시적 0초, 회복 없음 구분. 부적합 숫자 반영 차단 |
| 모바일 | 실행 확인 | 320px 복합 기록, 375px 입력, 편집기 200% 글자 확대, 줄인 모션. 실제 iOS 기기 시험은 아님 |
| 접근성 | 부분 | 의미 있는 라벨·버튼과 줄인 모션 검사. 전 화면 키보드·실제 IME 전수 검증은 미완료 |
| 계정 저장 | 서버 배포·로컬 통합 확인 | 암호화/PGlite/계정 격리 검증. 실제 로그인 계정의 운영 왕복은 아직 미확인 |
| 비회원 중단 복구 | 기존 한계 유지 | 화면 내부 이동의 입력 보존. 브라우저 종료 후 비회원 미저장 초안 복구를 보장하지 않음 |
| 워치 후속 결합 | 기존 경계 유지 | 자동 병합 없음. 새 복합 구성을 워치와 자동 정합하는 기능은 이번 범위가 아님 |
| 복수 독립 메모 | 미구현·범위 밖 | 한 일지의 기존 목적별 메모 편집만 지원. 여러 장 메모 데이터 계약은 별도 필요 |

## 실행 증거

서로 겹치는 실행의 테스트 수를 합산하지 않는다.

| 실행 | 결과 |
|---|---|
| 변경된 화면·스키마 중심 9파일 | 166/166 PASS |
| 계정 API 기능 지원 필드 | 69/69 PASS |
| 전체 기본 단위 검사 | 4,367 PASS / 15 FAIL / 35 SKIP |
| 위 실패 파일 + 최종 빠른 기록의 격리 재실행 | 139 PASS / 2 SKIP, 5파일 모두 통과 |
| 계정 저장 서버 및 암호화·격리 검사 | 79/79 PASS |
| 공유 계정 기록 검증 번들 | 95/95 PASS, 생성 번들 동일성 통과 |
| 계정 상태 검증 번들 | 2/2 PASS |
| 배포 환경·기기 연동 계약 | 각각 16/16, 10/10 PASS |
| 빠른 기록·계획 연결 브라우저 | 18/18 PASS (desktop/mobile) |
| 직접 운동↔글 전환 추가 후 모바일 브라우저 | 5/5 PASS |
| TypeScript / 브라우저 테스트 타입 / 생산 빌드 | 통과 |
| 구버전 호환 가드 결함 주입 | 가드 제거 시 426 기대에 200이 반환되어 테스트가 실제 실패함 |

전체 실행의 15 FAIL은 계획 후속 생성 6건의 시간 초과/비동기 대기와 실행 중 API 계약 변경 9건이었다. 전체 PASS로 바꾸어 기록하지 않는다. 실패한 파일 전부를 코드·단언 완화 없이 단일 worker로 다시 실행해 통과했다. 별도로 잘못 중복 실행한 KST 전체 검사는 중단했으며 통과 증거로 사용하지 않는다. 새 커밋의 GitHub 필수 게이트 판정은 별도다.

## 배포 상태

### 후속 키보드 검토

마지막 직접 브라우저 검사에서 빠른 기록의 단계 전환 시 DOM 재생성으로 키보드 초점이 본문으로 돌아가는 것을 확인했다. 새 단계 제목으로 초점을 이어주고, RPE 단계·최종 확인·글 쓰기의 초점 이동을 브라우저 단언에 추가했다. 전 화면 접근성 전수 통과를 의미하지 않는다.

- 서버: Supabase `account-journal` version 5, ACTIVE. 배포 완료 응답과 목록 재확인.
- 운영 경계 시험: 인증 없는 POST 401, 허용된 공개 사이트의 정상 CORS preflight 204, 외부 origin 403. 응답은 no-store.
- 클라이언트: 최신 변경 빌드 및 로컬 미리보기 확인. Git 커밋·원격 CI·공개 배포 영수증은 후속 기록에서 확인한다. 이 문서의 로컬 시험을 공개 사이트 배포 완료로 읽지 않는다.
- 빌드 경고: 기존 큰 번들·폰트 상대 경로 경고가 남는다. 빌드 실패는 아니며 별도 자산/분할 최적화 대상이다.

## 남은 일을 넘길 때

### CI 후속 정합성 수정 (2026-09-27)

`d6053d9`의 원격 CI는 계약 검사와 앱 품질 검사(기본 단위 4,382 PASS / 35 SKIP, KST 검사, 타입, 빌드)를 통과했다. 브라우저 첫 프로젝트는 152 PASS / 14 FAIL / 28 SKIP이었다. 배포는 실행되지 않았다.

실패는 이미 `1bcf0d9`에 들어 있던 오라클 중심 홈·분석 항목 분리·접힌 월별 세부 정보와 예전 브라우저 경로의 불일치였다. 수치·출처 제외·메모 비노출 단언은 유지하며 `훈련량`, `훈련 구성`, `월별 변화`와 세부 정보 펼치기를 실제로 누르게 수정했다. 저장 영수증은 월별 거리 항목으로 직접 이동했는지도 추가 검증한다.

홈은 새 분석 주 동작의 첫 화면 노출을 검사하고 보조 기록·계획 동작은 스크롤 후 접근성을 검사한다. 전체 홈 길이 1,100px 제한은 오라클 추가 영역 때문에 더 이상 같은 계약이 아니므로, 최근 하루 그룹 수와 기록·계획 구역 500px 상한으로 분리했다. 터치 표적·200% 글자·가로 넘침 검사는 유지한다. 44px 표적의 부동소수점 측정 오차만 0.001px 정밀도로 비교한다.

1. 이 변경 커밋을 기준으로 필수 CI 및 공개 클라이언트 배포 결과를 확인한다.
2. 공개 클라이언트의 합성 운동 기록 생성·읽기·수정 왕복을 실제 인증 계정에서 검증한다. 성공 전 `OI-SRC-PRODUCTION-ROUNDTRIP-001`은 OPEN이다.
3. 실제 iOS, 키보드·IME, 비회원 종료 복구는 검증/설계 공백으로 남긴다. 서버·UI 구현 완료와 구분한다.
4. 추가된 숫자를 개인 처방·에너지 합산에 사용하려면 별도 변환·적격성 계약부터 채택한다. 이번 구현을 권한 근거로 삼지 않는다.

[DRAFT_COMPLETE]
