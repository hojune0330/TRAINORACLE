# WORK_ORDER_DEEPSEEK_SPEC_WIDE_AUDIT 발행 보고 — 다각화 실측 검증 보고

> **작성자:** 저비용 검증 에이전트 (Claude, Opus 대비 ~10배 낮은 비용)
> **작성일:** 2026-08-06
> **검증 대상:** Opus 보고 「딥시크 전역 감사 지시서 발행 완료」 + `WORK_ORDER_DEEPSEEK_SPEC_WIDE_AUDIT.md` (d9dc4a8)
> **방법:** 지시서/보고서의 모든 수치 주장을 **독립 명령으로 재실행**하여 재현 여부를 판정. 원본 저장소는 결함 주입 실험 시 /tmp 사본에서만 손댐 (원본 워킹트리 무오염).
> **기준점:** origin/main = `d9dc4a8` (fetch 후 확인), 지시서 직전 커밋 = `b91ec4e`, 내 브랜치 작업과 무관하게 별도 worktree로 분리 검증.

---

## 1. 판정 요약

| 항목 | Opus 주장 | 내 실측 | 판정 |
|---|---|---|---|
| 지시서 커밋 | d9dc4a8 push | `git fetch` → `a29e5f2..d9dc4a8` 확인 | ✅ 확인됨 |
| 지시서 크기 | 1,180줄 | **1,204줄** (60,315 bytes) | ⚠️ 근사 (오차 2%) |
| 패킷 | 24개 (D-01~D-24) | 24개 유니크 헤더 확인 | ✅ 일치 |
| §2 재판정 금지 | C-1~C-12 | C-01..C-12 유니크 12개 확인 | ✅ 일치 |
| §4 토큰 규율 | 6개 | 4.1~4.6 섹션 6개 | ✅ 일치 |
| §5 함정표 | T-1~T-18 | 유니크 18개 확인 | ✅ 일치 |
| §8 절대 금지 | 15항 | 표 1~15행 | ✅ 일치 |
| §9 정지 조건 | 8항 | 표 1~8행 | ✅ 일치 |
| §14 형식 | OD-REQ | `### OD-REQ-<번호>. <한 문장>` + 선택지 A/B | ✅ 일치 |
| §10 산출물 | D-01~D-24 파일명 | 24개 파일명 목록 | ✅ 일치 |
| D-07 공허성 필수 | 6개 (CI 5 + F-2 1) | CI 목록 5 + validate-latest-owner-decision.mjs | ✅ 일치 |
| **§3 기준값 10개** | 493/31/9/7/131/55/224/847/10 | **b91 기준 전수 재현: 493/31/9/7/131/55/224/847/10** | ✅ **완전 일치** |
| **F-1 검증기** | 55 / CI 14 / 고아 41 | 55 / `comm -12`=14 / `comm -23`=41 | ✅ **완전 일치** |
| **F-1 T-16 오탐** | grep 15개, github.sh가 오탐 | 정확히 재현 (15 → comm 실존교집합 14) | ✅ **일치 (함정 입증)** |
| **F-2 스크립트** | 실존/통과/conflicts=12/CI 없음 | 실존 / `FORMATION_OWNER_BASELINE_VALID conflicts=12 latest_decision=governs runtime=false` / ci.yml 미등록 | ✅ **완전 일치** |
| **F-2 최신결정 참조** | (질문만 제기) | **🔴 읽지 않음 — 확정** | 🔴 **신규 발견** |
| **F-3 경로** | 795 / 112 (14%) | b91: 791/89 · d9dc: 802/95. 여러 변형 시도해도 795/112 불출 | ⚠️ **재현 불가 (수치 과대 의심)** |
| **F-4 규칙 ID** | 847 / 693 (82%) | b91 기준 **847 / 693 정확 일치**, 최다참조 GATE-BINDING-001=25도 일치 | ✅ **완전 일치** |
| 성능 | 87초 → 0.16초 (540배) | 단일 패스 0.06초(본 실측). 순진법 20개=0.31초 | ⚠️ 방향만 확인 |
| AGENTS.md §8/§0 | 신설 | §0 4번 항목·§8 표 확인 | ✅ 일치 |
| 백로그 헤더 | F-1~F-3 기록 | 3건 기록 확인 (F-4는 없음 — 보고 문구 그대로) | ✅ 일치 |
| deepseek-audit/README | 신설 + 판정권한 없음 | 실존 + `verdict_authority: NONE` 명시 | ✅ 일치 |

---

## 2. 🔴 신규 확정 3건 (Opus가 확인하지 못했거나 미제기)

### 2.1 F-2 검증기는 "latest"라는 이름이지만 최신 오너 결정을 읽지 않는다 (D-08의 핵심 질문에 대한 답)

`validate-latest-owner-decision.mjs`가 `read()`로 여는 파일 4개를 추출했다:

```
reports/review/FORMATION_LATEST_OWNER_DECISION_BASELINE.md   (recorded_at: 2026-07-17)
reports/review/FORMATION_SPEC_CONFLICT_REGISTER.csv          (FRV2-CONF-001~012)
reports/research/FORMATION_RESEARCH_PROTOCOL_V2.md
.omo/plans/trainoracle-formation-followup-deep-research.md
```

**`OWNER_DECISION_SESSION_SLOT_INTENSITY_2026_08_06.md` (오늘 날짜의 최신 결정, OD-SLOT-1~8)는 어디에서도 읽지 않는다.** 즉 이 검증기는 "2026-07-17 확정 baseline 문구가 방금의 고정 파일들에 보존돼 있는가"를 검사할 뿐, **2026-08-06의 최신 오너 결정이 반영됐는지(충돌하는 경우 승리하는지)는 전혀 검사하지 못한다.** 스크립트에 OD-SLOT/2026_08_06 문자열이 0건인 것도 확인했다.

→ D-08의 질문 4("이 검증기가 님의 최신 결정 문서를 읽고 있나?")에 대한 실측 답은 **"아니오"**. 단, 검증기 자체는 아래 2.2와 같이 유효하다.

### 2.2 F-2 검증기는 공허하지 않다 — 결함 주입 시 이름으로 잡아낸다 (D-07 스팟체크)

/tmp 사본에서 `FORMATION_LATEST_OWNER_DECISION_BASELINE.md`의 핵심 결정 문구 `LATEST_EXPLICIT_OWNER_DECISION_GOVERNS`를 `BROKEN_PHRASE`로 치환 (D-07 공허성 검사 방법):

```
① 정상본:        FORMATION_OWNER_BASELINE_VALID conflicts=12 latest_decision=governs runtime=false ; exit=0
② 결함 주입 후:   FORMATION_OWNER_BASELINE_INVALID baseline missing LATEST_EXPLICIT_OWNER_DECISION_GOVERNS ; exit=1
```

→ 검증기는 결함을 **이름으로** 잡아낸다 (값이 아니라 문구 존재 여부를 검사하는 한계는 있으나, "공허"가 아님은 확정). 이 프로젝트의 3대 함정(T-1/T-5/T-10) 중 T-10형의 "통과만 하는 검증기"가 **아님**을 입증한 첫 사례.

### 2.3 고아 검증기 41개 전수 실행 — 지금 전부 통과한다 (Opus: "아무도 모른다" 반만 맞음)

Opus의 F-1 결론 "41개가 고아 — 지금 통과하는지 실패하는지 아무도 모릅니다"에 대해, /tmp 전체 사본에서 **41개 전부를 실제 실행**했다:

```
고아 검증기 41개 중 → PASS=41  FAIL=0  ERR=0  TIMEOUT=0
```

(`validate-latest-owner-decision.mjs` 포함 자체 실행으로 통과, `*.test.mjs`는 `node --test`, `.sh`는 bash. 각 15초 타임아웃)

**해석:** 고아 41개가 "살아 있다"는 것이지 "유효하다"는 것은 아니다 — 2.2와 같은 공허성 검사(결함 주입)는 이번에 1개만 했고, 나머지 40개는 **수행 성공**만 확인했다. 따라서 Opus의 진단(기계 검증 자동화 부재)은 유효하되, "파일이 죽어 있다"는 암시는 사실이 아니다. 이 결과는 딥시크의 D-07(공허성 전수)의 사전 정보로 쓸 수 있다.

---

## 3. Opus가 "확인 못 함"이라 선언한 것 — 이번에 확인함

| 항목 | Opus 보고 | 내 실측 |
|---|---|---|
| CI 결과 (7a745ca 이후) | "조회 도구 응답 없음, 못 봄" | `gh run list`로 확인: **d9dc4a8 커밋의 TrainOracle CI = `success`** (contract-tests·app-quality·app-browser·deploy-pages 전부 success) |
| 7a745ca 이후 커밋 수 | 12개 | `git log 7a745ca..d9dc4a8 | wc -l` = **12** (일치) |

참고: `pages build and deployment`(gh-pages)는 별개 워크플로우로 `failure` 이력이 있으나, 이는 CI(코드 품질)와 무관한 정적 사이트 배포 건이다. 코드 검증 결과는 아니다.

---

## 4. ⚠️ 유일한 수치 불일치 — F-3 (795 / 112)

### 실측 결과 (D-03 명령을 그대로 재현)

| 기준점 | 유니크 백틱 경로 | 해석 실패 (미존재) |
|---|---|---|
| a29e5f2 (내 브랜치 기준) | 775 | 87 |
| **b91ec4e (지시서 커밋 직전)** | 791 | 89 |
| d9dc4a8 (현재 main) | 802 | 95 |
| Opus 보고값 | **795** | **112** |

### 시도한 계측 변형 (어느 것도 795/112 미도달)

- 접미사 매칭 없이 정확 경로만: 775/337 (miss 폭증 — 상대경로·조각이 다 잡힘)
- 확장자 추가(js/css/html): 861/93
- `../` 정규화 유무: ±몇 건

### 판정

- **방향(14% 수준의 파손 경로 존재)은 사실**이며, miss 목록도 확인했다: `../FOO.md`·`.omo/evidence/...`·`03_workflow_engine/*`·`docs/PATCH_NOTES/*`·`reports/review/deepseek-audit/D-22~24`(예정 산출물 참조 — 정상) 등.
- **그러나 정확한 수치 795/112는 내가 어떤 방법으로도 재현하지 못했다.** 112는 내 실측 최대(95)를 17건 초과한다.
- 가능한 설명: (a) Opus가 다른 계측(예: 확장자 무제한이거나 매크로/대문자 변형 포함)을 썼다, (b) 보고 시 약간의 계산 착오가 섞였다. **수치는 확정값이 아닌 "참고치"로 취급하고, 딥시크 D-03에게는 "실측 재현이 우선이고 값이 다르면 저장소 변경이 아니라 자기 계측을 의심하라"는 지시가 이미 들어가 있으므로 (§3·§9-6), 실제 감사에서는 이 숫자가 자가교정된다.**
- 보고서 품질 면에서 **"795/112 = 14%"를 공증 숫자처럼 박아둔 것은 다소 위험한 표현이다.** D-03은 이 숫자를 "출발점"으로 쓰되 실측 우선이므로 치명적이지는 않다.

---

## 5. 성능 주장 (87초 → 0.16초, 540배)

- 단일 패스 방식: **본 실측 0.06초** (Opus 0.16초와 같은 오더) — 결과 847행 일치. ✅
- 순진한 방법(20개 표본): 본 실측 0.31초 (Opus 2.0초). 847개 외삽 시 ~13초 (Opus 87초) — 환경(캐시/파일시스템) 차이로 보이며, **"순진한 방법이 수십 배 느리다"는 방향은 확정 가능하지만 비율(540배)은 환경 의존적**이다. 지시서에 "약 87초"라고 적힌 것은 본 실측 기준으로는 과대.
- 판정: 성능 차이의 **존재와 방향**은 사실, **비율 수치는 환경 의존 참고치**.

---

## 6. 지시서 자체 품질 관찰 (검수 관점)

1. **구성은 우수.** §8 금지는 읽기 전용·커밋 금지·`build-*.mjs`/`repair-*.mjs` 실행 금지·오너 결정 대신 금지·§14 형식 강제까지, 지난 세션에서 반복된 사고 패턴(검증기 오탐, 관문 테스트 공허, 파일 쓰기 검증기)을 모두 봉인했다.
2. **T-16 자기 고백은 정직하고 효과적이다.** "선행 감사자가 15로 잘못 적었다"는 문구는 사실이었고(내 재현에서 정확히 15→14), 다음 작업자가 같은 실수를 반복하지 않게 만든다.
3. **C-9 "DSB-INV-*는 기계 검증 0건"** — 이는 사실이며(ci.yml에서 DSB-INV 관련 검증기 부재), 같은 맥락에서 "CI 초록 = 검증됨" 그림을 깨는 AGENTS.md §8 신설도 타당하다.
4. **D-07이 "이번 감사의 심장"이라는 자리매김은 타당.** 내 2.2 스팟체크가 그 방법의 유효성을 실제로 증명했다.
5. **§3 \"추천·권장 표현 0건\"** — §11 품질 기준 표에 `추천\|권장\|해야 한다` 자가 검색 항목으로 존재. 다만 **지시서 본문 자신이 "권장은 강제 아니다"(OD-SLOT-7)라는 표현을 담고 있는 영역과의 충돌 여부는 딥시크 자가검색 때 확인 대상**이다 (검증 범위 밖, 후속 참고).

---

## 7. 결론

| 차원 | 판정 |
|---|---|
| Opus 보고서의 주요 주장 (발행·구조·F-1·F-2·F-4·§3 기준값·AGENTS.md/백로그/README 신설) | ✅ **대부분 정확 — 독립 재현으로 입증됨** |
| F-3 수치 (795/112) | ⚠️ **재현 불가 — 참고치로 격하 권장** |
| 성능 비율 (540배) | ⚠️ 방향만 사실, 비율은 환경 의존 |
| Opus가 확인 못 한 것 | ✅ 이번에 확인: CI d9dc4a8 `success`, 7a745ca 후 12커밋 |
| 신규 확정 사실 (이 보고서 추가) | 🔴 F-2는 최신 OD-SLOT 결정을 읽지 않음 (D-08 답: 아니오) · 🔴 F-2는 공허하지 않음 (결함 주입 인지) · 🔴 고아 41개 전부 현재 통과 |

**딥시크에게 넘어가기 전 조치 권고 (오너 판단 대기 항목이므로 내가 결정하지 않는다):**
1. F-3 수치를 지시서 §3/백로그/README에서 "참고치(재현 시 상이: b91=791/89, d9dc=802/95)"로 다듬을지 여부
2. F-2가 최신 결정(2026-08-06 OD-SLOT)을 읽도록 확장하는 것이 D-08 권고 범위인지, 아니면 CI 등록이 먼저인지 — D-08 산출물을 보고 오너가 판단
3. 고아 41개 중 실패 0건 확인된 사실을 백로그/README에 반영할지 여부 (D-07의 효율 개선 정보로 활용 가능)

> 이 보고서의 실측 산출물(결과 파일)은 `/tmp`에 있으며 세션 휘발로 소실될 수 있다. 필요한 경우 재실행 가능 (모든 명령은 지시서 §6 D-02/D-03/D-08에 기재된 것과 동일).

[DRAFT_COMPLETE]
