# TRAINORACLE_DESIGN_SYSTEM_IMPROVEMENT_EXECUTION_2026-09-12.md

## 트레인오라클 디자인 개선 실행 기록

```yaml
status: IN_PROGRESS
base_main: 00ca7c5e2e7f70d7c0f18e3e351c2392c51af23f
branch: codex/design-system-improvements-20260912
publication: PR_334_CI_PENDING
```

## 범위

[승인 계획](../plans/TRAINORACLE_DESIGN_SYSTEM_IMPROVEMENT_PLAN_2026-09-12.md)의 DS-00~07을 실행한다. 문구·흐름·훈련 수치·안전·계정 데이터 의미를 유지한다. 원본 일지나 실제 계정 비밀은 검수에 사용하지 않는다.

## 시작 확인

- GitHub main을 API로 직접 조회해 기준 해시와 동일함을 확인했다.
- 열린 PR은 #320 `codex/v3-full-plan-selection`이다. 해당 상세 처방 기능은 이번 작업으로 활성화하지 않는다.
- 신규 디자인 PR은 식별되지 않았다. 현재 main의 `design_handoff_plan_beta_extension/`은 참고 자료이며 새 동작의 승인 근거가 아니다.
- 기존 검토 보고서·40개 화면 캡처는 이전 기준 증거로 보존한다. 이번 변경의 검수 결과로 재사용하지 않는다.
- 원래 작업 폴더의 dirty 변경과 기존 미추적 자료는 삭제하지 않는다.

## 진행표

| 단계 | 상태 | 실제 결과/다음 관문 |
|---|---|---|
| DS-00 기준·문서 | IMPLEMENTED_REVIEWED | 기존 디자인 문서에 현행/역사적 범위 명시, 수정 문서 로컬 링크 검사 통과 |
| DS-01 공통 표현 | IMPLEMENTED_REVIEWED | 모달 알림·실측 탭 높이·폰트 실패·확대 글자 검증 및 독립 소스 검수 완료 |
| DS-02 계획 | IMPLEMENTED | 4702493의 처방 조판, b278d5f의 280px 배치, e625b0a의 root 글자 확대 보완. 동일 확대 e2e 2/2 재통과 |
| DS-03 홈·일지 | IMPLEMENTED_REVIEWED | 집중 검사·전후 시각 대조·실제 스크롤 및 버튼 도달성 확인 |
| DS-04 분석·도움말 | IMPLEMENTED_REVIEWED | 집중32개, 실제 비례 결함 주입, 독립 소스 검수 및 200% 상단바 보완 |
| DS-05 꾸미기 | IMPLEMENTED_REVIEWED | 서랍·가격·광학 크기·모달 알림 정리. 기존 자산/좌표/획득 규칙 유지, 스크롤·실제 히트 검사 |
| DS-06 계정 | IMPLEMENTED_REVIEWED | 집중59개. 실제 CSS·폰트 fixture에서 draft 3/3, conflict 14/14 통과. PNG 7개 보존 |
| DS-07 통합·공개 | CI_PENDING | PR #334 생성. CI→병합→배포→공개 화면 확인은 아직 남음 |

## 문서 정합성 결정

- 원본 `SKILL.md`의 과거 규칙은 역사적 설명으로 보존했다. 영문 전용·AI 신뢰도 % 의무·게임화 전면 금지는 현행 앱의 승인 범위가 아니다.
- `DESIGN.md`의 대시보드 정적 모션과 이전 베타/로컬 보관 설명을 현재 앱의 기능 계약으로 읽지 않도록 범위를 명시했다.
- 시각 기준의 기존 이모지 3슬롯 설명은 v3 자유 배치의 원본 계약과 연결했다. 저장 모델은 수정하지 않았다.
- 공통 코드 검사 통과와 실제 화면·조작 검증은 별도 증거로 남긴다.

## 검증

구현 후 실행한 검사만 아래에 추가한다. 과거 통과 수치를 현재 결과로 기입하지 않는다.

- 수정 문서 5개 내부의 로컬 Markdown 링크 검사: 통과. 첫 검사 도구는 루트 파일의 빈 상위 경로 처리로 실패했으며, 절대 경로 기준으로 도구를 교정한 뒤 다시 실행했다.
- `git diff --check`: 통과 (Windows 줄바꿈 정규화 경고는 존재).
- `app/scripts/capture-design-system-improvement.mjs`: 문법 검사 통과. 실제 화면 실행은 통합 빌드 후 수행하며, 이전 증거를 덮어쓰지 않는 새 출력 경로를 사용한다.
- DS-01의 새 시각 검사에 수정 전 소스를 넣은 격리 시험: 8개 실행, 기대한 5개 실패·3개 통과. [실행 기록](../review/evidence/design-system-improvement-20260912/DS01_MUTATION_EVIDENCE.md). 테스트0개로 끝난 최초 시도는 근거에서 제외했다.
- 중간 `audit-design-preservation.mjs` 실행: 보호 영역 변경0, 한글 소스 문구 차이0. 이는 중간 소스 검사이며 최종 동작 검수를 대신하지 않는다.
- DS-03 담당자 실행: 13개 파일 98개 테스트, 별도 홈·일지 CSS 검사 1개 통과. 원본 예시 데이터와 기존 자산을 읽기 전용으로 재사용하며 새 기록이나 개인정보를 만들지 않았다.
- DS-04 담당자 실행: 10개 파일 32개 테스트 및 타입 검사 통과. 18px 최소 막대 높이를 복원한 결함 주입에서 실제 비례 2.7px와의 차이를 검출했다. 기존 지표·집계는 변경하지 않았다. 담당자 보고와 부모의 통합 실행 결과는 구분한다.
- DS-06 담당자 실행: 10개 파일59개 통과(신규 표시 계약6개 포함). 44px 규칙을40px로 변조한 시험에서 지정 검사가 실패했다. 운영 main.tsx의 실제 CSS import와 단위 fixture의 한계를 따로 확인했다.
- 부모 중간 실행: 앱 타입 검사 및 빌드 통과. `design-system-improvement.spec.ts`의 폰트 실패 탐색·모달 알림 키보드 접근8개가 desktop/mobile/320px/reduced-motion에서 통과했다. Windows 시험 서버가 자동 종료되지 않아, 경로와4189포트를 확인한 해당 서버만 정리한 뒤 runner가 exit0으로 종료했다. 최종 스타일 후속 수정 이전 결과이므로 최종 전체 검수와 구분한다.
- DS-05 담당자 집중 계약18개, 저장/undo/상한/구매 관련32개, 최종 집중15개 통과 보고. 서로 겹칠 수 있으므로 합계65개의 독립 테스트로 보고하지 않는다.

## 독립 검수와 보완

- DS-00/01 첫 커밋에 대한 별도 읽기 전용 검수에서 두 가지 P2를 확인했다. 꾸미기 모달 외부의 검토 알림 닫기 버튼은 모달의 키보드 초점 순환에 포함되지 않았다. 저장 안내의 고정 위치는 선택된 장식의 편집 도구와 겹칠 수 있었다.
- 보완은 알림 복제가 아니라 기존 알림을 편집기 내부의 단일 표시 위치로 옮기는 방식이다. 알림 내용·표시 시간·저장 판정은 유지한다. 모달 안에서 키보드로 닫을 수 있는지, 알림이 정확히 하나인지, 도구와 겹치지 않는지를 별도 검사한다. 완료 판정은 수정 후 실행 결과로 남긴다.
- DS-03 독립 검수에서 최근 일지의 새 말줄임이 수행 상태를 가릴 수 있음을 확인했다. 수치 행은 줄바꿈으로 수정하고 제목의 기존 말줄임만 유지했다. 일지 상세 수치와 단위도 별도 span으로 분리해 긴 값이 접히도록 했다.
- DS-04/06 정확한 커밋의 읽기 전용 독립 검수에서는 확정 차단 결함이 없었다. 그러나 소스 검사만으로 시각 승인을 하지 않았다. 표 제목 줄바꿈·펼침 표식·공용 계정 버튼이 쓰이는 다른 화면을 후속 검수에 포함했다.

## 최종 통합 검수 진행 기록

- b278d5f 빌드의 `pass-03`: 실제 UI에서 45개 화면 캡처 완료. 320/375/768/1440px, 200% 균일 글자 확대 스트레스, 줄인 모션을 포함한다. 가로 넘침 0, 브라우저 오류 0, 등록된 Pretendard 폰트의 실제 로드 상태 확인. 네이티브 OS 글자 확대 또는 iPhone Safari 검증으로 간주하지 않는다.
- 통합 브라우저 시나리오 34개 중 32개 통과, 2개 실패. 두 실패는 동일한 MAIN 비교 제목의 root-font 200% 확대 미반영이다. 테스트를 완화하지 않고 표시 CSS를 수정한다. 이 실행을 전체 통과로 보고하지 않는다.
- 브라우저 시험 타입 검사 통과, 배포 환경 검사 12/12, 기기 연동 계약 검사 10/10 통과.
- 소스 보존 검사: `impl/`, `app/src/domain/`, `supabase/` 변경 0. 한글 문자열 차이는 최근 일지/민지 예시 버튼의 접근성 이름 2건이며 화면 본문을 새로 쓰지 않았다.
- 별도 읽기 전용 코드 검수에서 b278d5f의 알림 스크롤 겹침과 280px A/B 배치 수정은 코드상 해결로 판정했다. 수정 전 동일 브라우저 시험에서 실제 실패를 확인했으며, 수정 후 통합 시험에서 해당 항목은 통과했다.
- 별도 시각 검수에서 홈의 화면 하단 내용과 꾸미기 선택 도구 뒤의 내용에 대해 가림 의심을 제기했다. 단순한 스크롤 화면 경계인지 실제 접근 불가인지 추가 조작 검증하며, 캡처만으로 결함 종결하지 않는다.
- e625b0a: 비교 제목의 고정 px를 상대 rem으로 복원했다. 실패한 동일 e2e를 변경 없이 다시 실행해 desktop/320px 2/2 통과했다.
- 추가 모바일/줄인 모션 통합 검사: 확대 탭, 280px 비교, 폰트 요청 실패, 모달 알림 접근 8/8 통과했다.
- 로컬 전체 단위 검사: 3,686개 중 3,681 통과·5 실패. 원본 JSON을 보존했다. 실패한 세 파일을 동일 코드·기본 제한으로 격리 재실행해 25/25 통과했다. 최초 전체 실행을 3,686/3,686 통과로 표현하지 않는다. 동시에 시작한 KST 전체 검사는 다수 제한 초과로 중단했으며 통과 근거가 아니다. 최종 전체 결과는 격리된 CI에서 확인한다.
- `pass-04-readonly`: 홈320에서 13px 스크롤 후 제목 전체 표시, 홈375의 52px CTA 실제 히트 성공. 편집기375에서 50px 스크롤 후 지표와 도구 사이 7.8px 확보, 선택 도구·상단·하단 모든 버튼 실제 히트 성공. 초기 화면 잘림은 접근 불가로 재현되지 않아 불필요한 강제 빈 공간을 추가하지 않았다.
- 같은 추가 검수에서 200% 분석 상단의 뒤로가기 라벨을 한 줄로 보완했다. 320/375px에서 버튼64×44px, 제목 중심 오차0, 겹침0, 가로 넘침0을 확인했다. 회귀 검사9/9와 빌드 통과.

## 원격 전달

- [PR #334](https://github.com/hojune0330/TRAINORACLE/pull/334): 통합 디자인 개선. 초안 상태에서 전체 CI를 실행하며 마지막 검수 증거를 추가한다.
- PR #320은 이번 범위와 분리했고 변경하거나 병합하지 않았다.
- 460dea0 CI: 계약 검사 통과, 기본/KST 각각 3,694개 전체 단위 검사 통과. 전체 브라우저의 desktop 단계에서 137개 통과·28개 조건부 제외·2개 실패로 중단됐다. 후속 모바일 프로젝트가 통과했다고 주장하지 않는다.
- CI 추가 발견 1: 꾸미기 서랍 헤더가 새 래퍼 안으로 이동했는데 기존 모션 검사는 직접 자식 `header`를 찾았다. 실제 애니메이션이 적용되는 새 래퍼를 검사하도록 선택자만 수정하고 모션 이름·줄인 모션 단언은 유지했다.
- CI 추가 발견 2: 375px 상세 처방의 용어 도움말 터치 폭이 flex 축소로 42.9375px가 됐다. 공통 도움말 래퍼/버튼의 축소를 막고 최소 폭44px를 고정했다. 아이콘 시각 크기14px, 문구, 열기/닫기 동작은 유지했다. 기존 실제 터치 검사와 새 단위 계약으로 검증한다.
- 두 보완의 독립 소스 리뷰 승인. 모션·개인 상세 처방 시나리오를 4개 프로젝트에서 재실행해 32/32 통과(1.9분). 기존 터치 타깃·근거 팝업 겹침 검사를 완화하지 않았다. 도움말 단위 계약3/3 및 빌드 통과.

## 검증 한계

- 실제 iPhone Safari는 자동화한 데스크톱 Chromium의 모바일 크기 검사와 다르다.
- 외부 제공자 로그인·실제 메시지 전송은 이 디자인 작업의 fixture 검수로 완료됐다고 표시하지 않는다.
- 에셋 원본 PR·권리·획득 정책이 확인되지 않은 신규 자료는 출시하지 않는다. 기존 자산 기반 개선은 진행한다.

[DRAFT_COMPLETE]
