# INSTANT_PLAN_INTEGRATION_REPORT_2026-09-20.md

## 1. 오너가 먼저 볼 내용

상태: `PARTIAL_IMPLEMENTATION_READY_FOR_OWNER_REVIEW`. 전체 로드맵 완료가 아니다.

현재 기록/목표만/기록 없이 → 필요한 경험·훈련일·몸 상태 확인 → 추천 한 개와 실제 일정 → 명시적 시작 → 오늘 훈련 → 연결 일지까지 개발 브랜치에서 연결했다. 기본 추천 이외의 두 안과 상세 방법은 선택해서 펼친다. main과 공개 서비스는 바꾸지 않았다.

- **새 첫 화면:** 실제 기록과 목표 기록을 구분한다. 목표를 현재 실력으로 바꾸지 않는다.
- **계획 선택:** 날짜·오전/오후·주요 훈련·기초/회복을 먼저 보여준다. 접힌 후보도 실제 일정이 남는다.
- **기록 기반 안내:** 기존 승인된 종목·경험·방법에 한해 제안하고, 사용자가 기준 기록을 확인해야 시작한다. 새 수치나 적용 권한을 만들지 않았다.
- **오늘 훈련:** 저장된 세션의 방법을 보여주고 해당 세션의 일지로 이동한다. 일지 존재와 실제 완료는 같지 않다.
- **아직 없음:** 서버 삭제·휴지통·복구, 실제 승인된 제작자 프로그램 공급. 이 두 항목을 이번 PR의 완성 기능으로 소개하면 안 된다.

## 2. 두 작업과 PR 관계

| 구분 | 담당 및 범위 | 상태 |
|---|---|---|
| P | 별도 세션 `01a0ba67-0611-7232-bbf2-78aebdfa52cc`; 입력·추천·오늘 UI, 제작자 계약/레지스트리 | [Draft PR #344](https://github.com/hojune0330/TRAINORACLE/pull/344), 변경 동결 |
| I | 현재 세션; 기존 생성기·선택·안전·일지 연결, 실제 투영, 통합 검수 | `codex/plan-integration-20260920`, Draft PR로 제출 |

공통 기반 `d529b9a`, 개발 시작 main `f0d055810d92e71f5219aaef915120da6eba3f7a`.
P 원본 커밋 `321278dca1bbb4b206a7cd8735641f7d22f7b681`, 모바일 축소 후속 `c975cef542c8acfea69413f2e47cc64c61ec406a`를 I에서 함께 검사했다.

최종 제출은 P를 기반으로 한 I의 stacked PR로 구성한다. 오너 승인 후 P → I 순서로 병합 검토하며, P가 main에 반영되면 I의 base를 main으로 변경하고 변경 파일과 CI를 다시 확인한다. 양쪽 PR을 독립적인 전체 구현으로 중복 반영하지 않는다. 자동 병합은 설정하지 않는다.

## 3. 기존 계약을 유지한 구현

### 3.1 입력과 기준 기록

- `instant-plan-entry.ts`: 지원 7종목, 양수 유한 초, 소수점, 실제 달성일과 미래 날짜를 경계에서 재검사한다.
- CURRENT_RECORD만 기존 계정 범위의 선수 기록 저장소에 저장/재사용한다. GOAL_ONLY와 NO_RECORD는 현재 기록이나 처방 앵커가 아니다.
- 이번 PR은 선수 기록의 새로운 서버 동기화 구현이 아니다. 기존 계정 범위 저장소의 의미를 유지한다.
- `instant-plan-intake.ts`: 기존 승인된 방법 선택지만 제안한다. 명시적으로 고른 RPE 방법(`null`)은 미선택(`undefined`)과 구분하여 뒤로 갔다 와도 보존한다.
- 800m 121.5초, 1500m 263.4초, 3000m 655초, 5000m 1111초를 실제 생성·저장 경로에서 확인했다.
- 10km·하프·마라톤 또는 기존 상세 적격성이 없는 입력에 승인되지 않은 페이스를 발명하지 않는다.

### 3.2 추천과 일정

- 표시 정책 `SUPPORT_DURATION_RANGE_FIRST_V1`: 기존 BALANCED를 먼저 보여주는 제품 기본값이다. 과학적 최적 순위나 새 훈련량 정책이 아니다.
- `instant-plan-projection.ts`는 실제 `candidate.sessions`의 날짜·횟수·오전/오후를 투영한다. 없는 세션을 휴식 처방으로 둔갑시키지 않는다.
- 기간 내 세션을 오전 → 오후로 정렬한다. 주요 훈련 횟수는 실제 QUALITY 세션으로 센다.
- 가능일은 '주간'이 아니라 선택한 7/9/10일 계획 기간 내 가능일로 설명한다. 매일 훈련과 매일 고강도는 다르다.
- 375×667에서 9일 일정 전체와 시작 버튼을 첫 화면에 확인했다. 320px 및 실제 글자 200%에서는 정보 보존과 가로 잘림 방지를 우선하고 필요한 세로 스크롤은 허용한다.
- 기본값·근거·기준 기록 확인은 제거하지 않고 펼쳐 볼 수 있게 둔다.

### 3.3 저장·안전·오늘·일지

- 기존 저장과 계정 ACK 결과만 실제 활성화를 결정한다. SAVING/PENDING은 저장 완료가 아니다.
- 클릭 중복은 한 요청으로 막는다. 저장 실패 재시도는 실패한 정확한 후보/시작일을 재사용한다.
- ACCOUNT_PLAN_STALE/EVIDENCE_REQUIRED/REVIEW_REQUIRED는 재확인할 질문·다듬기 경로를 열고, 그 상태에서 무작정 재저장하지 않는다. 대기 중이거나 상태가 불명확한 쓰기는 계속 차단한다.
- 오늘 안내는 기존 안전 검사와 상세 처방 승인/만료/철회 검사를 표시 전에 확인한다. 이 사전 검사는 지시를 감추는 용도이며 사용자 대신 실행을 승인하지 않는다.
- 오늘 상세 훈련 근처에 기존 시작 전 확인을 연결했다. 미래 첫 훈련과 이미 기록이 있는 세션에는 새 START 버튼을 표시하지 않는다. 사용자가 몸 이상을 알렸을 때 정상 실행 안내를 동시에 계속 보여주지 않는다.
- 연결 일지는 계획/세션 지문과 날짜가 맞을 때만 기록 존재로 센다. 일지 저장만으로 progress를 완료로 변경하거나 오후 훈련까지 완료 처리하지 않는다.
- 조정형 계획 V4~V6의 기존 전용 화면은 유지한다. 이번 간편 Today 투영을 모든 구버전 화면에 적용했다고 주장하지 않는다.

## 4. 독립 읽기 검수와 수정

P 작업자에게 I 파일을 읽기 전용으로 검수하도록 요청했다. P가 I 코드를 직접 덮어쓰지 않았고, I가 원본 경로를 대조하여 수정했다. 다음 발견은 가설로 받은 뒤 코드와 테스트에서 확인했다.

| 발견 | 조치 | 확인 |
|---|---|---|
| 계정 재검토 거절 후 질문 복귀도 차단 | 대기/불명확 쓰기와 재검토 거절 분리 | 오류 코드 3종별 입력 복귀 테스트 |
| 오늘 안내에 최신 안전·승인 확인 연결 부족 | 표시 사전 검사 + 가까운 시작 확인 + 차단 결과 표시 | 최근 위험 및 승인 불가 투영 차단 |
| 연결 일지를 써도 '아직 기록 없음' | 정확한 계획 링크 일지 존재 반영 | 다른 날짜·다른 계획 지문 배제, progress 무변경 |
| 명시적 RPE 선택이 뒤로 가기 후 자동 상세로 복귀 | null/undefined 구분 | 명시적 RPE 유지 테스트 |
| 미래·기록 후에도 오늘 START 표시 가능 | 현재 예정이며 미기록인 세션에 한정 | 오늘 상태 분기와 기존 버튼 의미 유지 |
| 375px에서 9일 중 6일만 먼저 보임 | P 전용 컴포넌트 축소 + I 상단 중복 제거 | 9번째 날짜 및 시작 버튼 viewport 단언 |
| 기존 e2e가 구 첫 화면·숨긴 후보를 계속 가정 | 실제 입력/펼치기 경로로 테스트 업데이트 | 보호 목적 유지, 새 skip 없음 |

기존 체크를 통과시키려고 처방 숫자·통증 판정·계정 권한을 느슨하게 하지 않았다.

## 5. 실제 실행 결과와 한계

변경 분류: 사용자 입력, 계획 선택·계정 저장 경계를 연결하는 중요 변경. 현재는 배포 후보 확정 전 개발 검수다.

| 검사 | 결과 |
|---|---|
| app build (tsc + Vite) | PASS. 기존 폰트 경로/청크 크기/정적·동적 import 경고는 남음 |
| e2e TypeScript | PASS |
| 관련 단위·계약 65파일 | 487 PASS, 35 기존 SKIP, 0 FAIL |
| 최종 추가 회귀 3파일 | 13 PASS. 승인 불가 및 오전/오후 정렬 포함 |
| 기존 경로 desktop 브라우저 38케이스 | 첫 실행 35 PASS/2 프로젝트별 SKIP/1 구 locator 실패. 해당 locator 수정 후 아래 재실행에서 통과 |
| 핵심 입력·저장·일지·청소년·2회 훈련: desktop/mobile/reduced-motion | 45 PASS, 6 기존 프로젝트별 SKIP, 0 FAIL |
| diff whitespace | 이번 I 변경 PASS. 보존한 원문 A/B의 기존 Markdown hard-break 공백은 별도 보존 |
| GitHub CI | PR 생성 뒤 별도 상태 확인. 위 로컬 결과는 CI 완료나 배포 증거가 아님 |
| 공개 서비스 검증 | 미실행. 병합·배포 권한은 현재 보류 |

브라우저 검사에는 소수점 기록 → 기준 확인 → 저장 → 새로고침 → 같은 처방 → 연결 일지, 2개 탭의 동일/변경된 선택, 저장 실패 재시도, 오전/오후, 미기록, 안전 차단, 320/375px, 실제 글자 200%, 줄인 모션을 포함했다. 모든 기기·모든 계정 공급자·전체 앱 e2e 통과를 의미하지 않는다.

실제 로컬 브라우저 캡처: [375px 추천 일정](./assets/instant-plan-20260920/recommendation-375.png), [320px 추천 일정](./assets/instant-plan-20260920/recommendation-320.png), [실제 글자 200%](./assets/instant-plan-20260920/recommendation-real-double-text.png). 모두 가상 입력을 사용했으며 운영 선수 데이터가 아니다.

## 6. 완료하지 않은 부분과 다음 순서

| 순서 | 남은 작업 | 현재 상태 / 다음 완료 근거 |
|---|---|---|
| 1 | 서버 삭제·휴지통·복구 | [계약 초안](../../specs/reconstruct/PLAN_DELETION_AND_RESTORE_IMPLEMENTATION_CONTRACT.md)만 작성. 4개 OPEN. client/server lifecycle, CAS, 멱등성, 지연 쓰기·백업 재등장 방어와 일지 보존 코드 필요 |
| 2 | 실제 제작자 프로그램 공급 | `BLOCKED_SOURCE`. 실제 승인된 원본·허용 변환 근거 없음. 테스트 fixture를 상품으로 넣지 않음 |
| 3 | 실제 배치와 formation 원장의 용어 정합 | 9일 매일/경험자/MIX 재현에서 실제 QUALITY는 2·9일이고 예약 노출은 1·5·9일. 화면은 실제 세션을 사용함. 원장 의미 정리는 별도 코어 작업이며 3회 자동 증량으로 해결하지 않음 |
| 4 | 조정형 계획까지 간편 오늘 흐름 확대 | V4~V6의 기존 화면·저장 유지. 각 버전의 유효성/일지 참조 계약을 따라 별도 연결 필요 |
| 5 | 운영 전 통합 검수 | P → I 순서의 PR 검수, 계정 온라인 실제 사용자 경로, 최종 필수 CI. 아직 운영 DB/계정으로 검증하지 않음 |
| 6 | 오너의 완료 확인 후 병합·배포 | `WAITING_OWNER`. 지금은 main 병합, 자동 병합, 배포, 운영 migration 금지 |

## 7. 원문 보존과 상태 표시

- 원문 A/B/C/D와 [분업 지시서](../plans/TRAINING_PLAN_PARALLEL_DEVELOPMENT_ORDER_2026-09-20.md)는 유지한다.
- `PLAN_GENERATOR_SPEC.md`에 최소 입력·실제 일정 투영·기록 구분 경계를 추가했다. 이슈 수나 정본 상태를 기억으로 바꾸지 않았다.
- 제작자 계약은 실제 공급 승인이 아니며 삭제 계약은 삭제 런타임 증거가 아니다.
- 작업 분리와 로컬 통합 확인을 마쳤지만 전체 계획 완료, main 최신 배포, 공개 사용자 사용 가능 상태로 보고하지 않는다.

[DRAFT_COMPLETE]
