# PRESCRIPTION_SEQUENCE_STORAGE_IMPLEMENTATION_2026-09-03.md

```yaml
doc_id: trainoracle-prescription-sequence-storage-20260903
status: LOCAL_VERIFIED_SERVER_PUBLIC_GATE_OPEN
owner: COACH_HOJUNE
version: "0.3"
base_sha: a3d2b2717b2a2479bc183f7bf58ce3923b3c9d14
branch: codex/prescription-sequence-storage
new_numeric_templates: 0
new_calculation_models: 0
new_main_method_pairs: 0
canonical_promotion_allowed: false
production_deployment: NOT_PERFORMED
```

## 1. 무엇을 개선했나

새 상세 계획은 준비운동, 가속, 본운동, 반복/세트 사이 회복, 정리운동의
순서를 처방 안에 함께 저장한다. 저장 후 다시 열거나 백업에서 읽어도 같은
구조를 사용한다. 화면의 순서도와 실제 처방 숫자가 서로 달라지는 것을 막는
후속 기반 작업이다. 기존 네 종목의 반복 수, 페이스, 회복량은 바꾸지 않았다.

이번 증분을 '서로 다른 MAIN 두 방법 완성'이라고 부르지 않는다. 새 템플릿의
정확한 구성과 적용 범위를 채택하고 연결하는 작업은 여전히 남아 있다.

## 2. 스펙과 구현 경계

- [처방 계약](../../specs/reconstruct/TRAINING_SESSION_PRESCRIPTION_CONTRACT.md)
  §12.5를 먼저 추가했다. 독립 처방을 하나 더 저장하는 것이 아니라 원래 처방과
  정확히 일치해야 하는 V2 구조를 같은 fingerprint에 묶는다.
- `PaceTargetPlanPrescription.sequence`는 하위호환을 위한 선택 필드다.
  신규 생성은 저장하며, 기존 계획을 읽거나 진행 상태를 남긴다고 소급 추가하지 않는다.
- 앱 저장 검증과 코어 후보 선택 검증 모두 구조 파서와 원본 대조를 수행한다.
  확인값을 다시 계산했더라도 승인 구성과 달라지면 거부한다.
- 기존 네 템플릿에는 별도 본운동 종료 회복이 없어 NOT_APPLICABLE로 명시한다.
  거리 회복을 시간으로 바꾸거나 회복 거리를 본운동 거리에 합하지 않는다.
- 설명 receipt와 후보 식별자는 기존 전체 처방 fingerprint를 통해 새 구조까지 묶는다.
  예전 처방, 식별자와 설명 receipt는 그대로 읽는다.
- START/RESTART의 기존 안전·승인 재확인을 유지한다. 구간별 타이머나 반복 완료
  체크 기능을 새로 만든 것은 아니다.
- 비공개 계정 백업 payload는 구조를 유지하고, 공개 요약 카드에는 구조나 기록
  참조를 추가하지 않는다. 계정/서버 기능은 새로 활성화하지 않는다.

## 3. 실제로 잡은 연결 문제

첫 통합 실행에서 저장 스키마는 새 필드를 허용했으나 코어 후보 파서의 엄격한
키 목록이 이전 형식만 받아 신규 계획 선택이 거부됐다. 코어에도 선택 필드를
명시적으로 연결하고 동일한 구조 대조를 넣었다. 조건을 느슨하게 풀어 통과시킨
것이 아니며, 알 수 없는 다른 키는 계속 거부한다.

기존 MAIN 비교 테스트는 비활성 비교용 숫자를 바꾸면서 이전 snapshot을 유지해
불일치가 됐다. 이 테스트 입력만 레거시 비저장 구조로 명시했다. 실제 저장 구조를
바꾼 경우의 거부 테스트는 별도로 추가했다. 과거 판정을 그대로 통과시키려고
제품의 불일치 검사를 제거하지 않았다.

## 4. 최초 구현 실행 증거

아래는 독립 검수 전 실행 기록이다. 이 통과 기록으로 §6의 발견을 부정하지 않는다.

| 검사 | 관측 결과 |
|---|---|
| 앱 전체 | UTC 255파일/2154개 PASS, KST 255파일/2154개 PASS; `npm test` exit 0 |
| 코어 전체 | 22파일/750개 PASS |
| 앱/브라우저/코어 타입 검사 | PASS |
| 프로덕션 빌드 | PASS, `PlanBeta-DiipUBiZ.js`; 기존 Pretendard 경로 경고는 남음 |
| 처방 문서 검증기 테스트 | 53개 PASS |
| 관련 브라우저 | 36개 PASS, 4개 프로젝트. 개별 사례가 375/1440px로 덮어쓰므로 전부 320px 검사로 세지 않음 |
| 구조 저장·비교 최종 집중 검사 | 48개 PASS; 전체 검사와 중복되므로 합산 금지 |
| 백업 | 합성 계정/가짜 Supabase 전송 객체의 JSON 왕복 PASS. 실제 서버 전송 또는 계정 공개의 증거는 아님 |

브라우저는 새 계획 선택, 저장 구조 V2 확인, 재로드 후 구조 유지, 방법 화면,
현재 안전 재확인, 청소년/성인 동일 용량, 기록 변경 재확인, 미기록/오래된 기록,
줄인 모션과 200% CSS 글자 확대를 검사한다. 실제 iPhone Safari, 실제 화면낭독기,
브라우저 자체 확대, 워치 실데이터나 프로덕션 계정은 이번 검사 대상이 아니다.

### 결함 주입

반복 수, 운동 거리, 회복 시간/방식/단위, 종료 회복, 기록 참조, 준비/정리,
라벨 원문 10종을 바꾼다. 구조 문법은 유효하고 처방/후보 확인값도 다시 계산한
입력이다. 앱과 코어가 각각 거부하며 START/RESTART도 차단한다.

1. 앱의 원본 구조 대조를 일시 제거하자 10개 이름의 테스트가 실제 실패했다.
2. 원복 후 코어의 대조만 제거하자 동일 10개가 코어 판정 단언에서 실패했다.
3. 두 변경을 원복하고 집중 48개를 다시 실행해 전부 통과했다.

이 밖에 종료 회복 누락, V1/알 수 없는 버전, 알 수 없는 키, 빈 본운동을
거부한다. 잘못된 저장 파일은 원본 bytes를 남기며, 추정한 계획으로 덮어쓰지 않는다.
Terra의 코어 V1 주입도 7개 실패 후 원복/통과로 보고됐으나 부모의 위 두 실측과
별도 기록이며 합산하지 않는다.

## 5. 작업 분리와 다음 관문

- Terra high: 코어 projector, 타입 확장, 직접 코어 테스트의 제한된 구현.
- 주 작업자: 스펙, 앱/코어 입력 검증 연결, 화면, 저장/백업/위조 테스트 통합.
- 독립 소프트웨어 검수: 첫 head는 CHANGES_REQUIRED. §6의 수정 후 새 head 재검수가 필요하다.
  Fable/코치/스포츠과학 검수로 부르지 않는다.
- 관련 없는 두 worktree, 기존 증거와 사용자 데이터는 수정/삭제하지 않았다.
- 배포 상태는 로컬 통과와 별개이며 PR의 최종 SHA/CI/병합/배포 증거로만 갱신한다.

다음 구현 단위는 [두 번째 MAIN 출처 패킷](../review/SECOND_MAIN_METHOD_SOURCE_REVIEW_PACKET_2026-09-02.md)
§5의 정확한 용량·대상·주기 역할 결정과 그 방법의 전 구간 실행 연결이다.
현재 V2 snapshot은 기존 PACE_TARGET에서만 생성된다. 12x400m+100m roll-on이나
다른 시간형/복합형이 선수에게 선택 가능해졌다는 뜻이 아니다. 선택지 확대를
이유로 강도·양·빈도를 늘리는 동작은 추가하지 않았다.

## 6. 독립 검수 발견과 수정

Sol high는 `41756173370247b1945ae9ff12ad6c4259a1aa2f`를 독립 검수해
P1 두 건으로 CHANGES_REQUIRED를 반환했다. [PR #316 검수 기록](https://github.com/hojune0330/TRAINORACLE/pull/316#issuecomment-5525239661)을 따른다.

1. **원본과 구조의 공동 변조:** 기존 코어는 승인 메타데이터와 내부 일관성은
   확인했지만 반복·거리·회복을 승인된 표기식과 직접 대조하지 않았다. 원본,
   합계, sequence와 두 후보의 식별자를 모두 바꾸면 코어 선택은 통과했다.
   앱 저장과 START는 거부했으므로 실행 전체 우회라고 기록하지 않는다.
   코어가 승인 표기 파서 결과와 수치/합계를 대조하도록 수정했다. 부모가 만든
   네 종목/다섯 조건의 공동 변조 25개가 수정 전 실제 실패, 수정 후 통과했다.
   별도 코어 750개, 저장/비교/매트릭스 89개, 파서 정합성 60개도 재통과했다.
2. **공개 ID의 내용 노출:** `canonical-json-v1`은 전체 내용 문자열이다.
   그것을 포함한 candidateId를 공개 카드 `plan_id`로 보냈다. 기존 payload-only
   테스트는 이 경로를 놓쳤다. 공개 조회/쓰기에는 소유자와 계획에 묶인 별도
   `public-plan-card:v1:sha256:` ID를 사용하고, 생성 실패 때 원문 폴백을 금지했다.
   Terra의 수정 전 노출 테스트 실패 보고 후, 부모는 실제 상세 계획의 전송 인자,
   반복 게시, 다른 소유자, 기능 비활성, 인증 불일치, 비공개 프로필, crypto 없음/실패를
   포함한 15개 집중 검사를 직접 재실행해 통과를 확인했다.

추가한 변조 테스트의 TypeScript 역할 구분이 불충분해 타입 검사가 실패한 것도
수정했다. 휴식 세션에 처방을 붙이는 형태로 추론되지 않도록 QUALITY 조건을
명시했다. 수정 후 앱/코어/e2e 타입 검사와 빌드를 통과했다.
새 빌드는 `PlanBeta-C96qrWlw.js`, `public-profile-DNx97rOz.js`다.
전체 앱과 관련 브라우저 재검사 결과는 §7에 추가한다.

### 서버 후속 관문

공개 공유 수정은 **새 클라이언트가 보내는 데이터**의 수리다. 과거 서버 행의
존재·노출은 확인하지 않았고 정리도 하지 않았다. 과거 원문 ID 행을 이번 변경이
자동 덮어쓰거나 삭제하지 않으므로, 공유 공개 전에 서버 식별자 점검과 구 클라이언트
쓰기 방어가 필요하다. [공유 계약](../../specs/reconstruct/PLAN_BACKUP_PUBLIC_PROFILE_AND_SHARING_SPEC.md)
OI-PPPS-IDENTIFIER-001을 OPEN으로 추가했고, 실제 이슈 표는 6개 OPEN/4개 canonical
blocking으로 재계수했다. 이 작업은 공개 기능을 켜거나 데이터 삭제를 승인하지 않는다.

수정본의 소프트웨어 재검수는 아래 §7에 기록한다. 최종 CI, main 병합과 배포는
아직 별도 확인 대상이다.

## 7. 수정 후 로컬 완료 체크포인트

검사 대상 구현 SHA: `cff7ab1d7010cd8f8a425f12c4695a68a668faad`.
본 절을 추가하면서 제품 코드나 테스트 입력을 다시 바꾸지 않았다.

| 검사 | 최종 관측 결과 |
|---|---|
| 앱 전체 UTC | 256파일 / 2187개 PASS |
| 앱 전체 KST | 256파일 / 2187개 PASS |
| release-env / device-integration | `npm test`의 후속 명령 포함 exit 0 |
| 코어 전체 / 타입 | 750개 PASS / tsc exit 0 |
| 앱 / e2e 타입 | tsc exit 0 |
| 최종 빌드 | PASS, 위 §6 번들 |
| 최종 관련 브라우저 | 36개 PASS, 4개 프로젝트, 4.0분 |
| 시각 확인 | 375px/1440px 방법 화면과 저장 구조 안내 캡처 확인; 실제 모바일 기기는 아님 |

두 시간대의 같은 시험을 4374개의 서로 다른 기능 시험으로 합산하지 않는다.
공개 전송 시험은 mock 인자 검증이며 실제 Supabase 서버 쓰기가 아니다.
local preview는 검수 후 종료했다. 작업자가 사용하던 다른 worktree나 증거는 삭제하지 않았다.

PR: <https://github.com/hojune0330/TRAINORACLE/pull/316>.
Sol high는 위 구현 SHA를 다시 정적으로 검토해 **로컬 차단 결함 없음**으로
판정했다. 공동 변조와 공개 ID 원문 전송 경로가 수정됐음을 확인했지만 테스트를
독립 재실행하지는 않았다. 부모의 실행 결과와 모델의 코드 검토를 별개로 기록한다.
이는 Fable UX, 코치, 스포츠과학자 또는 서버 운영자의 승인서가 아니다.

원격 CI와 최종 PR head는 PR의 SHA별 댓글에서 확인한다. 이 문서는 로컬 통과를
main 병합·배포로 승격하지 않으며, OI-PPPS-IDENTIFIER-001도 OPEN이다.

[DRAFT_COMPLETE]
