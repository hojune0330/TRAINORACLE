# SESSION_METHOD_WORKFLOW_PROGRESS_2026-09-06.md

```yaml
doc_id: trainoracle-session-method-workflow-progress-2026-09-06
status: IMPLEMENTED_FOUNDATIONS_WITH_OPEN_WORKFLOW_GATES
base_main: 985669328dbcc7738afc9f390c9c325769b8251c
branch: codex/method-workflow-completion
whole_workflow_complete: false
new_template_activations: 0
new_scalar_adjustment_policies: 0
new_multi_main_runtime_policies: 0
canonical_promotion: false
issue_closure: false
```

## 1. 이번 작업의 의미

사용자가 승인한 상세 훈련 선택·조정·일지 연결 계획을 수행한다. 이전 PR #318의
기반 구현을 전체 기능 완료로 부르지 않는다. 새 방법을 조사하는 일, 실제로 선택하게
하는 일, 조정된 숫자를 저장하는 일, 수행 결과를 해석하는 일은 각각 완료 증거가 필요하다.

현재 공개된 네 가지 정확한 상세 템플릿과 기존 시간·RPE 계획은 유지한다.
이번 변경은 새 훈련량·페이스·회복값·성별 또는 연령별 계수를 만들지 않는다.
일지 메모를 읽지 않으며 기존 청소년·혼자 훈련하기의 승인 범위를 축소하지 않는다.

## 2. 실제 변경

| 구역 | 이번 산출물 | 정확한 상태 |
|---|---|---|
| 스펙 | 선택·조정 계약 v0.2, 현재 범위, 준비·구현 보고의 상충 문구 정리 | 문서 패치 완료. 정본 승격 아님 |
| 출처 | 전체 30행과 별도 중거리 참조, 추가 원문을 대조한 근거 보고 | 원문 확보 수준·누락값을 행별로 기록. 전부 활성화 가능한 상태는 아님 |
| 출처 정정 | LT-SEED-03의 1600m를 원문 직접 예시로 부르던 라벨 수정 | 실제 원문은 1 mile. 초안 숫자는 그대로 보존 |
| 추천 | 중립 기본값, 선택적 다양성/반복 선호, 선택 유지 | 실제 옵션 resolver와 후보 화면 연결. 두 계열 이상일 때만 선호 조작 표시 |
| 정체성 | 네 개 정확한 ID/버전/해시의 명시적 family/configuration 매핑 | 이름만 바뀐 방법을 새 계열로 부풀리지 않음. 알 수 없는 과거 참조는 추정하지 않음 |
| 이력 | 자기보고 완료와 실제 수행 준수의 의미 구분 | 원본 과거 참조·저장 JSON 보존. 실제 워치 준수 데이터 연결은 별도 |
| 배치 | 후보 다양성과 프레임 내 반복 배치 정책 분리 | 명시적 정책·실제 경험 문맥이 없으면 새로운 다중 배치는 거부 |
| 호환성 | 기존 V3 다중 구성 읽기와 새로운 선택/다음 주기 적용 구분 | 읽기는 보존하되 과거 저장 가능성이 새 실행 권한이 되지 않음 |
| 저장 | MAIN별 안정된 식별자와 동결된 초안 스냅샷 | 실제 계획 저장 잠금 안에서 재비교. 오래된 내용이나 수정 중 초안 저장 거부 |

MAIN 식별자는 날짜·프레임·종목·목적·경험·실제 슬롯 배치에서 생성한다.
방법·페이스·시간만 바뀌면 슬롯 정체성은 유지하고 내용 지문만 바뀐다.
날짜나 프레임이 바뀌면 과거 슬롯을 같은 운동으로 자동 이식하지 않는다.
스냅샷은 저장 경계의 검증 정보이며 새 영속 저장 버전이나 다중 선택 UI 완료를 뜻하지 않는다.

## 3. 독립 리뷰와 수정

| 발견 | 수정 | 남은 경계 |
|---|---|---|
| legacy 읽기 규칙이 새 successor에도 적용될 수 있음 | 일반 parser는 호환 읽기, 새 선택과 successor에는 현재 정책 검사 | 실제 운영 다중 배치 정책은 여전히 0건 |
| 정책이 실제 경험 대신 처방의 scope만 비교 | 다중 배치 시 실제 경험 입력이 없거나 정책과 다르면 거부 | 다음 통합은 검증된 intake 문맥을 모든 경로에 전달해야 함 |
| caller가 revision 갱신을 놓치면 저장 대기 중 변경 누락 | 잠금 획득 후 전체 후보 내용 지문 재검사 | 잠금·계정 범위·안전 재검사는 기존 경로도 함께 유지 |
| 과거 테스트의 가짜 해시가 이름만으로 계열에 연결됨 | 정확한 해시의 양성 대조군과 미지 해시 원본 보존 반례로 분리 | 미지 참조는 실제로 남아 있지만 추천용 계열은 미확정 |

## 4. 빠짐없이 남겨 두는 후속 작업

아래 순서는 이전 승인 계획을 대체하거나 축약하지 않는다. 이미 완료된 기반을
재구현하지 않고, 연결·검토가 끝나지 않은 부분을 이어가는 순서다.

| ID | 상태 | 다음 작업과 통과 기준 |
|---|---|---|
| M01 | SOURCE_REVIEW_READY | 5000m 두 번째 VO2 방법: 시간형 I 예시를 현행 5000m RP와 구분. 목표 강도 입력 모델·적용 범위·운동 및 모든 회복 경계를 검토 |
| M02 | SOURCE_REVIEW_READY | 12x400m+100m roll-on 등은 별도 방법 후보로 준비. 종료 roll-on과 세트 전환 보존. 고정 짝으로 등록하지 않음 |
| M03 | SOURCE_GAPS_RECORDED | 30행 전체와 중거리 참조의 정확한 회복·대상·주기·조정값 빈칸을 근거 보고의 행별 기준으로 해소. 수치 없는 것은 0으로 바꾸지 않음 |
| M04 | NOT_STARTED | 승인된 후보를 독립 family/configuration으로 등록. 연구 전사, 과학/현장 적용 검토, 실제 runtime 등록을 각각 기록 |
| M05 | FRAME_SCOPE_LIFECYCLE_GATE_PARTIAL | 정확한 프레임·전체 배치·비상세 처방 문맥과 유효기간·철회·검토 참조 검증 구현. 적격 구성은 독립 집합으로 평가. 실제 운영 정책 내용·선택 receipt 영속화·실행 경계 전체 전달은 미완 |
| M06 | SINGLE_DETAIL_TARGET_UI_PARTIAL | A/B별 단일 상세 위치의 독립 적용·취소·재확인 구현(섹션 12). 같은 조건의 서로 다른 2방법과 여러 MAIN의 동시 상세 선택은 미완 |
| M07 | SINGLE_DETAIL_SAVE_LINK_PARTIAL | 단일 상세 위치와 정확한 record anchor의 저장·새로고침 검증. 모든 MAIN의 구성별 원본·조정 receipt 저장과 버전별 복원은 미완 |
| M08 | SOURCE_OFFER_COMMIT_ADAPTER_PARTIAL | onApply receipt 재검사·workspace CAS host·원본/개인 binding·원본 전환 offer/commit adapter 구현(섹션 13, 17, 18). 운영 정책 공급자와 실제 조정 후보 저장 연결은 미완. scalar 범위를 새로 승인하지 않음 |
| M09 | TOTALS_RECEIPT_REVALIDATION_PARTIAL | 구간·회복·총량·시간·설명 binding의 독립 재계산 구현. 실제 채택된 전환의 공개 조정·저장·재조회는 미완 |
| M10 | SAVE_REVALIDATION_AND_REPLAY_PARTIAL | 잠금 후 요청·anchor·안전·템플릿 재검사 및 동일 초기 저장의 무변경 재시도 구현(섹션 14). 다중 키 물리적 원자성·잠금 미사용 탭·실제 조정 전체 연결은 미완 |
| M11 | ACTIVE_AND_ARCHIVED_ORIGINAL_OBSERVATION_PARTIAL | 현재 계획 및 앞으로 보관하는 V5 원본의 정확한 링크로 실제 거리·시간·페이스·RPE 표시. 일지에서 과거 처방 비교 진입 구현. 반복별 측정·추천 소비는 미완 |
| M12 | BOUNDED_ORIGINAL_ARCHIVE_AND_COVERAGE_PARTIAL | 최신 18개 기존 한도 안에서 앞으로 보관하는 원본을 V5로 유지. 구형 요약은 재구성하지 않음. 추천 범위 표시는 여전히 보관 날짜이며 실제 관찰기간 집계·장기 원장은 미완 |
| M13 | NOT_STARTED | 800/1500/3000 및 10K/하프/마라톤의 적격 목적 조합 확장. 각 종목을 개별 통과표로 관리하며 지원하지 않는 페이스를 발명하지 않음 |
| M14 | BASELINE_SEVEN_EVENT_JOURNEY_PARTIAL | 기존 7종목 생성·저장·일지·제한된 archive 시험과 단일 상세 선택 브라우저 검증 완료. 실제 조정 및 다음 주기까지의 전체 여정은 미완 |
| M15 | BASELINE_BROWSER_REVIEW_PARTIAL | 단일 상세 선택의 desktop/320px 및 일부 200% 글자 검증 완료. 375px·키보드·reduced motion·조정·긴 설명 복귀의 전체 조합과 Fable 최종 검수는 미완 |
| M16 | NOT_STARTED | 각 적용 PR의 검수→병합→CI→공개 화면 확인. 단위 테스트 PASS나 연구 문서를 배포/전체 완료로 바꾸지 않음 |

M01~M03은 추가 제품 승인 질문을 반복할 일이 아니라 이미 승인된 조사·검토 작업이다.
다만 원문에 없는 값 또는 별도 검토가 필요한 적용 판단은 작업자가 승인 기록을 지어내지 않는다.
M06~M10의 사용자 기능은 검토 가능한 실제 구성 없이 빈 버튼만 먼저 노출하지 않는다.

## 5. 이번 실행 검증

2026-09-06 실제 로컬 실행 결과다. 이전 PR의 숫자는 재사용하지 않는다.

| 실행 | 결과 | 범위 |
|---|---|---|
| 앱 전체 vitest | 270파일 / 2,327 PASS | 이번 통합 코드. 실패 0 |
| 코어 전체 vitest | 27파일 / 825 PASS | 반복 배치·새 활성화 경계 포함 |
| 앱/코어 TypeScript | PASS | 새 스냅샷·정책 타입 포함 |
| 카탈로그 검증 | PASS | 1 active / 29 draft / 30 total |
| 카탈로그 변조 회귀 | 37 PASS | 출처/활성화/숫자/마커 경계 |
| 브라우저 실제 여정 | 8 PASS | 방법 변경·재확인·날짜 target·저장·새로고침, 4프로젝트 |
| 320px 글자 200% | PASS | 날짜 선택 캡처 확인, 가로 넘침 없음 |
| 출처 표 로컬 대조 | 30행·고유 ID 30·로컬 링크 7 PASS | 과학적 채택 검증 아님 |
| 순수 산수 검사 | 8 PASS | 조건부 회복 합계와 단위 차이 등 |

브라우저 결과는 새로 추가할 다중 방법·조정 UI 검증이 아니다. 기존 단일 상세
방법의 실제 흐름을 검증했다. 선호 선택의 다중 계열 화면은 합성 후보 component
test로 검증했고 운영 카탈로그에 가짜 방법을 넣지 않았다.

- 초기 전체 앱 실행: 2,325 PASS / 1 FAIL. 실패는 미지 과거 해시를 이름만으로
  매핑하던 기존 테스트 단언이었다. 원본 보존 반례를 추가하고 대상 3건 PASS 확인.
- 스냅샷 및 저장 경쟁 테스트: 8 PASS. 비유한 값이 직렬화되는 초기 결함을 발견해
  JSON 스키마 검사를 추가한 뒤 재실행했다.
- 새 배치 정책 초기 코어 전체 실행: 819 PASS. 이후 독립 리뷰 수정은 최종 재실행 대상.
- 이후 리뷰 수정과 활성화 경계 6건을 포함한 최종 재실행은 위 825 PASS다.
- 로컬 build는 성공했으며 기존 font unresolved/chunk size 경고가 있었다.
- 새 제품 기능의 전체 완성, 신규 과학 승인, 병합 및 공개 배포를 이 문서로 주장하지 않는다.

## 6. PR 계약 검사 후속 수정

- PR #319의 첫 head `f0eb9d6`에서 app-quality와 app-browser는 통과했다.
  contract-tests는 출처 정정 후 다른 검증기에 남은 기존 6/9 집계 때문에 실패했다.
- 실제 카탈로그를 기준으로 직접 예시 5개, 변형 예시 10개로 관련 계약과 검증기를
  함께 수정했다. 연구용 노출 15개는 그대로이며 활성화 권한을 추가하지 않았다.
- 두 항목의 등급을 맞바꿔 총개수를 유지하는 변조와 원문 거리 차이 설명 제거를
  차단했다. 해당 회귀 테스트 47/47 PASS, CI 계약 단계의 Node 명령 28개 PASS.
- 첫 명령 추출은 배포 단계 명령까지 포함하여 잘못된 작업 디렉터리에서 종료됐다.
  계약 단계만 한정해 다시 실행한 결과가 위 28개 PASS다. 배포 검증으로 주장하지 않는다.
- 후속 커밋 `d8b2554`의 GitHub CI는 실행 `33977623783` 및 `33977627087`에서
  contract-tests/app-quality/app-browser 모두 PASS를 확인했다. deploy-pages는
  PR에서 skip이며, 아래 M12 추가 코드의 CI 결과는 아니다.

## 7. M12 후속 구현 및 자체 리뷰

- 추천 입력과 범위 설명을 같은 저장소 읽기 결과에서 만든다. 주입한 테스트 이력에는
  로컬의 날짜나 범위를 빌려 붙이지 않는다. 원본 저장 JSON과 18개 보관 상한은 유지한다.
- 방법 선택의 `추천에 참고한 이력`을 열면 보관 계획 수, 동일 종목 계획 수,
  종목 불명 과거 계획, 미기록 결과, 매핑 불명 참조를 볼 수 있다.
- 날짜는 명시적으로 UTC 보관 날짜다. 실제 운동 날짜와 연속 관찰 기간 또는
  24주 분석으로 표시하지 않는다. 진행 중인 계획은 여전히 포함되지 않는다.
- 자체 리뷰에서 읽기 실패를 빈 배열로 처리하던 경계를 발견했다. 새 범위 표시에서는
  `coverage: null`로 구분하고 0회 대신 읽기 실패 안내를 표시한다. 원본은 지우지 않는다.
- 결함 주입: 미기록 대신 완료를 세도록 바꿨을 때 최초 테스트는 두 상태가 각각
  1건이라 실패하지 않았다. 서로 다른 개수로 fixture를 보강한 뒤 다음 두 테스트가
  실제 실패함을 확인하고 정상 코드를 복원했다:
  `separates missing outcomes, unknown references and event coverage without mutating history`,
  `uses the same stored snapshot for recommendation counts and coverage`.
- 정상 복원 후 대상 4파일 28테스트 PASS. 앱 TypeScript, e2e TypeScript, build PASS.
  대상 Playwright 4프로젝트 PASS: desktop/mobile/320px/reduced-motion에서 범위 설명
  열기, 후반 MAIN 선택, 1111초 기록 확인, 저장 및 새로고침을 실행했다.
  320px 캡처를 직접 확인했고 가로 넘침이 없었다.
- 전체 첫 실행은 2,332 PASS / 1 timeout. 실패명은
  `keeps the chosen date when the first plan save fails and the athlete retries`.
  제한 시간 변경 없이 해당 파일을 재실행해 3/3 PASS. 이후 브라우저와 겹치지 않는
  전체 재실행은 271파일 / 2,333 PASS, 실패 0이다. 일회성 timeout의 원인을
  확정하지 않으며 최초 실패 기록도 보존한다.
- 새 방법, 조정 범위, 다중 MAIN 정책 또는 실제 관찰기간은 이번 변경으로 활성화하지 않았다.
  M01~M10 등의 미완 상태를 이 UI 변경으로 완료 처리하지 않는다.

## 8. M10/M11 후속 구현과 독립 검토

### 저장 직전 재확인

- 저장을 기다리는 동안 후보·선수 입력·게이트가 바뀌면 기존 요청을 거부한다.
- 잠금 획득 뒤 일지 기반 안전 상태와 템플릿 권한을 다시 확인한다. 읽기 실패를
  안전한 빈 일지로 바꾸지 않는다. 정상적으로 일지가 없는 경우는 기존 경로를 유지한다.
- 상세 처방의 기록 anchor가 변경·삭제·중복·손상·오래된 상태가 되면 재확인을 요청한다.
  단순 경과일 표시 변화는 처방 변경으로 취급하지 않는다. 숫자를 몰래 재바인딩하지 않는다.
- 독립 검토자의 합성 저장소 테스트에서 새로운 확정 결함은 발견하지 못했다.
  다만 localStorage 다중 키는 물리적 트랜잭션이 아니며 rollback까지 실패하면
  `PLAN_STORAGE_STATE_UNCERTAIN`이다. 동일 잠금을 쓰지 않는 다른 탭까지 직렬화하지 않는다.
  중복 저장 거부는 성공 응답 재생 방식의 idempotency가 아니다.

### 계획에 연결된 실제 일지

- `훈련 방법과 이유 > 주기·기록`에서 현재 계획의 정확한 회차·버전·fingerprint·날짜·
  오전/오후와 맞는 구조화 일지의 직접 입력값을 표시한다. 메모는 읽지 않는다.
- 미기록은 null, 동일 중복은 한 번, 충돌은 수치 표시 보류로 구분한다.
  부분 수행·계획 변경·휴식·건너뜀도 그대로 표시한다. 기존 적격 RPE 비교는 유지한다.
- 반복별 기록과 회복은 측정하지 않았음을 표시한다. 완료 체크를 실제 준수로 바꾸거나
  임의의 에너지 능력 진단·자동 증량에 연결하지 않는다.
- v4 요약 이력으로 과거 처방을 복원하지 않는다. 장기 관찰 원장과 추천 소비는 별도다.

### 이번 통합의 실행 결과

- 앱 전체: 275파일 / 2,390 PASS / 실패 0. 의도적 ErrorBoundary 및 feedback chunk
  실패 로그가 출력되지만 최종 테스트 결과는 통과다.
- 앱 TypeScript 및 build PASS, e2e TypeScript PASS. 기존 font 경로 및 큰 chunk 경고 유지.
- 브라우저 설명 여정: 12 PASS. desktop/mobile/320px/reduced-motion에서 기존·버전 설명,
  정확한 연결값, 새로고침, 복귀 위치, 200% 글자 확대와 가로 넘침을 검사했다.
- 독립 저장 경계 검토: 대상 6파일 69 PASS를 UTC/KST 각각 실행. 부모의 저장+anchor
  13 PASS와 관측 투영+설명 UI 47 PASS도 별도로 실행했다. 같은 테스트를 중복 합산하지 않는다.
- 새 원문 조사 산술: 6 PASS, 결함 주입 5종의 이름별 실패 확인. 실제 D9 실행 또는
  과학적 채택 증거가 아니라 연구 산술과 출력 권한 경계 테스트다.
- 관측 날짜 검사 제거 시 지정된 반례 실패를 확인하고 복원했다. 중간 테스트 작성 오류와
  dialog polyfill 부재는 수정 후 재실행했으며 최초 실패를 최종 통과로 덮어쓰지 않는다.
- 이번 코드의 CI·병합·배포는 별도 확인 대상이다. 이전 head의 CI를 재사용하지 않는다.

### CI 재확인에서 발견한 누락

- `00aeeb6`의 실행 33979209466/33979207533은 contract-tests와 app-quality는 PASS지만
  app-browser가 FAIL이었다. 원인은 방법 선택 테스트의 `summary`가 새 이력 안내까지
  두 개를 잡는 strict locator 오류다. 직접 자식 제목으로 범위를 좁혔다.
- 로컬 재실행에서 다음 실제 UI 결함이 드러났다. 이력 안내 터치 높이가 23.25px로
  44px 기준을 만족하지 않았다. CSS로 최소 높이와 focus-visible 표시를 보완했다.
  기준을 완화하거나 테스트를 삭제하지 않았다. 최초 4실패/4통과 기록은 보존한다.
- 부분 수행과 변경 수행은 동시에 성립할 수 있어 두 상태를 모두 표시하도록 수정했다.
- 최종 수정 후 앱 전체 재실행: 275파일 / 2,390 PASS / 실패 0. build와 e2e TypeScript PASS.
  방법 전환·후반 MAIN 선택·설명/실제 기록의 통합 Playwright 재실행은 20 PASS다.
  320px 및 모바일 실제 수치 화면 캡처를 직접 확인했다. 이 결과는 전체 브라우저 suite가
  아니라 명시된 세 파일, 네 프로젝트 범위다. 새 head의 전체 CI는 별도로 확인한다.

### 다음 실행 순서와 실제 선행 조건

1. [추가 원문 조사와 결정 대안](../research/SESSION_METHOD_SOURCE_GAP_FOLLOWUP_2026-09-06.md)
   §6을 기준으로 목표 페이스 모델·종료 회복·대상 범위·유한 구성 묶음을 필드별로 확정한다.
   사용자에게 모든 세션의 제품 승인 질문을 반복하지 않되 실제 검토자 서명을 생성하지 않는다.
2. M04 독립 family/configuration 등록 후 M05 배치 정책을 정확한 구성에 연결한다.
   고정 A/B 짝과 임의 slider 범위는 만들지 않는다.
3. M06~M09 실제 MAIN별 선택·유한 구성 조정·설명/합계/저장 스냅샷을 연결한다.
4. M10 다중 슬롯 트랜잭션/교차 탭 경로, M11 과거 원본과 실제 수행 연결,
   M12 장기 원장, M13 종목별 범위를 각각 검증한다.
5. M14 실제 사용자 여정, M15 Fable UX 독립 검토, M16 PR 검수 후 병합·배포를 수행한다.

새 템플릿 활성화와 전체 계획 완료는 아직 아니다. 위 순서를 UI 버튼 존재만으로 종결하지 않는다.

## 9. 일지 조회 완전성 후속 수정

- 현재 계획의 설명 화면이 `loadEntries()`의 빈 배열만 받아 저장소 손상과 실제 빈
  일지를 구분하지 못하는 경로를 확인했다. 원본을 지우거나 복원하지 않고 기존의
  계정 범위 필터와 `complete/uncertain` 조회 결과를 보존하는 읽기 경로로 연결했다.
- malformed JSON, 스키마에서 제외된 행, 저장소 읽기 예외는 관측 근거 조회 불가로
  표시한다. 실제 정상 빈 저장소는 연결된 일지 없음으로 계속 표시한다.
- 스펙 §11에 완전한 조회/조회 실패 구분을 명시했다. 처방 숫자, 활성화 범위,
  추천 알고리즘 및 네 OPEN 이슈의 상태는 변경하지 않았다.
- 수정 전 `b3c28e2` 빌드에 새 브라우저 반례를 실행하여 두 건 모두 조회 실패 문구
  단언에서 실패함을 확인했다. 정상 일지와 손상 일지를 구분하지 못하는 실제 반례다.
- 수정 후 대상 unit/component 4파일 23 PASS, 앱 build 및 e2e TypeScript PASS.
  빌드의 기존 font 경로/chunk 크기 경고는 여전히 남아 있다.
- 수정 후 설명 브라우저 1파일/4프로젝트 20 PASS. 손상 두 종류, 정상 관측값,
  새로고침, 기존/버전 설명, 복귀와 글자 확대를 포함하며 원본 계획·일지 보존도 검사했다.
  전체 앱/전체 브라우저 suite 결과를 이 좁은 재실행 수치로 대체하지 않는다.

## 10. 이어 읽을 실제 문서

- [현재 지원 범위](../../TRAINING_PLAN_CURRENT_SCOPE.md)
- [선택·조정 계약](../../specs/reconstruct/SESSION_METHOD_SELECTION_AND_ADJUSTMENT_CONTRACT.md)
- [전체 카탈로그](../../specs/reconstruct/ENERGY_SYSTEM_SESSION_TEMPLATE_CATALOG.md)
- [30행 근거 조사](../research/SESSION_METHOD_PROTOCOL_EVIDENCE_2026-09-06.md)
- [이전 구현 사후 리뷰](../review/SESSION_METHOD_POST_IMPLEMENTATION_REVIEW_2026-09-05.md)

## 11. 날짜별 선택과 적용/취소, 일지 왕복 통합

- 날짜별 후보 카드에서 개인 페이스 적용 대상을 직접 고를 수 있다.
  선택은 초안이며 적용/취소를 분리했다. 현재 범위는 A/B 공유 대상 이동이고,
  후보별 독립 다중 MAIN 편집이라고 부르지 않는다. 다른 훈련을 추가하지 않는다.
- 기존 날짜 선택기도 명시 적용 전까지 생성 결과를 변경하지 않는다.
  시작일/적격 대상/현재 선택이 바뀌면 미적용 초안을 버린다.
  적용 후 기존 기준 기록 재확인과 저장 시 권한 재검사를 유지한다.
- seven-event journey: 800/1500/3000/5000 기존 상세 4개와
  10K/하프/마라톤 RPE 3개를 실제 생성, 저장, 재조회, PARTIAL/MODIFIED
  일지 연결, 제한된 archive까지 검증했다. mutation lock만 시험용 대체이며
  이 7건이 다중 탭 브라우저 검증이나 새 상세 처방 활성화 증거는 아니다.
- 신규/갱신 unit/component 3파일 14 PASS, typecheck와 build PASS.
  desktop-chromium 및 320px touch-narrow 브라우저 2 PASS:
  카드 선택 취소, 다른 날짜 적용, 비가분 기록 1111초 확인,
  정확한 222.2초 저장/새로고침, 가로 넘침 없음, pageerror 없음.
  모바일 200% 글자 확대 캡처를 확인했다. 전체 suite 재실행은 아니다.
- [독립 권한 감사](../review/SESSION_METHOD_REMAINING_AUTHORITY_AUDIT_2026-09-06.md)는
  기존 baseline 연결의 과잉 차단과 신규 활성화의 실제 근거 공백을 구분했다.
  새로운 과학적 승인, Fable UX 승인 또는 전체 계획 완료 증거가 아니다.
- M06/M07/M14는 부분 진척이다. 후보별 독립 초안, 수치 조정 host와 저장 통합,
  신규 구성 및 동시 배치 정책, 새 장기 보존 경계, 최종 검수/배포는 남아 있다.
  다음 엔지니어링은 후보별 초안 격리 및 조정 host의 검증된 전환 연결이다.
  신규 처방값을 만들거나 승인된 기존 단일 상세 경로를 재승인 대기로 돌리지 않는다.

## 12. 후보별 단일 상세 배치 분리

섹션 11의 공유 target만 제공하던 범위를 확장했다. 후보 카드의 적용은
BALANCED/CONSERVATIVE별 독립 target map을 사용한다. 기존 공통 날짜 선택은
두 후보를 함께 고르는 명시 동작으로 유지하며, 독립 편집 이후에는 숨긴다.
한 후보의 선택은 다른 후보의 세션, 처방 수치 및 candidate identity를
변경하지 않는다. 두 후보를 묶는 pair identity는 새 내용에 맞춰 갱신한다.

- 스펙 섹션 4에 최초 수동 선택과 자동 support-only 적응의 경계를 명시했다.
- 실제 반례: 기존 selection 검증기가 독립 target 후보를
  STALE_CANDIDATE_FINGERPRINT로 거부했다. 최초 선택용 검증만 분리했다.
  정확히 한 상세 처방씩, 동일한 MAIN 처방 집합, 동일 layout와 support
  차이 규칙을 요구한다. 기존 자동 적응 검증은 그대로 남겼다.
- 적용/취소 전에는 해당 후보 확정을 막는다. 공통 target 초안도 미적용
  상태에서 최종 계획을 확정하지 못한다. 후보 UI는 변경되는 hash가 아닌
  후보 종류를 기준으로 펼침 상태를 유지한다.
- candidate 정렬이 달라도 scope/slot identity는 유지하며,
  content snapshot 변경 검사는 계속 별도로 수행한다.
- 신규 반례는 잘못된 주소, 재해시한 용량 변조, 다른 후보 오염,
  자동 support-only 적응으로의 우회, candidate 순서 변경을 포함한다.
- 관련 app 5파일 25 PASS. impl 전체 27파일 825 PASS.
  타입 검사, e2e 타입 검사, build PASS. 기존 font/chunk 경고는 유지된다.
- 브라우저 6 PASS: 공통 target, A만 편집, B만 편집의 세 경로를
  desktop-chromium/320px touch-narrow에서 확인했다. 기록 확인, 수락,
  실제 저장/새로고침, 222.2초 원본 보존과 가로 넘침 없음이 포함된다.
- 전체 앱 기본 동시성 실행은 2399 PASS/13 FAIL이었다. 12건은 5초 timeout,
  한 건은 archive 화면 질의 실패였다. --maxWorkers=4 재실행 결과는 아래에
  별도 기록하며 실패를 지우거나 과거 CI 결과로 대체하지 않는다.
- 동일 작업트리의 --maxWorkers=4 재실행은 279파일/2412 PASS,
  exit 0으로 완료했다. timeout/단언 변경 없이 동시성만 변경했다.

M06/M07/M10의 단일 상세 배치 연결이 진척되었으며 전체 완료는 아니다.
다음: 기존 조정 engine/editor의 candidate transaction host, 현재 상태와
receipt 재검사, 단일 snapshot commit 및 저장 계층 결속. 실제 신규 dose
전환은 검토된 registry가 있을 때만 활성화하고 시험용 권한을 운영에 넣지 않는다.
기존 네 ref의 청소년/자율 사용자 범위는 그대로 유지한다.

## 13. 조정 적용 host와 단일 snapshot commit 기반

- `revalidateAdjustmentReceipt`가 전달된 before/after, 모든 합계/delta와
  구조 차이를 원래 동작 시점 및 현재 시점의 독립 registry로 재검산한다.
  원래 명시 적용 시각을 현재 시각으로 바꿔 쓰지 않는다.
- `prescription-adjustment-commit.ts`는 후보 계보/MAIN slot/context/revision을
  묶은 workspace snapshot을 CAS 한 번으로 교체한다. 처방, 설명 버전과
  근거 참조, receipt가 한 snapshot에 들어가며 부분 필드 저장을 하지 않는다.
  실제 CAS/lock 구현은 소비 저장소의 책임이며 이 모듈이 localStorage의
  다중 키 원자성을 제공한다고 주장하지 않는다.
- `PrescriptionAdjustmentCommitHost`는 기존 editor의 적용/취소를 이
  controller에 연결한다. 열 때의 base revision을 유지하고, 실패한 draft는
  재시도할 수 있다. 화면 unmount/취소는 대기 중인 write를 무효화한다.
- 안전/context/권한 변경을 write lock 안에서 재확인한다. 중복 적용은
  한 요청으로 합치고, 저장 후 응답 유실은 정확한 receipt를 재검사하여
  write 없이 replay한다. getter 및 허용되지 않은 메모 필드의 유입을 거부한다.
- 합성 registry를 사용하는 app 연결 시험 42 PASS, impl 전체 831 PASS.
  이 수치는 실제 선수를 대상으로 한 처방 검증이나 신규 source 채택이 아니다.
  타입 검사와 build 결과는 아래 실행 기록에 별도로 보존한다.
- 최종 타입 검사/build PASS. 기존 font 경로 및 chunk 크기 경고는 남아 있다.
  [자체 검토 보고서](../review/ADJUSTMENT_COMMIT_HOST_SELF_REVIEW_2026-09-06.md)에
  공격 검토 수정점과 production 연결에서 아직 검증되지 않은 범위를 남겼다.
- **아직 public `PlanBeta`에서 이 host를 열지 않는다.** 실제 적격 전환이
  없는 버튼을 노출하지 않았다. 기존 PACE_TARGET 저장 스키마를 generic
  조정 snapshot으로 대체하지 않았으며, 시험용 권한/용량은 운영 manifest에
  들어가지 않았다. 따라서 M08/M09 공통 host 기반은 구현됐지만 실제 공개
  후보 저장·재조회와 신규 구성 활성화가 완료된 것은 아니다.
- 다음 실제 작업: source configuration과 개인 기록으로 해석된 sequence의
  identity를 구분한 production offer/adapter, 버전별 저장 계약과 정확한
  후보 투영 검증. 현재 4개 고정 baseline에 새 용량을 덧씌우지 않는다.

## 14. 초기 계획 저장의 정확한 재시도

- 저장 성공 후 응답만 놓친 동일 요청은 기존 snapshot을 그대로 반환한다.
  새 generatedAt/주기 계보/설명 receipt를 만들거나 계획·context를 다시 쓰지 않는다.
  기존 저장소 사용 가능 여부를 확인하는 `__to_probe__` 임시 점검은 유지한다.
- 현재 계정·초안·안전·기록·템플릿 권한을 확인한 뒤, 원래 저장 시각으로
  재구성한 전체 예상 상태와 실제 저장 상태를 비교한다. 날짜·후보·근거 변경,
  이미 기록한 진행 상태, 미래 저장 시각은 재시도로 인정하지 않는다.
- 필요한 adaptation context가 없거나 손상되거나 다른 후보를 가리키면
  성공으로 반환하거나 자동 복구하지 않고 `PLAN_STORAGE_STATE_UNCERTAIN`을 유지한다.
- 재시도 회귀 18 PASS. 재시도 성공 반환을 고의로 제거한 변조 실행은
  양성 5건이 모두 실패했다(13건 필터 제외). 변조는 즉시 원복했다.
- 기존 동시 저장 공격 시험 1건의 기대값은 동일 요청 거부에서 동일 결과 반환으로
  변경했다. 원본 저장 내용 불변 검사는 유지한다. 이 변경을 한 뒤 최종 재실행한다.
- 새 빌드의 실제 화면 6 PASS: desktop/320px에서 공유 target, A만, B만의
  선택·기록 확인·저장·새로고침, 원본 222.2초와 슬롯 유지, 가로 넘침 및 pageerror 없음.
- 추가 실제 두 탭 시험 4 PASS: desktop/320px의 동일 선택 재시도 및 날짜가
  다른 선택 거부. 같은 browser context의 두 탭, 실제 Web Locks와 localStorage를
  사용했다. 첫 저장 뒤 두 번째 요청이 전체 계획/context를 바꾸지 않음을 확인했다.
  처음 2건 실행의 1 FAIL은 새로고침이 홈으로 돌아가는 실제 동작을 시험에서
  누락한 것이었다. 계획 탭으로 재진입하도록 시험을 고친 뒤 4건 모두 통과했다.
  저장 도중 브라우저 강제 종료나 네트워크 응답 유실을 재현한 것은 아니다.
- 타입 검사/build/e2e 타입 검사 PASS. 전체 앱 최종 실행은
  `--maxWorkers=4`에서 282파일 / 2454 PASS, exit 0이다.
  첫 전체 실행의 2453 PASS / 옛 기대값 1 FAIL도 위 검토 보고서에 남겼다.

신규 훈련 구성/전환 승인, host의 public 연결, 다중 키 원자성, 전체 계획 완료,
PR 병합 또는 공개 배포를 이 패킷의 결과로 주장하지 않는다.

## 15. V5 원본 보관과 일지에서의 비교

- 앞으로 보관하는 계획은 기존 최신 18개 한도 안에서 V5 원본과 요약을 함께
  저장한다. 수동 보관·승인된 다음 계획 시작 모두 연결했고, 후자의 요약은 기존
  표시 프레임 범위를 유지한다. 원본 날짜·오전/오후·처방·계보는 별도로 보존한다.
- 구형 V3/V4 요약에는 원본을 만들어 넣지 않는다. 읽기 실패·미보관 원본을
  구분하고, 최근 계획을 예전 일지에 대신 붙이지 않는다. 신규 서버 전송은 없다.
- 일지에서 `계획한 훈련과 비교하기`를 열면 실제로 연결된 원본의 방법·이유·
  주기·기록을 볼 수 있다. 직접 입력 수치·부분 수행·변경 수행·미기록을 구분하며
  반복별 측정이나 수행 준수 판정은 만들어내지 않는다.
- 계정·storage 이벤트·일지 link 변경 시 열린 비교를 닫는다. 설명에서 돌아오면
  일지 진입 버튼에 포커스와 위치가 복원된다. 메모 원문은 비교 입력에 사용하지 않는다.
- 손상된 과거 이력을 빈 목록으로 덮어쓰던 보관 경계를 함께 수정했다. 기존
  저장 실패 rollback과 보관 한도·계정별 격리를 유지한다.
- 최종 전체 앱 284파일 / 2473 PASS. 대상 원본/UI/successor 35 PASS.
  타입 검사/build/e2e 타입 검사 PASS. 실제 보관→일지→원본 비교와 기존
  계획→일지 작성 흐름 8 PASS(desktop/mobile/320px/reduced-motion).
  확대 제목 잘림 의심은 실제 글자 좌표 시험에서 미재현으로 판정했다.
- 요약/hash 검사를 끈 결함 주입에서 2건이 실제 실패했고 정상 코드를 복원했다.
  상세 실패·수정·미재현 기록은
  [원본 보관 검토 보고서](../review/ARCHIVED_ORIGINAL_PLAN_JOURNAL_REVIEW_2026-09-06.md)에 있다.

M11/M12는 앞으로 보관하는 정확한 원본의 일지 비교까지 진척되었다. 장기 관찰
기간·반복별 측정·신규 추천 소비, 다른 방법/조정값/다중 MAIN 활성화와 전체
독립 검수·병합·배포는 여전히 별도 작업이며 전체 계획 완료가 아니다.

## 16. 다중 MAIN 배치 검토의 범위와 수명

- 계약 v0.4와 코어 검증에 정확한 주기·오전/오후 전체 배치·보조 처방 문맥,
  유효기간·철회 상태·노출/상호작용/안전 검토 참조를 연결했다. 시각은 호출자가
  명시하며 누락된 만료값을 무기한 승인으로 해석하지 않는다.
- 같은 목적의 독립 구성 집합을 검증한다. 특정 A/B 짝을 등록하거나 새로운
  반복 수·회복 시간·선수 적용 범위를 만들지 않았다. 실제 운영 배치 정책은 0건이다.
- 개인 기록·계산된 페이스·문구는 주기 범위 지문과 구분한다. 원본 상세 처방의
  숫자·템플릿 검증을 대신하지 않는다. 기존 단일 상세와 과거 계획 읽기를 유지한다.
- 정상 코드 재실행: 대상 58 PASS, 코어 전체 859 PASS, 코어 타입 및 앱 build PASS.
  만료 검사 제거는 1 FAIL, 범위 검사 제거는 3 FAIL로 결함 검출을 확인하고 복원했다.
- 실제 desktop/320px 선택·확인·저장·새로고침 6 PASS. 최종 전체 앱 실행은
  284파일 2473 PASS, 실패 0이다. PR 전달 결과는 같은 PR #319에 기록한다.
- M05는 부분 구현이다. 실제 운영 정책 수용, 정책 identity/선택 receipt 저장,
  저장·복원·실행·다음 주기 경계 전체의 평가 문맥 연결은 남아 있다.
  [범위와 유효기간 검토 보고서](../review/MAIN_PLACEMENT_POLICY_SCOPE_REVIEW_2026-09-06.md)

## 17. 원본 구성과 개인 목표값 연결

- 추천 목록의 source sequence 생성을 공통 `readPlanMethodDefinition`으로 옮겼다.
  기존 4개 정확한 ref의 본운동 구조를 읽으며 개인 기록·anchor ID는 포함하지 않는다.
  source configuration 지문과 독립 family/configuration 매핑을 함께 반환한다.
- `resolvePlanMethodPrescription`은 기존 저장 스키마로 숫자·원본 manifest·sequence를
  먼저 검증한다. 이어 source configuration, 전체 개인 처방 지문, anchor 내용 지문,
  반올림하지 않은 목표 초, 실제 sequence, 설명 버전·내용 지문·근거를 구분해 묶는다.
- 실제 후보 생성은 이 연결이 없으면 기존 시간·RPE 후보로 되돌린다. 임의의 숫자를
  다시 쓰거나 저장 검증을 완화하지 않는다. 권한/안전/기록 검사는 기존 경로가 담당한다.
- 과거 sequence 없는 처방은 읽기용 투영만 만들고 원본·저장 fingerprint를 수정하지
  않는다. 반환값은 로컬 조정 adapter용이며 공개 export나 새 저장 필드로 추가하지 않았다.
- 대상 3파일 50 PASS. 소수 초 4종목, 서로 다른 기록의 동일 source identity,
  설명 내용 변경, 수치/회복 변조, getter/추가 메모 필드, registry 원본 보존을 검사했다.
  후보 연결 검사를 끄면 2 FAIL로 실제 검출됨을 확인하고 정상 코드로 복원했다.
- 최종 전체 앱 285파일 2492 PASS, 실제 desktop/320px 4종목 생성·저장·새로고침
  및 위치 선택 12 PASS. app/e2e 타입과 build 및 두 계약 validator PASS.
  [원본·개인 계산 연결 검토 보고서](../review/SOURCE_RESOLVED_METHOD_BINDING_REVIEW_2026-09-06.md)
- 남은 것은 이 binding을 사용하는 실제 허용 전환 offer, source edge에서 개인화된
  before/after를 재현하는 adapter, 버전별 조정 저장 및 사용자 조정 여정이다.
  source/resolved 구분만으로 M08 전체 완료나 새 조정 활성화를 주장하지 않는다.

## 18. 원본 조정 전환과 저장 절차 연결

- `prepareSourceAdjustmentOffer`는 현재 원본에서 직접 허용된 모든 전환만
  가져온다. A/B 고정 짝, 역방향·연쇄 변경 허용, 자기 자신으로 바꾸는 가짜
  선택지는 만들지 않는다. 원본의 정책 수명과 근거 참조를 그대로 유지한다.
- 같은 종목의 명시적 RACE_PACE placeholder에 현재 record anchor 참조를
  연결하고 현재 후보/슬롯 revision을 별도 문맥 지문으로 묶는다. 개인 목표 초
  계산·거리/시간 환산·회복값 변경을 이 모듈에서 새로 수행하지 않는다.
- `createSourceAdjustmentCommitAdapter`는 기존 controller/host와 연결한다.
  설명을 정확한 원본 구성에서 가져와 resolved 구성에 결속한다. 운영자가
  제공하는 현재 계정·안전·후보 허용 판정을 진입과 저장 잠금 안에서 재검사한다.
- 저장 직전 원본 authority로 offer/receipt를 다시 계산한다. 기다리는 사이의
  만료·철회·후보 revision·허용 상태 변경은 저장 0건과 기존 snapshot 보존으로
  처리한다. 같은 명시 요청의 재시도는 한 번만 저장한다.
- 합성 구성으로 연결 대상 3파일 50 PASS를 직접 확인했다. 실제 운영 조정값,
  개인 새 페이스, 운영 정책은 추가하지 않았다. owner가 제공할 실제 저장소의
  잠금/원자성이나 공개 조정 여정의 완료를 이 결과로 주장하지 않는다.
- 전체 앱 286파일 / 2,518 PASS, TypeScript 및 build PASS. revision 결속을
  제거하면 2 FAIL로 검출됐고, 정상 복원 후 대상 50 PASS를 재확인했다.
- 전체 회귀 결과와 추가 검수는
  [원본 조정 연결 검토 보고서](../review/SOURCE_ADJUSTMENT_COMMIT_ADAPTER_REVIEW_2026-09-06.md)에 기록한다.

다음 순서는 실제 후보 저장 형식과 연결할 versioned adapter, 운영 정책 공급자,
정확한 구성 채택, 사용자 조정·복원·일지 비교의 종단 검증이다. M01~M16의
미완 항목을 삭제하거나 전체 완료로 바꾸지 않는다.

[DRAFT_COMPLETE]
