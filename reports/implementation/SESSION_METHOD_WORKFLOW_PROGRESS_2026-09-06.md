# SESSION_METHOD_WORKFLOW_PROGRESS_2026-09-06.md

```yaml
doc_id: trainoracle-session-method-workflow-progress-2026-09-06
status: IMPLEMENTED_FOUNDATIONS_WITH_OPEN_WORKFLOW_GATES
base_main: 985669328dbcc7738afc9f390c9c325769b8251c
branch: codex/method-workflow-completion
whole_workflow_complete: false
new_template_activations: 0
new_scalar_adjustment_policies: 0
new_multi_main_runtime_policies: 0
canonical_promotion: false
issue_closure: false
```

## 1. 이번 작업의 의미

사용자가 승인한 상세 훈련 선택·조정·일지 연결 계획을 수행한다. 이전 PR #318의
기반 구현을 전체 기능 완료로 부르지 않는다. 새 방법을 조사하는 일, 실제로 선택하게
하는 일, 조정된 숫자를 저장하는 일, 수행 결과를 해석하는 일은 각각 완료 증거가 필요하다.

현재 공개된 네 가지 정확한 상세 템플릿과 기존 시간·RPE 계획은 유지한다.
이번 변경은 새 훈련량·페이스·회복값·성별 또는 연령별 계수를 만들지 않는다.
일지 메모를 읽지 않으며 기존 청소년·혼자 훈련하기의 승인 범위를 축소하지 않는다.

## 2. 실제 변경

| 구역 | 이번 산출물 | 정확한 상태 |
|---|---|---|
| 스펙 | 선택·조정 계약 v0.2, 현재 범위, 준비·구현 보고의 상충 문구 정리 | 문서 패치 완료. 정본 승격 아님 |
| 출처 | 전체 30행과 별도 중거리 참조, 추가 원문을 대조한 근거 보고 | 원문 확보 수준·누락값을 행별로 기록. 전부 활성화 가능한 상태는 아님 |
| 출처 정정 | LT-SEED-03의 1600m를 원문 직접 예시로 부르던 라벨 수정 | 실제 원문은 1 mile. 초안 숫자는 그대로 보존 |
| 추천 | 중립 기본값, 선택적 다양성/반복 선호, 선택 유지 | 실제 옵션 resolver와 후보 화면 연결. 두 계열 이상일 때만 선호 조작 표시 |
| 정체성 | 네 개 정확한 ID/버전/해시의 명시적 family/configuration 매핑 | 이름만 바뀐 방법을 새 계열로 부풀리지 않음. 알 수 없는 과거 참조는 추정하지 않음 |
| 이력 | 자기보고 완료와 실제 수행 준수의 의미 구분 | 원본 과거 참조·저장 JSON 보존. 실제 워치 준수 데이터 연결은 별도 |
| 배치 | 후보 다양성과 프레임 내 반복 배치 정책 분리 | 명시적 정책·실제 경험 문맥이 없으면 새로운 다중 배치는 거부 |
| 호환성 | 기존 V3 다중 구성 읽기와 새로운 선택/다음 주기 적용 구분 | 읽기는 보존하되 과거 저장 가능성이 새 실행 권한이 되지 않음 |
| 저장 | MAIN별 안정된 식별자와 동결된 초안 스냅샷 | 실제 계획 저장 잠금 안에서 재비교. 오래된 내용이나 수정 중 초안 저장 거부 |

MAIN 식별자는 날짜·프레임·종목·목적·경험·실제 슬롯 배치에서 생성한다.
방법·페이스·시간만 바뀌면 슬롯 정체성은 유지하고 내용 지문만 바뀐다.
날짜나 프레임이 바뀌면 과거 슬롯을 같은 운동으로 자동 이식하지 않는다.
스냅샷은 저장 경계의 검증 정보이며 새 영속 저장 버전이나 다중 선택 UI 완료를 뜻하지 않는다.

## 3. 독립 리뷰와 수정

| 발견 | 수정 | 남은 경계 |
|---|---|---|
| legacy 읽기 규칙이 새 successor에도 적용될 수 있음 | 일반 parser는 호환 읽기, 새 선택과 successor에는 현재 정책 검사 | 실제 운영 다중 배치 정책은 여전히 0건 |
| 정책이 실제 경험 대신 처방의 scope만 비교 | 다중 배치 시 실제 경험 입력이 없거나 정책과 다르면 거부 | 다음 통합은 검증된 intake 문맥을 모든 경로에 전달해야 함 |
| caller가 revision 갱신을 놓치면 저장 대기 중 변경 누락 | 잠금 획득 후 전체 후보 내용 지문 재검사 | 잠금·계정 범위·안전 재검사는 기존 경로도 함께 유지 |
| 과거 테스트의 가짜 해시가 이름만으로 계열에 연결됨 | 정확한 해시의 양성 대조군과 미지 해시 원본 보존 반례로 분리 | 미지 참조는 실제로 남아 있지만 추천용 계열은 미확정 |

## 4. 빠짐없이 남겨 두는 후속 작업

아래 순서는 이전 승인 계획을 대체하거나 축약하지 않는다. 이미 완료된 기반을
재구현하지 않고, 연결·검토가 끝나지 않은 부분을 이어가는 순서다.

| ID | 상태 | 다음 작업과 통과 기준 |
|---|---|---|
| M01 | SOURCE_REVIEW_READY | 5000m 두 번째 VO2 방법: 시간형 I 예시를 현행 5000m RP와 구분. 목표 강도 입력 모델·적용 범위·운동 및 모든 회복 경계를 검토 |
| M02 | SOURCE_REVIEW_READY | 12x400m+100m roll-on 등은 별도 방법 후보로 준비. 종료 roll-on과 세트 전환 보존. 고정 짝으로 등록하지 않음 |
| M03 | SOURCE_GAPS_RECORDED | 30행 전체와 중거리 참조의 정확한 회복·대상·주기·조정값 빈칸을 근거 보고의 행별 기준으로 해소. 수치 없는 것은 0으로 바꾸지 않음 |
| M04 | NOT_STARTED | 승인된 후보를 독립 family/configuration으로 등록. 연구 전사, 과학/현장 적용 검토, 실제 runtime 등록을 각각 기록 |
| M05 | FRAME_SCOPE_LIFECYCLE_GATE_PARTIAL | 정확한 프레임·전체 배치·비상세 처방 문맥과 유효기간·철회·검토 참조 검증 구현. 적격 구성은 독립 집합으로 평가. 실제 운영 정책 내용·선택 receipt 영속화·실행 경계 전체 전달은 미완 |
| M06 | SINGLE_DETAIL_TARGET_UI_PARTIAL | A/B별 단일 상세 위치의 독립 적용·취소·재확인 구현(섹션 12). 같은 조건의 서로 다른 2방법과 여러 MAIN의 동시 상세 선택은 미완 |
| M07 | ACTIVE_SELECTION_AND_SESSION_DRAFT_RESTORE_PARTIAL | 단일 상세 위치/record anchor의 활성 계획 저장·복원과 별도 계정별 임시 조정 초안의 버전/receipt 복원 구현(섹션 19). 모든 MAIN의 조정 결과를 확정 계획으로 저장하는 경로는 미완 |
| M08 | SOURCE_OFFER_COMMIT_ADAPTER_PARTIAL | onApply receipt 재검사·workspace CAS host·원본/개인 binding·원본 전환 offer/commit adapter 구현(섹션 13, 17, 18). 운영 정책 공급자와 실제 조정 후보 저장 연결은 미완. scalar 범위를 새로 승인하지 않음 |
| M09 | TOTALS_RECEIPT_REVALIDATION_PARTIAL | 구간·회복·총량·시간·설명 binding의 독립 재계산 구현. 실제 채택된 전환의 공개 조정·저장·재조회는 미완 |
| M10 | SAVE_REVALIDATION_AND_REPLAY_PARTIAL | 잠금 후 요청·anchor·안전·템플릿 재검사 및 동일 초기 저장의 무변경 재시도 구현(섹션 14). 다중 키 물리적 원자성·잠금 미사용 탭·실제 조정 전체 연결은 미완 |
| M11 | ACTIVE_AND_ARCHIVED_ORIGINAL_OBSERVATION_PARTIAL | 현재 계획 및 앞으로 보관하는 V5 원본의 정확한 링크로 실제 거리·시간·페이스·RPE 표시. 일지에서 과거 처방 비교 진입 구현. 반복별 측정·추천 소비는 미완 |
| M12 | BOUNDED_ORIGINAL_ARCHIVE_AND_COVERAGE_PARTIAL | 최신 18개 기존 한도 안에서 앞으로 보관하는 원본을 V5로 유지. 구형 요약은 재구성하지 않음. 추천 범위 표시는 여전히 보관 날짜이며 실제 관찰기간 집계·장기 원장은 미완 |
| M13 | NOT_STARTED | 800/1500/3000 및 10K/하프/마라톤의 적격 목적 조합 확장. 각 종목을 개별 통과표로 관리하며 지원하지 않는 페이스를 발명하지 않음 |
| M14 | BASELINE_SEVEN_EVENT_JOURNEY_PARTIAL | 기존 7종목 생성·저장·일지·제한된 archive 시험과 단일 상세 선택 브라우저 검증 완료. 실제 조정 및 다음 주기까지의 전체 여정은 미완 |
| M15 | BASELINE_BROWSER_REVIEW_PARTIAL | 단일 상세 선택의 desktop/320px 및 일부 200% 글자 검증 완료. 375px·키보드·reduced motion·조정·긴 설명 복귀의 전체 조합과 Fable 최종 검수는 미완 |
| M16 | NOT_STARTED | 각 적용 PR의 검수→병합→CI→공개 화면 확인. 단위 테스트 PASS나 연구 문서를 배포/전체 완료로 바꾸지 않음 |

M01~M03은 추가 제품 승인 질문을 반복할 일이 아니라 이미 승인된 조사·검토 작업이다.
다만 원문에 없는 값 또는 별도 검토가 필요한 적용 판단은 작업자가 승인 기록을 지어내지 않는다.
M06~M10의 사용자 기능은 검토 가능한 실제 구성 없이 빈 버튼만 먼저 노출하지 않는다.

## 5. 이번 실행 검증

2026-09-06 실제 로컬 실행 결과다. 이전 PR의 숫자는 재사용하지 않는다.

| 실행 | 결과 | 범위 |
|---|---|---|
| 앱 전체 vitest | 270파일 / 2,327 PASS | 이번 통합 코드. 실패 0 |
| 코어 전체 vitest | 27파일 / 825 PASS | 반복 배치·새 활성화 경계 포함 |
| 앱/코어 TypeScript | PASS | 새 스냅샷·정책 타입 포함 |
| 카탈로그 검증 | PASS | 1 active / 29 draft / 30 total |
| 카탈로그 변조 회귀 | 37 PASS | 출처/활성화/숫자/마커 경계 |
| 브라우저 실제 여정 | 8 PASS | 방법 변경·재확인·날짜 target·저장·새로고침, 4프로젝트 |
| 320px 글자 200% | PASS | 날짜 선택 캡처 확인, 가로 넘침 없음 |
| 출처 표 로컬 대조 | 30행·고유 ID 30·로컬 링크 7 PASS | 과학적 채택 검증 아님 |
| 순수 산수 검사 | 8 PASS | 조건부 회복 합계와 단위 차이 등 |

브라우저 결과는 새로 추가할 다중 방법·조정 UI 검증이 아니다. 기존 단일 상세
방법의 실제 흐름을 검증했다. 선호 선택의 다중 계열 화면은 합성 후보 component
test로 검증했고 운영 카탈로그에 가짜 방법을 넣지 않았다.

- 초기 전체 앱 실행: 2,325 PASS / 1 FAIL. 실패는 미지 과거 해시를 이름만으로
  매핑하던 기존 테스트 단언이었다. 원본 보존 반례를 추가하고 대상 3건 PASS 확인.
- 스냅샷 및 저장 경쟁 테스트: 8 PASS. 비유한 값이 직렬화되는 초기 결함을 발견해
  JSON 스키마 검사를 추가한 뒤 재실행했다.
- 새 배치 정책 초기 코어 전체 실행: 819 PASS. 이후 독립 리뷰 수정은 최종 재실행 대상.
- 이후 리뷰 수정과 활성화 경계 6건을 포함한 최종 재실행은 위 825 PASS다.
- 로컬 build는 성공했으며 기존 font unresolved/chunk size 경고가 있었다.
- 새 제품 기능의 전체 완성, 신규 과학 승인, 병합 및 공개 배포를 이 문서로 주장하지 않는다.

## 6. PR 계약 검사 후속 수정

- PR #319의 첫 head `f0eb9d6`에서 app-quality와 app-browser는 통과했다.
  contract-tests는 출처 정정 후 다른 검증기에 남은 기존 6/9 집계 때문에 실패했다.
- 실제 카탈로그를 기준으로 직접 예시 5개, 변형 예시 10개로 관련 계약과 검증기를
  함께 수정했다. 연구용 노출 15개는 그대로이며 활성화 권한을 추가하지 않았다.
- 두 항목의 등급을 맞바꿔 총개수를 유지하는 변조와 원문 거리 차이 설명 제거를
  차단했다. 해당 회귀 테스트 47/47 PASS, CI 계약 단계의 Node 명령 28개 PASS.
- 첫 명령 추출은 배포 단계 명령까지 포함하여 잘못된 작업 디렉터리에서 종료됐다.
  계약 단계만 한정해 다시 실행한 결과가 위 28개 PASS다. 배포 검증으로 주장하지 않는다.
- 후속 커밋 `d8b2554`의 GitHub CI는 실행 `33977623783` 및 `33977627087`에서
  contract-tests/app-quality/app-browser 모두 PASS를 확인했다. deploy-pages는
  PR에서 skip이며, 아래 M12 추가 코드의 CI 결과는 아니다.

## 7. M12 후속 구현 및 자체 리뷰

- 추천 입력과 범위 설명을 같은 저장소 읽기 결과에서 만든다. 주입한 테스트 이력에는
  로컬의 날짜나 범위를 빌려 붙이지 않는다. 원본 저장 JSON과 18개 보관 상한은 유지한다.
- 방법 선택의 `추천에 참고한 이력`을 열면 보관 계획 수, 동일 종목 계획 수,
  종목 불명 과거 계획, 미기록 결과, 매핑 불명 참조를 볼 수 있다.
- 날짜는 명시적으로 UTC 보관 날짜다. 실제 운동 날짜와 연속 관찰 기간 또는
  24주 분석으로 표시하지 않는다. 진행 중인 계획은 여전히 포함되지 않는다.
- 자체 리뷰에서 읽기 실패를 빈 배열로 처리하던 경계를 발견했다. 새 범위 표시에서는
  `coverage: null`로 구분하고 0회 대신 읽기 실패 안내를 표시한다. 원본은 지우지 않는다.
- 결함 주입: 미기록 대신 완료를 세도록 바꿨을 때 최초 테스트는 두 상태가 각각
  1건이라 실패하지 않았다. 서로 다른 개수로 fixture를 보강한 뒤 다음 두 테스트가
  실제 실패함을 확인하고 정상 코드를 복원했다:
  `separates missing outcomes, unknown references and event coverage without mutating history`,
  `uses the same stored snapshot for recommendation counts and coverage`.
- 정상 복원 후 대상 4파일 28테스트 PASS. 앱 TypeScript, e2e TypeScript, build PASS.
  대상 Playwright 4프로젝트 PASS: desktop/mobile/320px/reduced-motion에서 범위 설명
  열기, 후반 MAIN 선택, 1111초 기록 확인, 저장 및 새로고침을 실행했다.
  320px 캡처를 직접 확인했고 가로 넘침이 없었다.
- 전체 첫 실행은 2,332 PASS / 1 timeout. 실패명은
  `keeps the chosen date when the first plan save fails and the athlete retries`.
  제한 시간 변경 없이 해당 파일을 재실행해 3/3 PASS. 이후 브라우저와 겹치지 않는
  전체 재실행은 271파일 / 2,333 PASS, 실패 0이다. 일회성 timeout의 원인을
  확정하지 않으며 최초 실패 기록도 보존한다.
- 새 방법, 조정 범위, 다중 MAIN 정책 또는 실제 관찰기간은 이번 변경으로 활성화하지 않았다.
  M01~M10 등의 미완 상태를 이 UI 변경으로 완료 처리하지 않는다.

## 8. M10/M11 후속 구현과 독립 검토

### 저장 직전 재확인

- 저장을 기다리는 동안 후보·선수 입력·게이트가 바뀌면 기존 요청을 거부한다.
- 잠금 획득 뒤 일지 기반 안전 상태와 템플릿 권한을 다시 확인한다. 읽기 실패를
  안전한 빈 일지로 바꾸지 않는다. 정상적으로 일지가 없는 경우는 기존 경로를 유지한다.
- 상세 처방의 기록 anchor가 변경·삭제·중복·손상·오래된 상태가 되면 재확인을 요청한다.
  단순 경과일 표시 변화는 처방 변경으로 취급하지 않는다. 숫자를 몰래 재바인딩하지 않는다.
- 독립 검토자의 합성 저장소 테스트에서 새로운 확정 결함은 발견하지 못했다.
  다만 localStorage 다중 키는 물리적 트랜잭션이 아니며 rollback까지 실패하면
  `PLAN_STORAGE_STATE_UNCERTAIN`이다. 동일 잠금을 쓰지 않는 다른 탭까지 직렬화하지 않는다.
  중복 저장 거부는 성공 응답 재생 방식의 idempotency가 아니다.

### 계획에 연결된 실제 일지

- `훈련 방법과 이유 > 주기·기록`에서 현재 계획의 정확한 회차·버전·fingerprint·날짜·
  오전/오후와 맞는 구조화 일지의 직접 입력값을 표시한다. 메모는 읽지 않는다.
- 미기록은 null, 동일 중복은 한 번, 충돌은 수치 표시 보류로 구분한다.
  부분 수행·계획 변경·휴식·건너뜀도 그대로 표시한다. 기존 적격 RPE 비교는 유지한다.
- 반복별 기록과 회복은 측정하지 않았음을 표시한다. 완료 체크를 실제 준수로 바꾸거나
  임의의 에너지 능력 진단·자동 증량에 연결하지 않는다.
- v4 요약 이력으로 과거 처방을 복원하지 않는다. 장기 관찰 원장과 추천 소비는 별도다.

### 이번 통합의 실행 결과

- 앱 전체: 275파일 / 2,390 PASS / 실패 0. 의도적 ErrorBoundary 및 feedback chunk
  실패 로그가 출력되지만 최종 테스트 결과는 통과다.
- 앱 TypeScript 및 build PASS, e2e TypeScript PASS. 기존 font 경로 및 큰 chunk 경고 유지.
- 브라우저 설명 여정: 12 PASS. desktop/mobile/320px/reduced-motion에서 기존·버전 설명,
  정확한 연결값, 새로고침, 복귀 위치, 200% 글자 확대와 가로 넘침을 검사했다.
- 독립 저장 경계 검토: 대상 6파일 69 PASS를 UTC/KST 각각 실행. 부모의 저장+anchor
  13 PASS와 관측 투영+설명 UI 47 PASS도 별도로 실행했다. 같은 테스트를 중복 합산하지 않는다.
- 새 원문 조사 산술: 6 PASS, 결함 주입 5종의 이름별 실패 확인. 실제 D9 실행 또는
  과학적 채택 증거가 아니라 연구 산술과 출력 권한 경계 테스트다.
- 관측 날짜 검사 제거 시 지정된 반례 실패를 확인하고 복원했다. 중간 테스트 작성 오류와
  dialog polyfill 부재는 수정 후 재실행했으며 최초 실패를 최종 통과로 덮어쓰지 않는다.
- 이번 코드의 CI·병합·배포는 별도 확인 대상이다. 이전 head의 CI를 재사용하지 않는다.

### CI 재확인에서 발견한 누락

- `00aeeb6`의 실행 33979209466/33979207533은 contract-tests와 app-quality는 PASS지만
  app-browser가 FAIL이었다. 원인은 방법 선택 테스트의 `summary`가 새 이력 안내까지
  두 개를 잡는 strict locator 오류다. 직접 자식 제목으로 범위를 좁혔다.
- 로컬 재실행에서 다음 실제 UI 결함이 드러났다. 이력 안내 터치 높이가 23.25px로
  44px 기준을 만족하지 않았다. CSS로 최소 높이와 focus-visible 표시를 보완했다.
  기준을 완화하거나 테스트를 삭제하지 않았다. 최초 4실패/4통과 기록은 보존한다.
- 부분 수행과 변경 수행은 동시에 성립할 수 있어 두 상태를 모두 표시하도록 수정했다.
- 최종 수정 후 앱 전체 재실행: 275파일 / 2,390 PASS / 실패 0. build와 e2e TypeScript PASS.
  방법 전환·후반 MAIN 선택·설명/실제 기록의 통합 Playwright 재실행은 20 PASS다.
  320px 및 모바일 실제 수치 화면 캡처를 직접 확인했다. 이 결과는 전체 브라우저 suite가
  아니라 명시된 세 파일, 네 프로젝트 범위다. 새 head의 전체 CI는 별도로 확인한다.

### 다음 실행 순서와 실제 선행 조건

1. [추가 원문 조사와 결정 대안](../research/SESSION_METHOD_SOURCE_GAP_FOLLOWUP_2026-09-06.md)
   §6을 기준으로 목표 페이스 모델·종료 회복·대상 범위·유한 구성 묶음을 필드별로 확정한다.
   사용자에게 모든 세션의 제품 승인 질문을 반복하지 않되 실제 검토자 서명을 생성하지 않는다.
2. M04 독립 family/configuration 등록 후 M05 배치 정책을 정확한 구성에 연결한다.
   고정 A/B 짝과 임의 slider 범위는 만들지 않는다.
3. M06~M09 실제 MAIN별 선택·유한 구성 조정·설명/합계/저장 스냅샷을 연결한다.
4. M10 다중 슬롯 트랜잭션/교차 탭 경로, M11 과거 원본과 실제 수행 연결,
   M12 장기 원장, M13 종목별 범위를 각각 검증한다.
5. M14 실제 사용자 여정, M15 Fable UX 독립 검토, M16 PR 검수 후 병합·배포를 수행한다.

새 템플릿 활성화와 전체 계획 완료는 아직 아니다. 위 순서를 UI 버튼 존재만으로 종결하지 않는다.

## 9. 일지 조회 완전성 후속 수정

- 현재 계획의 설명 화면이 `loadEntries()`의 빈 배열만 받아 저장소 손상과 실제 빈
  일지를 구분하지 못하는 경로를 확인했다. 원본을 지우거나 복원하지 않고 기존의
  계정 범위 필터와 `complete/uncertain` 조회 결과를 보존하는 읽기 경로로 연결했다.
- malformed JSON, 스키마에서 제외된 행, 저장소 읽기 예외는 관측 근거 조회 불가로
  표시한다. 실제 정상 빈 저장소는 연결된 일지 없음으로 계속 표시한다.
- 스펙 §11에 완전한 조회/조회 실패 구분을 명시했다. 처방 숫자, 활성화 범위,
  추천 알고리즘 및 네 OPEN 이슈의 상태는 변경하지 않았다.
- 수정 전 `b3c28e2` 빌드에 새 브라우저 반례를 실행하여 두 건 모두 조회 실패 문구
  단언에서 실패함을 확인했다. 정상 일지와 손상 일지를 구분하지 못하는 실제 반례다.
- 수정 후 대상 unit/component 4파일 23 PASS, 앱 build 및 e2e TypeScript PASS.
  빌드의 기존 font 경로/chunk 크기 경고는 여전히 남아 있다.
- 수정 후 설명 브라우저 1파일/4프로젝트 20 PASS. 손상 두 종류, 정상 관측값,
  새로고침, 기존/버전 설명, 복귀와 글자 확대를 포함하며 원본 계획·일지 보존도 검사했다.
  전체 앱/전체 브라우저 suite 결과를 이 좁은 재실행 수치로 대체하지 않는다.

## 10. 이어 읽을 실제 문서

- [현재 지원 범위](../../TRAINING_PLAN_CURRENT_SCOPE.md)
- [선택·조정 계약](../../specs/reconstruct/SESSION_METHOD_SELECTION_AND_ADJUSTMENT_CONTRACT.md)
- [전체 카탈로그](../../specs/reconstruct/ENERGY_SYSTEM_SESSION_TEMPLATE_CATALOG.md)
- [30행 근거 조사](../research/SESSION_METHOD_PROTOCOL_EVIDENCE_2026-09-06.md)
- [이전 구현 사후 리뷰](../review/SESSION_METHOD_POST_IMPLEMENTATION_REVIEW_2026-09-05.md)

## 11. 날짜별 선택과 적용/취소, 일지 왕복 통합

- 날짜별 후보 카드에서 개인 페이스 적용 대상을 직접 고를 수 있다.
  선택은 초안이며 적용/취소를 분리했다. 현재 범위는 A/B 공유 대상 이동이고,
  후보별 독립 다중 MAIN 편집이라고 부르지 않는다. 다른 훈련을 추가하지 않는다.
- 기존 날짜 선택기도 명시 적용 전까지 생성 결과를 변경하지 않는다.
  시작일/적격 대상/현재 선택이 바뀌면 미적용 초안을 버린다.
  적용 후 기존 기준 기록 재확인과 저장 시 권한 재검사를 유지한다.
- seven-event journey: 800/1500/3000/5000 기존 상세 4개와
  10K/하프/마라톤 RPE 3개를 실제 생성, 저장, 재조회, PARTIAL/MODIFIED
  일지 연결, 제한된 archive까지 검증했다. mutation lock만 시험용 대체이며
  이 7건이 다중 탭 브라우저 검증이나 새 상세 처방 활성화 증거는 아니다.
- 신규/갱신 unit/component 3파일 14 PASS, typecheck와 build PASS.
  desktop-chromium 및 320px touch-narrow 브라우저 2 PASS:
  카드 선택 취소, 다른 날짜 적용, 비가분 기록 1111초 확인,
  정확한 222.2초 저장/새로고침, 가로 넘침 없음, pageerror 없음.
  모바일 200% 글자 확대 캡처를 확인했다. 전체 suite 재실행은 아니다.
- [독립 권한 감사](../review/SESSION_METHOD_REMAINING_AUTHORITY_AUDIT_2026-09-06.md)는
  기존 baseline 연결의 과잉 차단과 신규 활성화의 실제 근거 공백을 구분했다.
  새로운 과학적 승인, Fable UX 승인 또는 전체 계획 완료 증거가 아니다.
- M06/M07/M14는 부분 진척이다. 후보별 독립 초안, 수치 조정 host와 저장 통합,
  신규 구성 및 동시 배치 정책, 새 장기 보존 경계, 최종 검수/배포는 남아 있다.
  다음 엔지니어링은 후보별 초안 격리 및 조정 host의 검증된 전환 연결이다.
  신규 처방값을 만들거나 승인된 기존 단일 상세 경로를 재승인 대기로 돌리지 않는다.

## 12. 후보별 단일 상세 배치 분리

섹션 11의 공유 target만 제공하던 범위를 확장했다. 후보 카드의 적용은
BALANCED/CONSERVATIVE별 독립 target map을 사용한다. 기존 공통 날짜 선택은
두 후보를 함께 고르는 명시 동작으로 유지하며, 독립 편집 이후에는 숨긴다.
한 후보의 선택은 다른 후보의 세션, 처방 수치 및 candidate identity를
변경하지 않는다. 두 후보를 묶는 pair identity는 새 내용에 맞춰 갱신한다.

- 스펙 섹션 4에 최초 수동 선택과 자동 support-only 적응의 경계를 명시했다.
- 실제 반례: 기존 selection 검증기가 독립 target 후보를
  STALE_CANDIDATE_FINGERPRINT로 거부했다. 최초 선택용 검증만 분리했다.
  정확히 한 상세 처방씩, 동일한 MAIN 처방 집합, 동일 layout와 support
  차이 규칙을 요구한다. 기존 자동 적응 검증은 그대로 남겼다.
- 적용/취소 전에는 해당 후보 확정을 막는다. 공통 target 초안도 미적용
  상태에서 최종 계획을 확정하지 못한다. 후보 UI는 변경되는 hash가 아닌
  후보 종류를 기준으로 펼침 상태를 유지한다.
- candidate 정렬이 달라도 scope/slot identity는 유지하며,
  content snapshot 변경 검사는 계속 별도로 수행한다.
- 신규 반례는 잘못된 주소, 재해시한 용량 변조, 다른 후보 오염,
  자동 support-only 적응으로의 우회, candidate 순서 변경을 포함한다.
- 관련 app 5파일 25 PASS. impl 전체 27파일 825 PASS.
  타입 검사, e2e 타입 검사, build PASS. 기존 font/chunk 경고는 유지된다.
- 브라우저 6 PASS: 공통 target, A만 편집, B만 편집의 세 경로를
  desktop-chromium/320px touch-narrow에서 확인했다. 기록 확인, 수락,
  실제 저장/새로고침, 222.2초 원본 보존과 가로 넘침 없음이 포함된다.
- 전체 앱 기본 동시성 실행은 2399 PASS/13 FAIL이었다. 12건은 5초 timeout,
  한 건은 archive 화면 질의 실패였다. --maxWorkers=4 재실행 결과는 아래에
  별도 기록하며 실패를 지우거나 과거 CI 결과로 대체하지 않는다.
- 동일 작업트리의 --maxWorkers=4 재실행은 279파일/2412 PASS,
  exit 0으로 완료했다. timeout/단언 변경 없이 동시성만 변경했다.

M06/M07/M10의 단일 상세 배치 연결이 진척되었으며 전체 완료는 아니다.
다음: 기존 조정 engine/editor의 candidate transaction host, 현재 상태와
receipt 재검사, 단일 snapshot commit 및 저장 계층 결속. 실제 신규 dose
전환은 검토된 registry가 있을 때만 활성화하고 시험용 권한을 운영에 넣지 않는다.
기존 네 ref의 청소년/자율 사용자 범위는 그대로 유지한다.

## 13. 조정 적용 host와 단일 snapshot commit 기반

- `revalidateAdjustmentReceipt`가 전달된 before/after, 모든 합계/delta와
  구조 차이를 원래 동작 시점 및 현재 시점의 독립 registry로 재검산한다.
  원래 명시 적용 시각을 현재 시각으로 바꿔 쓰지 않는다.
- `prescription-adjustment-commit.ts`는 후보 계보/MAIN slot/context/revision을
  묶은 workspace snapshot을 CAS 한 번으로 교체한다. 처방, 설명 버전과
  근거 참조, receipt가 한 snapshot에 들어가며 부분 필드 저장을 하지 않는다.
  실제 CAS/lock 구현은 소비 저장소의 책임이며 이 모듈이 localStorage의
  다중 키 원자성을 제공한다고 주장하지 않는다.
- `PrescriptionAdjustmentCommitHost`는 기존 editor의 적용/취소를 이
  controller에 연결한다. 열 때의 base revision을 유지하고, 실패한 draft는
  재시도할 수 있다. 화면 unmount/취소는 대기 중인 write를 무효화한다.
- 안전/context/권한 변경을 write lock 안에서 재확인한다. 중복 적용은
  한 요청으로 합치고, 저장 후 응답 유실은 정확한 receipt를 재검사하여
  write 없이 replay한다. getter 및 허용되지 않은 메모 필드의 유입을 거부한다.
- 합성 registry를 사용하는 app 연결 시험 42 PASS, impl 전체 831 PASS.
  이 수치는 실제 선수를 대상으로 한 처방 검증이나 신규 source 채택이 아니다.
  타입 검사와 build 결과는 아래 실행 기록에 별도로 보존한다.
- 최종 타입 검사/build PASS. 기존 font 경로 및 chunk 크기 경고는 남아 있다.
  [자체 검토 보고서](../review/ADJUSTMENT_COMMIT_HOST_SELF_REVIEW_2026-09-06.md)에
  공격 검토 수정점과 production 연결에서 아직 검증되지 않은 범위를 남겼다.
- **아직 public `PlanBeta`에서 이 host를 열지 않는다.** 실제 적격 전환이
  없는 버튼을 노출하지 않았다. 기존 PACE_TARGET 저장 스키마를 generic
  조정 snapshot으로 대체하지 않았으며, 시험용 권한/용량은 운영 manifest에
  들어가지 않았다. 따라서 M08/M09 공통 host 기반은 구현됐지만 실제 공개
  후보 저장·재조회와 신규 구성 활성화가 완료된 것은 아니다.
- 다음 실제 작업: source configuration과 개인 기록으로 해석된 sequence의
  identity를 구분한 production offer/adapter, 버전별 저장 계약과 정확한
  후보 투영 검증. 현재 4개 고정 baseline에 새 용량을 덧씌우지 않는다.

## 14. 초기 계획 저장의 정확한 재시도

- 저장 성공 후 응답만 놓친 동일 요청은 기존 snapshot을 그대로 반환한다.
  새 generatedAt/주기 계보/설명 receipt를 만들거나 계획·context를 다시 쓰지 않는다.
  기존 저장소 사용 가능 여부를 확인하는 `__to_probe__` 임시 점검은 유지한다.
- 현재 계정·초안·안전·기록·템플릿 권한을 확인한 뒤, 원래 저장 시각으로
  재구성한 전체 예상 상태와 실제 저장 상태를 비교한다. 날짜·후보·근거 변경,
  이미 기록한 진행 상태, 미래 저장 시각은 재시도로 인정하지 않는다.
- 필요한 adaptation context가 없거나 손상되거나 다른 후보를 가리키면
  성공으로 반환하거나 자동 복구하지 않고 `PLAN_STORAGE_STATE_UNCERTAIN`을 유지한다.
- 재시도 회귀 18 PASS. 재시도 성공 반환을 고의로 제거한 변조 실행은
  양성 5건이 모두 실패했다(13건 필터 제외). 변조는 즉시 원복했다.
- 기존 동시 저장 공격 시험 1건의 기대값은 동일 요청 거부에서 동일 결과 반환으로
  변경했다. 원본 저장 내용 불변 검사는 유지한다. 이 변경을 한 뒤 최종 재실행한다.
- 새 빌드의 실제 화면 6 PASS: desktop/320px에서 공유 target, A만, B만의
  선택·기록 확인·저장·새로고침, 원본 222.2초와 슬롯 유지, 가로 넘침 및 pageerror 없음.
- 추가 실제 두 탭 시험 4 PASS: desktop/320px의 동일 선택 재시도 및 날짜가
  다른 선택 거부. 같은 browser context의 두 탭, 실제 Web Locks와 localStorage를
  사용했다. 첫 저장 뒤 두 번째 요청이 전체 계획/context를 바꾸지 않음을 확인했다.
  처음 2건 실행의 1 FAIL은 새로고침이 홈으로 돌아가는 실제 동작을 시험에서
  누락한 것이었다. 계획 탭으로 재진입하도록 시험을 고친 뒤 4건 모두 통과했다.
  저장 도중 브라우저 강제 종료나 네트워크 응답 유실을 재현한 것은 아니다.
- 타입 검사/build/e2e 타입 검사 PASS. 전체 앱 최종 실행은
  `--maxWorkers=4`에서 282파일 / 2454 PASS, exit 0이다.
  첫 전체 실행의 2453 PASS / 옛 기대값 1 FAIL도 위 검토 보고서에 남겼다.

신규 훈련 구성/전환 승인, host의 public 연결, 다중 키 원자성, 전체 계획 완료,
PR 병합 또는 공개 배포를 이 패킷의 결과로 주장하지 않는다.

## 15. V5 원본 보관과 일지에서의 비교

- 앞으로 보관하는 계획은 기존 최신 18개 한도 안에서 V5 원본과 요약을 함께
  저장한다. 수동 보관·승인된 다음 계획 시작 모두 연결했고, 후자의 요약은 기존
  표시 프레임 범위를 유지한다. 원본 날짜·오전/오후·처방·계보는 별도로 보존한다.
- 구형 V3/V4 요약에는 원본을 만들어 넣지 않는다. 읽기 실패·미보관 원본을
  구분하고, 최근 계획을 예전 일지에 대신 붙이지 않는다. 신규 서버 전송은 없다.
- 일지에서 `계획한 훈련과 비교하기`를 열면 실제로 연결된 원본의 방법·이유·
  주기·기록을 볼 수 있다. 직접 입력 수치·부분 수행·변경 수행·미기록을 구분하며
  반복별 측정이나 수행 준수 판정은 만들어내지 않는다.
- 계정·storage 이벤트·일지 link 변경 시 열린 비교를 닫는다. 설명에서 돌아오면
  일지 진입 버튼에 포커스와 위치가 복원된다. 메모 원문은 비교 입력에 사용하지 않는다.
- 손상된 과거 이력을 빈 목록으로 덮어쓰던 보관 경계를 함께 수정했다. 기존
  저장 실패 rollback과 보관 한도·계정별 격리를 유지한다.
- 최종 전체 앱 284파일 / 2473 PASS. 대상 원본/UI/successor 35 PASS.
  타입 검사/build/e2e 타입 검사 PASS. 실제 보관→일지→원본 비교와 기존
  계획→일지 작성 흐름 8 PASS(desktop/mobile/320px/reduced-motion).
  확대 제목 잘림 의심은 실제 글자 좌표 시험에서 미재현으로 판정했다.
- 요약/hash 검사를 끈 결함 주입에서 2건이 실제 실패했고 정상 코드를 복원했다.
  상세 실패·수정·미재현 기록은
  [원본 보관 검토 보고서](../review/ARCHIVED_ORIGINAL_PLAN_JOURNAL_REVIEW_2026-09-06.md)에 있다.

M11/M12는 앞으로 보관하는 정확한 원본의 일지 비교까지 진척되었다. 장기 관찰
기간·반복별 측정·신규 추천 소비, 다른 방법/조정값/다중 MAIN 활성화와 전체
독립 검수·병합·배포는 여전히 별도 작업이며 전체 계획 완료가 아니다.

## 16. 다중 MAIN 배치 검토의 범위와 수명

- 계약 v0.4와 코어 검증에 정확한 주기·오전/오후 전체 배치·보조 처방 문맥,
  유효기간·철회 상태·노출/상호작용/안전 검토 참조를 연결했다. 시각은 호출자가
  명시하며 누락된 만료값을 무기한 승인으로 해석하지 않는다.
- 같은 목적의 독립 구성 집합을 검증한다. 특정 A/B 짝을 등록하거나 새로운
  반복 수·회복 시간·선수 적용 범위를 만들지 않았다. 실제 운영 배치 정책은 0건이다.
- 개인 기록·계산된 페이스·문구는 주기 범위 지문과 구분한다. 원본 상세 처방의
  숫자·템플릿 검증을 대신하지 않는다. 기존 단일 상세와 과거 계획 읽기를 유지한다.
- 정상 코드 재실행: 대상 58 PASS, 코어 전체 859 PASS, 코어 타입 및 앱 build PASS.
  만료 검사 제거는 1 FAIL, 범위 검사 제거는 3 FAIL로 결함 검출을 확인하고 복원했다.
- 실제 desktop/320px 선택·확인·저장·새로고침 6 PASS. 최종 전체 앱 실행은
  284파일 2473 PASS, 실패 0이다. PR 전달 결과는 같은 PR #319에 기록한다.
- M05는 부분 구현이다. 실제 운영 정책 수용, 정책 identity/선택 receipt 저장,
  저장·복원·실행·다음 주기 경계 전체의 평가 문맥 연결은 남아 있다.
  [범위와 유효기간 검토 보고서](../review/MAIN_PLACEMENT_POLICY_SCOPE_REVIEW_2026-09-06.md)

## 17. 원본 구성과 개인 목표값 연결

- 추천 목록의 source sequence 생성을 공통 `readPlanMethodDefinition`으로 옮겼다.
  기존 4개 정확한 ref의 본운동 구조를 읽으며 개인 기록·anchor ID는 포함하지 않는다.
  source configuration 지문과 독립 family/configuration 매핑을 함께 반환한다.
- `resolvePlanMethodPrescription`은 기존 저장 스키마로 숫자·원본 manifest·sequence를
  먼저 검증한다. 이어 source configuration, 전체 개인 처방 지문, anchor 내용 지문,
  반올림하지 않은 목표 초, 실제 sequence, 설명 버전·내용 지문·근거를 구분해 묶는다.
- 실제 후보 생성은 이 연결이 없으면 기존 시간·RPE 후보로 되돌린다. 임의의 숫자를
  다시 쓰거나 저장 검증을 완화하지 않는다. 권한/안전/기록 검사는 기존 경로가 담당한다.
- 과거 sequence 없는 처방은 읽기용 투영만 만들고 원본·저장 fingerprint를 수정하지
  않는다. 반환값은 로컬 조정 adapter용이며 공개 export나 새 저장 필드로 추가하지 않았다.
- 대상 3파일 50 PASS. 소수 초 4종목, 서로 다른 기록의 동일 source identity,
  설명 내용 변경, 수치/회복 변조, getter/추가 메모 필드, registry 원본 보존을 검사했다.
  후보 연결 검사를 끄면 2 FAIL로 실제 검출됨을 확인하고 정상 코드로 복원했다.
- 최종 전체 앱 285파일 2492 PASS, 실제 desktop/320px 4종목 생성·저장·새로고침
  및 위치 선택 12 PASS. app/e2e 타입과 build 및 두 계약 validator PASS.
  [원본·개인 계산 연결 검토 보고서](../review/SOURCE_RESOLVED_METHOD_BINDING_REVIEW_2026-09-06.md)
- 남은 것은 이 binding을 사용하는 실제 허용 전환 offer, source edge에서 개인화된
  before/after를 재현하는 adapter, 버전별 조정 저장 및 사용자 조정 여정이다.
  source/resolved 구분만으로 M08 전체 완료나 새 조정 활성화를 주장하지 않는다.

## 18. 원본 조정 전환과 저장 절차 연결

- `prepareSourceAdjustmentOffer`는 현재 원본에서 직접 허용된 모든 전환만
  가져온다. A/B 고정 짝, 역방향·연쇄 변경 허용, 자기 자신으로 바꾸는 가짜
  선택지는 만들지 않는다. 원본의 정책 수명과 근거 참조를 그대로 유지한다.
- 같은 종목의 명시적 RACE_PACE placeholder에 현재 record anchor 참조를
  연결하고 현재 후보/슬롯 revision을 별도 문맥 지문으로 묶는다. 개인 목표 초
  계산·거리/시간 환산·회복값 변경을 이 모듈에서 새로 수행하지 않는다.
- `createSourceAdjustmentCommitAdapter`는 기존 controller/host와 연결한다.
  설명을 정확한 원본 구성에서 가져와 resolved 구성에 결속한다. 운영자가
  제공하는 현재 계정·안전·후보 허용 판정을 진입과 저장 잠금 안에서 재검사한다.
- 저장 직전 원본 authority로 offer/receipt를 다시 계산한다. 기다리는 사이의
  만료·철회·후보 revision·허용 상태 변경은 저장 0건과 기존 snapshot 보존으로
  처리한다. 같은 명시 요청의 재시도는 한 번만 저장한다.
- 합성 구성으로 연결 대상 3파일 50 PASS를 직접 확인했다. 실제 운영 조정값,
  개인 새 페이스, 운영 정책은 추가하지 않았다. owner가 제공할 실제 저장소의
  잠금/원자성이나 공개 조정 여정의 완료를 이 결과로 주장하지 않는다.
- 전체 앱 286파일 / 2,518 PASS, TypeScript 및 build PASS. revision 결속을
  제거하면 2 FAIL로 검출됐고, 정상 복원 후 대상 50 PASS를 재확인했다.
- 전체 회귀 결과와 추가 검수는
  [원본 조정 연결 검토 보고서](../review/SOURCE_ADJUSTMENT_COMMIT_ADAPTER_REVIEW_2026-09-06.md)에 기록한다.

다음 순서는 실제 후보 저장 형식과 연결할 versioned adapter, 운영 정책 공급자,
정확한 구성 채택, 사용자 조정·복원·일지 비교의 종단 검증이다. M01~M16의
미완 항목을 삭제하거나 전체 완료로 바꾸지 않는다.

## 19. 임시 조정 초안의 저장·복원

- 버전 1 envelope로 현재 후보의 원본 baseline과 적용된 workspace snapshot을
  현재 탭의 계정별 sessionStorage 한 항목에 보관한다. 확정 계획·archive·서버
  백업·내보내기에는 넣지 않는다. 새 후보가 기존 초안을 자동 덮어쓰지 않는다.
- 복원은 현재 후보에서 독립적으로 가져온 baseline 및 현재 authority를 기준으로
  한다. 저장된 policy를 승인 근거로 읽지 않으며, 변경된 상태는 기존 controller의
  정확한 replay 검사로 숫자·설명·receipt·revision을 다시 검증한다.
- 기존 mutation lock 안에서 단일 키 비교/쓰기/재확인한다. 저장 실패는 성공으로
  알리지 않고, 손상·다른 버전·다른 후보의 바이트는 자동 삭제/덮어쓰기하지 않는다.
- 계정 전환은 열린 handle을 무효화한다. 익명 초안의 자동 계정 이전은 없다.
  취소는 정확히 열린 초안만 삭제하며, 내 데이터 전체 삭제에도 계정별 키를 포함한다.
- 연결 4파일 68 PASS. 새 session 계약 시험 18개에는 source offer → source
  adapter → session CAS → 재개까지의 합성 통합을 포함한다.
- Chromium native Web Locks/sessionStorage의 저장·reload/replay·별도 탭 격리·
  잠금 거부·취소 5항목 PASS. 시험 페이지/번들은 로컬 intercept만 사용했고 실제
  사용자 데이터와 외부 통신은 없었다. 운영 화면 E2E와 구분한다.
- 상세 결과와 보완 기록:
  [임시 조정 저장 검토 보고서](../review/ADJUSTMENT_WORKSPACE_SESSION_REVIEW_2026-09-06.md).
- 전체 앱 287파일 / 2,536 PASS. TypeScript/build PASS. 저장 버전 검사 제거는
  1 FAIL로 검출됐고 정상 코드를 복원했다. 운영 화면 연결 완료로 확대하지 않는다.

이 기능은 한 후보에서 적용한 변경을 이어보는 임시 초안이다. 후보 변경 수용 후
초안을 명시적으로 소비/정리하고, 새 baseline을 여는 화면 연결은 남아 있다.
확정 계획/일지 원본으로 보존하는 영속 형식 및 실제 운영 정책 연결도 여전히 미완이다.

## 20. 조정된 구간의 개인 목표값 연결

- 실제 저장 형식이 flat PACE_TARGET에 묶인 부분을 확인했다. 기존 검증을
  완화하지 않고 별도 candidate-only projection을 추가해 distance/time/mixed
  각 구간을 기존 원본 개인 기록과 연결한다.
- 같은 종목의 RP 거리형은 소수 초를 그대로 계산한다. 시간형 이동 거리나
  sprint/effort 숫자를 만들지 않는다. 원본 준비·정리 및 조정 구간 구조를 보존한다.
- 기록 ID가 그대로인 수정도 잡도록 source offer에 anchor 내용 지문을 추가하고
  target resolver에서 원본 anchor로 재계산한다. 기존 비활성 시험 초안은 현재
  문맥이 달라지면 재확인이 필요하며 active plan은 변경하지 않는다.
- 새 구성 설명이 없는데 기존 설명을 붙이지 않는다. 해당 구성 설명 필요 상태를
  반환한다. 실제 active-plan 저장과 UI 적용 완료가 아니다.
- 전체 앱 288파일 / 2,549 PASS, TypeScript/build PASS. 동일 ID의 기록 내용
  지문을 제외한 결함 주입은 1 FAIL로 검출됐고 정상 복원했다. 임시 조정 저장의
  Chromium 5항목도 다시 통과했다.
- [개인 목표값 연결 검토 보고서](../review/ADJUSTED_METHOD_TARGET_RESOLUTION_REVIEW_2026-09-06.md)에
  실행 결과·타입 오류 정정·남은 형식 전환을 기록한다.

## 21. 조정 결과와 설명의 버전 보존 (2026-09-07)

- 원본 처방·조정 receipt·개인 목표값·구간/합계·정확한 설명 버전을 함께 검증하는
  별도 snapshot codec을 구현했다. 새 active-plan kind나 저장 키를 추가한 것은 아니다.
- 과거 읽기와 현재 후보 재적용을 분리했다. 저장 당시 유효한 정책의 과거 열람은
  현재 실행 권한이 아니며, 현재 적용은 만료/문맥을 다시 검사한다.
- 설명 문장은 중복 저장하지 않고 정확한 구성의 설명 버전·내용 지문을 결속한다.
  조정했는데 원래 구성 설명을 붙이거나 같은 버전 아래 문장을 바꾸면 거부한다.
- 대상 2파일 / 39 PASS, TypeScript/build PASS. 만료 분기 반전 결함 주입은
  1 FAIL로 검출됐고 복원했다. Chromium 임시 초안 5항목 회귀도 PASS다.
- 상세 검수와 전체 회귀 결과는
  [조정 snapshot 검토 보고서](../review/ADJUSTED_METHOD_SNAPSHOT_REVIEW_2026-09-07.md)에 남긴다.

현재 M07/M08/M09는 부분 구현 상태를 유지한다. 실제 PlanSession 표현·화면 선택·
확정 저장·일지 consumer 연결 및 운영 구성 공급은 남아 있다. 위 16개 항목을
축약하거나 이 codec을 전체 사용자 기능 완료로 바꾸지 않는다.

## 22. 한 MAIN에 조정 결과를 붙이는 미확정 후보 (2026-09-07)

- snapshot을 실제 원본 후보의 정확한 MAIN/day/AM/PM에 결속하는 조립기를 구현했다.
  원본 후보 스키마·내용 지문·계획 시작일과 해당 세션의 원본 처방까지 확인한다.
- 선택한 MAIN만 별도 ADJUSTED_METHOD 표현으로 바꾸고 나머지 세션·프레임·
  주기 연결을 보존한다. 기존 후보 및 저장된 active plan은 수정하지 않는다.
- 결과는 NOT_ACCEPTED/선택 권한 NONE인 별도 미확정 후보다. 기존 flat 저장
  parser에 억지로 통과시키지 않으며 최종 전체 계획 선택 관문이 남아 있다.
- 자체 검수에서 시작일 결속 누락을 찾아 보완했다. 원본 처방 비교 및 날짜 결속
  제거 변조가 각각 실제 시험 실패로 검출됐다. 정상 대상 3파일 / 53 PASS.
- [미확정 후보 조립 검토 보고서](../review/ADJUSTED_PLAN_CANDIDATE_REVIEW_2026-09-07.md)에
  최초 fixture 실패·보완·전체 회귀 결과·남은 실제 소비 경계를 기록한다.

다음은 이 새 세션 표현을 소유하는 전체 계획 선택/저장/실행 스키마와 소비자 연결이다.
미확정 후보 조립을 실제 사용자 조정·저장·일지 완료로 바꾸지 않는다. M01~M16
전체 범위와 각 미완 상태를 유지한다.

## 23. 조정 표현의 전체 프레임 검토 범위 (2026-09-07)

- 기존 PACE_TARGET 전용 상세 카운트에 새 조정 표현을 그냥 넘기지 않도록,
  조정 구성과 주변 세션을 모두 결속하는 별도 검토 scope/정책 조회를 구현했다.
- 원본/목표 구성·정책, 실제 경험, 프레임/주기 연결, 노출 원장, 준비/정리,
  주변 RPE·시간과 모든 역할/날짜/AM/PM을 포함한다. 개인 기록 ID/목표 초는
  이 구성 검토 scope에서 제외하고 개인 snapshot/현재 기록 검증에 남긴다.
- 기간/철회/필수 검토 참조/정확한 scope와 중복 정책 ID를 확인한다. 운영 registry는
  0건이며 검토 참조를 자동 승인으로 만들지 않는다. 결과의 실행 권한도 NONE이다.
- 대상 2파일 / 29 PASS. 주변 세션을 해시에서 빼는 변조는 1 FAIL로 검출 후 복원.
- [조정 프레임 정책 검토 보고서](../review/ADJUSTED_PLAN_REVIEW_POLICY_REVIEW_2026-09-07.md)에
  최종 실행 증거와 남은 전체 선택/저장/소비자 통합 경계를 기록한다.

M05/M07/M08/M10의 실제 전체 선택 lock 연결과 새로운 영속 계획 표현은 여전히 미완이다.
이 scope 검사를 실제 안전 판정·과학 검토자의 서명·저장·사용자 기능 완료로 주장하지 않는다.

## 24. 오너 최종 채택 경로 승인 (2026-09-07)

오너가 `근거 검토 후 내가 최종 승인` 경로를 명시 승인했다.
[검수 절차 결정](../review/OWNER_TRAINING_ADOPTION_REVIEW_ROUTE_2026-09-07.md)에
원문 응답과 적용 경계를 기록했다. 구현자가 정확한 근거·구성·범위를 검토해
하나의 채택 패킷으로 제시하고, 오너의 실제 최종 응답을 받은 항목만 활성화한다.

아직 제시하지 않은 수치까지 승인된 것이 아니며 외부 전문가 서명을 만들지 않는다.
다음 우선 작업은 관련 스펙/runtime의 오너 채택 경로 정합성 패치 및 정확한 구성
채택 패킷 완성이다. 이 절차로 외부 전문가 승인만을 유일한 경로로 강제하는
과잉 차단을 해소하되, 기존 D9·개인정보·선택·검증 경계와 M01~M16 범위는 유지한다.

## 25. 오너 채택 권한 resolver 연결 (2026-09-07)

기존 baseline/외부 2인 검수와 구분되는 OWNER_REVIEWED_OPERATIONAL_ADOPTION
분기를 실제 상세 처방 권한 resolver에 추가했다. 최종 승인 패킷 지문과 검토 패킷,
전체 approval record 지문, 정확한 구성/종목/목적/결정/기간을 함께 확인한다.
과정 승인 ID를 개별 구성의 최종 승인 ID로 재사용하면 거부한다.

- 신규 및 기존 권한 테스트 2파일 53 PASS, TypeScript 검사 PASS.
- 최초 타입 검사는 시험 fixture의 undefined 가능성으로 실패했고, 누락 시 명시
  실패하도록 수정한 후 재실행해 통과했다.
- 이 변경 뒤 전체 앱 suite/build/browser는 아직 재실행하지 않았다. §23의 전체
  2,605 PASS는 이 권한 분기 추가 전 결과이며 최신 전체 결과로 재사용하지 않는다.
- 새 운영 registry 0건. 코드 경로 준비이며 새 수치 채택 또는 배포 완료가 아니다.
- 이 resolver는 기존 평면형 처방 범위다. 복합/조정 표현의 전체 선택·저장·일지
  통합과 정확한 채택 패킷을 계속 진행한다. M01~M16 및 기존 OPEN 이슈 유지.

## 26. 원문 재확인과 구체적 채택안 작성 (2026-09-07)

[오너 채택 패킷 v0.1](../review/SESSION_METHOD_OWNER_ADOPTION_PACKET_2026-09-07.md)을
작성했다. BASE 연속/분할, LT 연속/분할 3종, 리듬 거리/세트형 총 7개 검토 행을
정확한 운동·회복 구성으로 정리했다. 원문 사례와 제품 운영 제안을 분리했다.

Thompson, V.O2 역치/계산기/cruise 페이지와 PMID 21777153 초록을 다시 열었다.
PMID 8226473은 본문이 반환되지 않아 재검증 완료로 처리하지 않았다.
문서 제목/단일 최종 표식/7행 및 400m·300m 사례의 총량 계산을 실행해 확인했다.
이는 문서/산수 확인이며 사용자 처방 시험이나 효능 검증이 아니다.

ATP-PC/GLY/VO2 추가 구성, 개인 강도 모델, 정확한 +/- 정책 및 세트 분할,
주기/대상 범위, 설명 지문이 아직 남아 있어 최종 승인 요청은 발행하지 않았다.
이미 승인받은 검토 절차를 반복 질문하지 않고 패킷을 완성한 뒤 일괄 요청한다.
운영 활성화 0건, M01~M16 전체 범위 유지. 기존 30행 카탈로그를 대체하지 않는다.

## 27. 추가 계열 및 유한 조정 채택 초안 (2026-09-07)

오너 채택 패킷 v0.2에 VO2 3개, ATP-PC 2개, GLY 2개, REC/OFF 2개를 추가했다.
이전 7행과 함께 구성 표 16개 고유 ID를 실행해 재계수했다. 조정 표의 같은 ID나
결합 표기를 새 훈련으로 세지 않는다. OFF 역시 운동 방법 수가 아니다.

V.O2 인터벌 원문과 Haugen 2019 출판사 본문을 직접 확인했다. ATP/GLY의 앱
수치 제안은 원문 그대로인 것처럼 표시하지 않는다. 시간/횟수 및 세트 분할의
유한 선택 집합을 구체화했고, GLY 6회 대 2세트의 회복은 600초/780초로 검산했다.
동일 본운동량과 동일 부담·효과를 구분한다. 실제 개인 페이스 모델과 전체 프레임,
준비/정리, 근거 지문까지 완성한 뒤 최종 채택 요청해야 한다.

연구 초안 갱신이며 코드/운영 registry 변경 없음. 전체 구현 목표와 미완 통합 유지.

## 28. LT 참고 페이스 단위 확인 및 계산 시험 (2026-09-07)

오너 채택 패킷 v0.3에 현재 5km 기록 + 24~30초/마일의 코칭 근사 모델을 제안했다.
V.O2 2025 원문에서 단위를 직접 확인했다. VDOT 전체 공식 또는 측정 역치로
표시하지 않는다. 다른 종목을 우회 환산하거나 I/회복 페이스에 재사용하지 않는다.

연구 폴더의 비운영 계산과 node:test 13개 PASS를 확보했다. 18:31 같은 비배수와
소수점 입력, 마일/km, 오래된 기록·목표·잘못된 숫자 거부를 확인했다. 앱 import나
실제 선수 자료 사용은 없다. 2×10분/3×7분 제안이 원문의 총시간 지침과 다름도
명시했다. 이 모델로 전체 종목 개인화 또는 최종 채택 완료를 주장하지 않는다.

남은 사항: I/전 종목 모델, 전체 프레임/구성/설명 최종 패킷, 실제 조정 선택·저장·
일지·차기 주기 통합. M01~M16 및 모든 미완 항목 유지.

## 29. 누적 변경 재시험과 Git 인계 (2026-09-07)

검증 코드 HEAD: `f4c9ec7`. PR #319를 직접 조회해 open/draft/not merged 및 원격
head `ea2e532`를 확인한 후, 누적 로컬 커밋을 같은 브랜치로 푸시했다.

- 전체 app vitest: **292파일 / 2,616 PASS**, 실패 0, exit 0. 시작 15:12:27,
  실행 290.54초. 기존 ErrorBoundary/feedback 복구 시험의 의도적 stderr와 구분했다.
- `npm run build`: PASS. Pretendard 경로 해석과 500kB 초과 청크 경고는 남아 있다.
- 기존 v2 authority validator: PASS, 기존 활성 템플릿 4개 및 쓰기 버전 3 유지.
- 신규 브라우저 여정 시험·새 수치 활성화·병합·배포는 이번에 수행하지 않았다.
- 이전 연구 인계 §8의 외부 전문가 전용 경로 해석은 후속 §10에서 새 오너 절차로
  연결했다. 과거 원문·실패·검증 기록은 수정하지 않고 보존했다.

실제 통합점 재확인: `saveSelectedPlanCandidate`는 기존 생성 후보/PlanBetaState만
저장하고, `readJournalOriginalPlan`은 기존 active/archive 계획과 일지 링크만 읽는다.
새 ADJUSTED_PLAN_CANDIDATE는 이 둘에 아직 연결되지 않았다. 검토 준비 및 녹색
시험 결과로 그 간격을 완료 처리하지 않는다. 전체 목표와 M01~M16은 계속 진행 중이다.

## 30. 조정 처방과 기존 일지 링크의 동일성 연결 (2026-09-07)

오너 응답 `근거 검토 후 내가 최종 승인`을 검토 절차 결정으로 유지한다. 아직
제출하지 않은 수치 구성의 일괄 승인 또는 독립 외부 전문가 승인으로 해석하지 않는다.

기존 planned-session-link의 입력을 검증된 계획의 동일성 투영으로 분리했다.
기존 링크 스키마와 해시 계산은 유지하며, 조정 처방 전체 내용도 같은 방식으로
결속한다. 링크 생성 자체는 계획 선택·저장·실행 권한을 부여하지 않는다.

- 합성 조정 계획의 링크 왕복, 원래 처방으로 치환, 내용 변경, 날짜/오전·오후 변경,
  중복 세션, 목표 초 변경 시 이전 지문을 그대로 둔 변조를 검사했다.
- 기존 계획 조회 및 빠른 기록 화면까지 관련 4파일 **28 PASS**, 실패 0, exit 0.
- 공통 링크 타입 변경 후 `tsc --noEmit` PASS. 후속 테스트 한 건 추가 후에는
  위 28개 실행 결과를 확보했으며, 전체 앱 회귀 수치를 다시 측정한 것은 아니다.
- 실제 저장된 조정 계획을 조회한 시험이 아니라 합성 동일성 투영 시험이다.
  `saveSelectedPlanCandidate` 및 활성 계획 저장 스키마, 실제 UI 적용/취소,
  `readJournalOriginalPlan`의 조정 계획 저장소 조회와 다음 주기 연결은 아직 남아 있다.

다음 구현은 승인 자료와 별개로 활성 계획의 조정 처방 저장·재검증 경로를 연결하고,
명시적 적용/취소 및 실제 일지 왕복을 시험하는 것이다. 미승인 수치는 활성화하지 않는다.
배포·병합·전체 목표 완료를 주장하지 않는다.

## 31. 조정 계획의 명시적 선택 경로 (2026-09-07)

`selectAdjustedPlanForActivation`을 추가했다. 기존 생성 후보와 전체 내용이 일치하는지,
현재 D9/일지 안전 판정, 현재 경기 기록, 기존 템플릿 권한, 조정 구성/설명 및 전체
프레임 검토 정책을 다시 확인한다. 미리보기 캡처 시간이 아니라 선택 시각을 사용한다.
목적/경험 변경, 미리보기 지문 변경, 중복 원본 후보, 기록 삭제, 정책 만료/철회를 거부한다.

선택 결과에는 조정된 세션 자체와 별도 계획 ID를 넣는다. 이전 pair의 적응 권한과
옛 explanationReceipt를 복사하지 않는다. 변경하지 않은 세션은 보존한다.
새 주기 시작과 후속 주기 전진을 혼합하지 않으며, 후속 주기는 별도 연속성 거래가
연결되기 전에는 이 초기 선택 경로로 우회하지 못한다.

스펙 v0.12의 §19에 이 경계를 먼저 명시했다. 실제 이슈 표를 열어 확인한 4개 OPEN,
canonical blocker 0은 유지한다. 새 수치에 대한 오너 최종 채택을 만들지 않았다.

검증:
- 조정 관련 6파일: 83 PASS (초기 선택 시험 9개 포함).
- 전체 app 실행: 294파일 / 2,633 PASS, 실패 0, exit 0, 218.29초.
  마지막 추가 입력 검증 시험까지 포함한 새 전체 재계수라는 주장은 하지 않는다.
- 최종 선택 시험: 12 PASS. 추가 필드·getter 거부와 목적 변경, 중복 후보를 포함한다.
- 최종 `npm run build`: 타입 검사와 production build PASS. 기존 Pretendard 경로 및
  500kB 청크 경고는 유지되며, 이번 선택 함수는 아직 UI에서 호출하지 않는다.
- 결함 주입: 실제 조정 세션 대신 원래 세션을 반환하도록 한 임시 변경에서
  해당 시험 1 FAIL을 확인했다. 임시 변경을 복원한 뒤 12 PASS를 다시 확보했다.
- 기존 권한 validator: 활성 템플릿 4개 / 쓰기 버전 3 / 적응 edge 2 유지.
- 안전 판정 과정의 기존 저장소 가용성 probe를 원본에서 확인했다. 일시 probe 외
  쓰기는 없고 호출 전후 저장 내용이 동일함을 검사한다.

완료하지 않은 부분:
- 이 함수의 결과는 `NOT_SAVED`이다. 기존 PlanBetaState v3 저장기에 조정 처방을
  거짓 flat PACE_TARGET으로 넣지 않는다. 실제 활성 저장·재시작 읽기는 다음 통합이다.
- 화면에서 이 함수로 연결되는 적용/취소, 계정 잠금 내부 최종 재검증,
  실제 저장된 조정 계획의 원본 일지 조회·다음 주기 연결은 아직 미완이다.
- 전체 프레임 정책 registry는 계속 비어 있다. 합성 시험 정책을 운영 승인으로
  옮기지 않는다. 새 훈련이 사용자에게 활성화됐다는 보고도 하지 않는다.

다음 작업은 별도의 범위 재기획이 아니라 이 선택 결과의 버전별 저장/과거 읽기,
실제 UI 적용/취소, 일지/연속성 연결을 같은 M01~M16 계획에 따라 이어가는 것이다.
전체 목표·공개 검수·배포 완료는 아직 성립하지 않는다.

## 32. 활성 저장소와 실제 일지 원본 조회 연결 (2026-09-07)

조정 계획의 활성 저장 envelope v4를 추가했다. 기존 계정별 활성 계획 키와
동일한 변경 잠금을 사용한다. 이 버전은 보관 이력의 버전 번호와 별개다.
저장 직전에 현재 안전 판정, 기록, 조정 정책과 후보 동일성을 다시 확인한다.
취소·계정 변경·동시 변경은 저장하지 않고, 쓰기 실패 시 자신이 쓴 내용만 되돌린다.
동일한 미수행 계획의 재시도는 최초 선택 시각과 내용을 보존한다.

기존 v3 저장 및 보관 함수가 v4 계획을 덮어쓰거나 삭제하지 않도록 차단했다.
불변 선택 내용과 변경 가능한 수행 상태를 구분하고, 과거 읽기는 보존된 검토
버전으로 재구성한다. 과거 읽기 성공은 현재 실행 또는 재채택 권한이 아니다.

실제 일지 저장·조회 경로에 조정 계획 원본 비교를 연결했다. 날짜와 오전/오후,
세션 전체 내용의 지문으로 연결하며, 목표 초·운동/회복 구조와 당시 설명을 읽는다.
일지 미입력을 실제 수행값으로 채우지 않고 메모 원문을 근거에 넣지 않는다.
계정 변경 및 저장 내용 변조 시 기존 비교 화면을 유지하지 않는다.

검증 범위:
- 전체 app: 296파일 / 2,646 PASS, exit 0. 이후 읽기 모듈 분리와 구 저장소의
  보관 차단 보완은 아래 최종 관련 시험으로 검증했다. 전체 재실행으로 혼동하지 않는다.
- impl: 27파일 / 859 PASS, exit 0.
- 최종 관련 시험: 4파일 / 31 PASS, exit 0. 실제 활성 키 저장 및 일지 저장,
  기간 만료 후 과거 읽기, 변조, 계정 전환, 취소, 재시도, 쓰기 실패를 포함한다.
- JournalOriginalPlan 화면 시험은 실제 저장 경로와 합성 검토 자료를 사용한다.
  운영 registry에 시험 정책을 등록한 것이 아니며 새 전체 브라우저 여정도 아니다.
- 기존 일지 원본 브라우저 시나리오는 4환경에서 각각 성공 표시를 확인했다.
  실행기 종료가 멈춰 중단했고 exit 1이므로 브라우저 전체 PASS로 집계하지 않는다.
- 읽기 경로를 선택/생성 경로에서 분리해 초기 JS를 구현 중 576.04kB에서
  542.13kB로 줄였다. 이전 509.38kB보다 작아졌다는 의미는 아니다.
- 마지막 보관 차단 보완 후 타입 검사와 production build도 exit 0이다.
  최종 해당 청크는 542.27kB이며 기존 글꼴 경로/청크 크기 경고가 남아 있다.

남은 통합 관문:
1. 실제 계획 화면의 조정 편집·명시적 적용/취소를 새 저장 경로에 연결한다.
2. Home/PlanBeta의 v4 표시 및 수행 상태 변경을 연결한다. 기존 legacy loader는
   v4를 기존 계획으로 반환하지 않으므로 현재 저장 기능만으로 공개 완료가 아니다.
3. 조정 계획 보관·다음 주기 계보·관찰 연결 및 서버 보관 경로를 연결한다.
4. 수치별 근거 검토 묶음을 완성해 오너의 최종 채택을 받는다. 운영 및 보존 검토
   registry는 아직 비어 있고, 승인되지 않은 구성을 활성화하지 않았다.
5. 전체 실제 화면 왕복과 접근성·회귀 검수 후 PR 검수, 병합, 배포를 진행한다.

스펙 v0.13 §20에 저장/과거 읽기 경계를 반영했다. M01~M16의 전체 범위를
축소하거나 완료로 처리하지 않는다. 이번 단계는 실제 저장·일지 조회 통합이며,
계획 화면에서 사용자가 끝까지 수행할 수 있는 상태 또는 배포 완료가 아니다.

## 33. 실제 PlanBeta의 조정 계획 재열기 및 일지 진입 (2026-09-07)

PlanBeta 진입점이 버전별 저장 결과를 직접 구분하도록 연결했다. 검증된 v4는
날짜별 일정으로 열고, 읽기 실패를 '계획 없음'으로 바꾸거나 클라우드의 다른
계획으로 덮어쓰는 흐름에 보내지 않는다. 계정 전환과 외부 저장 변경 시 다시 읽는다.

조정 계획 화면은 날짜 선택, 날짜/요일, 오전 다음 오후 순으로 정확한 처방 구조와
참고 시간, 접힌 이유 설명을 표시한다. 계획에서 '이 훈련 일지 쓰기'를 누르면
기존 planned-session-link로 원본 세션을 연결한다. 일지에서 돌아온 링크도 검증해
해당 날짜를 연다. 클릭 직전에도 저장 내용을 다시 읽어 오래된 화면의 연결을 막는다.
숫자는 다시 계산하거나 승인하지 않으며 당시 저장 내용 그대로다.

검수 중 합성 계획의 저장 순서가 오후→오전인 경우 표시가 뒤집히는 것을 발견해
오전→오후 정렬을 추가했다. 원본 데이터의 순서나 지문은 변경하지 않는다.

- 관련 3파일 20 PASS. 마지막 클릭 직전 재검증 보완 후 신규 화면 5 PASS.
- 타입 검사 및 production build PASS(마지막 클릭 guard 전). 마지막 guard 뒤
  타입 검사도 exit 0으로 확인했다. 기존 글꼴 경로 및 큰 청크 경고는 남아 있다.
- 운영 검토 registry 대신 시험에서만 보존 근거를 주입하며 실제 저장 경로와
  PlanBeta 화면을 사용했다. 픽셀/모바일 브라우저 실측 완료는 아니다.

아직 남은 것은 편집기 최종 적용 경로, Home의 v4 카드, 수행 상태 쓰기,
보관·다음 주기·서버 연결, 운영 수치 승인과 전체 브라우저 검수다.
이번 화면은 이 연결들을 대신하는 최종 축소안이 아니다. 저장 안내에도 아직
서버 보관/다음 주기 연결 중임을 표시했고 시작·재시작 권한을 만들지 않았다.

## 34. 조정 계획 수행 기록의 실제 화면·저장 연결 (2026-09-07)

조정 계획 일정의 완료·휴식·건너뜀·통증 확인 버튼을 같은 활성 저장소에 연결했다.
전체 envelope 지문과 계정/날짜/오전·오후를 잠금 안에서 확인한 뒤 진행 상태만
저장한다. 불변 처방과 이유는 변경하지 않고 일지의 실제 수치를 생성하지 않는다.
통증 확인을 다른 진행 버튼으로 지우는 동작과 휴식 세션의 운동 완료를 거부한다.
이는 다음 주기 안전 판정이나 통증 해제 기능의 구현 완료를 뜻하지 않는다.

저장 성공 후 날짜 선택을 유지한다. 계정 변경, 이전 화면 지문, 없는 세션,
저장 후 예외에서 내용 복원 등을 시험했다. UI 시험도 실제 PlanBeta 버튼을 누르고
실제 저장값과 선택 날짜/처방 유지 여부를 검사한다. 운영 근거는 비어 있고 시험
포트에서만 합성 보존 근거를 주입한다.

신규 도메인 5건과 실제 화면 6건, 총 11 PASS를 확보했다. 초기 build는 시험용
잠금 mock의 제네릭 타입 오류로 실패했고, production 코드를 완화하지 않고
mock을 실제 인터페이스로 수정했다. 후속 타입 검사/빌드 exit 0, 기존 화면 포함
3파일 25 PASS, exit 0을 확인했다. 스펙 v0.14 §20.1에 수행 기록의 경계를 반영했다.

아직 편집기의 최종 적용, Home v4, 보관/다음 주기/서버 통합과 전체 브라우저 검수,
수치별 최종 채택이 남는다. 이번 수행 기록은 전체 목표나 공개 완료로 집계하지 않는다.

## 35. 홈의 조정 계획 연결 (2026-09-07)

Home이 버전별 저장 읽기를 사용해 조정 계획을 '계획 없음'으로 표시하지 않게 했다.
홈 표시용 세션 타입은 조정 구조를 보존하고 기존 flat 처방으로 바꾸지 않는다.
카드에는 실제 sequence 합계의 반복/거리만 표시하고 구간별 시간과 회복은
계획 화면에서 확인한다. 같은 날 오전/오후 순서를 유지하며 진행 기록이 있는
세션은 다음 예정 훈련에서 제외한다. 통증 기록은 별도 홈 경고로 유지한다.

누적 거리 패널의 주기 날짜 범위와 콘텐츠 노출 조건에도 실제 조정 계획의
존재를 반영했다. 에너지 원장 분석 입력에는 조정 계획을 기존 타입으로 위장해
넣지 않았다. 그 분석 연결은 별도 후속 점검 대상이다.

관련 홈/접근성/모드 4파일 37 PASS, exit 0. 새 시험은 검증된 조정 selection을
홈 모델과 실제 TrainingHome 카드에 전달해 2회/800m 표시, 계획 버튼 동작,
오전 완료 후 오후 이동을 확인한다. 실제 Home 전체의 계정별 브라우저 왕복은
아직 별도 검수 대상이다. 초기 build PASS 후 진행 제외/통증 경고 보완이 있다.

편집기 최종 적용·취소, 보관/다음 주기/서버, 수치 승인, 전체 브라우저 검수는
여전히 남아 있다. 기능 완료나 배포로 보고하지 않는다.

## 36. 최종 조정 계획 적용 화면과 저장 연결 (2026-09-07)

AdjustedPlanApplyReview를 추가했다. 변경할 날짜/오전·오후와 실제 구성/수치를
표시하고, 전체 계획 저장 버튼을 직접 누른 경우에만 기존 조정 계획 저장 거래를
호출한다. 편집기의 임시 변경 저장과 전체 계획 채택은 같은 동작으로 취급하지 않는다.
확인 화면에서는 아직 저장 전이라는 문구를 별도로 사용한다.

열었을 때의 요청 전체 지문을 고정하고, 적용 순간에 현재 draft와 요청을 비교한다.
검토 자료는 최신 readReview 공급자를 통해 잠금 안에서 읽는다. 취소/언마운트는
대기 중인 저장 요청을 무효화한다. 재시도는 같은 요청이며 숫자를 재생성하지 않는다.

실제 저장소와 연결한 화면 시험 4 PASS: 명시적 버튼 전 무저장, 정확한 조정 계획
저장, 취소 무저장, 오래된 draft 거부, 잠금 대기 중 취소를 포함한다(취소 관련
행동은 여러 시험에 걸쳐 검증). 운영 registry에 합성 정책을 넣지 않았다.

이 화면을 PlanBeta의 후보/편집기에서 생성한 검토안으로 호출하는 상위 연결은
아직 남는다. 독립 화면 시험을 실제 앱 전체의 편집→적용 완료로 부르지 않는다.
보관/다음 주기/서버, 수치 최종 승인, 전체 브라우저·과학/UX 검수도 남아 있다.

## 37. 기존 편집기에서 최종 계획 저장까지 연결 (2026-09-07)

AdjustedPlanEditFlow로 실제 PrescriptionAdjustmentEditor와 최종 적용 화면을
연결했다. 검토된 source offer의 구성을 편집기에 전달하고, 편집 결과 receipt와
실제 수치가 같은지 검사한 뒤 새 스냅샷/후보 지문을 만든다. 오래된 스냅샷의
조정 수치를 그대로 복사하지 않는다. 구성별 설명은 별도 최신 검토 공급자가 준다.

편집기 적용은 임시 후보 생성이며 active 저장은 하지 않는다. 최종 화면에서
명시적으로 저장할 때 기존 전체 계획/안전/계정/기록 재검증과 원자적 저장을 거친다.
편집 전 seed가 바뀌거나 draft가 무효화되면 최종 저장이 거부된다.

실제 편집기 radio 선택→적용→최종 확인→실제 저장 시험 및 중간 취소/조건 변경
3 PASS. 기존 편집기/commit host/최종 확인을 포함한 4파일 30 PASS, exit 0.
초기 시험은 원래 구성을 선택해 적용이 비활성화된 탓에 실패했다. production의
같은 구성 차단을 없애지 않고 다른 구성 선택을 시험하도록 수정했다.

아직 PlanBeta 후보 화면의 운영 진입점/검토 공급자 연결은 남는다. 이 복합 화면
시험은 그 진입점까지 공개됐다는 증거가 아니다. 운영 registry는 여전히 비어 있다.
보관/다음 주기/서버 및 전체 실제 브라우저/과학/UX 검수와 수치 최종 승인도 남는다.

## 38. PlanBeta 후보 화면 진입점 연결 (2026-09-07)

PlanBeta의 후보별 조정 진입점을 연결했다. 독립된 검토 공급자가 반환한 seed의
전체 후보/생성 결과/선수 근거/인테이크/시작일/안전 확인이 현재 화면과 동일할 때만
조정 버튼을 전달한다. 공급자 오류나 불일치는 원래 후보를 유지하며 조정만 제외한다.
기준 기록 재확인 대기와 세션 위치 변경 대기 중에는 조정 진입도 막는다.

진입 후 기존 편집기→최종 확인→저장 흐름을 호출하고, 성공 시 상위 저장 reader를
갱신해 새 일정으로 이동한다. 취소는 후보로 돌아오며 선택 revision 변경은 무효화한다.
아직 운영 resolver/근거 등록은 없다. 선택적 공급자 경로 구현을 공개 활성화로
혼동하지 않는다. 새 수치의 최종 채택 전에는 운영 버튼이 노출되지 않는다.

검증: 후보 진입/기존 상세 페이스 흐름/조정 편집 3파일 19 PASS, exit 0.
타입 검사와 production build PASS. 신규 시험은 버튼 없음/검토 경로 전달 시 노출/
비활성 조건을 검증한다. 실제 PlanBeta 인테이크부터 합성 운영 공급자까지 사용하는
전체 왕복 및 모바일 실측은 아직 추가 검수 대상이다.

후속: 운영 근거 공급자와 수치별 채택 묶음, 보관/다음 주기/서버, 분석 연결 및
전체 브라우저/과학/UX 검수. 전체 목표와 M01~M16 완료를 선언하지 않는다.

## 39. 조정 원본 보관 및 과거 일지 조회 (2026-09-07)

계정별 조정 원본 보관을 추가했다. 보관 자료에는 불변 선택과 진행 상태를 포함한
검증된 v4 원본, 보관 시각, 전체 지문을 넣는다. 최대 최근 18개를 유지한다.
현재 계획과 같은 잠금 안에서 원본/계정/저장 상태를 확인하며, 실패 시 자신의
보관 쓰기만 되돌린다. 현재 계획을 지우거나 다음 주기를 생성하지 않는다.

일지 조회는 현재 계획 다음에 조정 원본 보관을 조회한다. 설명 ID만 비교하지 않고
보존된 근거와 원본 전체를 재검증해 정확한 당시 설명을 반환한다. 실제 일지 저장,
보관, 이후 활성 키 제거를 합성 시험으로 수행해 ARCHIVED 원본 조회를 확인했다.
보관 함수 자체가 활성 키를 제거한 것은 아니다.

신규 시험 3 PASS: 실제 일지 원본 재조회/계정 격리, 반복 보관/변조 거부,
보관 실패 롤백 및 활성 원본 보존. 운영 등록이나 다음 주기 활성화를 시험한 것은
아니다. 다음 주기 거래에서 보관을 호출하는 연결, 계정 연결/삭제 및 서버/백업의
새 보관 키 수명주기 통합도 아직 남아 있다. 운영 활성화 전 필수 검수 항목이다.

## 40. 보관 원본 삭제 경로 및 전체 앱 회귀 확인 (2026-09-07)

새 조정 원본 보관 키가 전체 데이터 삭제 목록에 빠져 있음을 확인하고 추가했다.
게스트와 모든 계정별 보관 키를 포함하며, 내용이 손상되어 파싱되지 않아도 키를
삭제한다. 다른 앱의 데이터는 보존한다. 실제 사용자 데이터 삭제는 실행하지 않았고
합성 localStorage 자료로만 시험했다. 서버 삭제/동기화 구현 완료를 의미하지 않는다.

관련 삭제 및 화면 전환 시험 2파일 36 PASS. 이후 코드를 수정하지 않은 상태로
전체 앱 vitest를 재실행해 302파일, 2670 PASS, 실패/보류 0, exit 0을 확인했다.
실행 JSON은 app/test-results/workflow-full-unit-review.json에 생성됐다(로컬 산출물).
앞선 관측에서 화면 전환 실패와 실행 도중 수정된 삭제 시험 실패가 있었으므로
그 실행은 성공 근거로 사용하지 않는다. 새 안정된 트리의 실행 결과만 위에 기록했다.

오너 승인 방식은 기존 결정 문서의 `근거 검토 후 내가 최종 승인`을 유지한다.
개별 수치 최종 승인, 운영 resolver/등록, 다음 주기 거래, 서버/백업 연결,
실제 브라우저 전체 여정과 독립 검수는 여전히 남는다. 부분 연결과 단위 시험을
전체 M01~M16 완료나 배포 증거로 취급하지 않는다.

## 41. 다음 후보 추천에 조정 구성 이력 연결 (2026-09-07)

기존 loadPlanMethodHistorySnapshot은 일반 계획 보관 이력만 읽었다. 같은 실제
추천 입력 경로에 검증된 조정 원본 보관을 연결했다. 조정 전 템플릿 대신 실제
선택한 source configuration의 family/configuration/version을 전달한다. 완료는
사용자가 보고한 PERFORMED, 미기록은 MISSING, 휴식/건너뜀/통증은 NOT_PERFORMED로
유지한다. 실측 거리·페이스나 생리적 적응을 만들어내지 않는다.

계정/종목을 구분하고, 현재 계획의 보관 사본 및 기존 candidate ID 중복은 제외한다.
보관 근거가 없거나 보관/현재 상태를 읽지 못하면 coverage를 null로 둔다. 알 수
없는 이력을 0회 수행으로 표시하지 않는다. 기존 일반 이력과 조정 이력의 보관 수,
누락/미연결 수와 보관 기간도 함께 집계한다. 계약 v0.16 §20.3에 반영했다.

검증: 관련 5파일 37 PASS, 타입 검사 PASS, production build PASS. 신규 시험은
실제 저장→진행 상태 인코딩→보관→추천 입력→recommendMethods 계산을 연결해
5가지 결과를 확인했다. 계정/종목 제외, 활성 사본 제외, 손상/근거 없음/현재 상태
확인 불가도 검사했다. 시험에서 활성 키를 제거해 과거 상태를 만들었으며,
실제 다음 주기 거래가 그 제거를 수행한 것은 아니다.

초기 타입 검사는 중첩 prescription 판별 후 바깥 session 타입이 좁혀지지 않아
실패했다. 단언으로 숨기지 않고 필요한 필드를 명시해 교정한 뒤 재통과했다.
기존 폰트 경로 및 500kB 번들 경고는 남는다. 이번 변경 후 전체 앱 2670 PASS를
재실행한 것은 아니므로 이전 §40 결과를 이번 변경의 전체 회귀 증거로 쓰지 않는다.

남은 핵심: 보관과 활성 계획 교체의 한 거래, 조정 계획의 전진 계보/다음 주기
선택, 운영 근거 resolver와 최종 채택, 서버/백업, 전체 브라우저 사용자 여정.
이력 입력 연결은 구현됐지만 다음 주기 전체 완성으로 보고하지 않는다.

## 42. 조정 계획의 다음 주기 준비 경계 (2026-09-07)

기존 next-frame 저장기는 v3 전용이며, adjusted selection은 첫 주기만 수용한다.
이를 우회해 v4를 v3로 변환하거나 새 initial lineage를 저장하지 않았다. 다음
거래에서 사용할 prepareAdjustedNextFrame을 추가해 전체 v4/보존 근거와 예상
지문, 현재 안전, 기존 통증 hold, 일정 시작 여부와 표시 구간 진행 상태를 검증한다.

출력은 기존 programLineageId/startedAt을 보존하고 기존 규칙으로 한 프레임 전진한
위치, 이전 원본 지문, 실제 결과별 횟수, 미응답 수, 넘어갈 수 있는 이유를 담는다.
모두 응답한 경우와 표시 기간이 지난 경우를 구분한다. 후자는 미기록을 완료로
바꾸지 않는다. 날짜는 현지 달력으로 비교하고 오전/오후를 별도 슬롯으로 센다.
9.5일 실시간 경과의 검증이나 훈련량 변경 권한으로 해석하지 않는다.

계약 v0.17 §20.4에 반영했다. 신규 8개를 포함한 관련 3파일 32 PASS 및 타입 검사
PASS. 실제 생성 fixture를 확장해 7/9/10일·일 2회 구성을 검증했다. 무저장,
계보 보존, 완료/기간 경과/통증/현재 위험/변경된 원본/근거 없음/미래 시작/과거
시작일/접근자 입력 차단을 확인했다. 합성 fixture는 운영 승인으로 등록하지 않았다.

이 함수는 아직 실제 다음 주기 화면·저장 거래에 연결되지 않았다. 다음 구현은
준비 결과를 fresh 후보 생성에 전달하고, 같은 mutation lock 아래 이전 지문을
재확인하여 보관과 새 선택 저장을 묶는 것이다. 그 과정에서 successor historical
reader가 초기 주기 전용으로 제한된 부분도 함께 확장해야 한다. 기존 제한을
단순 제거하면 계보 초기화 또는 검증 우회가 되므로 저장/읽기 시험을 함께 요구한다.
운영 resolver/정확한 수치 최종 승인/서버·백업/전체 브라우저 검수도 여전히 남는다.

## 43. 검증된 조정 원본으로 실제 다음 후보 생성 (2026-09-07)

generateAdjustedNextFrameFromDraft를 기존 생성기에 연결했다. 현재 계정의 실제
active v4를 읽고 예상 지문/안전/완료 조건을 확인한 다음, 기존 legacy 이력 대신
검증된 이전 진행 횟수를 생성기에 전달한다. 생성 후 계정과 active 지문을 다시
확인한다. 다른 종목은 같은 프로그램의 successor로 조용히 이어 붙이지 않는다.

출력은 일반 generated 결과와 구분되는 adjusted_next_frame_draft이며 전용
REVIEWED_SUCCESSOR_TRANSACTION을 요구한다. 이전 계획은 계속 active로 남는다.
후보가 생성됐다고 이전 조정 구성이 다음 후보에 자동 채택되는 것은 아니다.
조정 후보의 검토/비교 및 최종 저장·계보 reader 확장은 별도 남은 관문이다.

신규 실제 생성 시험은 후보 2개에 이전 결과 횟수 전달, 무교체, 생성 도중 원본
변경, active 없음/지문 변경/종목 변경을 검사했다. 전체 앱 vitest는 날짜 보정 전
303파일 2688 PASS, 실패/보류 0, exit 0이었다. 로컬 JSON:
app/test-results/adjusted-next-generation-review.json.

추가 검토에서 다음 시작일이 있어도 formation은 오늘을 사용하고, completeIntake가
시작일을 제거하는 경로를 발견했다. successor 경로의 내부 formation 기준일을
예약 날짜로 전달하고 반환 intake에도 같은 날짜를 보존했다. 이 경로의 신규
시험은 수정 도중 실제 실패했으며, 수정 후 관련 3파일 37 PASS 및 타입 검사 PASS.
날짜 보정 뒤 전체 2688개를 다시 실행했다고 주장하지 않는다. 일반 신규 계획의
기존 날짜 동작은 이번 변경에서 별도로 바꾸지 않았다.

PR #319를 직접 조회해 Draft/open/미병합, base 9856693 및 변경 전 head ebe99fb를
확인했다. 작업은 같은 브랜치로 계속하며 검수/병합/배포 완료를 주장하지 않는다.
다음 작업은 이 distinct draft를 검토된 조정 후보 선택으로 넘기고, 보관/새 선택
저장/전진 계보/과거 읽기를 하나의 successor 경로로 완성하는 것이다.

## 44. 다음 조정 계획 선택·보관·저장·과거 읽기 거래 (2026-09-07)

selectAdjustedPlanSuccessor와 saveSelectedAdjustedSuccessor를 추가했다. 실제 이전
v4를 보존 근거로 읽고, 예상 지문/현재 안전/기존 통증/종목/진행 횟수 일치와 새
후보의 기록·전체 배치 검토를 재확인한다. 초기 선택 함수는 여전히 successor를
거부한다. 이전 원본 보관과 새 active 저장은 같은 계정 mutation lock 아래에서
수행하고 두 키를 확인한다. 실패 시 자신의 쓰기만 되돌리며, 다른 탭의 값은
덮어쓰지 않고 상태 불확실로 보고한다. 이미 바뀐 predecessor 재요청은 STALE_BASE로
거부하므로 다음 계획의 진행을 초기화하지 않는다(재요청 성공 영수증은 아님).

불변 선택에 continuation을 선택적으로 추가했다. 초기 선택은 기존 형식을 유지한다.
successor는 이전 envelope/selection 지문과 이전 periodization을 보존하고 전진한다.
historical reader는 이 메타데이터와 처방을 재구성한다. 이것이 전체 보관 사슬의
독립 서명 검증은 아니며, 로컬 지문이나 주기 메타데이터는 처방/실행 권한이 아니다.
실제 이전 원본과의 결속은 저장 거래에서 수행한다. 계약 v0.19 §20.6에 명시했다.

AdjustedPlanEditFlow 및 최종 확인 화면에 predecessor 문맥을 전달하는 경로를
연결했다. 다음 계획 저장 문구는 이전 계획 보관/전환임을 알리며, 초기 조정의
"다른 날짜는 그대로" 문구를 잘못 재사용하지 않는다. 화면을 연 뒤 predecessor가
바뀌면 저장을 거부한다. 상위 일정→다음 후보 진입점과 운영 공급자는 아직 남는다.

검증: 관련 11파일 96 PASS, 타입 검사와 production build PASS. 추가로 같은
시험에 새 주기 일지 저장/ACTIVE 원본 읽기를 넣은 후 successor 9 PASS를 재확인했다.
실제 생성→최종 확인 화면→저장, 이전 일지 ARCHIVED 읽기, 새 일지 ACTIVE 읽기,
1→2→3 주기 계보와 두 이전 원본 보관, 보관/active 쓰기 실패, 다른 탭 개입,
취소/계정 변경/검토 철회/안전/손상 보관/초기 API 우회를 검사했다. 시험용 근거는
운영 등록하지 않았다. 전체 앱/실브라우저/배포 검수 완료로 확대해서 주장하지 않는다.

운영 준비 검토 사항: 현재 whole-frame scope는 continuity 결과 횟수와 exposure
ledger fingerprint까지 포함한다. 운영 채택안을 선수별 일지 횟수/날짜마다 다시
승인하는 구조로 만들지 않도록 검토 가능한 구조 범위와 개인별 fresh 검증을
분리해야 한다. 기존 보존 정책 버전의 역사 읽기는 유지하면서 정리할 후속 사항이다.
새 수치 최종 승인, 운영 resolver, 상위 다음 주기 화면 연결, 서버/백업 및 전체
사용자 여정 검수는 남으며 M01~M16 전체 완료/병합/배포를 선언하지 않는다.

## 45. 오너 최종 승인 경로와 재사용 가능한 구성 검수 범위 (2026-09-07)

오너 응답은 `근거 검토 후 내가 최종 승인`이다. 기존 결정 문서의 절차 승인을
재확인했으며, 아직 제시하지 않은 구성의 수치 승인으로 확대하지 않았다.

계약 v0.20 §20.7과 STRUCTURAL_V2 검수 범위를 추가했다. 종목/인구집단/경험/
선택권한/프레임/모든 날짜·오전오후 배치/구성 버전/보조 훈련량/MAIN 횟수는
유지한다. 이전 주기 종류는 유지하되 개인별 완료·휴식 등 횟수와 노출 식별자는
재사용할 구성 검수 지문에서만 제외한다. 전체 후보/스냅샷과 실제 저장 전 검증은
여전히 원본을 결속하므로 이 변경은 개인별 안전 검사나 전진 조건을 생략하지 않는다.
기존 정책은 버전 필드 없이 v1로 읽는다. 새 범위를 적용하려면 명시적 v2 정책과
그 범위의 별도 채택이 필요하며, 알 수 없는 버전/복수 일치 정책은 거부한다.
운영 registry는 계속 비어 있고 신규 활성화는 없다.

§44의 날짜 관련 추정을 정정한다. 실제 현재 exposure 식별자는 주기 내 일자
번호이며 절대 날짜가 아니다. 시작일만 바꾸면 기존 v1 검수 지문도 유지된다.
실제 문제는 이전 결과 횟수가 구성 검수에 포함되는 점이었다. 실패한 합성 테스트의
입력을 실제 후보 스키마와 대조해 확인했으며, 스키마를 느슨하게 바꾸지 않았다.

검증: 관련 11파일 100 PASS, 타입 검사 PASS. 실제 저장된 이전 계획의 한 결과를
COMPLETED에서 RESTED로 바꾸고 기존 생성기로 다음 후보를 다시 만들었다. 새
구성 검수 지문은 같고 개인 후보 지문은 달라짐을 확인했다. 초기/후속 저장 및
과거 읽기 회귀 검사도 포함한다. 중간 실행에서는 저장 테스트 1건이 5초 제한을
초과했다. v1 정책에도 v2 검사를 불필요하게 반복하던 코드를 제거하고, 테스트
제한을 늘리지 않은 동일 명령으로 100개 통과를 확인했다.

남은 작업은 상위 일정에서 다음 후보·편집·확정으로 이어지는 화면 연결, 정확한
구성/대상/한계를 담은 최종 채택 패킷, 승인 후 운영 공급자 연결, 서버/백업,
전체 사용자 여정 및 독립 검수이다. 이번 범위는 전체 계획 완료나 공개 배포가 아니다.

## 46. 실제 일정에서 다음 주기 비교·편집·최종 저장으로 연결 (2026-09-07)

PlanBeta의 조정 일정에 `다음 주기 준비` 진입점을 연결했다. 새 화면은 시작일,
현재 몸 상태, 명시적으로 고른 경기 기록을 받아 실제 successor 생성기를 사용한다.
기존 기록 선택을 자동 재사용하지 않는다. 생성 전/편집 진입 전 계정과 이전 전체
상태 지문을 다시 읽으며, 계정·storage 이벤트는 열린 초안을 닫는다. 후보 비교와
날짜별 오전/오후 처방을 확인한 뒤 검토 공급자가 일치하는 후보만 편집기로 넘긴다.
공급자 없음/불일치 상태는 안내하고 legacy 저장으로 우회하지 않는다.

초기 후보와 다음 후보의 공급자 문맥 비교를 공통 함수로 묶었다. generated,
candidate, intake, athlete evidence, gate, current check, start date가 모두 맞아야
진입한다. 편집 후 별도 최종 저장에서 이전 원본 보관과 새 active 전환 거래를
호출하고, 성공하면 실제 새 일정으로 돌아온다. 취소는 이전 원본을 그대로 보존한다.
기존 smooth scroll/reduced-motion 훅으로 질문·비교 화면 진입과 키보드 초점을 연결했다.

실제 연결 시험 중 화면의 stale 검사에 저장 전 원본 읽기를 넣으면 거래가 자신의
정상 active 쓰기까지 외부 변경으로 오인해 롤백하는 문제가 재현됐다. 화면의
현재성은 계정/마운트/열린 predecessor로 제한하고, 저장 바이트 재검사는 실제
거래의 CAS가 담당하도록 수정했다. 최종 확인 후 다른 writer가 바꾼 값은 여전히
거부하고 보존하는 반례를 추가했다. 거래의 원본 검사 자체를 제거한 것이 아니다.

검증: 실제 PlanBeta 일정→다음 생성→조정 editor→최종 확인→보관/저장→새 일정
통합을 포함해 화면 4파일 17 PASS. 취소, 검토 없음/다른 날짜의 잘못된 provider,
현재 통증, 이벤트 없이 변경된 저장소, 계정 전환, 최종 확인 후 외부 쓰기를 검사했다.
타입 검사 포함 production build PASS. 기존 폰트 unresolved/500kB 번들 경고는 남는다.
실제 브라우저의 320/375px·200% 글자·키보드/모션 전체 검수는 아직 하지 않았으며
컴포넌트 시험을 이를 대체하는 증거로 쓰지 않는다.

M06~M10/M14의 상위 화면 연결은 전진했지만 운영 registry는 여전히 비어 있다.
따라서 현재 공개 사용자에게 새 조정 구성을 제공했다거나 전체 MAIN 다중 구성이
완료됐다고 하지 않는다. 다음 우선 작업은 정확한 채택 패킷/실제 운영 공급자,
검토용 브라우저 시연과 전체 여정, 서버/백업 및 M01~M16 잔여 조건이다.

## 47. 조정 계획 개인 보관 파일과 읽기 검증 (2026-09-07)

기존 일지 공유/전체 백업 형식에는 조정 계획 원본이 없음을 확인했다. 개인 페이스
근거를 기존 공유 파일에 자동 혼합하지 않고 별도 personal-backup.v1을 추가했다.
활성 v4와 최대 18개 보존 archive를 함께 검증·내보낸다. 계정과 두 저장 키를 읽기
전후 대조하고, 손상된 archive는 누락 처리하지 않고 전체 내보내기를 거부한다.
원본과 export 시각/전체 지문을 검증하며 앱 외부로 전송하거나 메모를 읽지 않는다.

파일 읽기는 앱이 별도로 보존한 근거로 각 계획을 재검증한다. 파일 안에 쓰인 검토
문구나 지문만으로 권한을 만들지 않으며 결과는 read_only/NONE/NOT_RESTORED다.
현재 active를 쓰거나 과거 계획을 다시 활성화하지 않는다. 복원 UI/거래와 서버
보관은 여전히 별도 미완 관문이다. 계약 v0.22 §20.9에 범위를 명시했다.

실제 PlanBeta의 저장과 이용 안내에 명시적 다운로드 버튼을 연결했다. 개인 기록과
훈련 진행을 포함하므로 공유하지 말 것, 메모 미포함, 다시 불러오기 화면 준비 중을
함께 표시한다. 성공하지 않은 다운로드를 완료로 알리지 않는다.

검증: 관련 2파일 10 PASS, 타입 검사 PASS. 실제 successor+predecessor 파일의
동일 원본 왕복 읽기, 앱 근거 없음, 수치/진행 변조, 미래 시각, stale active,
손상 archive, 읽는 중 다른 writer, 메모 비포함, 명시적 버튼 전 다운로드 없음과
버튼 클릭 시 파일 생성·기존 원본 보존을 확인했다. OS 다운로드 완료/실브라우저
복원/서버/전체 백업 복구가 검증됐다는 뜻은 아니다. 전체 목표는 계속 미완이다.

## 48. 개인 계획 파일의 명시적 과거 원본 복원 (2026-09-07)

개인 파일 선택→원본 개수 확인→자기 파일 확인→과거 보관함 추가 화면을 연결했다.
기존 계획이 없는 화면과 조정 일정의 저장 안내에서 진입할 수 있다. 파일 읽기
도중 취소/계정 변경은 revision으로 무효화하며 파일 선택을 바꾸면 확인을 초기화한다.
현재 active 일정은 없애거나 새로 만들지 않는다. 복원된 archive는 기존 일지의
원본 조회가 읽는 동일한 계정별 보관함이다. 서버 저장이나 자동 실행 복원이 아니다.

실제 mutation lock 안에서 파일/앱 보존 근거/기존 보관함을 재검증한다. 기존 원본과
active 선택이 있으면 유지한다. 파일 내부 중복은 active 진행본을 과거 보관본보다
먼저 취급한다. 18개를 넘으면 기존 자료를 잘라내지 않고 거부한다. 쓰기 전후 계정/
active/archive/request를 대조하고 실패 시 자신의 쓰기만 되돌린다. 기존 지문이
같다는 이유로 다른 사용자의 파일임을 판정할 수 없으므로 자기 파일 확인은 사용자
선언이며 인증 증거가 아님을 계약 v0.23 §20.10에 명시했다.

검증: 관련 3파일 12 PASS, 타입 검사 포함 production build PASS. 실제 원본 내보내기
→현재 계획 없는 상태→과거 복원, 재요청 중복 보존, 파일 active/보관본 중복 우선순위,
기존 active 유지, 동의/근거/lock/현재 request 누락, 손상 보관함 유지, 쓰기 실패,
화면 확인 전 버튼 비활성 및 실제 복원 결과를 확인했다. 18개 한도와 외부 writer/
파일 읽기 취소의 모든 경합 조합, 실제 브라우저·전체 앱 회귀는 후속 검수 대상이다.
기존 font unresolved/번들 크기 경고는 유지된다. 배포/서버 복구 완료를 주장하지 않는다.

다음 우선순위는 정확한 구성·적용 대상·근거의 최종 오너 채택 패킷과 운영 공급자를
연결하는 작업이다. 합성 승인 자료로만 통과한 구현을 공개 기능 완료로 대체하지 않는다.
여러 MAIN/종목별 구성 확대, 실제 브라우저 전체 여정, 서버 계정 격리/보관, 독립 검수와
병합·배포가 남는다. M01~M16 전체 목표는 유지하며 완료 표시하지 않는다.

## 49. 최종 채택 준비의 원문 조건과 구조화 산수 (2026-09-07)

오너 검수 절차 승인 후 정확한 수치 승인안 완성을 계속 진행했다. 채택 패킷 v0.4는
원문 I 주간 거리 조건과 LT 제품 변형의 차이, 불완전 수집/미기록/9.5일 자료를
주간 거리로 오인하지 않을 조건을 추가했다. 기록 없는 사용자를 일괄 제외하지 않고
별도 채택 기본 구성과 출처 조건 확인 경로를 구분한다. 자동 강도 증가는 허용하지 않는다.

v0.5에서는 기존 16개 검토 행과 추가 유한 변형 8개를 연구용 구성 데이터로 옮겼다.
이는 운영 registry가 아니며 앱에서 사용하지 않는다. 반복/세트/종료 회복과 도움닫기
경계를 펼쳐 합산하며, 시간과 거리의 근거 없는 환산은 하지 않는다. 행 순서가 바뀌어도
변형의 부모 참조는 ID로 유지한다. 기존 독립 GLY 두 행은 변형 수로 중복 계산하지 않는다.

실제 node:test 2파일 22 PASS(구성9 + LT13). 오너 표기 2×(10×400m)의 회복18+1회/
1260초, 마지막 roll-on, 세트 분할 회복 증가, 도움닫기와 빠른 구간 분리, 미지정값 및
OFF의 관찰값 null을 확인했다. 이 시험은 연구용 산수이며 실제 앱 채택·과학적 효과·
대상 적합성·배포 증거로 사용하지 않는다. 소스 조건 반례 전체의 앱 연결은 미완이다.

남은 우선 작업은 준비/정리와 대상/주기 배치, I 개인 참고 모델, 설명/출처의 정확한
버전 및 전체 승인 패킷을 완성하는 것이다. 실제 운영 공급자, MAIN 복수 슬롯/전 종목,
브라우저 전체 여정, 서버 및 배포까지 M01~M16 범위는 그대로 유지한다.

## 50. 준비·정리 포함 검토용 전체 세션 (2026-09-07)

기존 manifest 네 구성과 pace-sequence의 실제 준비/정리 경계를 대조했다. 준비15분,
20초 점진가속4회/사이40초3회, 정리10분이라는 기존 수치에 마지막60초 전환을
별도 제안한 지원 구성 P-SUPPORT-MAIN-01@0.1을 채택 패킷 v0.6에 추가했다.
60초는 기존 승인에서 가져온 값이 아닌 미승인 운영 제안으로 명시했다.

연구용 세션 조립은 MAIN 지원1760초와 본운동을 분리한다. LT-B 전체3020초,
거리형 전체시간 미확정, BASE/REC/OFF 지원시간 추가 없음으로 검사했다. 기존
운영/과거 계획은 바꾸지 않았다. 모든 MAIN에 적합하다는 과학적 판정도 아니다.

실제 연구 시험2파일25 PASS. 이는 구성 산수와 입력/반환 객체 검증에 한정된다.
준비·정리의 정확한 초안이 생겼으나 대상/주기/장소·수행 조건, 설명 지문과
운영 스키마 연결·최종 채택·사용자 여정·서버·배포는 미완이다. 전체 목표 유지.

## 51. 독립 후보의 대상 범위 제안 (2026-09-07)

채택 패킷 v0.7과 연구용 범위 조회에 기존16행을1:1 연결했다. 목적별 전체 원형을
검토하고 이전 훈련의 고정 짝은 사용하지 않는다. 청소년/성인·SELF/COACH_REQUIRED의
제안 범위를 동일하게 유지하되 실제 선택 권한 검사는 우회하지 않는다. 장거리의 GLY
목적도 보존한다. 경기기록 유무로 경험을 추정하거나 개인800m RP를 타종목으로 환산하지 않는다.

실제 범위 시험6 PASS. 앞의 구성/페이스25 PASS와 별개이며 실제 운영 처방 활성화는
여전히0건이다. 검토용 후보 조회가 실제 프레임 적격성 판정은 아니라고 명시했다.
특정 종목의 MIX, DEVELOPING ATP에서2개 독립 방법이 아직 없다는 공백도 드러냈다.
같은 방법의 횟수변형으로 채우지 않는다. 원래30행과 M01~M16 목표는 유지한다.

다음은 공백 원형의 근거/정확한 구조, I참고모델, 실제 프레임 배치와 설명버전 완성이다.
최종 채택 전 검토자료를 runtime registry에 넣거나 배포완료로 보고하지 않는다.

## 52. I 구성의 현재5km 참고 모델 제안 (2026-09-07)

V.O2 공식 Interval Pace 절에서5km 경기 페이스의 체감 참고 안내를 확인했다.
정확한 I 계산과 동일시하지 않는 TO-HARD-5K-RP-REFERENCE@0.1을 연구용으로
추가했다. 현재 실제5km 기록으로1km/400m 참고 시간만 계산하며 본운동2/3/4분과
회복은 해당 구성에서 가져온다. 시간을 거리로 바꾸거나 다음주기 속도를 올리지 않는다.

패킷v0.8에 출처·표시문구·적용한계와 미완범위를 명시했다. 연구용4파일35 PASS.
원본 비배수/소수점 산수, 구성별 회복의 카탈로그 대조, 오래된/목표/타종목 거부와
미승인 변형 거부를 확인했다. 출처의 주간거리 조건이나 선수별 효과의 검증은 아니다.
전체 I 모델과5km 기록 없는 사용자 경로, 나머지 구성·프레임·최종채택 및 실제앱
연결/서버/브라우저/배포는 남는다. 전체 목표는 계속 미완이다.

## 53. 실제 엔진 전사에서 발견한 표현 한계 (2026-09-07)

연구 산수와 별도로 impl의 실제 V2 parser/totals를 실행했다. 새 재현4개와 기존
거리회복13개,2파일17 PASS. 이 PASS는 아래 한계의 재현 성공이지 수정완료가 아니다.

1. 회복 하나에100m와120초를 동시에 넣으면 거부한다. 필요한 것은 동시 목표가
   아니라100m 이후120초라는 순서 있는 복합 회복이다.
2. 3×2×300m의 마지막 자식 recoveryAfter에100m를 넣어도 실행/집계되지 않는다.
   부모 세트회복으로 대체되는 기존 의미 때문이다. 잘못 전사하면 roll-on은
   반복사이300m+종료100m만 남아 필요한600m보다200m가 빠진다.
3. 도움닫기20m+빠른10m를4회 일반 자식으로 놓으면 quality120m/반복8개가 된다.
   라벨만으로 빠른40m/블록4회/도움닫기80m를 분리할 수 없다.
4. WALK_OR_STAND는 기존 회복 enum에 없으며 단순 치환은 지시 손실이다.

선택 계약v0.24 §21에 버전된 후속 표현과 전체 연결 관문을 추가했다. 기존
V1/V2 의미나 과거 저장본을 소급 수정하지 않는다. 이 문제는 추가 오너 승인을
핑계로 보류할 사항이 아니라 이미 승인된 구현범위의 표현 작업이다. 다음 구현은
순서 있는 복합회복·구간역할/블록집계의 버전된 지원과 저장/설명/일지 전파다.
단일단위만 남기거나 회복을 본운동으로 넣어 우회하지 않는다. 운영 활성화는 미완이다.

## 54. V3 순서 회복·구간 역할·반복 블록 구현 (2026-09-07)

impl에 독립 V3 parser/totals를 구현했다. 기존 V1/V2 파일의 의미는 변경하지 않았다.
회복은 순서목록이며 마지막 recoveryAfter도 명시적으로 보존한다. WORK/BUILDUP/
PREPARATION과 SET/REPETITION/SEQUENCE를 구분해 도움닫기와빠른구간,반복블록을
각각 집계한다. WALK_OR_STAND도 실제 모드로 검증한다. 안전/채택 권한은 만들지 않는다.

3×2×300m은 work1800m, roll-on600m와추가240초를 보존하며 전체회복시간은null이다.
4×(20m도움닫기+10m빠른구간)는 반복블록4/본운동40m/도움닫기80m다. 오너표기
2×10×400m도20회/8000m/회복1260초로 계산한다. phase별 준비/본운동/정리를 분리했다.

새12개와 기존 관련114개,4파일126 PASS. 전체impl29파일875 PASS,타입검사PASS.
반환값동결,알수없는필드,중복ID,getter미실행,잘못된회복,중첩반복소유,부분미상으로
overflow숨김 방지와기존상한유지를 확인했다. 새형식의 실제 운영선택/저장/UI가
완료됐다는 뜻은 아니다. 다음은 V3 비교/설명/정확한후보와스냅샷/일지전파 통합이다.
독립 parser만으로 새운영구성을 활성화하지 않는다. 스펙v0.25 §21.4에명시했다.

## 55. V3 방법 비교와 원본 식별 (2026-09-07)

V3 MAIN 비교와 전체 원본 content identity를 추가했다. 이름/ID/기록참조/반복횟수/
준비정리 변경은 새방법으로 세지 않으며 빈단일래퍼도 정규화한다. 구간역할·운동
단위/값·목표·복합회복순서/마지막회복은 차이코드로 남긴다. 차이는 재검토 대상이지
채택/효과동등의 증거가 아니다. 전체identity는 기존 canonical helper의 V3별도
namespace를 사용해 모든phase/횟수/참조를 보존한다. 공개할 서명/승인토큰이 아니다.

V3두파일18 PASS,타입검사PASS. 반복/이름만변경한가짜후보,준비만변경,회복순서변경,
구간역할/단위변경,빈래퍼,불법형식과직렬화동일성을 확인했다. 스펙v0.26§21.5반영.
비교helper가생긴것이지 운영카탈로그/선택UI/저장/일지가V3로연결된것은아니다.
다음은 설명/출처와정확한V3원본을기존조정snapshot경로로전달하는통합이다.

[DRAFT_COMPLETE]
