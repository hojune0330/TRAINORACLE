# ACCOUNT_PLAN_ISOLATION_AND_SECOND_DEVICE_VERIFICATION_2026-09-10.md

## 상태

- status: PRODUCTION_ISOLATION_VERIFIED_SECOND_DEVICE_LOGIN_PENDING
- scope: 온라인 훈련 계획의 계정 격리와 새 기기 복원
- production_write_retained: false
- personal_plan_content_opened_by_test: false
- actual_second_device_login: PENDING_USER_AGE_AND_TERMS_INPUT

## 확인한 경계

### 운영 데이터베이스 A/B 격리

운영 데이터베이스의 기존 계획 묶음과 같은 암호화 행 형태를 트랜잭션 내부 시험 계정 A에 복제했다. 개인 계획 본문은 복호화하거나 결과로 반환하지 않았다.

1. A는 자신의 계획 색인을 읽을 수 있다.
2. A는 자신의 계획 조각을 읽을 수 있다.
3. A는 자신의 저장 영수증을 읽을 수 있다.
4. B는 A의 색인·조각·영수증을 모두 읽지 못한다.
5. 로그인 사용자 역할은 계획 테이블을 직접 조회할 권한이 없다.
6. 전체 시험은 `rollback`으로 끝난다.
7. 후속 조회에서 시험 사용자·색인·조각·영수증이 모두 남지 않았다.

실행 결과: PASS

재실행 가능한 원본은 `supabase/tests/account_plan_production_isolation_rehearsal.sql`이다. 실제 실행 전에는 운영 계획 색인이 적어도 하나 있어야 하며, 오류가 발생하면 PASS로 간주하지 않는다.

### 로컬 실제 PostgreSQL 격리

- PostgreSQL 17 독립 연결 세 개로 `isolation` 시나리오 실행: PASS
- A가 잠금을 보유한 동안 B의 별도 계획 저장: PASS
- 상대 계정의 조각·영수증 조회 차단: PASS
- 동일 작업 식별자의 계정별 독립성: PASS
- 검사 종료 후 임시 PostgreSQL 프로세스 중지: PASS

결함 주입에서는 상대 계정 영수증을 반환하도록 메모리 안의 SQL만 바꾸자 동일 검사가 실패했다. 이 실패는 제품 실패가 아니라 검사가 격리 결함을 실제로 탐지한다는 증거다.

### 깨끗한 두 번째 기기 복원 계약

첫 기기 역할의 서비스가 과거 계획과 현재 계획을 서버에 저장한 뒤 닫혔다. 두 번째 기기 역할에는 새 로컬 버퍼를 사용해 첫 기기의 로컬 데이터를 전달하지 않았다.

1. 두 번째 기기의 계획 로컬 저장소가 비어 있음을 먼저 확인한다.
2. 서버에서 현재 계획만 우선 복원한다.
3. 과거 계획은 사용자가 이력을 요청할 때 추가로 불러온다.
4. 최종 계획 문서가 첫 기기에서 서버 확인을 받은 문서와 정확히 같다.

집중 테스트 결과: 31/31 PASS

## 아직 남은 실제 화면 확인

별도 Edge 브라우저에서 TrainOracle의 완전히 빈 첫 화면까지 확인했다. 이 브라우저에는 일지와 계획이 없다. 같은 실제 계정으로 로그인하면 서버 계획이 복원되는 최종 화면을 확인할 수 있다.

로그인 전에 나이 확인과 필수 약관 동의가 필요하다. 실제 생년월일을 추정하거나 다른 값으로 대신 입력하지 않았으므로 이 한 단계는 사용자 입력 대기 상태다. 입력 후 확인할 항목은 다음과 같다.

1. 로그인 직후 온라인 계획 보관함이 한 건 이상으로 표시되는가.
2. 저장된 계획을 열면 기존 계획과 날짜·세션 구조가 같은가.
3. 새 브라우저에 원래 없던 기기 계획을 서버 계획으로 잘못 표시하지 않는가.
4. 새로고침 후에도 온라인 계획이 유지되는가.
5. 브라우저 오류가 없는가.

## 판정

계정 간 저장 권한 격리는 운영 데이터베이스에서 확인됐다. 새 기기 복원 로직은 완전히 빈 로컬 저장소를 사용한 계약 검사에서 확인됐다. 실제 두 번째 브라우저의 로그인 후 화면만 완료 증거가 없으므로 `PUBLIC_SECOND_DEVICE_VERIFIED`라고 주장하지 않는다.

[DRAFT_COMPLETE]
