# 개인 목표 페이스 — 오너 결정 기록

```yaml
decision_record:
  id: DECISION-PERSONAL-PACE-001
  decided_by: OWNER
  decided_at: "2026-07-27"
  basis: DECISION_BRIEFING_PERSONAL_PACE.md
  verdict: 브리핑 권고안 전부 승인
  execution: 토큰 소모가 큰 작업은 하위 에이전트에게 위임
```

> 이 문서는 **승인된 결정의 정본**이다. 이후 어떤 작업자도 이 결정을
> 임의로 바꾸지 않는다. 바꿔야 한다면 오너에게 다시 묻는다.

---

## 1. 승인된 결정 (6건 전부)

| # | 사안 | **승인된 결정** |
|---|---|---|
| 1 | 큰 갈림길 | **길 2 (환산 모델 도입)** — 단 순서는 권고대로 (1→2→3→4→5) |
| 2 | 목표기록 표시 | **2단 표시 승인** ("오늘 목표" / "목표 달성 시") |
| 3 | 오래된 기록 취급 | **만료 없음** — 지우지 않고 역할·달성일·경과 기간을 항상 표시 (§3, 오너 지적으로 정정) |
| 4 | 범위 표기 확정 | **오너·코치가 정함** — 코드 자동 선택 금지 |
| 5 | 미성년 | **당분간 성인만** — 현행 `minorAllowed: false` 유지 |
| 6 | 표기 정리 | **기계용 필드 추가** — 원본 `notationPattern` 보존 |

## 2. 결정 1의 정확한 의미 — 목적지와 순서

**목적지는 길 2다.** 개인 페이스는 부가기능이 아니라 코칭의 본체다.
길 1(경기 페이스만)은 **최종 목표가 아니라 4단계로 가는 검증 발판**이다.

```
1단계  선수 기록 입력            ← 승인 불필요, 즉시 착수
2단계  카탈로그 기계용 표기 추가   ← 원본 보존
3단계  길 1로 템플릿 2개 실작동    ← 배선 검증
4단계  환산 모델 도입 (길 2)      ← 목적지
5단계  구역 템플릿 13개 개방
```

**3단계를 4단계 앞에 둔 이유는 안전이 아니라 진단 가능성이다.**
배선(기록→계산→표시→폴백)이 먼저 돌아야, 4단계에서 문제가 생겼을 때
원인이 환산 모델인지 배선인지 구분할 수 있다.

> ⚠️ **3단계에서 멈추면 이 결정을 위반한 것이다.** 3단계는 경유지다.

## 3. 결정 3 정정 — 유효기간이 아니라 **역할과 경과 표시**

> 🔴 **오너 지적으로 정정된 항목이다.** 이전 버전에서 에이전트가
> `3개월`/`12개월` 경과 시 계산 거부(만료)를 제안했다. **이는 잘못이었다.**
> 오너 지적: *"유효기간은 없어야 되는 거 아냐?"* — **맞다.**

### 3-1. 왜 만료가 잘못인가 — 스펙이 이미 금지하고 있었다

`specs/reconstruct/FORMATION_LOAD_AND_STATISTICAL_RULES_CONTRACT.md` §9:

> "There is **no evidence-backed universal freshness cutoff** for this population."
> "A future cutoff is **product policy, not a scientific threshold**."

`specs/reconstruct/TRAINING_SESSION_PRESCRIPTION_CONTRACT.md` §4:

> `PB` — "**A PB can be old; it is never assumed to be current** without its state."
> `RECENT_RESULT` — "**stale is not silently current**."

**스펙이 요구하는 것은 "만료"가 아니라 "표시"다.** 3년 전 5000m 최고기록은
여전히 최고기록이다. 금지되는 건 그것을 **조용히 현재 실력처럼 쓰는 것**이다.

에이전트가 제안한 만료는 두 가지를 동시에 위반했다.

| 위반 | 내용 |
|---|---|
| 스펙 위반 | 근거 없다고 명시된 컷오프를 코드가 발명 |
| 오너 지침 위반 | "지우거나 하라는 게 아니야" — 만료는 사실상 버리는 것 |

### 3-2. 정정된 규칙

```yaml
기록_취급:
  만료: 없음               # 어떤 기록도 만료되지 않는다
  자동_삭제: 없음           # 오래된 기록도 지우지 않는다
  항상_함께_표시:
    - 역할                 # 현재 실력 / 시즌 맥락 / 목표
    - 달성일
    - 경과 기간            # "3년 2개월 전" — 이것이 곧 경고다
  오늘_기준_기록_선택: 사람  # 코드가 자동 선택하지 않는다
  거부하는_경우:
    - 달성일_없음           # 언제 세운 기록인지 모르면 경과를 표시할 수 없다
    - 종목_거리_없음         # 거리 없으면 페이스 계산이 불가능
```

**경과 기간을 항상 보여주면 컷오프가 필요 없다.**
`"2023-05-10 (3년 2개월 전)"`이 그 자체로 경고다.
따라서 **오너께 여쭐 개월 수는 더 이상 존재하지 않는다.**

### 3-3. 진짜 축은 "기간"이 아니라 "역할" — 스펙에 이미 있다

오너 지적: *"훈련 기록을 시즌 기록처럼 선수의 훈련 계획에 쓸지 말지를
정하는 걸 말해?"* → **이 축이 맞다.** 스펙의 `purpose` 필드가 그것이다.

| 기록 종류 | `purpose` | 뜻 |
|---|---|---|
| 최근 경기 결과 | `CURRENT_CAPABILITY` | 현재 실력으로 인정 |
| 최고기록 (PB) | `CURRENT_CAPABILITY` | 현재 실력으로 인정 (오래돼도) |
| 시즌기록 (SB) | `SEASON_CONTEXT` | 시즌 맥락. `seasonId` 필수 |
| 목표기록 (GOAL) | `ASPIRATIONAL_TARGET` | **절대 현재 실력이 될 수 없음** |

`freshnessState`(`CURRENT`/`STALE`/`UNKNOWN`)는 **날짜에서 자동 계산되는
값이 아니다.** 엔진은 이 값을 **입력으로 받는다**(`anchor.ts:23`).
즉 무엇을 현재 실력으로 인정할지는 **사람이 정하는 것**이며,
코드가 나이로 판정하는 것이 아니다.

### 3-4. 남은 미결 사항 — 훈련 기록은 앵커 목록에 **없다**

스펙이 허용하는 앵커 종류는 7가지뿐이다.

```
RECENT_RESULT | PB | SB | GOAL | COACH_REFERENCE | RPE_ONLY | SPRINT_BENCHMARK
```

전부 **경기 결과·코치 기준·스프린트 벤치마크**다.
**연습 중 기록이나 타임트라이얼은 목록에 없다.**

따라서 **현재 스펙에서는 훈련 기록을 페이스 기준으로 쓸 수 없다.**
오너 질문의 "훈련 기록을 시즌 기록처럼 쓸지"는 **스펙을 바꾸는 결정**이며
에이전트가 정할 수 없다. → §9에 미결로 올린다.

**P1·P2·P3는 이 미결과 무관하게 진행 가능하다.** 세 지시서는 모두
경기 기반 기록만 다루므로, 나중에 훈련 기록을 추가하기로 결정되면
`purpose`/`kind`를 확장하는 방식으로 얹을 수 있다.

## 4. 결정 4·6이 함께 지키는 원칙

두 결정은 같은 뿌리다: **코드가 훈련 도메인 판단을 대신하지 않는다.**

- 범위(`3~4×`)는 코치가 그날 정하라고 문서가 일부러 열어 둔 여백이다.
  코드가 자동으로 메우면 문서 의도를 코드가 덮어쓴다.
- 그래서 확정값은 **사람이 정하고 문서에 남긴다.** 코드에 숨기지 않는다.
- 원본 `notationPattern`은 **지우지 않는다.** 옆에 기계용을 덧붙인다.
  (오너 지침: "구분하고 실제 활용하기 위한 작업이지 지우는 게 아니다")

## 5. 승인되지 **않은** 것 — 착각 방지

| 항목 | 상태 |
|---|---|
| 미성년 개인 페이스 처방 | ❌ 미승인. 현행 차단 유지 |
| 카탈로그 원본 표기 수정·삭제 | ❌ 금지. 추가만 허용 |
| 코드가 범위값 자동 선택 | ❌ 금지 |
| 경과 기간 표시 없는 페이스 계산 | ❌ 금지 |
| 기록 만료·자동 삭제 | ❌ 금지 (§3 정정) |
| 코드가 나이로 현재 실력 판정 | ❌ 금지 — 사람이 정한다 |
| 훈련 기록을 페이스 기준으로 사용 | ❌ 미결. 스펙 앵커 목록에 없음 (§3-4, §9) |
| 60m 미만 스프린트 페이스 | ❌ 금지 (안전 불변식) |
| 일지 원문을 처방 숫자에 반영 | ❌ 금지 (안전 불변식) |
| D9 차단 우회 | ❌ 금지 (안전 불변식) |
| 3단계에서 작업 종료 | ❌ 결정 위반 |

## 6. 실행 방식 — 하위 에이전트 위임

오너 지시: **"토큰 사용량 많은 건 하위 에이전트가 하도록"**

| 작업지시서 | 내용 | 성격 | 토큰 |
|---|---|---|---|
| `WORK_ORDER_P1` | 선수 기록 입력·저장 | 구현 | 큼 → **위임** |
| `WORK_ORDER_P2` | 카탈로그 기계용 표기 추가 | 문서 | 중간 → **위임** |
| `WORK_ORDER_P3` | 페이스 계산 배선·화면 표시 | 구현 | 큼 → **위임** |
| (P4) 환산 모델 | 리겔·VDOT 도입 | 도메인 | **오너 재승인 후 별건** |

**P4는 지시서를 미리 쓰지 않는다.** P3 결과를 보고 오너께
환산 모델 근거를 다시 확인받아야 한다. 지금 쓰면 승인 없이
착수될 위험이 있다.

## 7. 조사로 확인된 출발점 (하위 에이전트 필독)

실제 코드를 열어 확인한 사실이다. 추정이 아니다.

| 확인 항목 | 실제 상태 |
|---|---|
| 페이스 계산식 | ✅ `impl/src/prescription/runtime.ts:177` 있음 |
| 앱이 처방 엔진 사용 | ❌ `@impl/prescription` import 0건 |
| 앱 저장 구조 처방 종류 | `REST`, `RPE_TIME_RANGE` **둘뿐** — 페이스 자리 없음 |
| 선수 기록 입력 화면 | ❌ 없음 (온보딩·계획입력 모두 없음) |
| 경기 일지(`RaceEntry`) | ⚠️ 있으나 `record`가 **자유 텍스트**, 종목 거리 필드 **없음** |
| `GoalPace` 타입 | ⚠️ 있으나 `secondsPerKm`만. 종목 기록이 아님 |
| 카탈로그 파서 통과 | ❌ 30개 중 **0개** |
| 표기 읽기 화면 | ✅ 있음. "개인 기록으로 계산하지 않아요" 안내 포함 |

### 7-1. 특히 주의 — 기존 경기 일지를 PB로 쓸 수 없다

`RaceEntry.record`는 `z.string()` 자유 텍스트다. "18분 30초", "18:30",
"1830" 무엇이든 들어간다. **종목 거리 필드도 없다.**

따라서 **기존 경기 일지에서 PB/SB를 자동으로 뽑아낼 수 없다.**
P1은 별도의 구조화된 기록 저장소를 만든다. 기존 일지는 건드리지 않는다.

---

## 8. 모든 작업의 상위 지침

이 결정은 [`PRODUCT_NORTH_STAR.md`](./PRODUCT_NORTH_STAR.md)에 종속된다.
충돌하면 North Star가 이긴다. 특히:

- 판단이 서지 않으면 **멈추고 오너에게 묻는다**
- 실패 시 **덜 보여준다** (숫자 못 보여주는 건 불편, 잘못 보여주면 부상)
- 문서는 **지우지 않는다**

---

## 9. 오너께 남은 미결 질문 (1건)

> 🔵 **2026-07-27 추가:** 이 질문에 대해 오너가 방향을 말씀하셨다.
> **"프로필·일지에 기록이 쌓일수록 좋은 훈련 계획을 준다"** — 점진적 코칭.
> 별도 문서에 정리했다: [`PROGRESSIVE_COACHING_DESIGN_2026-07-27.md`](./PROGRESSIVE_COACHING_DESIGN_2026-07-27.md)
>
> 그 문서의 결론: 오너 구상 대부분은 **경기 기록 + 환산 모델(길 2)** 로
> 달성된다. 따라서 아래 질문(타임트라이얼을 앵커로 추가할 것인가)은
> **여전히 미결이지만 급하지 않다.** P1·P2·P3를 막지 않는다.

> **훈련 기록(연습·타임트라이얼)을 페이스 기준으로 쓸 것인가?**

현재 스펙이 허용하는 앵커는 경기 결과·코치 기준·스프린트 벤치마크뿐이고
**훈련 기록은 목록에 없다**(§3-4).

| 선택지 | 뜻 | 영향 |
|---|---|---|
| ㉮ 현행 유지 | 경기 기록만 페이스 기준 | 스펙 변경 0건. 경기 안 나가는 선수는 페이스를 못 받음 |
| ㉯ 훈련 기록 추가 | 타임트라이얼도 기준으로 허용 | **스펙 변경 필요.** 앵커 종류·`purpose`·검증 상태 재설계 |

**㉯를 고르면 스펙 문서를 고쳐야 한다.** 스펙은 곧 하드코딩된 안전 규칙이므로
에이전트가 임의로 고칠 수 없다. 오너 승인이 필요하다.

**이 질문의 답을 기다리지 않고 P1·P2·P3는 착수한다.** 세 지시서는 경기
기반 기록만 다루며, 나중에 ㉯로 결정되면 확장으로 얹을 수 있다.
