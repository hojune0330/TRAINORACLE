# Todo 3 Candidate Identity Closure Report

Verdict: PASS. DoneClaim ready.

BALANCED keeps existing support ceilings. CONSERVATIVE collapses only applicable BASE/EASY
support maxima to their minima. Complete QUALITY JSON, topology, coordinates, intents, detailed
prescriptions, REST structure, and recovery companions remain unchanged between candidates.

The independent-gate bypass is closed at the shared identity boundary. Candidate IDs now bind
canonical frame and session content with deterministic SHA-256; pair IDs bind both ordered
candidate IDs. Generation and detailed binding derive these identities, while selection, v3
reload, pair semantics, and adaptation recompute them. Coordinated support and QUALITY changes
under retained IDs reject as `STALE_CANDIDATE_FINGERPRINT`.

The final screen re-gate corrected one stale test expectation only. The adaptation review now
asserts three changed support sessions, explicitly excludes unchanged Day 9 VO2 QUALITY from
`바뀐는 것`, and retains the visible `그대로인 것` contract for session roles and training intent.

Verification totals and exact commands are in `green-todo3-focused.log` and
`verification-summary.md`. The production-import four-event result is
`manual-production-matrix.json`. The exact current app/impl patch, including untracked source
files, is `reviewed.diff` with SHA-256
`930ffc4fa6567dd6799840b5ebe2b0ac036dfe83c7cbb22d20ed279adc16815b`.

Residual boundary: Todo 3 does not implement template expansion, reverse adaptation, race
placement, successor activation, or Todo 9 UX/spec reconciliation. No canonical authority,
training number, commit, or deployment claim was added.
