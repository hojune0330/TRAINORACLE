# Todo 4 DoneClaim Evidence

## Outcome

`DO_NOT_APPROVE`, zero new detailed-template activations. This is the valid owner-delegation B result: the two independent lanes did not approve matching candidate identities and receipt digests. The executable manifest remains the four existing approvals: `V2-SEED-05`, `MD-800-01`, `MD-1500-01`, and `MD-3000-01`.

The source gate bytes were verified before use at SHA-256 `82061a918835ef9e73d663b8a3e27fbf3ec543f70e964ebfb6980b36041e7f41`. `review-receipts.json` records both reviewer IDs, each `DO_NOT_APPROVE` result, locally recomputed full SHA-256 candidate/source-block digests, the exact extraction method, candidate mismatches, and OPEN gaps. No truncated reviewer digest was copied.

## Independent-gate diagnosis and root fix

Three hypotheses were tested. H1 was confirmed: the old delegated schema checked review fingerprints only for SHA-256 shape, so an arbitrary valid hash survived. H2 was confirmed: a partial selected template ref reached the strict stored-intake parser before fallback and returned `MINIMUM_PROFILE_INCOMPLETE`. H3 was refuted by caller tracing: authority resolution and draft intake, rather than the manifest or impl parser, were the common boundaries.

- Each coaching and sports-science receipt now explicitly records approved phase, population, notation, total arithmetic, repetition recovery, set recovery, singleton intent, same-event pace, exact content/source authority, youth transfer, and female/sex transfer. Limits are closed codes.
- Every dimension fingerprint is recomputed with the existing deterministic canonical JSON/SHA-256 implementation over schema version, reviewer, lane, template ID/version/content digest, source digests, event, intent, dimension, status, limits, and structured decision. Shape-correct arbitrary hashes fail.
- The two receipts must have different reviewers and identical template/content/source identities and structured decisions. Re-fingerprinted duplicate-reviewer and coordinated notation disagreement cases fail.
- One common manifest gate checks ACTIVE/ELIGIBLE lifecycle, decision time, strict expiry, and revocation before either baseline or delegated authorization. The compiled delegated registry remains empty, so zero additions remain fail-closed.
- At the request/intake boundary only, an explicitly partial template ref is normalized to null. The strict stored schemas remain unchanged. Generation returns two RPE-only candidates, `PACE_TARGET_FALLBACK_INCOMPLETE_TEMPLATE_REF`, and no malformed ref bytes.
- Existing owner-approved entries remain pinned by exact template ID, version, content fingerprint, target event, and singleton compatible intent. D9 evaluation remains earlier than authority fallback.

## Verification

- Authority/intake TDD: independent-gate RED 4 failures / 19 pass; GREEN 39/39.
- Focused app authority/manifest/multi-event/flow: 75/75.
- v3/storage/adaptation/parser/copy identity slice: 113/113.
- Prescription quality matrix: 16/16.
- Impl generation/intent/support-pair/selection/adaptation: 468/468.
- Catalog and research validator mutation suites: 38/38.
- App typecheck, impl typecheck, and app production build: exit 0.
- Production-import matrix: four approved pairs bound both candidates; cross-event, ATP-PC, and each missing template-ref component generated two RPE-only candidates with no retained template ref.
- `git diff --check`: recorded in final provenance.

## Authority Counts

The reconstructed research catalog validator reports `1 active / 29 draft / 30 total`; this is research-catalog status, not executable authority. The executable manifest contract reports exactly four ACTIVE/ELIGIBLE approvals. The delegated runtime registry reports zero. No manifest, catalog, specification, issue status, or canonical authority text changed in Todo 4.

## Residual Risks

- No additional template can activate until both independent lanes produce unconditional, current, non-revoked receipts over one matching artifact digest and both population-transfer dimensions.
- The four retained templates continue under their prior explicit owner approvals; Todo 4 does not manufacture new youth or female/sex evidence for them.
- Athlete-facing template-choice copy is deferred to Todo 9. No runtime auto-ranking was added here.
- The Todo 1 authority validator still exits 1 with `baseline reduction edge is absent`; this known sequential-task receipt was not edited or presented as a Todo 4 current-runtime validator. The affected catalog/research validators pass.
