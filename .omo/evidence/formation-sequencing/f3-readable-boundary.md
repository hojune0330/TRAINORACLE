# Formation Readability Check

Browser captures:

- `f3-readable-boundary-mobile.png`: Chrome at 375x667
- `f3-readable-boundary-desktop.png`: Chrome at 1440x900

The local environment has no Markdown renderer, so Chrome displayed the source text. In both captures, the first visible boundary is `오늘 훈련은 바뀌지 않습니다.` followed immediately by the statement that Formation is a future draft and does not instruct training or replace coach judgment.

This verifies the required first-screen message. Repository Markdown renderers will provide the normal heading/table presentation after review or publication.
