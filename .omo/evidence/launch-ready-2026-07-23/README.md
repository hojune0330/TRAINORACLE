# Launch-ready visual evidence

- `*.png` files are the immutable pre-fix inputs used by the first independent reviews. Those captures were created with JPEG bytes under an incorrect extension; the reviews record that defect.
- `*.jpg` files are fresh post-fix captures used for release verification.
- The implementation report links only the post-fix `.jpg` evidence.
