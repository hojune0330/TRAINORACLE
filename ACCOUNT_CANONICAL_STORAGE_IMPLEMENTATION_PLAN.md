# ACCOUNT_CANONICAL_STORAGE_IMPLEMENTATION_PLAN.md

```yaml
doc_id: TRAINORACLE-ACCOUNT-CANONICAL-STORAGE-20260908
spec_id: ACCOUNT_CANONICAL_STORAGE_IMPLEMENTATION_PLAN
title: TrainOracle 계정 중심 저장·복구 구현 계획
version: 1.1
round: OWNER_APPROVED_REVIEW_REVISION
status: OWNER_APPROVED_IMPLEMENTATION_SCOPE
owner: COACH_HOJUNE
open_issues_total: 0
canonical_blocking_count: 0
canonical_promotion_allowed: false
production_release: NOT_RELEASED
```

## 1. 승인과 범위

오너는 계정 복구형 암호화, 30일 수정본·휴지통, 4단계 개발 순서를 승인했다.
이 문서는 새 개발 범위의 지시서이며, 다른 문서의 이슈를 닫거나 정본을
승격하거나 운영 DB·과금 변경을 승인하는 증거가 아니다. 위 이슈 수는 이
문서 자체의 표에 대한 수치다. 아래 미완 작업은 이슈 종결과 별개다.

로그인 사용자는 계정의 일지·계획·꾸미기를 사용한다. 기기는 서버의 대체
원본이 아니라 미전송 초안과 마지막 확인 사본을 보관한다. 일반 글과 비밀
글은 본인 계정으로 복구하며, 비밀 글은 공유·분석에서 제외한다. 서비스는
기술적으로 복호화할 수 있으므로 종단간 암호화라고 안내하지 않는다.

원문 보호에는 메모·비밀 포스트잇·텍스트 스티커도 포함한다. 서버 보관 승인을
외부 LLM 전송, 원문 감사 로그, 자동 공유, 처방 숫자 입력 승인으로 넓히지 않는다.
이전 지침은 삭제하지 않고 새 경로와 구형 공개 경로의 적용 범위를 구분한다.

## 2. 사용자 경험과 저장 규칙

- 자동 저장된 초안은 완료된 일지·훈련 수행·통계·포인트·공유 입력이 아니다.
  기존 기록 완료 동작만 확정한다. 글을 수정했다고 보상을 다시 주지 않는다.
- 로컬 초안 보호와 서버 전송 간격을 분리한다. 입력이 800ms 멈추거나 명시적
  저장 시 전송하되, 모든 키 입력을 30일 수정 이력으로 쌓지 않는다.
- 전송된 operation ID의 내용은 고정한다. 새 입력은 별도 로컬 버전으로
  보존하고, 늦게 온 저장 확인이 새 초안을 지우지 못하게 한다.
- 서버 revision 비교로 충돌을 감지한다. savedAt 또는 기기 시각으로 한쪽을
  버리지 않는다. 양쪽을 보존하며 실시간 공동편집 엔진은 이번 범위가 아니다.
- `초안 저장 중 / 계정에 저장됨 / 연결 대기 / 로그인 필요 / 충돌 확인`과
  `불러오는 중 / 기록 없음 / 조회 실패 / 형식 확인 필요`를 구분한다.
- 이미 선택한 현재 계획은 로그인 후 자동 표시한다. 과거 계획을 현재로
  바꾸거나 새 후보를 채택하는 일과 구분한다. 기존 안전 게이트는 유지한다.
- 새 기기에서 계획을 읽는 데 필요한 템플릿·설명·개인 근거 버전까지 복원
  가능해야 한다. 개인 입력과 공개 근거는 분리하며 불필요한 정보는 복제하지 않는다.
- 이전 revision은 대체된 시각, 휴지통은 삭제가 서버에 반영된 시각부터 30일.
  미해결 충돌본은 자동 만료하지 않는다. 원문 없는 최소 삭제 표식은 오래된
  기기의 부활 방지용으로 별도 유지한다. 계정 완전 삭제는 별도 절차를 따른다.

## 3. 기술 경계

인증은 기존 Supabase를 사용한다. 새 범용 동기화 플랫폼을 도입하지 않는다.
계정별 durable outbox, 서버 CAS(expectedRevision), operation UUID, 처리 영수증을
사용한다. 클라이언트의 계정 ID 대신 서버 인증 주체를 확인한다.

일지 기반의 암호문 저장은 AES-GCM 표준 구현을 사용하며 계정·문서·키 버전을
인증 부가정보로 결합한다. 암호문 형식 검사는 실제 복호화/본문 스키마 검증과
다르다. 배포 키 생성·주입·회전·복구는 서버 운영 단계에서 검증한다.

서버 계정 원본 + 미전송 초안 overlay를 사용한다. 동일 계정 다중 탭과 전환 중
응답을 격리한다. 잠금은 최적화이며 최종 버전/중복 검사는 서버가 책임진다.
기존 스키마를 읽되 새 문서 쓰기로 구형 writer를 우회시키지 않는다.

현재 계획의 불변 본문, 현재 계획 포인터, 진행 상태는 구분한다. 기존 스냅샷
검증·안전 상태를 버리지 않는다. 스티커는 배열 위치가 아닌 고유 ID로 연결한다.
일지 확정과 보상 이벤트를 함께 기록하고 보상 처리는 재시도 가능하게 분리한다.
포인트 규칙은 변경하지 않는다. 이전·복원은 재지급 사유가 아니다.

## 4. 실행 단계와 진행표

| 단계 | 완료 조건 | 이번 시작 상태 |
|---|---|---|
| 1 저장 기반 | 스펙, 암호화, 서버 revision/중복/충돌, 사용자 접근, 구형 writer 차단 | IN_PROGRESS |
| 2 일지 한 편 끝까지 | 자동 보관·종료·새 기기 로그인·수정·충돌·30일 복구 | IN_PROGRESS |
| 3 계획·꾸미기·기존 기록 | 근거 포함 계획 복원, 스티커 ID, 기존 금고 이전, 보상 중복 방지 | IN_PROGRESS: 기존 일지 명시적 이전만 착수 |
| 4 운영 검수·공개 | 실제 A/B 계정, 두 기기, 백업/키 복원, UX 검수, 단계별 배포 | NOT_STARTED |

### 4.1 최초 개발 단위의 범위 기록

다음 문단은 최초 기반 커밋의 범위이며 현재 구현 상태를 뜻하지 않는다.
첫 개발 단위는 비활성 서버 저장 기반과 순수 전송 상태 코어다. 당시 IndexedDB,
서버 인증/암호화 endpoint, 화면 자동 저장, 본문 도메인 검증, 구형 writer 전환,
삭제/보관기간 작업을 연결하지 않았다. 당시 기존 syncNow와 화면은 그대로 유지했다.
따라서 이 단위만으로 온라인 저장 완성을 주장하거나 기능 스위치를 켜면 안 된다.

현재 전송 코어는 이미 암호화된 envelope를 다루는 하위 구성요소다. 오프라인의
평문 입력을 서버 키로 즉시 암호화할 수 있다는 뜻이 아니다. 다음 단위에서 기기
초안 보호와 인증된 서버 암호화 endpoint를 별도로 연결한다. 같은 operation의
재요청을 매번 새로운 nonce로 암호화하여 서로 다른 요청으로 만들지 않는다.
서버는 기존 영수증의 원문을 본인 범위에서 복호화하여 요청 내용·문서·기준 버전과
대조하고 같은 요청이면 기존 결과를 반환한다. 경합 시에도 같은 비교를 수행한다.
본문 도메인 검증과 키 접근 통제 전에는 이 경로를 공개하지 않는다.

### 4.2 2026-09-08 일지 통합 진행

IndexedDB 암호화 버퍼, 인증·암호화 endpoint, 네 작성 폼의 확정/수정 저장,
계정별 메모 복원, 30일 수정 이력·삭제·복원 RPC 및 화면을 구현 중이다.
0034는 로컬 DB 시험을 통과했으나 운영 DB 적용과 자동 만료 실행은 미완료다.
800ms 자동 전송과 앱 내부 이동 보호는 전용 초안 패널에 적용했고, 네 기존
작성 폼 전체의 입력 중 초안 복원까지 끝났다는 뜻은 아니다.

일지 가져오기·백업 복원·구형 서버 직접 쓰기 차단·같은 훈련 중복 기록의 서버
원자성은 별도 통합이 남아 있다. 현재 계획 포인터·진행·근거 및 꾸미기·비밀
포스트잇의 계정 복원도 남아 있다. 기능을 공개하지 않은 이유는 이 미완성
구현이며, 오너가 제품 방향 승인을 하지 않았기 때문이 아니다.

완료 보고는 구현 완료, 검증 완료, PR 반영, 병합, 공개 배포를 구분한다.
작업 종료 시 미완성 항목과 실행 중인 작업 유무, 사용자 조치 대기 여부를
명시한다. 실제로 실행하지 않는 후속 작업을 진행 중이라고 표현하지 않는다.

### 4.3 일곱 잔여 작업의 실행 계약

4.2는 직전 통합 커밋의 진행 기록이다. 이후 구현과 운영 확인은
`reports/implementation/ACCOUNT_STORAGE_SEVEN_TASK_DELIVERY_2026-09-08.md`에서
단계별로 추적하며, 코드 존재를 운영 반영으로 간주하지 않는다.

1. 네 작성 폼의 입력 상태는 확정 일지와 분리한 암호화 초안으로 보관한다.
   건너뛴 필드와 실제 입력을 구분하며 초안에는 통계·보상을 부여하지 않는다.
   충돌은 최신 서버 버전과 비교한 명시적 선택으로만 해결한다. 선택하지 않은
   버전도 보존하며, 보존 한도 초과를 자동 삭제로 해결하지 않는다.
2. 파일 자체는 기기에서 해석한다. 사용자가 확인한 활동·백업 항목만 계정에
   저장한다. 실제 서버 확인, 전송 대기, 충돌, 실패를 나누고 부분 성공을
   전체 성공으로 표시하지 않는다. 기존 메모·출처·계획 연결을 보존한다.
3. 서버는 검증된 요청만 수락한다. 최종 일지와 같은 계획 세션의 식별 관계,
   보상 대상 날짜는 한 트랜잭션으로 기록한다. 복원·수정·재전송은 신규 보상의
   근거가 아니다. 포인트는 기존 방문 1P·기록일 4P 규칙을 유지한다.
4. 현재 계획 포인터와 진행·원래의 근거를 함께 보관한다. 저장된 근거 묶음은
   운반된 과거 증거이지 독립 승인자가 아니다. 새 기기에서의 수행 권한은 기존
   검토·안전 규칙을 계속 통과해야 한다. 과거 계획을 새 처방으로 지어내지 않는다.
5. 꾸미기 위치·크기·회전·글 스티커는 같은 계정의 암호화 상태로 복원한다.
   되돌리기는 캔버스 편집을 되돌리며 이미 산 재료나 사용 포인트를 취소하지 않는다.
   기존 유료 재료는 검증된 서버 구매로 가장하지 않는다. 전환 이전 계정이 명시적으로
   이전하는 첫 꾸미기 상태에 한해 `UNVERIFIED_LEGACY` 출처로 보존한다. 구현 기준은
   전환 후 30일·계정당 1회·카탈로그 유효 항목 최대 128개이며 신규 적립 포인트는 0이다.
   기존 사용 포인트는 별도 이전 이력으로 보관하고 새로 쓸 수 있는 포인트를 만들지 않는다.
   이 조건은 운영 기능 활성화나 실제 이전 완료를 뜻하지 않는다.
   비밀 일지 원문·비밀 초안은 본인 보관 대상으로 유지한다. 독립 포스트잇 도구가
   실제로 존재하는지는 파일로 확인하며, 명칭만으로 구현 완료를 주장하지 않는다.
6. 만료 정리는 30일이 지난 교체본·삭제 복구본과 그 복사본에만 적용한다.
   현재 문서·미해결 충돌·중복 방지 영수증은 자동 정리하지 않는다. 암호화 키와
   데이터 백업을 함께 복구하는 검증은 별도 증거를 남긴다.
7. 실제 계정 분리·두 기기 왕복·최종 CI와 공개 화면을 확인한 뒤 배포 완료로
   보고한다. 운영 기능 스위치를 켜는 시점까지 기존 기기 원본은 보존한다.

## 5. 기존 기록 이전과 공개 관문

로그인 전 기록은 대상 계정을 확인한 뒤 이관한다. 종류별 원본·ID·내용·개수와
서버 응답을 대조하고, 실패한 항목은 보존한다. 읽지 못한 항목을 버리지 않는다.
기존 금고의 잠금이 풀리고 실제 원문을 복호화한 뒤에만 글 이전을 완료한다.
이전이 끝나도 이번 작업에서는 기기 원본을 자동 삭제하지 않는다.

계정별 전환 시 기존 writer 차단과 최종 서버 버전 확보를 함께 수행한다. 실패 시
새 쓰기를 중지하고 초안을 보존한다. 구형 덮어쓰기를 다시 열거나 데이터 삭제로
롤백하지 않는다. 현존 계획 V3/V6 저장 테이블은 새 기반 추가만으로 전환되지 않는다.

백업 복구는 30일 수정 이력과 별개다. 서버 본문·키·삭제 표식을 함께 복원하여
검증한다. 이전 검토의 RPO 1시간/RTO 4시간은 확정 요구가 아니며 운영 구성·비용·
복원 실측 뒤 결정한다. 과금은 승인 전 실행하지 않는다.

## 6. 필수 검수와 후속 체크리스트

- [ ] 새 기기에서 로그인만으로 같은 일지와 현재 계획 표시
- [ ] 원본 기기 없이 필요한 계획 근거를 확보하여 검증
- [ ] 다른 계정·익명 접근, 만료 세션, 구형 직접 쓰기 차단
- [ ] 전송 중 추가 입력, 응답 유실, 중복 요청, 같은 ID에 다른 내용 거절
- [ ] 두 기기 수정/삭제 경합, 충돌본 보존, 반복 복구와 부활 방지
- [ ] 로컬 저장 실패, 브라우저 종료, 다중 탭, 로그아웃 실패 표시
- [ ] 비밀 글·스티커 본문 보호, 키 오류/교체/복구, 원문 로그 0
- [ ] 재확정·복원·이관의 포인트 중복 0, 동시 구매 중복 차감 0
- [ ] 실제 계정과 모바일 환경 검수, Fable UX 검수, 최종 커밋 CI
- [ ] 서버/키 복원 리허설과 운영 비용 확인 후 제한 계정부터 공개

## 7. 변경 이력

| 분류 | 내용 |
|---|---|
| 추가 | 계정 중심 원본, 로그인 복구형 글 보관, 30일 복구, 스티커 보호 |
| 수정 | 수동 local-first와 savedAt 승자 규칙을 신규 경로에서 대체 |
| 유지 | 원문 LLM/감사 금지, 안전 게이트, 기존 기록·승인 이력, 보상 규칙 |
| 보류 | 실시간 공동편집, 범용 동기화 플랫폼, 과금, 자동 운영 공개 |

[DRAFT_COMPLETE]
