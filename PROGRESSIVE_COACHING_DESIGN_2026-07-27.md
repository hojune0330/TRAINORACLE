# 점진적 코칭 — 데이터가 쌓일수록 좋아지는 훈련 계획

```yaml
design_record:
  id: DESIGN-PROGRESSIVE-COACHING-001
  stated_by: OWNER
  recorded_at: "2026-07-27"
  answers: PERSONAL_PACE_DECISION_2026-07-27.md §9 (미결 질문)
  status: 방향 확정. 임계값 정의는 미결
  blocking_spec_issue: OI-FA-MINIMUM-EVIDENCE-001 (P1, OPEN)
```

## 0. 오너가 말한 것 (원문)

> "훈련 기록을 일지나 본인의 프로필에 작성하면 그걸 삼아 훈련을 짜준다고 하자.
> 즉 회원가입하여서 프로필에서 상세한 개인 기록들을 잘 써줄 수록 훈련 계획을
> (예: 400m 인터벌 2000m 크루즈, 템포런 페이스 설정 등) 잘 짜준다고.
> 기록을 대조해봐서 긴 거리보다 짧은 거리가 더 빠른 감이 있으면 장거리 능력을
> 키우도록 유도하는 조언도 할 수 있게 되는 등, 회원가입이후 오래 일지도 쓰고
> 프로필을 잘 쓸 수록 좋은 훈련 계획이 생기는 거지. 처음이나 초반에는 러프한
> 계획만 받아볼 수 있는 거고."

**이것은 새 기능 제안이 아니다. 이 제품이 원래 그렇게 설계되어 있었다.**
아래는 조사로 확인한 근거다.

---

## 1. 이 방향은 이미 스펙의 핵심 미결 과제다

`specs/reconstruct/TRAINING_PLAN_FORMATION_AND_ADAPTATION_SPEC.md` §15
Open Issues 표에 이렇게 있다.

| ID | 우선순위 | 차단 | 상태 |
|---|---|---|---|
| `OI-FA-MINIMUM-EVIDENCE-001` | **P1** | **YES** | **OPEN** |

> "**Required history by frame type**, freshness, typed absence/staleness,
> Physio-source acceptance, statistical privacy, and suppression policies
> are unresolved."
>
> Resolution needed: "**Accept pilot thresholds/source versions** or keep
> sources rejected and statistics/counts/refs fully suppressed."

**"프레임 종류별로 필요한 이력이 얼마인가"** 가 바로 오너가 말한
*"처음엔 러프한 계획, 오래 쓰면 좋은 계획"* 이다.
**이건 P1 등급이고 계획 생성 자체를 차단(`Canonical blocking: YES`)하는
미결 과제다.**

즉 오너의 이번 발언은 **제품의 가장 큰 미결 하나에 대한 방향 제시**다.

---

## 2. 🔴 가장 중요한 제약 — "일지"와 "프로필"은 전혀 다른 경로다

> 🔵 **2026-07-27 정정.** 이 절의 초판에서 에이전트가 일지 활용을
> *"영구 금지"* 라고 썼다. **과장이었다.** 스펙의 금지 조항 대부분은
> `before_separate_adoption`(별도 승인 **전에는**)이라는 조건부이고,
> 승인 권한자는 `adoption_authority: COACH_HOJUNE` 즉 **오너**다.
> 오너 지적: *"끝까지 막아내기 보다는, 필요시와 요구시에는 열어도 돼."*
> — **맞다.** 정정 내용과 개방 조건은
> [`OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md`](./OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md) §4에 있다.
>
> **조건 없이 닫혀 있는 것은 딱 하나다** — 자유 서술 **원문 자체**를
> 계획 숫자로 번역하는 것(`plan_generator_raw_text: forbidden`).
> 구조화 수치(분포·통계·비교)는 오너 결정으로 열 수 있고, 실제로 열렸다.

오너는 *"일지나 본인의 프로필에 작성하면"* 이라고 두 경로를 함께 말씀하셨다.
**스펙에서 이 둘은 완전히 다르게 취급된다. 섞으면 안전 규칙 위반이다.**

### 2-1. 일지 **원문**은 계획 생성에 절대 들어갈 수 없다

`TRAINING_PLAN_FORMATION_AND_ADAPTATION_SPEC.md` §5:

```yaml
raw_note_input_allowed: false
```

`DAILY_LOG_AND_CHECKIN_SPEC.md`:

```yaml
plan_generator_raw_text: forbidden
analytics_raw_text: forbidden
coach_surface_raw_text: forbidden
```

**우회 시도까지 막혀 있다.** 같은 스펙 §5:

```yaml
any_raw_ANALYZABLE_TRAINING_NOTE_ancestor_in_FormationSourceRef: reject
analyzable_note_structured_derivative_before_separate_plan_adoption: reject
raw_text_quote_summary_token_or_embedding: reject
```

그리고 테스트 케이스로 못 박혀 있다.

| ID | 상황 | 기대 결과 |
|---|---|---|
| `FA-TC-012` | 원문 일지가 있음 | 형성·적응 **전 구간에서 원문 배제** |
| `FA-TC-031` | 원문을 **구조화 파생물 두 단계로 세탁** | **전체 조상 추적으로 거부** |

**즉 "일지를 요약해서 계획에 넣는다"도 금지다.** 요약·토큰·임베딩까지
명시적으로 거부 목록에 있다.

### 2-2. 그럼 일지는 무엇을 할 수 있나

`DAILY_LOG_AND_CHECKIN_SPEC.md`의 허용 처리:

```yaml
analyzable_note_allowed_processing:
  - transient_local_validation
  - transient_risk_extraction        # 위험 신호 감지
  - transient_athlete_analysis
  - reason_code_generation
  - privacy_safe_structured_signal_generation
```

**일지 원문의 역할은 "위험 신호 감지"다.** 계획을 만드는 게 아니라
**계획을 막는** 쪽이다. `SafetyBlockRef`는 작업을 멈출 수 있지만
후보를 만들거나(`form`), 순위를 정하거나(`rank`), 설명할 수(`explain`) 없다.

그리고 안전 방향으로만 작동한다.

> "Sanitized analyzable-note D9 ancestry is allowed only for `BLOCKED`,
> `D9_UNKNOWN`, or `STALE`; **it can never produce `GENERATION_ALLOWED`**."

**일지는 "하지 마"라고 말할 수 있고 "이렇게 해"라고 말할 수 없다.**

### 2-3. 따라서 오너 구상은 이렇게 나뉜다

| 오너가 말한 것 | 실제 경로 | 가능 여부 |
|---|---|---|
| 프로필에 상세 기록 작성 | 구조화 필드 | ✅ **가능** — 이것이 주 경로 |
| 일지에 **구조화된** 세션 기록 | 구조화 필드 | ✅ 가능 |
| 일지 **자유 서술 원문**을 계획 숫자로 번역 | 원문 | ❌ 금지 (조건 없음) |
| 일지에서 **구조화 수치**를 불러와 분포·통계·비교 | 구조화 필드 | ✅ **오너 결정으로 열림** (§2 정정) |
| 일지가 오래 쌓여 계획이 좋아짐 | 구조화 필드의 축적 | ✅ 가능 (원문 아닌 필드) |

**결론: 오너 구상의 핵심은 "프로필·구조화 기록"이 담당한다.**
일지는 **위험 신호로 계획을 안전하게 막는 역할**이며, 이것도 코칭의
일부지만 방향이 반대다.

> 이 구분을 지키는 이유: 자유 서술을 처방 숫자로 바꾸면
> "어제 좀 무리했다"가 강도 감소로, "컨디션 좋았다"가 강도 증가로
> 자동 번역된다. 그 번역은 근거가 없고, 사람 몸에 그대로 적용된다.

---

## 3. "짧은 거리가 더 빠르면 장거리를 키우라고 조언" — 이것은 길 2가 필요하다

오너가 든 예시다. 5000m 기록 대비 1500m 기록이 상대적으로 좋다면
장거리 능력 보강을 권하는 조언.

**이 판단은 서로 다른 종목 기록을 같은 척도로 비교해야 한다.**
그런데 현재 코드는 종목이 다르면 계산을 거부한다.

```
impl/src/prescription/anchor.ts:70-72
  if (input.anchor.eventDistanceM !== input.targetEventDistanceM)
    return "CROSS_EVENT_MODEL_REQUIRED"
```

**즉 이 조언 기능은 결정 1의 "길 2"(환산 모델 도입)가 있어야 가능하다.**

| 기능 | 필요한 것 |
|---|---|
| 5000m 기록 → 5000m 인터벌 페이스 | 현재 코드로 가능 (길 1) |
| 5000m 기록 → 400m 인터벌 페이스 | **환산 모델 필요** (길 2) |
| 1500m·5000m 비교 → 약점 진단 | **환산 모델 필요** (길 2) |
| 2000m 크루즈, 템포런 페이스 | **환산 모델 필요** (길 2) |

**오너가 예로 든 것이 전부 길 2 이후다.**
`400m 인터벌`, `2000m 크루즈`, `템포런` 모두 경기 종목 거리가 아니다.

> 이것은 결정 1(길 2가 목적지)이 옳았다는 강한 근거다.
> 동시에 **길 1에서 멈추면 오너가 원하는 제품이 아니라는 확인**이다.
> 결정 정본 §2의 "3단계에서 멈추면 결정 위반" 경고가 정확했다.

---

## 4. 🔴 함정 — "많이 쓰면 좋아진다"를 개수 임계값으로 만들면 안 된다

**이것이 이 문서에서 가장 조심해야 할 부분이다.**

바로 앞서 작업자가 "기록 유효기간 3개월/12개월"이라는 숫자를 만들어 넣어 오너 지적을
받았다(`PRODUCT_NORTH_STAR.md` §5 오판 사례 6). **같은 함정이 여기 또 있다.**

`FORMATION_LOAD_AND_STATISTICAL_RULES_CONTRACT.md` §10:

```yaml
minimum_evidence_policy:
  descriptive_single_observation: allowed
  aggregate_requires_visible_n: true
  baseline_classification_threshold: UNKNOWN      # ← 모른다고 명시
  readiness_threshold: PROHIBITED                 # ← 금지
  injury_risk_threshold: PROHIBITED               # ← 금지
```

> "**No fixed number of sessions** creates a stable baseline, readiness
> state, or injury-risk classification."

**"일지 10개를 쓰면 정밀 계획"같은 개수 임계값은 스펙이 이미
"근거 없음(UNKNOWN)"으로 판정해 둔 것이다.**

### 4-1. 안전한 설계 — 개수가 아니라 **어떤 사실이 있는지**로 나눈다

```
❌ 잘못된 설계 (근거 없는 임계값)
   일지 0~4개   → 러프
   일지 5~19개  → 보통
   일지 20개 이상 → 정밀

✅ 올바른 설계 (사실 존재 여부)
   종목 기록 없음                  → 체감강도만 (현재 상태)
   목표 종목 기록 1개 있음          → 그 종목 페이스 계산 가능
   서로 다른 종목 기록 2개 이상     → 종목 간 비교·약점 조언 가능
   구조화 세션 기록 있음            → 훈련 부하 표시 가능
```

**차이가 왜 중요한가.** 5000m 기록 하나만 있어도 5000m 인터벌 페이스는
정확히 계산된다. 일지 개수와 무관하다. 반대로 일지 100개를 써도 종목
기록이 없으면 페이스를 만들 수 없다.

**즉 계획의 정밀도를 결정하는 것은 "양"이 아니라 "어떤 종류의 사실이
존재하는가"다.** 스펙도 그래서 `descriptive_single_observation: allowed`
(관측 하나도 사실로 표시 가능)라고 적혀 있다.

### 4-2. 그래도 "오래 쓰면 좋아진다"는 성립한다

개수 임계값 없이도 오너 의도는 자연히 달성된다.

| 시간이 지나면 | 자연히 생기는 것 | 그래서 가능해지는 것 |
|---|---|---|
| 경기를 뛴다 | 종목 기록이 늘어난다 | 그 종목 페이스 계산 |
| 다른 종목도 뛴다 | 종목 조합이 생긴다 | 종목 간 비교·약점 조언 |
| 최근 경기가 생긴다 | 더 최신 기준이 생긴다 | 사람이 더 나은 기준을 고를 수 있다 |
| 세션을 구조화해 남긴다 | 훈련 부하 이력 | 부하·편향 인포그래픽 |

**"임계값을 넘어서" 좋아지는 게 아니라, "쓸 수 있는 사실이 늘어서"
좋아진다.** 결과는 오너 의도와 같고, 근거 없는 숫자는 만들지 않는다.

---

## 5. 회원가입과의 관계

현재 앱은 **회원가입 없이** 동작하고 데이터는 브라우저에만 있다(README).
계정 코드는 일부 존재한다(`app/src/domain/account/` — `auth.ts`, `sync.ts`,
`supabase-client.ts`, `tombstone.ts`).

오너 구상은 **회원가입을 "더 좋은 계획"의 동기로 쓰는 것**이다.
이건 제품 전략으로 타당하다. 다만 순서 문제가 있다.

| 선택지 | 뜻 | 위험 |
|---|---|---|
| ㉮ 로컬 우선 | 회원가입 없이도 기록 입력·페이스 계산 | 기기 바꾸면 사라짐 |
| ㉯ 계정 필수 | 회원가입해야 기록 입력 | 진입 장벽. 베타 사용자 이탈 |
| ㉰ 로컬 먼저, 나중에 승격 | 로컬로 쓰고 원할 때 계정 연동 | 동기화 복잡도 |

**권고: ㉰.** 근거는 두 가지다.

1. **이미 그렇게 설계돼 있다.**
   `specs/reconstruct/LOCAL_FIRST_SYNC_AND_PROMOTION_CONTRACT.md`
   가 "로컬 저장 일지를 나중에 서버/계정으로 승격"하는 계약이다.
2. **P1 작업지시서가 이미 로컬 저장이다.** 계정을 필수로 바꾸면 P1을
   다시 써야 하고, 백엔드 작업이 페이스 기능을 막는다.

**즉 "회원가입하면 좋아진다"는 맞지만, "회원가입해야 시작된다"로
만들지 않는다.** 로컬에서 값을 먼저 증명하고, 보관·동기화를 계정의
가치로 제시한다.

---

## 6. 오너 구상이 결정 정본 §9를 어떻게 답했는가

§9 질문은 *"훈련 기록을 페이스 기준으로 쓸 것인가"* 였다.

오너 답변을 스펙 용어로 옮기면 이렇다.

| 오너가 말한 것 | 스펙 대응 | 판정 |
|---|---|---|
| 프로필의 상세 개인 기록 | 구조화 기록 (경기 기록 포함) | ✅ 현행 앵커로 가능 |
| 400m 인터벌·2000m 크루즈 페이스 | 종목 간 환산 | ⚠️ **길 2 필요** |
| 종목 비교 후 약점 조언 | 종목 간 환산 + 분석 | ⚠️ **길 2 필요** |
| 일지 원문을 계획 숫자로 번역 | 원문 → 계획 | ❌ 금지 (조건 없음) |
| 일지의 구조화 수치 불러오기 | 분포·통계·비교 | ✅ **오너 결정으로 열림** |
| 일지 구조화 세션 기록 | 구조화 필드 | ✅ 가능 |
| 데이터 쌓이면 정밀해짐 | `OI-FA-MINIMUM-EVIDENCE-001` | ⚠️ **개수 임계값 금지**, 사실 존재 기준으로 |

**§9의 "훈련 기록(타임트라이얼)을 앵커로 추가"는 여전히 별개 미결이다.**
오너 구상 대부분은 **경기 기록 + 환산 모델**로 달성되므로, 타임트라이얼
앵커 추가 없이도 진행할 수 있다. 이 판단은 §8에 남긴다.

---

## 7. 작업지시서에 미치는 영향

| 지시서 | 영향 | 조치 |
|---|---|---|
| `WORK_ORDER_P1` | **없음** | 그대로 착수. 기록 입력은 이 구상의 전제다 |
| `WORK_ORDER_P2` | **없음** | 그대로 착수 |
| `WORK_ORDER_P3` | **없음** | P1·P2 후 착수 |
| (P4) 환산 모델 | **중요도 상승** | 오너 예시 대부분이 P4 이후 기능임이 확인됨 |

**세 지시서는 수정할 필요가 없다.** 이 구상은 그 위에 얹히는 것이며,
P1이 만드는 구조화된 기록 저장소가 정확히 이 구상의 토대다.

**다만 P4의 성격이 달라졌다.** 이전에는 "더 많은 템플릿을 열기 위한
환산"이었으나, 이제 **오너가 원하는 코칭 기능의 전제 조건**이다.
`템포런`, `크루즈 인터벌`, `약점 진단` 전부 P4 없이는 불가능하다.

---

## 8. 오너께 남은 결정 (3건)

이 문서는 **방향을 기록한 것이고, 아래는 아직 정해지지 않았다.**

### 8-1. 계정 정책

| | 내용 |
|---|---|
| **권고** | ㉰ 로컬 먼저, 나중에 계정 승격 (§5) |
| 필요한 판단 | 회원가입을 필수로 할지, 선택으로 할지 |

### 8-2. 정밀도 구분 기준

| | 내용 |
|---|---|
| **권고** | 개수 임계값 금지. "어떤 사실이 있는가"로 구분 (§4-1) |
| 필요한 판단 | 이 방식으로 충분한지, 아니면 오너가 개수 기준을 원하는지 |
| 주의 | 개수 기준을 쓰려면 그 숫자의 근거가 필요하다. 스펙은 `UNKNOWN`으로 판정해 뒀다 |

### 8-3. 타임트라이얼 앵커 (§9 잔여)

| | 내용 |
|---|---|
| **권고** | 지금은 추가하지 않는다. 경기 기록 + 환산으로 대부분 달성된다 |
| 필요한 판단 | 경기를 안 뛰는 사용자에게도 페이스를 줄 것인가 |
| 주의 | 추가하려면 스펙 변경(앵커 종류·검증 상태) 필요 |

**세 결정 모두 P1·P2·P3 진행을 막지 않는다.**

---

## 9. 절대 지킬 것 (이 구상을 구현하는 모든 작업자에게)

1. **일지 원문을 계획 숫자에 반영하지 않는다.** 요약·토큰·임베딩도
   금지다. 세탁 경로까지 스펙이 막아 뒀다(§2-1).
2. **개수 임계값을 새로 만들지 않는다.** "N개 이상이면 정밀"은 스펙이
   `UNKNOWN`으로 판정한 영역이다(§4).
3. **데이터가 적을 때 추측으로 메우지 않는다.** 러프한 계획은
   "적게 보여주는 것"이지 "대충 추정하는 것"이 아니다.
4. **약점 진단은 환산 모델 승인 후에만 한다.** 종목 간 비교는
   `CROSS_EVENT_MODEL_REQUIRED`로 막혀 있다(§3).
5. **회원가입을 기능의 잠금장치로 쓰지 않는다.** 로컬에서 먼저
   가치를 증명한다(§5).

> 상위 지침은 [`PRODUCT_NORTH_STAR.md`](./PRODUCT_NORTH_STAR.md)다.
> 충돌하면 North Star가 이긴다.
