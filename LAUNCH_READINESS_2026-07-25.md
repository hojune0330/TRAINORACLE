# 오픈 준비 상태 — 수치화

```yaml
doc_id: trainoracle-launch-readiness-2026-07-25
date: 2026-07-25
기준: 실측(코드·테스트·라이브 응답 확인). 추정치는 "추정"으로 표시.
선행문서: reports/review/WO011_PRODUCT_FACT_DECISION_01_LAUNCH_SCOPE (PR #93)
          reports/implementation/LAUNCH_READY_IMPLEMENTATION_REPORT_2026-07-23.md
          LAUNCH_BACKEND_AND_ACCOUNT_PLAN.md
```

---

## 0. 한 줄 요약

**"로컬 일지 앱"으로 오픈하면 오늘 가능하다 (98%).
"계정·동기화 앱"으로 오픈하면 법률 문서가 없어서 불가능하다 (58%).**

> 이 문서를 쓰면서 A-1·A-2·A-3을 **바로 구현했습니다**(오너 결정이 필요 없는
> 항목이라서). 그래서 A안 준비도는 92% → **98%**가 되었습니다.
> 남은 2%는 사장님이 "열자"고 하시는 것뿐입니다.

두 개는 완전히 다른 제품이고, 남은 작업량도 다르다.
**어느 쪽으로 여느냐가 첫 번째 결정이다.**

---

## 1. 오픈 범위 두 가지 — 먼저 이걸 고르셔야 합니다

| | **A안: 로컬 일지만** | **B안: 계정 포함** |
|---|---|---|
| 준비도 | **98%** | **58%** |
| 데이터 위치 | 사용자 기기에만 | 서버(Supabase)에 사본 |
| 개인정보처리방침 | **불필요** (수집 안 함) | **필수** (없으면 위법) |
| 이용약관 | 권장 | **필수** |
| 만 14세 미만 | 그냥 써도 됨 | **법정대리인 동의 필요** |
| 남은 필수 작업 | **0건** (방금 완료) | **7건** |
| 예상 소요 | **즉시 가능** | **3~6주** (법률 검토 대기 포함) |

> PR #93에서 사장님이 이미 승인하신 첫 공개 범위는
> **"대한민국 / 로컬 일지만 / 계정·서버 동기화 제외"** = **A안**입니다.
> 그 결정이 유효하다면 아래 A안 3건만 처리하면 오픈할 수 있습니다.

---

## 2. A안(로컬 일지만) — 98%

### 이미 끝난 것 (실측)

| 항목 | 상태 | 증거 |
|---|---|---|
| 라이브 배포 | ✅ | `https://hojune0330.github.io/TRAINORACLE/` → **HTTP 200** |
| 일지 쓰기·보기·삭제 | ✅ | 화면 9종 동작 |
| 훈련계획 베타 | ✅ | 2026-07-24 배포 완료 |
| 워치 파일 가져오기 | ✅ | TCX/GPX |
| 백업 내보내기·되돌리기 | ✅ | 이번 주 완성 |
| 삭제 부활 차단 | ✅ | tombstone |
| 자동 테스트 | ✅ | **220개 통과 / 28파일** |
| 브라우저 시나리오 | ✅ | e2e **13종** × 4환경 |
| 배포 안전장치 | ✅ | 3개 CI 통과해야만 게시 |
| PWA 설치 | ✅ | manifest + service worker |
| 계정 기능 노출 | ✅ **꺼져 있음** | `.env` 없음 → flag OFF |

### 남은 것이었던 3건 — **이번에 전부 구현 완료** ✅

| # | 항목 | 무엇이 문제였나 | 어떻게 고쳤나 |
|---|---|---|---|
| **A-1** ✅ | 문의 경로 | 앱 안에 연락처가 **0건**이었다. 사용자가 문제를 겪어도 알릴 방법이 없었다 | 가이드 화면에 문의 버튼(GitHub Issues) + 기기변경·삭제 FAQ 2개 추가 |
| **A-2** ✅ | 에러 화면 | `ErrorBoundary` **없음**. JS 오류 1개면 **흰 화면**이 되고 기기에만 있는 일지에 닿을 수 없었다 | 전역 오류 경계 + **오류 화면에서 바로 백업 내려받기** |
| **A-3** ✅ | 전체 삭제 | 개별 일지 삭제만 있고 "내 데이터 전부 지우기"가 **없었다** | 계정 화면에 2단계 확인 후 전체 삭제 |

세 건 다 **새 정책이 아니라** 버그 방어와 이미 승인된 삭제권의 완성이라 제 판단으로 구현했습니다.
문의처는 README에 이미 쓰여 있던 GitHub Issues를 그대로 썼습니다 — 새 주소를 지어내지 않았습니다.

**A-2에서 특히 신경 쓴 것**: 오류 화면에 "다시 시도"만 두면 같은 오류가 반복될 때 탈출구가 없습니다.
그래서 **백업 내려받기 버튼을 먼저** 뒀고, 그 버튼은 이미 깨진 상태를 가정해 도메인 코드를 거치지 않고
localStorage를 직접 읽습니다.

**A-3에서 특히 신경 쓴 것**: 전체 삭제를 해도 **삭제 기록(tombstone)은 남깁니다.**
안 남기면 나중에 동기화할 때 서버 사본이 되살아나 삭제가 무효가 됩니다.
tombstone에는 id와 시각만 있고 본문은 없으므로 남겨도 내용이 남지 않습니다.

---

## 3. B안(계정 포함) — 58%

### 코드는 거의 다 됐습니다

| 항목 | 상태 |
|---|---|
| 로그인(이메일 OTP·구글) | ✅ 구현 |
| 서버 동기화 | ✅ 구현 (옵트인, 기본 OFF) |
| 메모 제외 업로드 | ✅ 기본값 |
| 기기 간 삭제 전파 | ✅ 이번에 구현 |
| RLS 보안 정책 | ✅ 작성 |
| **DB 마이그레이션 실행** | ❌ **대시보드 접근 필요** |

### 막고 있는 건 코드가 아니라 서류입니다

WO011 제품사실 설문 **49행 중 34행이 아직 `UNKNOWN`** (= **69% 미결**).
그중 오픈을 실제로 막는 것:

| # | 항목 | 상태 | 누가 정해야 하나 |
|---|---|---|---|
| **B-1** | **개인정보처리방침** | ❌ 없음 | 사장님 + 법률 |
| **B-2** | **이용약관** | ❌ 없음 | 사장님 + 법률 |
| **B-3** | **사업자 정보** (상호·주소·연락처) | ❌ `UNKNOWN` | 사장님 |
| **B-4** | **만 14세 미만 처리** | ❌ `UNKNOWN` | 사장님 + 법률 |
| **B-5** | 데이터 보관 기간 | ❌ `UNKNOWN` | 사장님 |
| **B-6** | 위탁업체 고지 (Supabase 등) | ❌ `UNKNOWN` | 사장님 |
| **B-7** | 마이그레이션 실행 | ❌ 미실행 | 사장님(대시보드) |

> **B-1~B-4가 없는 상태로 계정을 열면 개인정보보호법 위반입니다.**
> 이건 제가 코드로 우회할 수 있는 종류의 문제가 아닙니다.
> 특히 사용자층에 **중·고교 선수**가 있으면 만 14세 미만 문제가 반드시 걸립니다.

---

## 4. 그래서 뭘 하면 되나 — 추천

```
지금 ─────► A안으로 오픈 (3건, 1~2일)
              실제 사용자가 일지를 쓰기 시작
              개인정보를 아예 수집 안 하니 법적 부담 0
                  │
                  ▼
            사용 반응 보면서 B-1~B-4 준비 (법률 검토)
                  │
                  ▼
나중에 ────► 계정 기능 켜기 (flag만 ON, 코드는 이미 있음)
              기존 사용자는 "가입하면 지금까지 쓴 일지가 그대로 올라감"
```

이 순서의 장점: **계정 코드를 이미 다 만들어놨기 때문에**, 법률 서류가 준비되는 시점에
`.env` 두 줄 + 마이그레이션 1회 실행이면 켜집니다. 다시 개발할 게 없습니다.

---

## 5. 사장님이 지금 결정하실 것 — 4개

아래만 답해주시면 나머지는 제가 진행합니다.

| # | 질문 | 선택지 | 기본 추천 |
|---|---|---|---|
| **Q1** | 어느 범위로 오픈? | A안(로컬만) / B안(계정포함) | **A안** — PR #93 기존 결정과 일치 |
| **Q2** | 문의는 어디로? | 이메일 주소 / GitHub Issues / 둘 다 | **GitHub Issues** (즉시 가능, 무료) |
| **Q3** | 오픈 날짜? | 특정일 / "준비되면" | — |
| **Q4** | 첫 사용자는? | 지인 소수 / 공개 | **지인 소수** 먼저 |

---

## 6. 지난 턴에서 남겨둔 4건 — 쉽게 다시 설명

이전 답변이 어려웠다는 지적을 받아 다시 씁니다.

### ① 마이그레이션 실행 — **B안 갈 때만 필요**

DB에 표 하나 만드는 SQL을 한 번 실행하는 일입니다. 제가 파일은 다 썼고, Supabase 사이트에 로그인해서 붙여넣기만 하면 됩니다. 제 계정이 없어서 못 합니다.
**A안이면 지금 안 해도 됩니다.**

### ② F-3: 메모만 쓴 일지가 백업에서 빠짐

"오늘 무릎이 아팠다" 메모만 쓰고 거리·시간은 안 적은 일지가 있습니다.
기본 백업은 **메모를 일부러 제외**하므로(프라이버시 보호), 이런 일지는 **내용이 통째로 빈** 상태가 됩니다.

- **선택 1**: 그냥 둔다 → 사용자가 백업 파일을 열어보고 "왜 없지?" 함
- **선택 2**: "메모만 있는 일지 3개는 이 백업에 포함되지 않았어요" 안내 표시
- **제 추천: 선택 2** (30분, 안내만 추가하고 동작은 안 바꿈)

### ③ F-5: 삭제 기록 500개 상한

지운 일지를 500개까지 기억합니다. 501개째를 지우면 가장 오래된 기록이 밀려나고, **이론적으로** 그 일지가 되살아날 수 있습니다.
하루 1개씩 지워도 **1년 4개월**이 걸립니다. 현실적으로 닿기 어려운 숫자입니다.

- **선택 1**: 500 유지 → 사실상 문제없음
- **선택 2**: 2000으로 올림 → 용량 54KB → 216KB (여전히 작음)
- **제 추천: 선택 1** (그대로 두기)

### ④ F-6: 실수로 지운 일지 복구

**이건 제가 정하면 안 되는 문제입니다.** 두 원칙이 정면으로 충돌하기 때문입니다.

- "지우면 진짜 지워진다" ← 지금 방식. 통증·감정 메모를 지운 사람에겐 이게 안전
- "실수로 지운 걸 되살릴 수 있다" ← 편하지만, 지운 게 30일간 어딘가 남아있다는 뜻

같은 걸 동시에 만족시킬 수 없습니다. **어느 쪽이 우리 사용자에게 맞는지**가 제품 철학의 문제라 사장님 결정이 필요합니다.

- **선택 1**: 지금처럼 즉시 완전 삭제 + **"되돌릴 수 없어요" 경고만 추가** ← **추천**
- **선택 2**: 휴지통 30일
- **선택 3**: 지금 그대로 (경고도 없음)

---

## 7. 종합 점수

| 영역 | 점수 | 근거 |
|---|---|---|
| 핵심 기능 | **97%** | 일지·계획·가져오기·백업·전체삭제 전부 동작 |
| 코드 품질 | **95%** | 220 테스트, 타입검사·빌드 통과 |
| 보안 (로컬) | **90%** | 서버 전송 없음, 공격형 감사 완료 |
| 보안 (서버) | **85%** | RLS 4정책, 단 마이그레이션 미실행 |
| 장애 대응 | **90%** | ErrorBoundary + 오류 화면 백업 (A-2 완료) |
| 사용자 지원 | **85%** | 문의 버튼 + FAQ (A-1 완료) |
| **법률·개인정보 (A안)** | **95%** | 수집 자체를 안 하므로 해당 없음 |
| **법률·개인정보 (B안)** | **10%** | 방침·약관·사업자정보 전무 |

**A안 종합: 98% · B안 종합: 58%**

테스트: **234개 통과 / 30파일** (이번 작업 전 220 → +14)

---

## 8. 이 문서가 하지 않는 것

- 오픈 승인이 아닙니다. 날짜와 범위는 사장님이 정합니다.
- 법률 자문이 아닙니다. B안은 실제 전문가 검토가 필요합니다.
- 위 퍼센트는 **확인된 코드·파일·응답 기준**입니다. 확인 못 한 건 "미확인"으로 적었지 추측으로 채우지 않았습니다.
