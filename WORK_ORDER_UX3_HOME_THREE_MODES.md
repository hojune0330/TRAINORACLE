# 작업지시서 UX3 — 홈 3모드 개편 (환영 · 훈련 · 일지)

```yaml
work_order_metadata:
  order_id: WORK_ORDER_UX3
  issued_by: FABLE_INDEPENDENT_REVIEWER (발행) / TOTAL_RESPONSIBILITY_HOLDER (승인)
  issue_date: 2026-08-19
  supersedes: 없음 (WORK_ORDER_UX2와 병존 — §2-1 정직성 원칙을 계승)
  decision_basis:
    - reports/review/FABLE_HOME_PERSONA_REVIEW_2026-08-19.md
    - reports/review/evidence/fable-home-2026-08-19/*.png (375×667 실측)
  branch_pattern: codex/work-order-ux3-task{N}
  report_channel: 본 지시서를 발행한 GitHub PR/이슈 댓글
```

---

## 왜 하는가 — 한 문장

> **지금 홈은 "일지 유저의 홈"을 전원에게 강제한다.**
> 첫 방문자는 소유할 것이 없는데 "내 기록"을 보고, 계획 유저는 "오늘 뭐 뛰지?"의
> 답(다음 훈련)이 폴드 아래에 있다. 일지 유저만 만족한다.

실측 근거(2026-08-19, main 빌드, 375×667):

| 상태 | 폴드 안 | 문제 |
|---|---|---|
| S1 첫 방문(빈 상태) | H1 "내 기록" + 기록 CTA + 기분/몸상태/날씨 선택기 | 가치 제안 0줄. 민지 예시(최강 온보딩 자산)는 스크롤 6/6 최하단 |
| S3 계획 보유 | S1과 동일 | "다음 훈련" 카드가 폴드 아래 — 훈련 *전* 질문에 첫 화면이 침묵 |

수백 페르소나를 다 만족시킬 필요는 없다. 실제 축은 3개다:
**처음 온 사람 / 훈련하러 온 사람 / 기록하러 온 사람.**
판별 입력(`entries`, `plan`)은 `Home.tsx`가 이미 전부 로딩하고 있으므로,
새 저장소·새 감지 장치 없이 **조건부 렌더링만으로** 구현한다.

---

## 0. 불변 규칙 (위반 시 작업 전체 반려)

1. **브리핑 정직성 (WORK_ORDER_UX2 §2-1)**: 값이 없으면 그 항목을 렌더링하지 않는다.
   거짓 카드·빈 껍데기 카드 금지. 이 원칙은 새로 만드는 환영 홈에도 동일하게 적용된다.
2. **P3(일지 유저) 무퇴행**: `entries.length ≥ 1 && plan === null`에서 홈의 섹션
   구성·순서는 현행과 동일해야 한다. 이 조건의 스냅샷 계약 테스트가 Task 4의 필수 산출물이다.
3. **안전 표면 유지**: 통증 4+ 배너(PainReview), "이 기기에만 저장" 계열 신뢰 라벨,
   "지도자·보호자와 상의" 문구는 어떤 모드에서도 제거·약화 금지.
4. **역할 흉내 금지**: 계정 feature flag OFF, role 개념 부재. 코치용/부모용 홈·문구를
   이번 작업에서 만들지 않는다(§비목표). 현 데이터 모델에서 흉내 내면 거짓 기대만 만든다.
5. **엔진 무접촉**: 계획 생성·D9·Safety Gate·처방 로직 수정 금지. 이 작업은 화면
   합성(`Home.tsx`)·레이아웃(`TrainingHome.tsx`)·뷰모델(`home-view-model.ts`) 한정.
6. **컴포넌트 삭제 금지**: 모드에 따라 숨기는 것은 조건부 렌더링으로만. 파일 삭제·
   기능 제거 아님 (NORTH_STAR "지우지 마라").
7. **디자인 토큰만 사용**: 새 색·새 px 값 발명 금지. `--ink`/`--surface`/`--space-*`/
   `--fs-body*`/`--bw-line`/`--app-touch-min` 등 기존 토큰으로만 스타일링. radius 0 유지.
8. **터치 타겟 ≥ 44px** (`--app-touch-min`), 모든 인터랙티브 요소에 접근 가능한 이름.
9. **테스트 없는 완료 보고 금지**: 각 Task의 검증 절이 통과해야 완료. 완료 보고는
   "PR 업로드, 리뷰/병합 대기"까지만 주장한다.
10. **증거 제출**: 모드 A/B/C 각각 375×667 스크린샷. 홈 스크롤 컨테이너는
    `main.app-scroll-region`(내부 스크롤)이므로 fullPage 캡처가 아니라
    `el.scrollTop` 조작으로 구간 캡처할 것 (`app/e2e-review/home-audit*.mjs` 참고 가능).

---

## 1. 디자인 의도 — 구현 전에 읽을 것

이 개편의 미학은 장식이 아니라 **화면이 답하는 질문을 사람에 맞추는 것**이다.

- **원칙 1 · 한 화면, 한 질문.** 홈의 폴드는 그 사람의 오늘 첫 질문에 답한다.
  - 처음 온 사람: *"이 앱이 뭔데?"* → 가치 제안 + 두 갈래 입구 + 실물 예시
  - 계획 있는 사람: *"오늘 뭐 뛰지?"* → 다음 훈련 카드
  - 기록하는 사람: *"오늘 남기자"* → 현행 그대로 (이미 옳다)
- **원칙 2 · 소유격은 소유가 생긴 뒤에.** 빈 상태의 "내 기록"은 거짓에 가깝다.
  환영 홈의 H1은 앱이 해줄 일을 말하고, 기록이 생긴 뒤에야 "내"가 등장한다.
- **원칙 3 · 입력 요구보다 가치 증명이 먼저.** 기분/몸상태/날씨 선택기는 기록을
  시작한 사람의 도구다. 첫 화면의 절반을 입력 요구로 채우지 않는다.
- **원칙 4 · 가장 좋은 설득은 실물.** 민지의 예시 일지는 빈 상태에서 최강의
  온보딩 자산인데 최하단에 매장돼 있다. 환영 홈에서는 폴드 안으로 승격한다.
- **원칙 5 · 정직한 연기.** 코치·부모를 위한 화면은 만들 수 없는 단계다.
  만들 수 있는 것(통증 배너·기기 저장 라벨·JSON 내보내기)을 유지하는 것이
  그들에 대한 현 단계의 최선이며, 그 이상을 약속하는 문구를 쓰지 않는다.

---

## 2. Task 1 — 뷰모델에 `homeMode` 판별 추가 (P1, 선행 필수)

**대상:** `app/src/domain/home-view-model.ts` (+ 해당 도메인 테스트)

**할 일:**
1. `TrainingHomeViewModel`에 `homeMode: "WELCOME" | "TRAINING" | "JOURNAL"` 필드 추가.
2. 판별 규칙 (이 우선순위 그대로):
   ```
   WELCOME  : entries.length === 0 && plan === null
   TRAINING : plan !== null && nextTraining !== null
   JOURNAL  : 그 외 전부
   ```
   - `nextTraining`이 `null`인 계획 보유자(계획 종료 등)는 TRAINING이 아니라
     JOURNAL이다 — 다음 훈련이 없는데 "훈련 홈"을 그리면 거짓 카드가 된다(§0-1).
3. 판별은 `buildTrainingHomeViewModel` 안에서만. 화면 컴포넌트에서 조건을
   재계산하지 않는다(재는 자와 검사하는 자를 같게 — UX2 R1).

**검증:**
- 도메인 단위 테스트 4케이스: (빈+무계획→WELCOME) (기록만→JOURNAL)
  (계획+다음훈련→TRAINING) (계획 있으나 nextTraining null→JOURNAL)
- `grep -n "homeMode" app/src/domain/home-view-model.ts` ≥ 1건

---

## 3. Task 2 — 모드 A · 환영 홈 (P1)

**대상:** `app/src/screens/home/TrainingHome.tsx`, `app/src/screens/Home.tsx`,
`app/src/styles/app.css` (training-home 블록)

**폴드 설계 (위에서 아래로):**

| 순서 | 요소 | 확정 카피 |
|---|---|---|
| ① | H1 (기존 `training-home__intro` 자리) | **"달리기 일지를 남기고, 내 기록으로 훈련 계획을 받아요."** |
| ② | 신뢰 한 줄 (H1 바로 아래, `--fs-body-sm`, `--ink-3`) | **"모든 데이터는 이 기기에만 저장돼요."** |
| ③ | 동급 CTA 2개 (세로 스택, 각각 현행 primary와 같은 시각 무게) | **[오늘 기록 남기기]** → `onWriteLog?.("post-session")` / **[훈련 계획 만들기]** → `onOpenPlan` |
| ④ | 민지 예시 승격 카드 | 라벨 **"이렇게 쓰여요"** + 버튼 **"민지의 예시 일지 보기"** → `onOpenGuide` (기존 최하단 진입과 같은 목적지) |

**숨기는 것 (조건부 렌더링, 삭제 아님):**
- 기분/몸상태/날씨 선택기(`todayContext`) — 첫 기록 전에는 미노출.
- "하루 마무리 기록하기" 보조 버튼 — 기록 0건에서 마무리할 하루가 없다.
- `DeviceJournal`(최근 기록) — 빈 상태에서 빈 섹션 금지(§0-1).
- 홈 최하단의 기존 민지 버튼 — ④로 승격됐으므로 WELCOME에서는 중복 제거.

**유지하는 것:** 헤더(브랜드+더보기), 서비스 입구 3줄(내 일지/훈련 계획/분석)은
폴드 아래에 현행대로. PainReview는 조건상 빈 상태에서 원래 미노출이므로 무접촉.

**디테일:**
- 두 CTA는 시각적으로 **동급**이다. 한쪽을 ghost로 격하하지 않는다 — 일지와 계획은
  이 제품의 두 입구이며 우열을 홈이 정하지 않는다.
- 민지 카드에 스크린샷·이미지 자산을 새로 만들지 않는다. 텍스트 카드 + 진입 버튼까지만.

**검증:**
- 계약 테스트: 빈 상태 렌더에서 ①②③④ 존재, `todayContext`·"하루 마무리" 부재.
- e2e 또는 계약: [훈련 계획 만들기] 탭 → 계획 인테이크 진입.
- 375×667 스크린샷: ①~④가 폴드(첫 화면) 안에 들어오는지 실측 — 안 들어오면
  카피를 줄이지 말고 여백(`--space-*`)을 조정해 맞춘다.

---

## 4. Task 3 — 모드 B · 훈련 홈 (P1)

**대상:** `app/src/screens/home/TrainingHome.tsx`

**할 일:**
1. `homeMode === "TRAINING"`일 때 섹션 순서 변경:
   **다음 훈련(현행 `training-home__next` 섹션 그대로) → 오늘 기록 섹션 → 이하 현행.**
   다음 훈련 카드의 내용·라벨·aria-label은 현행 유지 — 위치만 최상단으로.
2. intro H1은 "내 기록" 유지 가능(재검토는 비목표 — 아래 §비목표 4).
   단, 다음 훈련 섹션이 intro보다 위로 갈지 아래로 갈지는 **intro 아래·오늘 섹션 위**로
   확정한다(헤더/제목 구조 보존, 문서 아웃라인 h1 유지).

**검증:**
- 계약 테스트: TRAINING 모드에서 첫 번째 `section`이 다음 훈련임을 고정
  (리뷰 보고서 회귀 요건 2항).
- 375×667 스크린샷: 다음 훈련 카드가 폴드 안에 있는지 실측.

---

## 5. Task 4 — 모드 C 보존 계약 + 모드 전환 테스트 (P1)

**대상:** `app/src/screens/home/` 계약 테스트 (신규 파일 가능:
`TrainingHome.modes.contract.test.tsx` 등, 기존
`HomeSafeAccessibility.contract.test.tsx` 컨벤션을 따를 것)

**할 일 (리뷰 보고서 회귀 방지 요건 1·3·4항):**
1. **A→C 전환**: 첫 기록 저장 직후 홈 재렌더 시 WELCOME이 JOURNAL로 바뀌는 테스트.
2. **P3 보존 스냅샷**: `entries ≥ 1 && plan === null`에서 섹션 순서가 현행
   (intro → 오늘 → 최근 기록 → 서비스)과 동일함을 고정.
3. **정직성 유지**: JOURNAL·TRAINING 모드에서 briefing 값이 없을 때 브리핑 요소가
   DOM에 없음을 재확인(기존 테스트가 이미 있으면 모드 분기 후에도 통과함을 확인).

**검증:** 위 테스트 3종이 새 코드에서 통과하고, Task 2·3 변경을 되돌리면
실패함을 1회 증명(스태시 방식 가능 — 증명 로그를 보고에 포함).

---

## 6. Task 5 — 하단 탭 이름 충돌 해소 (P2)

**대상:** `app/src/components/AppChrome.tsx:24-29` (`TAB_ITEMS`)

**문제:** `일지` vs `기록`(경기기록 화면). 한국어에서 둘 다 "기록"으로 읽혀
신규 사용자가 구분 불가 (리뷰 A-1).

**할 일:**
1. `log` 탭 라벨 `기록` → **`경기기록`** (공백 없이 4자 — 5탭 폭에서 줄바꿈 없어야 함).
2. 같은 라벨을 참조하는 테스트/aria 텍스트를 grep으로 전수 찾아 갱신:
   `grep -rn '"기록"' app/src app/e2e` → 현재 건수를 먼저 보고에 적고, 갱신 후 잔여
   건이 있으면 각각 사유를 명시(예: 문장 속 일반명사 "기록"은 대상 아님).
3. 375×667에서 탭바 5개 라벨이 한 줄 유지되는지 스크린샷 확인. 줄바꿈이 나면
   `경기 기록` 대신 폰트 크기 조정이 아니라 **라벨 후보 재보고** (임의 축약 금지).

**검증:** 탭바 스크린샷 + grep 전후 건수 보고.

---

## 7. 비목표 (이번에 하지 않는 것)

| 안 함 | 근거 |
|---|---|
| 코치용/부모용 홈, 역할 감지, 계정 온보딩 | 계정 flag OFF·role 부재 — 역할/계정이 생기는 시점의 과제로 **명시적 연기** |
| EngagementStrip·DecorationShop·TrashBin 개편 | 별도 과제(UX2 §2-2 계보). 이번엔 WELCOME에서의 노출 여부만: **빈 상태 표시가 정직하면 유지, 빈 껍데기면 숨김** — 판단 근거를 보고에 적을 것 |
| 모드 B/C의 H1 "내 기록" 교체 | 재검토 여지는 있으나 오너 결정 대기 — 카피 변경 범위 확산 방지 |
| 계획 엔진·처방·진행 기록 로직 | §0-5 |
| 신규 온보딩 플로우(투어·코치마크) | 별도 대형 작업 — 환영 홈의 정적 구성까지만 |

---

## 8. 수용 기준 (전체)

1. 세 모드가 Task 1의 판별 규칙과 정확히 일치하며, 화면 쪽 중복 판별이 없다.
2. 모드 A 폴드에 가치 제안·신뢰 한 줄·동급 CTA 2개·민지 진입이 모두 들어온다(실측).
3. 모드 B 폴드에 다음 훈련 카드가 들어온다(실측).
4. 모드 C는 현행과 픽셀 구성 동일(스냅샷 계약).
5. `tsc`·기존 홈 계약 테스트(`HomeSafeAccessibility`, `Task10Accessibility`) 전부 통과.
   ⚠️ Node 20 로컬에서 crypto/memo-vault 계열 41건 실패는 알려진 환경 이슈 —
   회귀로 취급하지 말고 CI(Node 24) 결과로 판정.
6. 증거 스크린샷 3모드 × 폴드/스크롤 구간을 PR에 첨부.
7. 완료 보고는 "PR 업로드, 리뷰 대기"까지만 주장.

---

*이 지시서는 `reports/review/FABLE_HOME_PERSONA_REVIEW_2026-08-19.md`의 판정
(P1 첫 방문자 ❌ · P2 계획 유저 ❌ · P3 일지 유저 ✅ 보존 · P4/P5 정직한 연기)을
코드로 옮긴다. 리뷰의 회귀 방지 요건 4항은 Task 3·4·5에 분배되어 있다.*
