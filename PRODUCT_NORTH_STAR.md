# PRODUCT_NORTH_STAR.md — 최고 지침

> **이 저장소에서 작업하는 모든 사람과 모든 AI 작업자는 이 문서를 가장 먼저
> 읽는다. 다른 어떤 문서보다 우선한다. 다른 문서가 이 문서와 충돌하면
> 이 문서가 이긴다.**

```yaml
문서지위: 최상위_지침
우선순위: 모든_문서보다_우선
변경권한: 오너(COACH_HOJUNE)만
```

---

## 1. TrainOracle는 무엇이 되려 하는가

**AI 코치다.** 기록 보관함이 아니다.

최종적으로 이 제품은 선수의 훈련 일지를 읽고 이렇게 해야 한다.

| 해야 하는 일 | 구체적으로 |
|---|---|
| **훈련을 분석한다** | 무엇을 했고 그게 어떤 의미인지 |
| **훈련을 설계한다** | 개인 기록에 맞는 목표 페이스와 세션 |
| **조언한다** | 코멘트, 경고, 다음에 할 일 |
| **보여준다** | 인포그래픽으로 성과와 예측 성과 |
| **부족한 것을 짚는다** | 모자란 에너지 시스템, 훈련 부하 |
| **편향을 잡는다** | 치우친 훈련 계획, 치우친 성과 |
| **위험을 감지한다** | 일지 속 작은 신호에서 부상 조짐 |

**그리고 이것은 한 번에 완성되는 게 아니라 쌓여서 좋아진다.**
프로필·기록이 풍부해질수록 계획이 정밀해지고, 처음에는 러프한 계획만
나온다. 이 구조와 그 안의 금지선은 **§6-2**와
[`PROGRESSIVE_COACHING_DESIGN_2026-07-27.md`](./PROGRESSIVE_COACHING_DESIGN_2026-07-27.md)에 있다.

**이것이 목적지다.** 지금 만드는 모든 기능은 여기로 가는 길 위에 있어야
한다.

## 2. 문서가 왜 이렇게 많은가

측정값(git 추적 파일 기준, 2026-07-27):

| 구분 | 개수 | 세는 방법 |
|---|---|---|
| 문서 `.md` 전체 | **436** | `git ls-files '*.md'` |
| └ 자동생성 증거 제외 | **277** | `.omo/evidence/` 제외 |
| └ `.omo/` 전부 제외 | **250** | 사람이 쓴 문서에 가까움 |
| 코드 `.ts`/`.tsx` 전체 | **156** | `git ls-files '*.ts' '*.tsx'` |
| └ `app/src` + `impl/src` | **128** | 제품 코드만 |

**문서가 코드의 약 2.2~2.8배다.** (`277:128` ≈ 2.2, `436:156` ≈ 2.8)

> 숫자를 인용할 때는 **어느 기준인지 함께 적는다.** 기준을 안 밝힌 비율은
> 다음 작업자를 오도한다. 실제로 이 문서의 이전 판은 근거가 다른 두 숫자를
> 나눠 "3.4배"라고 적었고, 그 전에는 `node_modules`를 세어 "1,044개"라고
> 적었다. 둘 다 틀렸다.

처음 오는 작업자는 이걸 "정리 안 된 상태"로 오해한다. **틀렸다.**

코칭은 사람의 몸을 다룬다. 잘못된 훈련 처방은 부상이고, 잘못 넘긴 위험
신호는 더 큰 부상이다. 그래서 이 제품은:

- **위험 신호 규칙**을 문서로 못 박는다 (`RULE_SPEC_D1_D9.md`, 1,898줄)
- **일지 원문 취급 경계**를 문서로 못 박는다 (`NOTE_SAFETY_SIGNAL_...`, 187줄)
- **분석·시각화 데이터 계약**을 문서로 못 박는다 (`ANALYSIS_AND_VISUALIZATION_...`, 639줄)
- **지표 산출 공식**을 문서로 못 박는다 (`METRIC_ALGORITHM_CONTRACT.md`, 414줄)

**이 문서들은 하드코딩이다.** 코드가 아직 안 쓰였을 뿐, 규칙 자체는 이미
정해진 것이다. 지우면 규칙이 사라진다.

### 문서를 대할 때의 원칙

| 하라 | 하지 마라 |
|---|---|
| 구분하고 찾기 쉽게 만들라 | **지우지 마라** |
| 어디에 뭐가 있는지 지도를 만들라 | 요약해서 원본을 대체하지 마라 |
| 현재 유효한 것을 표시하라 | "안 쓰는 것 같다"고 판단하지 마라 |
| 활용할 수 있게 색인하라 | 통합한다며 내용을 뭉개지 마라 |

**"정리"는 활용하기 위한 구분이지 삭제가 아니다.**

## 3. 절대 넘지 않는 선

이 규칙들은 협상 대상이 아니다. 어떤 작업 지시도 이걸 무효화할 수 없다.

```yaml
안전_불변식:
  몸상태_이상시: 계획_생성_차단
  몸상태_불명시: 차단_또는_사람검토
  몸상태_해제: 의학적_승인이_아님

  일지_원문: 위험신호_감지용이며_처방_숫자_입력이_아님
  일지_원문_서버전송: 금지
  일지_원문_모델컨텍스트: 금지

  60m미만_스프린트_페이스환산: 금지
  종목간_환산: 오너승인된_모델_없이_금지
  오래된기록: 침묵하고_계산하지_말_것_반드시_명시

  미검토_템플릿: 활성화_금지
  실패시_동작: 항상_안전한_쪽으로_폴백
```

**폴백 원칙:** 무언가 실패하면 **덜 보여준다.** 숫자를 못 보여주는 건
불편이지만, 잘못 보여주면 부상이다.

## 4. 지금 어디까지 왔는가

### 작동하는 것 (배포됨)

| 화면 | 기능 |
|---|---|
| 일지 | 훈련 기록 작성·수정·삭제·휴지통 |
| 추이 | 훈련량 그래프 |
| 훈련계획 | 7·9·10일 계획 후보, 오전/오후 분리 |
| 계정 | 가입·로그인·기기 간 동기화 |
| 가져오기 | 워치 기록 가져오기, 백업 복원 |

### 아직 안 되는 것 (= 남은 일)

| 목표 | 현재 |
|---|---|
| 개인 목표 페이스 처방 | 체감강도까지만. **엔진은 있고 연결 안 됨** |
| 일지 기반 분석·코멘트 | 미구현 |
| 인포그래픽 성과·예측 | 추이 그래프만 |
| 에너지 시스템 균형 진단 | 미구현 |
| 훈련 부하·편향 감지 | 미구현 |
| 일지 위험신호 감지 | **문서만 있고 미구현** |

## 5. 작업자가 착각하기 쉬운 것 — 실제 사례

이 절은 실제로 일어난 오판을 기록한다. 같은 실수를 반복하지 않기 위해서다.

### 사례 1 — "안전하게" 하려다 만들 기능을 깎아냄

한 작업자가 개인 페이스 계산을 "위험하니 나중에" 미루고, 체감강도만 쓰는
템플릿 2개를 여는 안을 냈다. **오너가 반려했다.**

개인 페이스는 부가기능이 아니라 **코칭의 본체**다. 안전하게 만드는 것과
목표를 축소하는 것은 다르다. **안전 장치를 붙여서 본체를 만들어야지,
본체를 빼는 게 안전이 아니다.**

### 사례 2 — 금지 규칙의 뜻을 넓게 오해함

같은 작업자가 `GOAL_ANCHOR_FORBIDDEN`을 "목표기록으로 페이스를 만들면
안 된다"로 읽었다. **틀렸다.** 실제 뜻은 "목표기록을 **현재 능력으로
취급하면** 안 된다"이다. 목표를 별도 항목으로 보여주는 건 가능하다.

**금지 규칙을 볼 때는 코드와 문서를 직접 열어 그 규칙이 막는 것이
정확히 무엇인지 확인한다.** 이름만 보고 추측하지 않는다.

### 사례 3 — 이미 있는 걸 없다고 판단함

같은 작업자가 페이스 계산을 "만들어야 할 것"으로 분류했다. 실제로는
`runtime.ts:177`에 `calculateSameEventRacePace`가 이미 있었다.

**없다고 결론 내리기 전에 `grep`으로 찾아본다.**

### 사례 4 — 도구 결과를 검증 없이 보고함

같은 작업자가 "문서 1,044개"라고 보고했다. `node_modules`를 센 것이었고
git 추적 기준 실제는 436개였다.

**숫자를 보고하기 전에 그 숫자가 무엇을 셌는지 확인한다.** 이후 "3.4배"도
기준이 다른 두 숫자를 나눈 값이라 또 틀렸다. §2에 기준별 실측치를 적어 뒀다.

### 사례 5 — 문서에 적힌 것을 코드가 받아들인다고 가정함

같은 작업자가 "카탈로그 30개 중 22개는 숫자가 채워져 있으니 바로 쓸 수
있다"고 봤다. 실제로 파서에 30개를 전부 넣어 보니 **0개가 통과했다.**

파서가 받는 형식은 `N×Dm @Em RP · rS″` 하나뿐인데(`notation.ts:6`),
카탈로그는 `@T`, `@I`, `@E`, `@5K RP`, `3~4×`, `800m+200m+200m` 같은
**코치 표기법**으로 적혀 있다. 둘은 애초에 다른 언어다.

**"문서에 값이 있다" ≠ "코드가 그 값을 읽을 수 있다".** 연결 작업을
계획하기 전에 **실제로 넣어서 돌려 본다.** 표를 눈으로 보고 판단하지
않는다.

### 사례 6 — 스펙이 근거 없다고 적어 둔 임계값을 작업자가 새로 만듦

같은 작업자가 개인 페이스 기획에서 **"기록 유효기간"** 을 제안했다.
`3개월 이내 정상 / 3~12개월 경고 / 12개월 초과 계산 거부`.

**그런데 스펙에 이미 반대로 적혀 있었다.**

`FORMATION_LOAD_AND_STATISTICAL_RULES_CONTRACT.md` §9:
> "There is **no evidence-backed universal freshness cutoff** for this
> population." / "A future cutoff is **product policy, not a scientific
> threshold**."

`TRAINING_SESSION_PRESCRIPTION_CONTRACT.md` §4:
> `PB` — "**A PB can be old**; it is never assumed to be current without
> its state." / `RECENT_RESULT` — "**stale is not silently current**."

**스펙이 요구한 것은 "만료"가 아니라 "표시"였다.** 3년 전 최고기록은
여전히 최고기록이다. 금지되는 건 그것을 **조용히 현재 실력처럼 쓰는 것**이다.

작업자는 이 두 문서를 읽지 않고 숫자를 만들었고, 오너가
*"유효기간은 없어야 되는 거 아냐?"* 라고 지적해 정정됐다.

**두 가지를 동시에 위반했다.**

| 위반 | 내용 |
|---|---|
| 스펙 위반 | 스펙이 근거 없다고 적어 둔 컷오프를 숫자로 만들어 넣음 |
| 오너 지침 위반 | "지우거나 하라는 게 아니야" — 만료를 붙이면 그 기록은 못 쓰는 기록이 된다 |

**교훈: 임계값을 만들고 싶어지면 그 주제의 스펙을 먼저 검색한다.**
`grep -rn "freshness\|cutoff\|threshold" specs/` 한 번이면 찾을 수 있었다.
작업자가 "합리적"이라고 느낀 숫자는 훈련 도메인에서 근거가 아니다.

## 6. 새 작업자의 첫 3단계

1. **이 문서를 읽는다.**
2. **[`CURRENT_IMPLEMENTATION_HANDOFF_2026-07-27.md`](./CURRENT_IMPLEMENTATION_HANDOFF_2026-07-27.md)**
   에서 현재 상태와 다음 작업을 확인한다.
3. **작업 전에 관련 스펙 문서를 연다.** 특히 안전·일지·분석 영역을
   건드린다면 §2에 나열한 계약 문서를 반드시 읽는다.

## 6-1. 개인 목표 페이스 — **오너 승인 완료 (2026-07-27)**

개인 목표 페이스 처방은 **더 이상 대기 중이 아니다. 승인되었다.**

- 🔴 **결정 정본:** [`PERSONAL_PACE_DECISION_2026-07-27.md`](./PERSONAL_PACE_DECISION_2026-07-27.md)
  — 승인된 6개 결정, 승인되지 **않은** 것 목록, 5단계 순서
- 🔴 **제품 방향 기록:** [`PROGRESSIVE_COACHING_DESIGN_2026-07-27.md`](./PROGRESSIVE_COACHING_DESIGN_2026-07-27.md)
  — 점진적 코칭(기록이 쌓일수록 좋은 계획). **일지 원문 → 계획 잠극 금지**,
  **개수 임계값 금지**, 오너 예시가 전부 P4 이후인 이유를 담았다
- 실행 계획: [`PLAN_A_FIRST_TEMPLATE_ACTIVATION.md`](./PLAN_A_FIRST_TEMPLATE_ACTIVATION.md)

**승인된 목적지는 "길 2"(환산 모델 도입)다.** 경기 페이스만 되는
상태는 목적지가 아니라 배선을 검증하는 경유지다.
**경유지에서 멈추면 결정 위반이다.**

### 하위 에이전트 작업지시서 (순서대로)

| 순서 | 지시서 | 내용 | 선행조건 |
|---|---|---|---|
| 1 | [`WORK_ORDER_P1_ATHLETE_RECORDS.md`](./WORK_ORDER_P1_ATHLETE_RECORDS.md) | 선수 기록(PB/SB/최근/목표) 입력·저장 | **없음 — 즉시 착수** |
| 2 | [`WORK_ORDER_P2_MACHINE_NOTATION.md`](./WORK_ORDER_P2_MACHINE_NOTATION.md) | 카탈로그 기계용 표기 추가 (원본 보존) | 없음 (문서 작업) |
| 3 | [`WORK_ORDER_P3_PACE_WIRING.md`](./WORK_ORDER_P3_PACE_WIRING.md) | 페이스 계산 배선·2단 표시 | **P1 + P2 병합 후** |
| 4 | (미작성) 환산 모델 | 리겔·VDOT 도입 | **오너 재승인 필요** |
| 5 | [`WORK_ORDER_P5_JOURNAL_IMPORT_AND_CHARTS.md`](./WORK_ORDER_P5_JOURNAL_IMPORT_AND_CHARTS.md) | 일지 불러오기 + 분포·그래프·통계 | **P1 병합 후** |
| UX1 | [`WORK_ORDER_UX1_TAP_FATIGUE.md`](./WORK_ORDER_UX1_TAP_FATIGUE.md) | 손가락 피로 줄이기 (스크롤·글자량·누른 느낌) | **없음 — 즉시 착수** (§6-5) |

> **P4의 지위가 바뀌었다 (2026-07-27).** 오너가 원하는 코칭(400m 인터벌,
> 2000m 크루즈, 템포런 페이스, 종목 비교 진단) 전부가 종목 간 환산을
> 전제로 한다. 따라서 P4는 **"더 많은 템플릿을 여는 부가기능"이 아니라
> 제품 목표의 전제 조건**이다. 근거:
> [`PROGRESSIVE_COACHING_DESIGN_2026-07-27.md`](./PROGRESSIVE_COACHING_DESIGN_2026-07-27.md) §3.
> 그렇다고 승인 없이 착수하지는 않는다.

**P4 지시서는 일부러 쓰지 않았다.** 승인 없이 착수되는 것을 막기
위함이다. P3 결과를 오너께 보고한 뒤 환산 모델 근거를 다시 확인받는다.

### 🔴 기록에 유효기간은 없다 — 오판 사례로 기록해 둔다

**기록은 만료되지 않는다. 오래된 기록도 지우지 않는다.**
대신 **역할·달성일·경과 기간을 항상 함께 표시**한다.

이것은 스펙에 이미 있던 규칙이다.

- `FORMATION_LOAD_AND_STATISTICAL_RULES_CONTRACT.md` §9
  — "There is **no evidence-backed universal freshness cutoff** for this
  population." / "A future cutoff is **product policy, not a scientific
  threshold**."
- `TRAINING_SESSION_PRESCRIPTION_CONTRACT.md` §4
  — "**A PB can be old**; it is never assumed to be current without its state."
  / "**stale is not silently current**."

**그런데 에이전트가 이 스펙을 읽지 않고 `3개월`/`12개월` 경과 시
계산 거부(만료)를 제안했다.** 오너가 *"유효기간은 없어야 되는 거 아냐?"*
라고 지적해 정정했다. → **오판 사례 6번**(§5 참조)

무엇을 현재 실력으로 인정할지는 **사람이 고른다.** 엔진의
`freshnessState`는 **입력값**이며(`anchor.ts:23`이 읽기만 한다)
날짜에서 자동 계산되는 값이 **아니다.**

### ⚠️ 남은 미결 — 훈련 기록을 페이스 기준으로 쓸 것인가

스펙이 허용하는 앵커는 7종뿐이고 **훈련 기록·타임트라이얼은 없다.**

```
RECENT_RESULT | PB | SB | GOAL | COACH_REFERENCE | RPE_ONLY | SPRINT_BENCHMARK
```

추가하려면 **스펙 변경**이 필요하므로 작업자가 정할 수 없다.
결정 정본 §9에 오너 질문으로 올려 두었다.
**P1·P2·P3는 이 미결과 무관하게 진행한다.**

## 6-2. 점진적 코칭 — 기록이 쌓일수록 좋아지는 계획 (오너 방향, 2026-07-27)

> 오너: *"회원가입해서 프로필에 상세한 개인 기록을 잘 써줄수록 훈련 계획을
> 잘 짜준다. … 처음이나 초반에는 러프한 계획만 받아볼 수 있는 거고."*

**이것이 제품의 코칭 구조다.** 정본: [`PROGRESSIVE_COACHING_DESIGN_2026-07-27.md`](./PROGRESSIVE_COACHING_DESIGN_2026-07-27.md)

이 방향은 새 기능 제안이 아니라 스펙의 **P1 등급 미결 과제**다 —
`OI-FA-MINIMUM-EVIDENCE-001` (*"Required history by frame type"*,
`Canonical blocking: YES`, 상태 OPEN).

### 🔴 이 구조를 구현할 때 절대 어기면 안 되는 두 가지

**(1) 일지 원문은 훈련 계획에 절대 들어갈 수 없다.**

```yaml
raw_note_input_allowed: false          # 계획 형성 스펙 §5
plan_generator_raw_text: forbidden     # 일지 스펙
raw_text_quote_summary_token_or_embedding: reject   # 요약·토큰·임베딩도 금지
```

`FA-TC-031`은 **두 단계로 세탁한 파생물까지 전체 조상 추적으로 거부**한다.
일지 **원문**의 역할은 위험 신호 감지이며 `GENERATION_ALLOWED`를 만들 수 없다.

> 🔵 **범위 정정 (2026-07-27).** 이 금지는 **자유 서술 원문에만** 걸린다.
> 일지에서 **구조화 수치**(거리·시간·페이스·분포·통계)를 불러와 계획에
> 참고하는 것은 **오너 결정으로 열렸다.** 스펙의 다른 금지 조항들은
> `before_separate_adoption` 조건부이고 승인 권한자가
> `adoption_authority: COACH_HOJUNE`다. 오너 지적:
> *"끝까지 막아내기 보다는, 필요시와 요구시에는 열어도 돼."*
> → [`OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md`](./OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md) §4

**(2) "많이 쓰면 좋아진다"를 개수 임계값으로 만들지 않는다.**

```yaml
minimum_evidence_policy:            # FORMATION_LOAD_AND_STATISTICAL_RULES §10
  baseline_classification_threshold: UNKNOWN
  readiness_threshold: PROHIBITED
  injury_risk_threshold: PROHIBITED
```

> "**No fixed number of sessions** creates a stable baseline…"

`일지 20개 이상이면 정밀 계획`은 **만료 기준을 만들어 넣은 오판(사례 6)과
똑같은 잘못이다.** 계획의 정밀도는 **양이 아니라 "어떤 종류의 사실이
존재하는가"** 로 갈린다. 5000m 기록 하나만 있어도 5000m 인터벌 페이스는
정확하고, 일지 100개가 있어도 종목 기록이 없으면 페이스를 만들 수 없다.

## 6-3. 기준 기록은 사람이 고른다 (오너 결정, 2026-07-27)

> 오너: *"본인이나 코치가 어떤 훈련을 할지 선택하게 하는 거지. 번거롭거나
> 손이 한번 더 눌러야 하는 상황이 있더라도 그게 필요할 듯."*

**PB 기준 / SB 기준을 사람이 버튼으로 고른다. 코드가 자동 선택하지 않는다.**
고르지 않은 쪽과의 **차이도 항상 함께 보여준다.**

이것은 스펙이 원래 요구한 설계다 — `anchor.ts:23`은 `freshnessState`를
**읽기만** 하고, 스펙은 *"stale is not silently current"*라고 못 박았다.
**손이 한 번 더 가지만, 그 한 번이 오래된 기록을 현재 실력으로
둔갑시키는 것을 막는다.**

**차이를 보여줄 때 하나의 "부하 점수"로 합치지 않는다.** 부하 계약 §3이
`total biological load`와 `interchangeable universal load`를
금지 해석으로 명시했다. **페이스 차이와 목표 시간 차이로 보여준다.**

정본: [`OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md`](./OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md)

## 6-4. 시즌 창은 18개월이고, 그건 만료가 아니다 (오너 결정, 2026-07-27)

> 오너: *"시즌길이는 보통 1년이지만 보수적으로 오늘로부터 1년 6개월 전으로
> 잡을게. 현재 경기력 평가 지표로 표현하기도 해줘. **pb기간무관.**
> 시즌밖기록으로. 기록이나 자료가 많은 사람들은 아까말한 분포도나 그래프
> 통계적으로 보면서 파악할수있도록."*

```yaml
시즌_창:
  기준점: 오늘                       # 고정 날짜가 아니라 롤링
  범위: 오늘로부터 18개월 이내
  적용_대상: SEASON_BEST 만
  PB_적용: 없음                      # "pb기간무관"
  범위_밖_기록:
    삭제: 없음
    저장_거부: 없음
    계산_거부: 없음
    처리: "시즌 밖" 으로 표시
```

**스펙에 시즌 길이 정의가 없었다.** `grep -rn "season" specs/`로 전수
확인했다. 스펙은 SB에 *"A season must be named"*만 요구하고 그 **길이는
정의하지 않았다.** 그러니까 18개월은 스펙 위반이 아니라 **오너가 빈칸을
채운 제품 정책**이다. 부하 계약 §9도 이렇게 적어 두었다 —
*"A future cutoff is **product policy**, not a scientific threshold."*

### 🔴 18개월은 다시 만료가 되기 쉽다 — 이 경계를 지킨다

18개월은 날짜에서 계산되는 값이다. 그래서 사례 6(§5)의 유효기간 오판이
**같은 모습으로 재발할 수 있다.** 경계를 못 박는다.

| 하는 것 | 허용 |
|---|---|
| "현재 시즌" / "시즌 밖" **라벨** 만들기 | ✅ |
| 목록을 세 그룹으로 **나눠 보여 주기** | ✅ |
| 그래프 기본 범위를 18개월로 잡기 | ✅ (범위 변경 가능해야 함) |
| 시즌 밖 기록으로 **계산을 거부** | ❌ |
| 시즌 밖 기록을 **숨기거나 삭제** | ❌ |
| `PERSONAL_BEST`에 창을 적용 | ❌ **`pb기간무관`** |
| 창 안이라고 `freshnessState`를 자동 결정 | ❌ 사람이 정한다 |

### "현재 경기력 평가 지표"는 화면 라벨이다 — `purpose`가 아니다

오너가 그렇게 부르자고 하셨고, **화면에서는 그렇게 부른다.**
다만 **엔진에 넘어가는 `purpose`는 바뀌지 않는다.**

| 화면 그룹 | 실제 `purpose` |
|---|---|
| 현재 경기력 지표 | `RECENT_RESULT` → `CURRENT_CAPABILITY` |
| 현재 경기력 지표 | 시즌 창 안 `SEASON_BEST` → **`SEASON_CONTEXT`** (그대로) |
| 개인 최고기록 | `PERSONAL_BEST` → `CURRENT_CAPABILITY` (기간 무관) |
| 시즌 밖 기록 | `SEASON_BEST` → `SEASON_CONTEXT` |

**시즌 창 안에 있다는 이유로 SB를 `CURRENT_CAPABILITY`로 올리면
엔진이 거부한다** (`anchor.ts:35-40`, 스펙 §4 불변식).

### 기록 많은 사용자에게는 분포·그래프·통계를 보여준다

오너 요구다. 다만 **"N개 이상이면 개방" 같은 개수 임계값을 만들지 않는다.**
부하 계약 §10 `minimum_evidence_policy`가
`baseline_classification_threshold: UNKNOWN`,
`descriptive_single_observation: allowed`라고 적었다.
**기록 1개부터 그래프는 켜져 있다. 기록이 쌓이면 그래프가 저절로
읽을 만해진다. 잠갔다 여는 장치는 필요 없다.**

그래프·통계는 `specs/reconstruct/ANALYSIS_AND_VISUALIZATION_DATA_CONTRACT.md`
가 지배한다. 특히:

- `analysis_can_create_plan_options: false` — **분석 화면이 계획을 만들지 않는다**
- `hidden_interpolation_allowed: false` — **빈 구간을 이어 붙이지 않는다**
- `aggregate_requires_visible_n: true` — **표본 수 n을 항상 보여준다**
- `missing_data_must_be_visible: true` / `stale_data_must_be_visible: true`
- `analysis_can_clear_D9: false` — **분석이 안전 게이트를 열지 못한다**

정본: [`OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md`](./OWNER_DECISION_ANCHOR_CHOICE_2026-07-27.md) §3, §6
작업지시서: [`WORK_ORDER_P5_JOURNAL_IMPORT_AND_CHARTS.md`](./WORK_ORDER_P5_JOURNAL_IMPORT_AND_CHARTS.md)

## 6-5. 손가락이 피로하면 기록을 안 쓴다 (오너 방향, 2026-07-27)

> 오너: *"손이 많이 눌리기 때문에 너무 아래로 스크롤 하거나 계속 한 화면에
> 텍스트가 너무 많다거나 하면 피로감이 커지거든. 클릭했을 때 아주 조금의
> 이펙트만 있다면 넘어가는 기분과 해소감을 느끼며 더 누르고 싶은 욕구도
> 생겨."*

이건 꾸미는 얘기가 아니다. **이 제품의 모든 계산은 사용자가 넣은
기록에서 나온다.** 일지 쓰기가 피곤해서 안 쓰게 되면 페이스도, 분포도,
계획도 만들 수 없다. **입력 화면의 피로는 데이터 문제다.**

작업지시서: [`WORK_ORDER_UX1_TAP_FATIGUE.md`](./WORK_ORDER_UX1_TAP_FATIGUE.md)

### 실측부터 한다 — 화면 얘기도 추측으로 하지 않는다

2026-07-27 에 390×844 에서 재 봤다. 재고 나서 두 가지 짐작이 틀렸다는 게
드러났다.

| 재기 전 짐작 | 실측 결과 |
|---|---|
| "작은 라디오 버튼이 문제다" | 13×13 동그라미는 **171×64 라벨 안**에 있다. 44px 이미 지킴 |
| "누른 느낌을 새로 만들어야 한다" | `app.css:442` 에 `scale(0.97)`/`90ms` 가 **이미 있다** |

실제 문제는 다른 데 있었다.

| 화면 | 스크롤 깊이 | 길이를 만드는 구획 |
|---|---|---|
| 훈련 후 일지 | 2.01 화면 | 객관 기록 393px (29%) |
| 하루 마무리 | 2.14 화면 | 통증 부위 754px (52%) |

그리고 눌러도 반응 없는 요소가 21개였다 (`input`·`select`·`textarea`).

**화면 작업에도 §5 사례 6의 교훈이 적용된다.** "합리적으로 보이는" 짐작
대신 브라우저에서 재고 시작한다.

### 🔴 밀도를 줄인다는 것이 글자를 지운다는 뜻은 아니다

오너 지침: *"문서들은 정리하라는게 구분하고 실제 활용하기위한 작업을
하는 거지 지우거나 하라는 게 아니야"* — **화면 글자에도 똑같다.**

| 하는 것 | 허용 |
|---|---|
| 다 답한 구획을 한 줄로 **접기** | ✅ 펼치면 그대로 있다 |
| 안 쓰는 구획을 **닫힌 상태로 시작** | ✅ 누르면 열린다 |
| 보여주기만 하는 그림을 버튼 뒤로 | ✅ 정보 손실 없음 |
| 안전 안내문 줄이기 | ❌ **절대 금지** |
| 저장 실패 안내 줄이기 | ❌ 원인 + 다음 행동 둘 다 필요 |
| 동의·용도 설명 줄이기 | ❌ 동의를 받는 문구다 |
| 표본 수·출처·경과 기간 빼기 | ❌ 계약이 요구한다 |
| 접혔다고 값에 `0` 넣기 | ❌ 없는 건 없다고 남긴다 |

**접기는 보기 방식이고, 값은 그대로 저장된다.**

### 반응은 아주 조금이어야 하고, 이미 있는 값을 따라간다

저장소에 움직임 값이 하나로 모여 있고 계약 테스트가 잠근다
(`app/src/components/Motion.contract.test.tsx`).

| 값 | 어디에 |
|---|---|
| `--ease-out-strong: cubic-bezier(0.23, 1, 0.32, 1)` | 모든 움직임의 가속 곡선 |
| `scale(0.97)` / `90ms` | 버튼 누른 느낌 |
| `150ms` / `200ms` / `150ms` | 도움말 · 저장 알림 |
| `@media (prefers-reduced-motion: reduce)` | 움직임 줄이기 설정에서는 fade 만 |

**새 효과를 넣을 때 새 시간·새 곡선·새 색을 만들지 않는다.** 이미 있는
값을 따라 쓴다. 색·서체 토큰의 단일 소스는 저장소 루트의
`colors_and_type*.css` 다 (ADR A3). `app.css` 에 다시 정의하면
두 값이 갈라진다.

효과를 추가하면 **계약 테스트에 확인 줄도 함께 추가한다.**
기존 줄은 지우지 않는다.

---

## 7. 판단이 서지 않을 때

**멈추고 오너에게 묻는다.**

훈련 도메인 판단(어느 종목을 열지, 환산 모델을 뭘 쓸지, 미성년 정책,
어떤 기록을 현재 실력으로 인정할지)은 **작업자가 정하지 않는다.**
임의로 정한 숫자는 근거 없는 훈련 처방이 되고, 그건 선수에게 위험하다.

**특히 "임계값"을 만들고 싶어질 때 멈춰라.** 며칠·몇 개월·몇 회처럼
경계를 긋는 숫자는 대개 스펙이 "근거 없음"으로 이미 판정해 둔 것이다.
실제로 그렇게 만료 기준을 만들어 넣은 오판이 있었다(§6-1).

모르면 `판단보류`로 남기고 올린다. 그게 정답이다.

---

**이 제품은 사람의 훈련과 몸을 다룬다. 깊고 진중하게 작업한다.**
