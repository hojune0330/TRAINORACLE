# 작업지시 C — 문서 색인 만들기 (저비용 작업자용)

```yaml
work_order:
  id: WO-C-DOC-INDEX
  목적: 문서를 구분하고 찾아 쓸 수 있게 색인한다
  대상작업자: 저비용 작업자
  코드수정: 절대 금지
  삭제·이동: 절대 금지
  선행필독: PRODUCT_NORTH_STAR.md
```

> **시작 전 [`PRODUCT_NORTH_STAR.md`](./PRODUCT_NORTH_STAR.md)를 읽으십시오.**

## 0. 작업자가 반드시 지킬 3가지

1. **`app/`, `impl/`, `specs/` 안의 파일은 절대 건드리지 않는다.** 이 작업은
   문서 색인만 만든다.
2. **어떤 파일도 삭제하거나 이동하지 않는다.** 판정만 적는다.
3. **문서 내용을 읽고 요약하지 않는다.** 아래에 적힌 기계적 규칙만 적용한다.
   내용 판단이 필요하면 `판단보류`로 적고 넘어간다.

## 1. 배경 (읽기만)

문서가 436개(자동생성 증거 제외 시 277개), 제품 코드가 128개다.
**이 문서들은 낭비가 아니다.** 이 제품은
사람의 몸을 다루는 코칭 시스템이라, 위험 신호 규칙·일지 취급 경계·지표
산출 공식을 문서로 못 박아 둔다. 코드가 아직 안 쓰였을 뿐 규칙은 이미
정해진 것이다.

**문제는 문서가 많은 게 아니라 어디에 뭐가 있는지 모른다는 것이다.**
이 작업은 **찾아 쓸 수 있는 색인**을 만든다. 지우는 작업이 아니다.

## 2. 만들 결과물 — 파일 1개

`DOCUMENT_MAP.md` 를 저장소 최상위에 새로 만든다. 그 외 파일은 만들지도
고치지도 않는다.

## 3. 작업 절차

### 3-1단계: 목록 뽑기

```bash
cd /저장소경로
git ls-files "*.md" | grep -v "^.omo/evidence/" > /tmp/doclist.txt
wc -l /tmp/doclist.txt
```

`.omo/evidence/`는 자동 생성 증거파일이라 제외한다. 약 275줄이 나오면 정상이다.

### 3-2단계: 각 문서의 최종 수정일 뽑기

```bash
while read f; do
  echo "$(git log -1 --format=%cs -- "$f")|$f"
done < /tmp/doclist.txt | sort -r > /tmp/docdated.txt
```

### 3-3단계: 판정 2가지를 붙인다

문서마다 **① 상태**와 **② 주제** 두 가지를 적는다. 주제가 있어야 나중에
"부상 신호 관련 문서 어디 있지?"를 바로 찾을 수 있다.

#### ① 상태 판정

**위에서부터 순서대로** 적용한다. 먼저 걸리는 것으로 확정하고 멈춘다.

| 순서 | 조건 | 상태 |
|---|---|---|
| 1 | 경로가 `specs/active/` 로 시작 | `유효-스펙` |
| 2 | 경로가 `specs/reconstruct/` 로 시작 | `초안-스펙` |
| 3 | 파일명이 `README.md`, `PRODUCT_NORTH_STAR.md`, `TRAINORACLE_SPEC_INDEX.md` | `유효-길잡이` |
| 4 | 파일명에 `HANDOFF` 포함 + 수정일이 가장 최근 1건 | `유효-인계` |
| 5 | 파일명에 `HANDOFF` 포함 + 그 외 | `과거-인계` |
| 6 | 파일 안에 `PROPOSAL_ONLY` 또는 `SUPERSEDED` 가 있음 | `대체됨` |
| 7 | 파일 안에 `[DRAFT_COMPLETE]` 문자열이 있음 | `완료-기록` |
| 8 | 경로가 `reports/` 로 시작 | `기록물` |
| 9 | 위 어디에도 안 걸림 | `판단보류` |

> 순서 주의: `대체됨`이 `완료-기록`보다 먼저다. 둘 다 해당하면 `대체됨`이다.

```bash
grep -lE "PROPOSAL_ONLY|SUPERSEDED" 파일경로
grep -l "\[DRAFT_COMPLETE\]" 파일경로
```

#### ② 주제 판정

**파일명에** 아래 단어가 있으면 해당 주제를 붙인다. 여러 개면 먼저 걸리는
하나만. 하나도 없으면 `기타`.

| 파일명에 있으면 | 주제 |
|---|---|
| `RULE`, `SAFETY`, `D1_D9`, `D9` | `안전규칙` |
| `NOTE`, `SIGNAL`, `LOG`, `JOURNAL`, `CHECKIN` | `일지·위험신호` |
| `ANALYSIS`, `METRIC`, `VISUALIZATION`, `TREND` | `분석·시각화` |
| `PLAN`, `FORMATION`, `MICROCYCLE`, `CALENDAR` | `훈련계획` |
| `PRESCRIPTION`, `TEMPLATE`, `ENERGY`, `SESSION` | `처방·템플릿` |
| `ACCOUNT`, `SYNC`, `PRIVACY`, `EXPORT`, `FEDERAT` | `계정·프라이버시` |
| `RESEARCH`, `EVIDENCE`, `SOURCE` | `연구근거` |
| `DESIGN`, `UI`, `SCREEN` | `디자인` |
| `WORK_ORDER`, `WO0`, `HANDOFF`, `DECISION` | `작업관리` |
| 위 어디에도 없음 | `기타` |

**파일명만 본다. 파일을 열어 읽지 않는다.**

### 3-4단계: 표 만들기

`DOCUMENT_MAP.md` 에 **아래 형식 그대로** 쓴다. 표의 열은 정확히
`수정일 | 상태 | 주제 | 경로` 4개다. 열을 늘리거나 줄이지 않는다.

```markdown
# DOCUMENT_MAP.md — 문서 지도

이 파일은 파일 경로·파일명·수정일만 보고 기계적으로 분류한 색인이다.
문서 내용의 가치를 판단한 것이 아니며, 여기 실린 어떤 문서도
"불필요"로 표시되지 않는다. `판단보류`는 "사람이 정해야 함"이라는
뜻이지 "쓸모없음"이 아니다.

작성일: (오늘 날짜)
대상: git 추적 `*.md` 중 `.omo/evidence/` 제외
분류 규칙: WORK_ORDER_C_DOC_CLEANUP.md 3-3단계

## 먼저 읽을 문서

| 순서 | 문서 | 용도 |
|---|---|---|
| 1 | PRODUCT_NORTH_STAR.md | 최고 지침 — 무엇을 만드는 제품인가 |
| 2 | README.md | 저장소 전체 안내 |
| 3 | (가장 최근 HANDOFF 파일명) | 현재 상태와 다음 작업 |
| 4 | TRAINORACLE_SPEC_INDEX.md | 스펙 찾아보기 |

## 상태별 개수

| 상태 | 개수 | 이 상태의 뜻 |
|---|---|---|
| 유효-스펙 | ? | 지금 지켜야 하는 확정 규칙 |
| 초안-스펙 | ? | 규칙 후보, 아직 확정 아님 |
| 유효-길잡이 | ? | 어디부터 볼지 알려주는 문서 |
| 유효-인계 | ? | 가장 최신 인계 문서 |
| 과거-인계 | ? | 지난 인계 문서 (이력용, 보존) |
| 대체됨 | ? | 후속 문서가 있음 (이력용, 보존) |
| 완료-기록 | ? | 초안 작업이 끝난 문서 |
| 기록물 | ? | 검증·감사 기록 (증거용, 보존) |
| 판단보류 | ? | 사람이 상태를 정해야 함 |
| **합계** | **?** | = 3-1단계 목록 줄 수와 같아야 함 |

## 주제별 개수

| 주제 | 개수 |
|---|---|
| 안전규칙 | ? |
| 일지·위험신호 | ? |
| 분석·시각화 | ? |
| 훈련계획 | ? |
| 처방·템플릿 | ? |
| 계정·프라이버시 | ? |
| 연구근거 | ? |
| 디자인 | ? |
| 작업관리 | ? |
| 기타 | ? |
| **합계** | **?** |

## 전체 목록 (수정일 내림차순)

| 수정일 | 상태 | 주제 | 경로 |
|---|---|---|---|
| 2026-07-27 | 유효-길잡이 | 기타 | PRODUCT_NORTH_STAR.md |
| 2026-07-27 | 유효-인계 | 작업관리 | CURRENT_IMPLEMENTATION_HANDOFF_2026-07-27.md |
| ... | ... | ... | ... |
```

> 두 합계(상태별·주제별)는 **서로 같아야 하고**, 3-1단계 목록 줄 수와도
> 같아야 한다. 다르면 어딘가 빠뜨린 것이다.

### 3-5단계: 검증

아래 **5개가 모두 통과해야** 제출한다. 하나라도 어긋나면 제출하지 않는다.

```bash
# 1) 전체 목록 줄 수 == 3-1단계 목록 줄 수
grep -c "^| 20[0-9][0-9]-" DOCUMENT_MAP.md
wc -l < /tmp/doclist.txt
#    → 위 두 숫자가 같아야 한다

# 2) 모든 줄에 열이 4개인가 (파이프 5개). 아무것도 안 나와야 정상
grep "^| 20[0-9][0-9]-" DOCUMENT_MAP.md | awk -F'|' 'NF!=6 {print NR": "$0}'

# 3) 상태 칸에 허용된 9개 단어만 쓰였는가
grep "^| 20[0-9][0-9]-" DOCUMENT_MAP.md | awk -F'|' '{gsub(/ /,"",$3); print $3}' \
  | sort -u
#    → 유효-스펙/초안-스펙/유효-길잡이/유효-인계/과거-인계/대체됨/
#      완료-기록/기록물/판단보류  이외의 값이 있으면 오류

# 4) 주제 칸에 허용된 10개 단어만 쓰였는가
grep "^| 20[0-9][0-9]-" DOCUMENT_MAP.md | awk -F'|' '{gsub(/ /,"",$4); print $4}' \
  | sort -u
#    → 안전규칙/일지·위험신호/분석·시각화/훈련계획/처방·템플릿/
#      계정·프라이버시/연구근거/디자인/작업관리/기타  이외면 오류

# 5) 코드·스펙·앱을 안 건드렸는가 + 새 파일이 1개뿐인가
git status --short
#    → "?? DOCUMENT_MAP.md" 한 줄만 나와야 한다
```

5번에서 `app/`, `impl/`, `specs/`, `reports/` 가 보이면 **제출하지 말고
되돌린다**: `git checkout -- 해당파일`

삭제된 파일( `D ` 로 시작하는 줄 )이 하나라도 있으면 **즉시 전부 되돌린다**:
`git checkout -- .` — 이 작업에서 삭제는 0건이어야 한다.

## 4. 제출

```bash
git add DOCUMENT_MAP.md
git commit -m "docs: 문서 지도 추가 — 문서 N개를 상태·주제 2축으로 색인

목적: 문서를 찾아 쓸 수 있게 색인한다. 정리=구분이며 삭제가 아니다.
- 상태 9종(유효-스펙/초안-스펙/유효-길잡이/유효-인계/과거-인계/
  대체됨/완료-기록/기록물/판단보류)
- 주제 10종(안전규칙/일지·위험신호/분석·시각화/훈련계획/처방·템플릿/
  계정·프라이버시/연구근거/디자인/작업관리/기타)

파일 경로·파일명·수정일만 사용한 기계적 분류다. 문서 내용의 가치는
판단하지 않았다. 삭제 0건, 이동 0건, 코드·스펙 수정 0건.
판단보류 N건은 오너의 결정이 필요하다."
git push origin main
```

## 5. 하지 말아야 할 것

- 문서를 지우거나 옮기기 — **금지** (삭제는 0건이어야 한다)
- "이건 필요 없어 보인다", "중복 같다" 같은 의견 적기 — **금지**
- `판단보류`를 임의로 다른 상태로 바꾸기 — **금지**
- 표에 없는 열(요약·비고·중요도 등) 추가하기 — **금지**
- 문서를 열어 내용을 읽고 주제를 바꾸기 — **금지** (주제는 파일명만으로 정한다)
- 상태·주제 단어를 새로 만들기 — **금지** (9종·10종 고정)

애매하면 `판단보류`로 두고 넘어간다. 그게 정답이다. **판단보류가 많은 것은
실패가 아니다.** 잘못 단정하는 것이 실패다.

## 6. 완료 기준

- `DOCUMENT_MAP.md` 1개만 추가됐다. 삭제·이동·수정 0건.
- 전체 목록 줄 수 = 상태별 합계 = 주제별 합계 = 3-1단계 목록 줄 수.
- 상태 칸에 9종, 주제 칸에 10종 외의 값이 없다.
- `git status`가 `?? DOCUMENT_MAP.md` 한 줄뿐이다.

## 7. 이 색인을 나중에 어디에 쓰는가 (작업자는 읽기만)

이 색인이 있으면 다음 작업이 가능해진다. 그래서 이 작업을 하는 것이다.

| 나중에 할 일 | 이 색인에서 뽑아 쓰는 것 |
|---|---|
| 일지 기반 훈련 분석 구현 | 주제 = `일지·위험신호` 문서 전부 |
| 성과·예측 인포그래픽 구현 | 주제 = `분석·시각화` 문서 전부 |
| 부족한 에너지시스템 판정 | 주제 = `처방·템플릿` + `훈련계획` |
| 편향된 훈련계획 감지 | 주제 = `훈련계획` + `분석·시각화` |
| 안전 게이트 구현·검증 | 주제 = `안전규칙`, 상태 = `유효-스펙` |

즉 이 표는 **"코딩 전에 어떤 규칙 문서를 읽어야 하는가"를 알려주는 목차**다.
