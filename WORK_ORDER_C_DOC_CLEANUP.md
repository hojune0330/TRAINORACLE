# 작업지시 C — 문서 정리 (저비용 작업자용)

```yaml
work_order:
  id: WO-C-DOC-CLEANUP
  목적: 문서를 찾기 쉽게 만든다
  대상작업자: 저비용 작업자
  코드수정: 절대 금지
  삭제: 절대 금지
```

## 0. 작업자가 반드시 지킬 3가지

1. **`app/`, `impl/`, `specs/` 안의 파일은 절대 건드리지 않는다.** 이 작업은
   문서 목록만 만든다.
2. **어떤 파일도 삭제하거나 이동하지 않는다.** 판정만 적는다.
3. **문서 내용을 읽고 요약하지 않는다.** 아래에 적힌 기계적 규칙만 적용한다.
   내용 판단이 필요하면 `판단보류`로 적고 넘어간다.

이유: 이 저장소는 안전 규칙이 문서에 적혀 있다. 잘못 지우면 규칙이 사라진다.

## 1. 배경 (읽기만)

문서가 434개, 실제 코드가 128개다. 문서가 3.4배 많다. 어느 문서가 현재
유효한지 알 수 없어서 매번 전부 읽어야 한다. 이 작업은 **한 장짜리 목록**을
만들어 다음 사람이 3개만 읽고 시작할 수 있게 한다.

## 2. 만들 결과물 — 파일 1개

`DOCUMENT_MAP.md` 를 저장소 최상위에 새로 만든다. 그 외 파일은 만들지도
고치지도 않는다.

## 3. 작업 절차

### 3-1단계: 목록 뽑기

```bash
cd /저장소경로
git ls-files "*.md" | grep -v "^.omo/evidence/" > /tmp/doclist.txt
wc -l /tmp/doclist.txt
```

`.omo/evidence/`는 자동 생성 증거파일이라 제외한다. 약 275줄이 나오면 정상이다.

### 3-2단계: 각 문서의 최종 수정일 뽑기

```bash
while read f; do
  echo "$(git log -1 --format=%cs -- "$f")|$f"
done < /tmp/doclist.txt | sort -r > /tmp/docdated.txt
```

### 3-3단계: 기계적 판정

각 줄에 아래 규칙을 **위에서부터 순서대로** 적용한다. 먼저 걸리는 것으로
확정하고 멈춘다. 규칙에 안 걸리면 `판단보류`.

| 순서 | 조건 | 판정 |
|---|---|---|
| 1 | 경로가 `specs/active/` 로 시작 | `유효-스펙` |
| 2 | 파일명이 `README.md` 이거나 `TRAINORACLE_SPEC_INDEX.md` | `유효-길잡이` |
| 3 | 파일명에 `HANDOFF` 포함 + 수정일이 가장 최근 1건 | `유효-인계` |
| 4 | 파일명에 `HANDOFF` 포함 + 그 외 | `과거-인계` |
| 5 | 파일 안에 `[DRAFT_COMPLETE]` 문자열이 있음 | `완료-기록` |
| 6 | 파일 안에 `PROPOSAL_ONLY` 또는 `SUPERSEDED` 가 있음 | `대체됨` |
| 7 | 경로가 `reports/` 로 시작 | `기록물` |
| 8 | 위 어디에도 안 걸림 | `판단보류` |

문자열 확인은 이렇게 한다.

```bash
grep -l "\[DRAFT_COMPLETE\]" 파일경로
grep -lE "PROPOSAL_ONLY|SUPERSEDED" 파일경로
```

### 3-4단계: 표 만들기

`DOCUMENT_MAP.md` 에 아래 형식으로 쓴다. **수정일 내림차순**으로 정렬한다.

```markdown
# DOCUMENT_MAP.md — 문서 지도

이 파일은 기계적 규칙으로 자동 분류한 목록이다. 문서 내용을 판단한 것이
아니다. `판단보류` 항목은 사람이 정해야 한다.

작성일: (오늘 날짜)
대상: git 추적 문서 중 `.omo/evidence/` 제외

## 먼저 읽을 문서 3개

| 문서 | 용도 |
|---|---|
| README.md | 저장소 전체 안내 |
| (가장 최근 HANDOFF 파일명) | 현재 상태와 다음 작업 |
| TRAINORACLE_SPEC_INDEX.md | 스펙 찾아보기 |

## 판정별 개수

| 판정 | 개수 |
|---|---|
| 유효-스펙 | ? |
| 유효-길잡이 | ? |
| 유효-인계 | ? |
| 과거-인계 | ? |
| 완료-기록 | ? |
| 대체됨 | ? |
| 기록물 | ? |
| 판단보류 | ? |

## 전체 목록

| 수정일 | 판정 | 경로 |
|---|---|---|
| 2026-07-27 | 유효-인계 | CURRENT_IMPLEMENTATION_HANDOFF_2026-07-27.md |
| ... | ... | ... |
```

### 3-5단계: 검증

아래 3개가 모두 맞아야 제출한다.

```bash
# 1) 표의 줄 수 == 목록 줄 수 (하나도 빠뜨리지 않았는가)
grep -c "^| 20" DOCUMENT_MAP.md
wc -l < /tmp/doclist.txt

# 2) 코드를 안 건드렸는가 (반드시 아무것도 안 나와야 함)
git status --short | grep -E "app/|impl/|specs/"

# 3) 새로 만든 파일이 1개뿐인가
git status --short
```

2번에서 뭐라도 나오면 **제출하지 말고 되돌린다**: `git checkout -- 해당파일`

## 4. 제출

```bash
git add DOCUMENT_MAP.md
git commit -m "docs: 문서 지도 추가 — 275개 문서 기계적 분류

기계적 규칙만 적용했고 문서 내용은 판단하지 않았다.
코드·스펙 파일은 수정하지 않았다. 삭제·이동 0건.
판단보류 N건은 사람의 결정이 필요하다."
git push origin main
```

## 5. 하지 말아야 할 것

- 문서를 지우거나 옮기기 — **금지**
- "이건 필요 없어 보인다" 같은 의견 적기 — **금지**
- `판단보류`를 임의로 다른 판정으로 바꾸기 — **금지**
- 문서 내용을 요약해서 표에 넣기 — **금지** (표에는 경로·날짜·판정만)

애매하면 `판단보류`로 두고 넘어간다. 그게 정답이다.

## 6. 완료 기준

- `DOCUMENT_MAP.md` 1개만 추가됐다.
- 표의 줄 수가 목록 줄 수와 같다.
- `git status`에 다른 변경이 없다.
