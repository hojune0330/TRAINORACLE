# 기획 A — 첫 상세 처방 템플릿 활성화

```yaml
plan:
  id: PLAN-A-FIRST-TEMPLATE
  목적: 훈련계획에 처음으로 구체적 숫자를 넣는다
  범위: 템플릿 2개 (LT-SEED-01, V2-SEED-02)
  결정필요: 오너 승인 1건
  status: 설계안_오너승인대기
```

## 1. 한 줄 요약

지금 훈련계획은 "40~50분, 체감강도 3~4"까지만 말한다. 이 기획은 여기에
**"20분 역치 지속주"** 와 **"3분 인터벌 5회, 사이 조깅 2분"** 두 가지
구체적 세션을 처음으로 넣는다.

## 2. 조사에서 나온 사실 (기존 계획과 다른 점)

기존 문서들은 "비스프린트 종목 1개를 골라 템플릿 패킷을 만든다"고 되어
있었다. 실제 카탈로그 30개를 전수 조사한 결과, **그 전제가 사실과 달랐다.**

| 조사 항목 | 결과 |
|---|---|
| 전체 템플릿 | 30개 |
| 숫자(세트·반복·회복)가 이미 채워진 것 | **22개** |
| 재료가 비어 있어 손댈 수 없는 것 | 8개 |
| 출처가 `DIRECT_SOURCE_EXAMPLE`(직접 인용) | 소수 |
| 출처가 `SOURCE_ADAPTED`(가공됨) | 다수 |

즉 **막힌 곳은 "숫자를 만드는 일"이 아니라 "숫자를 써도 된다고 승인하는
일"이다.** 22개는 이미 숫자가 있다. 잠겨 있을 뿐이다.

따라서 이 기획은 새 숫자를 만들지 않는다. **이미 있는 것 중 가장 안전한
2개를 여는 절차**를 설계한다.

## 3. 왜 2개인가, 왜 하필 이 2개인가

### 후보 비교

| 템플릿 | 내용 | 출처 | 페이스 기준 | 판정 |
|---|---|---|---|---|
| **LT-SEED-01** | 20분 역치 지속주 | `DIRECT_SOURCE_EXAMPLE` | `RPE_ONLY`, 코치참조 | ✅ **선정** |
| **V2-SEED-02** | 3분 인터벌 5회 · 회복 2분 | `DIRECT_SOURCE_EXAMPLE` | `RPE_ONLY`, 코치참조 | ✅ **선정** |
| V2-SEED-05 | 1000m 5회 @5K 페이스 | `SOURCE_ADAPTED` | 개인기록 기반 | ❌ 제외 |
| LT-SEED-03 | 1600m 크루즈 4회 | `DIRECT_SOURCE_EXAMPLE` | `RPE_ONLY` | 🔶 다음 차례 |
| AP/GL 계열 | 스프린트·젖산 | 접근제한 출처 | — | ❌ 제외 |

### 선정 근거

**1) 출처를 직접 인용했다.** 두 템플릿 모두 `DIRECT_SOURCE_EXAMPLE`이다.
누군가 가공한 게 아니라 원문 예시 그대로다. 가공된 것(`SOURCE_ADAPTED`)은
"누가 왜 그렇게 바꿨나"를 먼저 검증해야 한다.

**2) 개인 페이스를 계산하지 않는다.** 둘 다 `paceAnchorKinds`가
`[RPE_ONLY, COACH_REFERENCE]`다. 선수 기록으로 페이스를 역산하지 않는다.
이게 결정적이다 — 페이스 계산이 들어가면 기록의 최신성·같은종목 여부 같은
검증이 줄줄이 따라온다.

**3) V2-SEED-05를 제외한 이유가 여기 있다.** 매력적인 후보였지만
`paceAnchorKinds`에 `GOAL`(목표기록)이 들어 있다. 그런데 코드는 이미
`GOAL_ANCHOR_FORBIDDEN`으로 막고 있다(`impl/src/prescription/anchor.ts:43`).
**문서와 코드가 어긋나 있다.** 이걸 먼저 정리하지 않고 열면 모순이 사용자에게
드러난다. 다음 차례로 미룬다.

**4) 스프린트 계열 전면 제외.** 출처가 `SOURCE_URL_RECORDED_ACCESS_RESTRICTED`
(URL만 있고 내용 확인 불가)이고, 코드도 `SPRINT_RACE_PACE_FORBIDDEN`으로
막는다. 근거 없이 열 수 없다.

**5) 2개인 이유.** 1개면 "역치만 되고 최대산소는 왜 안 되나"가 바로 나온다.
3개 이상이면 검토량이 늘고 실패 시 원인 추적이 어렵다. 서로 다른 성격
(지속주 1 + 인터벌 1) 2개가 최소 단위다.

## 4. 사용자에게 보이는 변화

### 지금

```
DAY 3 · 오전
지구력 향상 · 40~50분 · 체감강도 3~4
```

### 이후

```
DAY 3 · 오전
역치 향상 · 20분 지속주 · 체감강도 5~6

  [상세 보기]
  20분간 일정한 속도로 이어서 달립니다.
  "대화는 짧게만 가능한" 정도의 강도입니다.

  ⓘ 이 세션은 코치용 훈련 예시를 옮긴 것입니다.
     회원님 기록으로 계산한 페이스가 아닙니다.
     몸 상태가 좋지 않으면 중단하세요.
```

**핵심: 페이스(1km당 몇 분)는 여전히 안 준다.** 세션 구조와 시간만 준다.
페이스는 개인 기록 검증이 끝난 뒤의 다음 단계다.

## 5. 안전 장치 — 4겹

| 겹 | 무엇 | 왜 |
|---|---|---|
| 1 | 몸 상태 이상(D9) 시 계획 자체가 안 만들어짐 | **기존 유지** |
| 2 | 미성년자에게는 상세 세션 미표시 | `minorAllowed: false` |
| 3 | 경력 밴드가 초급이면 미표시 | 초심자에게 역치·인터벌은 이름부터 위험 |
| 4 | 화면에 "코치용 예시" 명시 + 중단 조건 표시 | 개인 처방으로 오해 방지 |

4겹 중 **하나라도 실패하면 기존 RPE 표시로 되돌아간다**(fail-closed).
숫자를 못 보여주는 건 불편일 뿐이지만, 잘못 보여주면 부상이다.

## 6. 작업 순서

작업자별로 나눴다. 각 단계는 앞 단계가 끝나야 시작한다.

### 1단계 — 카탈로그 승인 표시 (문서, 저비용 작업자)

`specs/reconstruct/ENERGY_SYSTEM_SESSION_TEMPLATE_CATALOG.md`에서
**LT-SEED-01과 V2-SEED-02 두 항목만** 아래로 바꾼다.

```yaml
lifecycleStatus: DRAFT        →  ACTIVE
eligibilityStatus: REVIEW_REQUIRED  →  ELIGIBLE
allowedEventGroups: []        →  [MIDDLE_DISTANCE, LONG_DISTANCE, ROAD_RUNNING]
allowedExperienceBands: []    →  [INTERMEDIATE, ADVANCED]
```

나머지 28개는 **손대지 않는다.**

검증: `node specs/test-packages/validate-detailed-prescription-catalog.mjs`
→ `28/30 inert, 2 active`가 나와야 한다. 검증기 수정도 이 단계에 포함된다.

### 2단계 — 엔진 연결 (코드, 숙련 작업자)

`impl/src/prescription/runtime.ts`의 `preparePrescriptionRuntime`에
템플릿 ID·버전을 받는 인자를 추가한다. 현재는 호출자가 `ACTIVE`를 그냥
넘기면 믿는 구조라, 이게 조사에서 나온 `PERS-ALIGN-004` 문제다.

계약 테스트로 고정할 것:
- `ACTIVE` 아닌 템플릿 요청 → 거부
- 미성년 + `minorAllowed: false` → 거부
- 초급 경력 → 거부
- 몸 상태 이상 → 거부
- 위 거부 시 **RPE 표시로 정상 폴백**

### 3단계 — 화면 (코드, 숙련 작업자)

`PlanSessionDetails.tsx`에 상세 표시를 넣는다. 4겹 중 하나라도 실패하면
기존 화면 그대로.

### 4단계 — 검증 (숙련 작업자)

- 기존 테스트 449개 전부 통과 (impl 98 + app 308 + e2e 143)
- 신규 계약 테스트 통과
- 모바일 375px 가로넘침 없음
- 라이브 배포 후 실제 확인

## 7. 오너 결정이 필요한 것 — 1건

> **"LT-SEED-01(20분 역치 지속주)과 V2-SEED-02(3분 인터벌 5회)를
> 중급·상급 중장거리 선수에게 열어도 되는가?"**

승인해 주시면 1단계부터 진행합니다.

### 함께 확인해 주실 것

1. **경력 밴드** — 중급부터가 맞습니까, 상급만 열까요?
2. **미성년** — 계속 막습니까? (권고: 유지)
3. **"코치용 예시입니다" 문구** — 이 표현이 맞습니까?

2·3번은 답 없으시면 위 권고대로 갑니다. **1번만 답해주셔도 시작 가능합니다.**

## 8. 이 기획이 하지 않는 것

- 나머지 28개 템플릿을 열지 않는다.
- 페이스(1km당 몇 분)를 계산하지 않는다.
- 스프린트(100·200·400m)를 열지 않는다.
- 미성년 정책을 바꾸지 않는다.
- 몸 상태(D9) 규칙을 완화하지 않는다.
- 날짜 달력이나 하루 2회 규칙을 건드리지 않는다.

## 9. 되돌리기

1단계만 되돌리면 전부 원복된다. 카탈로그 2개 항목을 `DRAFT`로 되돌리면
엔진이 자동으로 거부하고 화면은 기존 RPE 표시로 돌아간다. 코드를 되돌릴
필요가 없다. 이게 4겹 구조를 택한 이유다.
