# 기획 A — 개인 목표 페이스 처방 활성화

```yaml
plan:
  id: PLAN-A-PERSONAL-PACE
  목적: 선수 기록을 대입해 개인 목표 페이스를 계산해 처방한다
  전제: 이 제품의 최종 목표는 코칭이다 (PRODUCT_NORTH_STAR.md 참조)
  status: 설계안_오너승인대기
  대체된_이전안: RPE_ONLY 2개 활성화 (오너 반려)
```

> **이 문서를 읽기 전에 [`PRODUCT_NORTH_STAR.md`](./PRODUCT_NORTH_STAR.md)를
> 먼저 읽으십시오.** 이 기획은 그 최종 목표의 첫 단계입니다.

## 0. 이전 안을 왜 폐기했는가 (같은 실수 방지)

직전 버전에서 나는 **`RPE_ONLY` 템플릿 2개만 여는 안**을 올렸다. 개인 페이스
계산을 "다음 단계"로 미뤘다. **오너가 반려했고, 반려가 옳다.**

내가 틀린 지점 셋:

1. **`GOAL_ANCHOR_FORBIDDEN`을 "목표기록으로 페이스를 만들면 안 된다"로
   읽었다.** 실제 코드(`anchor.ts:42-43`)를 다시 보니 그런 뜻이 아니다.
   `GOAL`은 **희망사항이므로 "현재 능력"으로 취급하면 안 된다**는 뜻이다.
   목표를 처방에 쓰는 것 자체를 막는 게 아니라, **현재 실력과 목표를 구분
   없이 섞는 것**을 막는 규칙이다.
2. **`calculateSameEventRacePace`가 이미 구현돼 있는 걸 놓쳤다.**
   `runtime.ts:177`에 개인 기록 기반 페이스 계산이 이미 있다. 없는 걸
   만드는 게 아니라 **연결하는 일**이었다.
3. **가장 큰 오류 — 제품의 최종 목표를 축소해서 이해했다.** 이 제품은
   "안전한 계획 후보 보여주기"가 아니라 **코칭**이다. 개인 페이스는 미룰
   수 있는 부가기능이 아니라 **본체**다.

## 1. 이 기획이 하는 일

선수의 **경기 목표 · 최고기록(PB) · 시즌기록(SB) · 최근 경기 결과**를 받아
**개인 목표 페이스를 계산해서** 훈련 처방에 넣는다.

```
지금:  "3분 인터벌 5회 · 체감강도 5~6"
이후:  "1000m 5회 · 목표 3분 42초 · 사이 조깅 2분 30초"
```

> **조사 결과 한 줄 요약:** 계산 엔진은 이미 있다. 그런데 **카탈로그 30개
> 중 엔진이 읽을 수 있는 표기가 0개**다(§2-1, 실제 실행으로 확인).
> 그래서 이 기획의 진짜 첫 과제는 "템플릿 활성화"가 아니라
> **선수 기록 입력(1단계)** 과 **표기 격차 해소(결정 1)** 다.

## 2. 이미 있는 것 / 없는 것 (실측 조사 결과)

**중요: 아래는 코드를 실제로 실행해서 확인한 결과다.** 표를 눈으로 읽고
추정한 것이 아니다.

| 구성요소 | 위치 | 상태 |
|---|---|---|
| 페이스 계산 공식 | `runtime.ts:177` `calculateSameEventRacePace` | ✅ 구현됨 |
| 앵커 종류 7가지 | `anchor.ts:27-56` | ✅ 구현됨 |
| 기록 최신성 판정 | `types.ts:31` `CURRENT`/`STALE`/`UNKNOWN` | ✅ 구현됨 |
| 같은 종목 검증 | `anchor.ts:70` `CROSS_EVENT_MODEL_REQUIRED` | ✅ 구현됨 |
| 표기 파서 | `notation.ts:6` | ✅ 구현됨 (단, §2-1 참조) |
| 런타임 배선 | `runtime.ts:198` `preparePrescriptionRuntime` | ✅ 구현됨 |
| **파서가 읽을 수 있는 카탈로그 템플릿** | 30개 중 **0개** | ❌ **없음** |
| **앱 화면 연결** | — | ❌ 없음 |
| **선수 기록 입력** | — | ❌ 없음 |
| **템플릿 활성화** | 30개 전부 `DRAFT` | ❌ 잠김 |

### 2-1. 가장 중요한 발견 — 카탈로그와 파서는 다른 언어다

카탈로그 30개의 `notationPattern`을 **전부 파서에 넣어 봤다. 0개 통과했다.**

파서가 받는 형식은 **한 가지뿐**이다 (`notation.ts:6`):

```
[세트수×(] 반복수 × 거리m [)] @ 종목거리m RP [· r회복초″] [· R세트회복분′]
예: 5×1000m @5000m RP · r150″
    2×(10×400m) @5000m RP · r60″ · R3′
```

카탈로그는 **코치 표기법**으로 적혀 있다.

| 카탈로그 표기 | 파서가 거부하는 이유 |
|---|---|
| `20′ @T` | 시간 반복 + `@T`(역치)는 종목거리가 아님 |
| `3×1600m @T · r1~2′` | `@T`에 거리가 없음, `1~2′` 범위 회복 |
| `5×3′ @I · r2′ jog` | `@I`(인터벌)는 종목거리가 아님 |
| `5×1000m @5K RP · r2′30″` | `5K`를 `5000m`로 안 읽음, `2′30″` 복합시간 |
| `3~4×500m @GOAL 1500m RP` | `3~4` 범위 반복수, `GOAL` 접두어 |
| `3×(800m+200m+200m)` | 반복거리가 섞여 있음 |
| `30~45′ @E` | 강도구역만 있고 처방 구조가 없음 |

**결론: "22개는 숫자가 채워져 있다"는 나의 이전 판단은 틀렸다.** 숫자가
채워진 것과 코드가 읽을 수 있는 것은 다르다.

### 2-2. 그럼 몇 개나 살릴 수 있는가 (실측)

실제로 재작성해서 파서에 넣어 봤다.

| 템플릿 | 원본 | 재작성 | 결과 |
|---|---|---|---|
| `V2-SEED-05` | `5×1000m @5K RP · r2′30″` | `5×1000m @5000m RP · r150″` | ✅ 통과 |
| `GL-SEED-01` | `3~4×500m @GOAL 1500m RP · r2~3′` | `3×500m @1500m RP · r150″` | ✅ 통과 |
| `LT-SEED-02/03` | `3×1600m @T · r1~2′` | 종목거리 불명 → 불가 | ❌ |
| `GL-SEED-02` | `3×(800m+200m+200m)` | 반복거리 혼합 → 불가 | ❌ |
| 나머지 26개 | 시간형·구역형·범위형 | — | ❌ |

**즉 지금 구조로 페이스가 나올 수 있는 템플릿은 사실상 2개다.**
그나마 둘 다 범위(`3~4`)나 단위(`5K`)를 사람이 하나로 확정해 줘야 한다.
이건 **훈련 도메인 결정**이므로 오너 승인이 필요하다.

## 3. 공식 — 무엇이 되고 무엇이 안 되는가

### 3-1. 같은 종목이면 계산된다 (이미 구현)

```
목표 반복 시간 = 기준기록초 × (반복거리 ÷ 기준거리)
```

예: 5000m 18분 30초(1110초) 선수의 1000m 반복
→ `1110 × (1000 ÷ 5000) = 222초 = 3분 42초`

### 3-2. 다른 종목은 아직 못 한다 — 이게 진짜 과제다

오너 말씀대로 **"목표·PB·SB를 대입하면 맞는 공식"**을 만들려면 종목 간
변환이 필요하다. 5000m 기록으로 400m 반복 페이스를 내려면 환산 모델이
있어야 한다. 코드는 지금 이걸 `CROSS_EVENT_MODEL_REQUIRED`로 **거부**한다.

**이건 결함이 아니라 미구현이다.** 환산 모델(리간드·VDOT·표준환산표 중
어느 것을 쓸지)은 훈련 도메인 결정이므로 **오너 승인이 필요하다.**

### 3-3. 목표기록(GOAL)을 어떻게 쓸 것인가 — 핵심 설계

`GOAL`은 현재 능력이 아니다. 그렇다고 버릴 것도 아니다. **둘을 분리해서
동시에 보여주는 것**이 답이다.

| 기록 종류 | 용도 | 표시 |
|---|---|---|
| `RECENT_RESULT` / `PB` / `SB` | **오늘 훈련 페이스 산출** | "오늘 목표: 3분 42초" |
| `GOAL` | **목표 달성 시 페이스 참고** | "목표 달성 시: 3분 30초" |

이렇게 하면 `GOAL_ANCHOR_FORBIDDEN`의 취지(희망을 현실로 착각 금지)를
지키면서, 오너가 원하는 "목표를 대입한 페이스"도 제공한다.

### 3-4. 기록이 오래됐을 때 — **제안이며 현재 동작이 아님**

**현재 코드**: `freshnessState`가 `CURRENT`가 아니면 계산을 **거부**한다.

```ts
// anchor.ts:23-24
if (anchor.freshnessState !== "CURRENT") return "ANCHOR_NOT_CURRENT"
```

즉 `STALE`이든 `UNKNOWN`이든 페이스가 안 나오고 체감강도로 폴백한다.
이건 안전한 기본값이다.

**제안(결정 6)**: `STALE`은 계산하되 반드시 **명시**한다.

```
오늘 목표: 3분 42초
ⓘ 8개월 전 기록 기준입니다. 최근 기록을 넣으면 더 정확해집니다.
```

`UNKNOWN`은 현행대로 거부한다. 언제 세운 기록인지도 모르는 값은
어떤 안내를 붙여도 근거가 없다.

**침묵하고 계산하는 것이 가장 위험하다.** 오래된 기록으로 나온 페이스를
현재 실력인 것처럼 보여주면 선수가 무리한다. 그래서 제안을 채택하더라도
안내 문구는 **선택이 아니라 필수**다.

## 4. 안전 규칙 — 완화하지 않는다

| 규칙 | 유지 여부 |
|---|---|
| 몸 상태 이상(D9) 시 처방 차단 | **유지** |
| 60m 미만 스프린트 페이스 환산 금지 | **유지** |
| 다른 종목 환산은 모델 승인 전까지 거부 | **유지** |
| 미성년자 상세 처방 | **오너 결정 필요** |
| 일지 원문을 처방 근거로 사용 금지 | **유지** |

마지막 항목이 특히 중요하다. 일지에 적힌 "무릎이 좀 아팠다" 같은 문장은
**위험 신호로 감지해서 사람 검토를 부르는 용도**이지, 처방 숫자를 바꾸는
입력이 아니다. 이 경계는 `NOTE_SAFETY_SIGNAL_AND_REVIEW_STATE_CONTRACT.md`에
있고 절대 넘지 않는다.

## 5. 작업 순서

§2-1의 발견 때문에 순서가 바뀐다. **템플릿을 여는 게 1단계가 아니다.**
파서가 못 읽는 템플릿은 열어도 페이스가 안 나온다.

### 1단계 — 선수 기록 입력 화면 (앱) · 오너 승인 불필요

경기 목표 · PB · SB · 최근 결과를 입력받는다. 각 기록마다 **종목 거리와
달성 날짜**를 함께 받는다(최신성 판정에 필수).

이 단계는 안전 규칙을 건드리지 않으므로 **지금 바로 시작할 수 있다.**
기록이 없으면 어떤 페이스도 나올 수 없으므로 이게 진짜 첫 병목이다.

### 2단계 — 카탈로그·파서 격차 해소 · **오너 결정 필요 (결정 1)**

두 갈래 중 하나를 골라야 한다.

- **2-A안 (파서를 넓힌다)**: `5K`→`5000m`, `2′30″`→`150초`, `@T`/`@I`를
  강도구역으로 인식. 카탈로그를 안 고치고 코드가 코치 표기법을 배운다.
  대신 `@T`처럼 종목거리가 없는 표기는 **여전히 페이스를 못 낸다.**
  강도구역은 개인 기록이 아니라 별도 모델이 필요하기 때문이다.
- **2-B안 (카탈로그에 기계용 표기를 더한다)**: `notationPattern`은 그대로
  두고, 사람이 확정한 `machineNotation` 필드를 옆에 추가한다.
  예: `3~4×500m @GOAL 1500m RP` → `machineNotation: "3×500m @1500m RP · r150″"`
  **원본 표기를 지우지 않는다.**

권고: **2-B안.** 원본 코치 표기를 보존하고, 범위를 하나로 확정하는 판단이
문서에 명시적으로 남는다. 2-A안은 파서가 범위(`3~4`)를 만나면 어느 쪽을
고를지 코드가 임의로 정하게 되는데, 그건 근거 없는 훈련 처방이다.

### 3단계 — 템플릿 활성화 · **오너 승인 필요**

2단계로 기계 표기가 생긴 템플릿만 `lifecycleStatus: ACTIVE` +
`eligibilityStatus: ELIGIBLE`로 바꾸고 `allowedEventGroups`,
`allowedExperienceBands`를 채운다. 지금 후보는 `V2-SEED-05`,
`GL-SEED-01` **2개뿐**이다.

### 4단계 — 페이스 계산 연결 (엔진)

`preparePrescriptionRuntime`에 선수 기록을 넘긴다. 함수는 이미 있으므로
배선과 계약 테스트가 주 작업이다.

### 5단계 — 화면 표시

목표 페이스 + 기준 기록 + 최신성 안내를 함께 보여준다. 계산 실패 시
체감강도 표시로 폴백한다.

### 6단계 — 종목 간 환산 모델 (오너 승인 후 별건)

## 6. 오너 결정이 필요한 것

### 결정 1 — 카탈로그·파서 격차를 어떻게 메울 것인가 ⭐ **가장 시급**

§5 2단계의 **2-A안(파서 확장) / 2-B안(기계 표기 추가)** 중 선택.
권고는 2-B안. **이 결정 없이는 어떤 템플릿도 페이스를 낼 수 없다.**

### 결정 2 — 종목 간 환산 모델을 쓸 것인가

5000m 기록으로 400m 페이스를 낼 것인가?

- **A안**: 같은 종목만 계산. 5000m 기록 → 1000m·1600m 반복만.
  **안전하지만 종목 수만큼 기록을 요구한다.**
- **B안**: 환산 모델 도입. 기록 하나로 모든 거리 대응.
  **편리하지만 모델 선택 근거가 필요하다.**

권고: **A안으로 시작**해서 실제로 부족한 게 확인되면 B안. 다만 오너께서
"모두 맞는 공식"을 말씀하셨으므로, B안을 원하시면 **어느 환산표를 쓸지**
알려주십시오.

> 참고(실측): 카탈로그 30개는 표기 방식이 이렇게 갈린다.
>
> | 표기 유형 | 개수 | A안으로 페이스가 나오는가 |
> |---|---|---|
> | `@T`/`@I`/`@E` 강도구역 | 13 | ❌ 종목거리가 없어 불가 |
> | `@` 자체가 없음(스프린트·회복 등) | 13 | ❌ |
> | `RP`(경기 페이스) 포함 | 2 | ⭕ 재작성하면 가능 |
> | 기타(`@95% vVO2max`, `@90~100%`) | 2 | ❌ |
>
> 강도구역 13개는 A안으로는 영원히 페이스가 안 나온다. 강도구역→페이스
> 변환은 사실상 결정 2의 B안(환산 모델)과 같은 문제다.

### 결정 3 — 목표기록(GOAL) 표시 방식

§3-3의 "오늘 목표 / 목표 달성 시" 2단 표시가 맞습니까?

> 참고: 카탈로그에서 `GOAL`을 앵커로 허용하는 템플릿은 `GL-SEED-01`,
> `V2-SEED-05` 2개다. `GL-SEED-01`에는 이미
> `STOP_IF_GOAL_MISREPRESENTED_AS_CURRENT_CAPABILITY` 정지 조건이 붙어 있어
> §3-3의 2단 분리 설계와 방향이 일치한다.

### 결정 4 — 범위 표기를 누가 확정하는가

`3~4×500m`의 `3~4`, `r2~3′`의 `2~3`을 하나로 정해야 파서가 읽는다.
오너가 정할지, 코치 검토 항목으로 남길지 결정이 필요합니다.

### 결정 5 — 미성년 선수

현재 카탈로그는 전부 `minorAllowed: false`입니다. 개인 페이스 처방을
미성년에게도 열 것인지, 코치 연결 시에만 열 것인지 결정이 필요합니다.

### 결정 6 — 기록이 오래됐을 때(STALE)

**주의: 현재 코드는 `STALE`이면 계산 자체를 거부한다** (`anchor.ts:23-24`
→ `ANCHOR_NOT_CURRENT`). §3-4에 쓴 "계산하되 안내한다"는 **현재 코드
동작이 아니라 제안**입니다. 채택하시면 코드 변경이 필요합니다.

계산하고 안내(제안) / 현행 유지(거부) / 몇 개월까지 유효로 볼지.

## 7. 이 기획이 하지 않는 것

- 일지 원문을 처방 숫자에 반영하지 않는다.
- 몸 상태(D9) 규칙을 완화하지 않는다.
- 60m 미만 스프린트 페이스를 만들지 않는다.
- 오너 승인 없이 종목 간 환산을 하지 않는다.
- 30개 템플릿을 한꺼번에 열지 않는다.

## 8. 되돌리기

카탈로그의 `lifecycleStatus`를 `DRAFT`로 되돌리면 엔진이
`TEMPLATE_NOT_ACTIVE`로 거부하고 화면은 체감강도 표시로 복귀한다.
코드 롤백이 필요 없다.
