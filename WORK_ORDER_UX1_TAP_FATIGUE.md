# 작업지시서 UX1 — 손가락 피로 줄이기 (스크롤·글자량·누른 느낌)

**작성일:** 2026-07-27
**받는 사람:** 하위 에이전트
**선행조건:** 없음 — 즉시 착수 가능 (P1~P5와 겹치는 파일 없음)
**성격:** 화면 작업. 계산·처방 로직은 건드리지 않는다.

---

## 0. 오너가 말한 것

> "손이 많이 눌리기 때문에 너무 아래로 스크롤 하거나 계속 한 화면에
> 텍스트가 너무 많다거나 하면 피로감이 커지거든. 클릭했을 때 아주 조금의
> 이펙트만 있다면 넘어가는 기분과 해소감을 느끼며 더 누르고 싶은 욕구도
> 생겨."

세 가지 요구다.

| # | 요구 | 이 지시서에서 부르는 이름 |
|---|---|---|
| 1 | 아래로 너무 많이 스크롤하지 않게 | **스크롤 깊이** |
| 2 | 한 화면에 글자가 너무 많지 않게 | **글자량** |
| 3 | 누르면 아주 조금 반응해서 넘어가는 느낌·해소감 | **누른 느낌** |

---

## 0-A. 이 지시서 리뷰 결과 (2026-07-27, 착수 후 발견)

착수해서 실제로 재 보니 이 지시서에 통과할 수 없는 조건과 서로 부딪히는
조건이 있었다. 고친 내용을 여기 남긴다. **아래 표가 원본보다 우선한다.**

| # | 무게 | 무엇이 문제인가 | 근거 | 어떻게 고쳤나 |
|---|---|---|---|---|
| D1 | 치명적 | **재는 자와 검사하는 자가 다르다** | §1-1 은 390x844(스크롤영역 787px)에서 재고 §5-3 은 mobile-chromium(393x727, 스크롤영역 670px)에서 검사한다. 같은 1581px 가 2.01 화면과 2.36 화면으로 갈린다. touch-narrow(511px)는 3.42 화면이다. | 비율 상한을 폐기하고 px 상한으로 바꾼다. 화면 수는 참고로만 적는다. |
| D2 | 치명적 | **목표가 산술적으로 불가능** | §5-3 '다 채운 뒤 1.3 화면' = 1023px. 지금 1693px 이라 670px 를 줄여야 한다. 그런데 지시서가 허용한 접기 총량은 객관기록 349 + (강도시스템146+RPE160+예상강도131-44x3)=305 = 654px 이다. 16px 부족하다. | 달성 가능한 목표로 바꾼다. §2-1 을 다 해도 1039px 이 최선이다. |
| D3 | 치명적 | **§2-1 과 §4-3(나) 가 서로를 부순다** | §2-1 은 '사용자가 답을 넣으면 그 즉시 접는다'. §4-3(나)는 '사람이 펼친 구획은 답이 들어와도 접지 않는다'. RPE 를 처음 고르는 사람은 아직 아무것도 펼친 적이 없어서 (나)의 보호를 못 받는다. 고른 순간 버튼 10개가 사라져 자기가 뭘 눌렀는지 확인할 수 없다. | 즉시 자동 접기를 폐기한다. 2026-07-28 오너 결정으로 **다음 구획을 건드릴 때만** 부분 복구한다 (§0-D). |
| D4 | 중대 | **통과할 수 없는 grep 을 요구한다** | §5-5 는 log-entry 에서 '높음|낮음|좋음|나쁨' 0건을 요구하지만 EveningCheckin.tsx:14-15 의 MOOD_LABELS/SLEEP_QUALITY_LABELS 와 RaceSelfChecks.tsx:5 에 이미 3건 있다. 사용자가 고르는 선택지 이름이고 앱의 판정이 아니다. | 검사 범위를 'FormSec 이 summary 로 그리는 문자열'로 좁힌다. |
| D5 | 중대 | **서명 이름이 실제와 다르다** | §2-1 은 done/summary 를 못 박았고 §4-2 검산표 6줄도 done 기준이다. done 은 '답했다'는 판정을 FormSec 에 넣는 이름인데, 판정은 각 화면이 한다며 §4-3 이 스스로 부인한다. | collapsible/defaultOpen/autoOpenWhen 으로 바꾼다. 접기 여부와 답 여부를 분리한다. |
| D6 | 보통 | **§1-2 합계가 §1-1 과 240px 어긋난다** | 훈련 후 1581 vs 구획합계 1341, 하루 마무리 1685 vs 1445. 둘 다 정확히 240px 차이 — TopBar+IndexCard+StickyBar 여백이다. 어디까지 재고 있는지 안 적혀 있어서 다음 사람이 같은 자로 못 잰다. | 무엇을 포함해 재는지 명시한다. |
| D7 | 보통 | **줄 수 45줄에 재는 방법이 없다** | §3-2 '74줄 -> 45줄'. 줄이 무엇인지(텍스트 노드? 렌더된 line box?) 정의가 없어 검사로 못 만든다. 게다가 접기로 줄이면 스크롤 깊이와 같은 것을 두 번 재는 셈이다. | 줄 수 목표를 폐기하고 스크롤 깊이 하나로 통합한다. |

### 0-B. 고쳐진 목표 — px 로 잰다

화면 수(비율)를 상한으로 쓰지 않는다. 분모가 기기마다 달라서 같은 화면이
2.01 도 되고 3.42 도 된다. **줄여야 하는 것은 px 다.**

재는 곳: `main.app-scroll-region` 의 `scrollHeight`. TopBar·IndexCard·
StickyBar 여백을 모두 포함한 값이다 (§1-1 과 같은 자, §1-2 와는 240px 차이).

| 화면 | 손대기 전 | 상한 | 근거 |
|---|---|---|---|
| 훈련 후 (빈 상태) | 1581px | **1300px 이하** | 객관 기록 접기 −349px |
| 하루 마무리 (빈 상태) | 1685px | **1300px 이하** | 몸 그림 접기 −476px |
| 경기 일지 | 956px | **1000px 이하** | 손대지 않는다. 회귀만 막는다 |

`touch-narrow`(320px)는 글자가 더 접혀서 같은 화면이 더 길다. 별도 상한을 둔다.

| 화면 | 손대기 전 | 상한 |
|---|---|---|
| 훈련 후 (빈 상태) | 1747px | **1350px 이하** |
| 하루 마무리 (빈 상태) | 1700px | **1350px 이하** |
| 경기 일지 | 970px | **1000px 이하** |

**"다 채운 뒤" 상한은 폐기한다.** §5-3 이 요구한 1023px 는 이 지시서가
허용한 접기를 전부 해도 1039px 가 최선이라 산술적으로 닿을 수 없다.
채운 뒤에는 값이 늘어나므로 길어지는 게 정상이고, 빈 상태를 줄이는 것이
오너가 말한 "처음 열었을 때 아득한 느낌"을 없앤다.

### 0-C. 폐기한 요구

| 원본 | 왜 폐기하나 |
|---|---|
| §2-1 답하면 자동으로 접기 | §4-3(나)와 충돌한다 (D3). RPE 를 고른 순간 버튼 10개가 사라지면 자기가 뭘 눌렀는지 확인할 수 없다. → **2026-07-28 오너 결정으로 부분 복구됨. 아래 §0-D 참조** |
| §3-2 한 화면 45줄 | 재는 방법이 정의돼 있지 않다 (D7). 접기로 줄이면 스크롤 깊이와 같은 것을 두 번 재는 것이다 |
| §5-3 화면 수 비율 상한 | 분모가 기기마다 다르다 (D1). px 로 바꿨다 |
| §5-3 다 채운 뒤 1.3 / 1.4 | 산술적으로 불가능하다 (D2) |
| §5-5 log-entry 전체 판정어 0건 | 기존 코드가 이미 3건 위반한다 (D4). 선택지 이름은 앱의 판정이 아니다 |
| §2-1 `done` / §4-2 검산표의 `done` 열 | 판정을 FormSec 에 넣는 이름이다 (D5). `collapsible`·`defaultOpen`·`autoOpenWhen` 으로 바꿨다 |

**폐기했다고 안 한 게 아니다.** 객관 기록 접기와 몸 그림 접기는 그대로 했고,
줄어든 px 은 위 표에 실측으로 적혀 있다.

### 0-D. 자동 접기 부분 복구 — 오너 결정 2026-07-28

D3 로 폐기했던 자동 접기를, 오너에게 **"언제 접으면 자연스러운가"** 를 물어
받은 답으로 되살렸다.

> **"건드릴때."**

즉 **답을 넣은 순간이 아니라, 다음 구획을 건드린 순간** 접는다.
D3 이 지적한 해악(방금 누른 값을 확인할 수 없음)은 그대로 피한다.

| 무엇 | 어떻게 |
|---|---|
| 판정 기준 | **값이 아니라 순서.** `useSectionTouchOrder` (`shared.tsx`) 가 구획을 건드린 순서를 기록한다 |
| 왜 값이 아닌가 | 값으로 짜면, 예상 강도를 먼저 채운 사람이 RPE 를 누르는 **그 순간** RPE 가 접힌다 = D3 그 자체. 실제 시험 하나가 정확히 그 순서다 (`e2e/intensity-assessment.spec.ts:16-17`) |
| 보호 장치 | 사람이 손으로 펼친 구획은 코드가 접지 않는다 (§4-3(나) 유지) · 자동 접기는 구획마다 한 번 · 보여줄 값(`summary`)이 없으면 접지 않는다 |
| 잠금 | `FormSec.contract.test.tsx` +7 (19개) · `AutoCollapse.contract.test.tsx` 신규 7개 |

**실측 (mobile-chromium / touch-narrow, 스크롤영역 670 / 511px):**

| 상태 | 전 | 후 |
|---|---|---|
| 빈 화면 | 1250 / 1266 | **1268 / 1283** — 접기 버튼 44px 때문에 **늘었다** |
| RPE 채운 뒤 | 1581 / 1747 | **1152 / 1167** — **−429 / −580px** |

🔴 **빈 화면이 18px 늘어난 것을 숨기지 않고 적어 둔다.** 자동 접기는
**답이 있을 때만** 이득이다. 그래서 상한을 빈 상태(`LIMIT`)와
채운 상태(`FILLED_LIMIT` 1200 / 1220)로 **나눠서** 잠갔다.

### 0-E. 현재 접기 계약의 우선순위

이 문서의 아래쪽 초안 표 중 `done` 이나 "답을 넣는 즉시 접기"를 말하는
부분은 0-D 이전의 기록이다. 구현·리뷰 때는 다음 표가 우선한다.

| 항목 | 현재 계약 |
|---|---|
| 기존 화면 보호 | `collapsible` 이 없으면 `FormSec` 은 항상 펼쳐진다 |
| 처음 상태 | `defaultOpen` 또는 `autoOpenWhen` 으로만 펼친다 |
| 자동 접기 | 값이 있고 다음 구획을 건드렸을 때만 한 번 접는다 |
| 금지 | 값을 넣는 순간 접기, 시간·횟수 기반 접기, summary 없는 구획 접기 |
| 재열기 | 사용자가 다시 펼치면 코드가 자동으로 다시 접지 않는다 |
| 증거 | `FormSec.contract.test.tsx` 와 `AutoCollapse.contract.test.tsx` |

---

## 1. 먼저 알아야 할 것 — 이미 실측했다

**추측으로 시작하지 말 것.** 2026-07-27 에 실제 브라우저(390×844,
`mobile-chromium` 과 같은 크기)에서 재 봤다. 아래 숫자가 출발점이다.

### 1-1. 스크롤 깊이 실측

스크롤은 `document` 가 아니라 `<main class="app-scroll-region">` 에서 일어난다.
보이는 높이는 `787px` 다 (탭바 제외).

| 화면 | 스크롤 높이 | 화면 수 | 판정 |
|---|---|---|---|
| 첫 화면 (빈 홈) | 844 | 1.07 | 괜찮다 |
| 일지 종류 고르기 | — | 1.00 | 괜찮다 |
| **훈련 후 일지** | **1581** | **2.01** | 🔴 손댈 곳 |
| **하루 마무리** | **1685** | **2.14** | 🔴 손댈 곳 |
| 경기 일지 | 956 | 1.21 | 괜찮다 |
| 훈련계획 입력 | — | 1.00 | 괜찮다 |

훈련 후 일지는 다 채우면 **1693px (2.15 화면)** 까지 늘어난다.

### 1-2. 어느 구획이 길이를 먹는가 (실측, `FormSec` 단위)

**훈련 후 일지** — 구획 합계 1341px

| 구획 | 높이 | 비중 |
|---|---|---|
| 강도 시스템 | 146 | 11% |
| 세션 제목 | 85 | 6% |
| 거리 · 시간 · 평균 페이스 | 114 | 9% |
| RPE · 주관 강도 | 160 | 12% |
| 예상 강도 | 131 | 10% |
| **객관 기록 · 0개** | **393** | **29%** |
| 메모 · 손글씨처럼 | 312 | 23% |

**하루 마무리** — 구획 합계 1445px

| 구획 | 높이 | 비중 |
|---|---|---|
| 수면 | 112 | 8% |
| 수면 질 | 87 | 6% |
| 체중 · 안정시 심박 | 103 | 7% |
| **통증 부위 · 정도** | **754** | **52%** |
| 오늘 감정 | 102 | 7% |
| 오늘의 한 줄 | 287 | 20% |

**두 화면의 길이는 각각 한 구획이 절반 가까이 만든다.** 전체를 조금씩
줄이는 게 아니라 **이 두 구획만 손대면 된다.**

### 1-3. 누른 느낌 실측 — 절반은 이미 있다

```css
/* app/src/styles/app.css:442 — 이미 있다 */
button:not(:disabled):not([aria-disabled="true"]):active {
  transform: scale(0.97);
  transition: transform 90ms var(--ease-out-strong);
}
```

`<button>` 은 이미 눌리면 아주 조금 줄어든다. 오너가 원한 것과 같은 방향이다.
**새로 만들 게 아니라 빠진 데를 채우는 일이다.**

훈련 후 일지의 눌리는 요소를 종류별로 셌다.

| 종류 | 개수 | `:active` 반응 |
|---|---|---|
| `<button>` | 36 | ✅ 있다 |
| `<input type=text>` | 6 | ❌ 없다 |
| `<input type=number>` | 3 | ❌ 없다 |
| `<input type=radio>` | 2 | ❌ 없다 |
| `<select>` | 1 | ❌ 없다 |
| `<textarea>` | 1 | ❌ 없다 |

**21개가 눌러도 아무 반응이 없다.**

다만 입력칸을 `scale(0.97)` 로 줄이면 글자 커서가 흔들린다.
입력칸에는 **다른 방식**이 필요하다 (§3-3).

### 1-4. 이미 잠겨 있는 것 — 마음대로 바꾸면 테스트가 깨진다

`app/src/components/Motion.contract.test.tsx` 가 `app.css` 를 파일로 읽어서
문자열을 확인한다. 아래를 지우거나 바꾸면 **테스트가 실패한다.**

| 잠긴 값 | 뜻 |
|---|---|
| `--ease-out-strong: cubic-bezier(0.23, 1, 0.32, 1)` | 모든 움직임의 가속 곡선 |
| `transform: scale(0.97)` / `transition: transform 90ms` | 버튼 누른 느낌 |
| `popover-enter 150ms` | 도움말 |
| `saved-toast-enter 200ms` / `saved-toast-exit 150ms` | 저장 알림 |
| `:focus-visible` + `outline: 2px solid var(--focus-ring)` | 키보드 초점 |
| `@media (prefers-reduced-motion: reduce)` 블록 | 움직임 줄이기 설정 |
| `transition: all` 없음 / 맨 `ease-in` 없음 / `scale(0)` 없음 | 금지 목록 |
| 마우스 효과는 `@media (hover: hover) and (pointer: fine)` 안에만 | 터치에서 hover가 붙는 문제 |

**효과를 추가하면 이 테스트에 확인 줄도 함께 추가한다.**
추가만 하고 기존 줄은 지우지 않는다.

### 1-5. 이미 있는 e2e 검사 — 새로 만들지 말고 늘린다

`app/e2e/touch-audit.ts` 가 이미 다음을 잰다.

| 함수 | 하는 일 |
|---|---|
| `auditTouchTargets` | 44px 최소 크기 + 좌우 잘림 |
| `expectNoHorizontalOverflow` | 가로 스크롤 없음 |
| `expectSaveAboveNavigation` | 저장 버튼이 탭바 위에 있음 |
| `seedTouchAuditEntries` | 기록이 있는 상태 만들기 |
| `openEntry` | 일지 화면 열기 |
| `TOUCH_PROJECTS` | `mobile-chromium`, `touch-narrow` |

**스크롤 깊이 검사만 새로 만들고, 나머지는 이 함수들을 쓴다.**

### 1-6. 착각하기 쉬운 것 두 개 (확인했으니 손대지 말 것)

**(가) 13×13px 라디오는 결함이 아니다.**

```
{"name":"post-session-memo-purpose","selfW":13,"selfH":13,
 "labelW":171,"labelH":64,"labelText":"나만의 메모"}
```

동그라미 자체는 13px 지만 감싼 `<label>` 이 171×64 다. 손가락이 닿는
영역은 64px 다. **44px 규칙을 이미 지킨다.** 동그라미를 키우지 말 것.

**(나) 색·서체·간격 토큰을 새로 만들지 말 것.**

토큰의 단일 소스는 저장소 루트의 `colors_and_type*.css` 이고
`main.tsx` 가 직접 불러온다 (ADR A3). `app.css` 에 색을 다시 정의하면
두 값이 갈라진다. **있는 토큰만 쓴다.**

---

## 2. 만들 것 — 스크롤 깊이 줄이기

### 2-1. 값을 확인한 뒤 다음 구획을 건드리면 한 줄로 접는다 (핵심 변경)

지금 `FormSec` 은 답을 했는지 안 했는지와 **무관하게 늘 같은 높이**다.
RPE 를 고른 뒤에도 버튼 10개가 그대로 남아 160px 를 차지한다.

`app/src/screens/log-entry/shared.tsx` 의 `FormSec` 에 선택형 접기 계약을 더한다.
`collapsible` 을 주지 않은 기존 화면은 그대로 펼쳐진다.

```tsx
export function FormSec({
  lb, help, collapsible, defaultOpen, autoOpenWhen, collapseWhenLeft,
  onTouch, summary, expandHint, children,
}: {
  readonly lb: string
  readonly help?: TermId
  readonly collapsible?: boolean
  readonly defaultOpen?: boolean
  readonly autoOpenWhen?: boolean
  readonly collapseWhenLeft?: boolean
  readonly onTouch?: () => void
  readonly summary?: string
  readonly expandHint?: string
  readonly children: ReactNode
}) { /* ... */ }
```

접힌 모습 — 높이 **44px** 한 줄:

```
RPE · 주관 강도            7/10        고치기 ›
```

규칙:

- `collapsible` 이 없으면 **지금과 똑같이** 동작한다 (다른 화면 안 깨짐)
- 접힌 줄 전체가 하나의 `<button>` 이다 → `scale(0.97)` 이 저절로 붙는다
- 접힌 줄 높이는 **44px 이상**
- `summary` 는 **사용자가 넣은 값만** 보여준다. 판정·평가 문구 금지
  (예: `"RPE 7/10"` ✅ / `"강도 높음"` ❌)
- **접혀도 값은 그대로 저장된다.** 접기는 보기 방식일 뿐이다
- 스크린리더에는 `aria-expanded` 로 알린다

🔴 **접는 시점을 지어내지 말 것.** 사용자가 답을 넣는 순간에는 절대
접지 않는다. 값이 있는 구획에서 다음 구획을 **건드릴 때만** 접는다.
사람이 다시 펼친 구획은 자동으로 다시 접지 않으며, 시간·개수 규칙도 만들지
않는다. 이 순서 규칙은 §0-D 와 `AutoCollapse.contract.test.tsx` 로 잠근다.

### 2-2. 객관 기록 구획을 닫힌 상태로 시작한다 (−349px)

`IntensityAssessmentField.tsx` 의 `객관 기록` 구획이 **아무것도 안 넣어도
393px** 다. 종류 고르는 `<select>` 와 칸 5개가 늘 펼쳐져 있기 때문이다.

닫힌 모습:

```
객관 기록 · 0개                        + 추가
```

- 닫힌 높이 목표: **44px** → **349px 절약 (훈련 후 일지의 22%)**
- `+ 추가` 를 누르면 지금 그대로 펼쳐진다
- 이미 넣은 항목이 1개 이상이면 **펼친 채로 시작**한다
- 필수 입력이 아니다. `objectiveComponents: []` 로 저장돼도 문제없다
  (`PostSessionForm.tsx:47` 의 `explicitOrMissing` 이 이미 처리한다)

🔴 **`explicitOrMissing` 동작을 바꾸지 말 것.** 안 넣은 칸은
`MISSING` 으로 남아야 한다. 접었다는 이유로 `0` 을 넣으면 안 된다.

### 2-3. 몸 그림을 닫힌 상태로 시작한다 (−476px)

하루 마무리의 `통증 부위` 구획이 **754px** — 그 화면 길이의 52% 다.
안을 뜯어보면:

| 부분 | 높이 | 누를 수 있나 |
|---|---|---|
| 몸 그림 (SVG) | 약 476 | ❌ **못 누른다** |
| 부위 버튼 10개 (2열 5행) | 약 236 | ✅ 여기서 고른다 |
| 안내 문구 | 약 42 | — |

`BodyDiagram.tsx:35` 의 `<svg aria-hidden="true">` 이고, 안의
`<circle data-body-part>` 에 `onClick` 이 **없다.** 즉 그림은
**보여주기만 하는 그림**이고, 실제 선택은 전부 버튼에서 일어난다.

- 그림을 `[몸 그림으로 보기]` 버튼 뒤로 넣는다 (기본 닫힘)
- 버튼 10개는 **그대로 둔다.** 위치·크기·순서 변경 금지
- 통증을 하나라도 고르면 그림을 **자동으로 펼친다** (어디를 골랐는지
  한눈에 보여주는 게 그림의 일이다)
- **−476px → 하루 마무리 1685 → 약 1209 (1.54 화면)**

🔴 **`aria-hidden="true"` 를 떼지 말 것.** 그림은 버튼과 같은 정보를
중복해서 담고 있다. 스크린리더에 두 번 읽히면 오히려 나쁘다.

### 2-4. 목표 숫자

| 화면 | 지금 | 목표 | 방법 |
|---|---|---|---|
| 훈련 후 일지 (빈 상태) | 2.01 화면 | **1.6 화면 이하** | §2-2 |
| 훈련 후 일지 (다 채운 뒤) | 2.15 화면 | **1.3 화면 이하** | §2-1 + §2-2 |
| 하루 마무리 (빈 상태) | 2.14 화면 | **1.6 화면 이하** | §2-3 |
| 하루 마무리 (다 채운 뒤) | — | **1.4 화면 이하** | §2-1 + §2-3 |
| 경기 일지 | 1.21 화면 | 그대로 (건드리지 않음) | — |

목표를 못 맞추면 **억지로 맞추지 말고 보고에 남긴다.** 글자를 지워서
맞추는 건 금지다 (§3-1).

---

## 3. 만들 것 — 글자량 줄이기

### 3-1. 🔴 먼저, 지우면 안 되는 글자

오너 지침은 **"구분하고 실제 활용하기 위한 작업이지 지우는 게 아니다"** 다.
화면 글자에도 그대로 적용된다. **아래는 한 글자도 줄이지 않는다.**

| 지우면 안 되는 것 | 근거 |
|---|---|
| 안전 안내문 (`PainReviewBanner` 등) | 안전 문구는 짧게 만들 대상이 아니다 |
| 저장 실패 안내 (`shared.tsx` `StickyBar`) | 두 문장 다 필요하다 (원인 + 다음 행동) |
| 메모 용도 선택 설명 | 동의를 받는 문구다 |
| 표본 수 · 출처 · 경과 기간 표시 | 계약이 요구한다 (P1·P5) |
| `판단보류` · `기록 없음` 같은 상태 문구 | 없는 걸 없다고 말하는 문구다 |

**줄일 수 있는 것은 "구획 이름과 보조 설명"뿐이다.**

### 3-2. 접기로 글자를 줄인다 (지우지 않고)

§2-1 의 접기가 글자량 문제도 같이 해결한다. 다 답한 구획의 설명글이
한 줄 요약으로 바뀌므로 **화면에 보이는 글자 수가 줄고, 글자는
사라지지 않는다.** 펼치면 그대로 다시 나온다.

실측: 훈련 후 일지 한 화면(787px)의 글자 수는 275자, 줄 수는 74줄이다.
`RPE`·`예상 강도`·`강도 시스템` 세 구획이 접히면 그 안의 보조 문구
(`매우 쉬움` / `최대` / `km` `min` `/km` 등)가 요약 한 줄로 바뀐다.

**목표: 한 화면에 동시에 보이는 줄 수를 74 → 45 이하로.**
글자를 삭제해서가 아니라 접혀서 줄어야 한다.

### 3-3. 입력칸도 누르면 반응하게 (−21개 → 0개)

§1-3 에서 21개가 무반응이었다. 입력칸은 줄이면 커서가 흔들리니
**테두리만 반응하게** 한다.

```css
/* app/src/styles/app.css 에 추가 */
:where(input, select, textarea):not(:disabled):active {
  border-color: var(--ink-2);
  transition: border-color 90ms var(--ease-out-strong);
}
```

- `90ms` 와 `var(--ease-out-strong)` 을 **버튼과 똑같이** 쓴다.
  다른 값을 쓰면 화면 안에서 반응 속도가 두 가지가 된다
- `transform` 을 쓰지 않는다 (커서 흔들림)
- 색은 있는 토큰만 쓴다 (`--ink-2`). 새 색 만들지 말 것
- `transition: all` 금지 (테스트가 잡는다)

### 3-4. 구획이 접힐 때 아주 짧게 정착시킨다 (해소감)

오너가 말한 **"넘어가는 기분과 해소감"** 이 여기다. 답을 넣어 구획이
접힐 때, 접힌 줄이 아주 짧게 자리를 잡는다.

```css
@keyframes formsec-settle {
  from { opacity: 0.55; transform: translateY(-2px); }
  to   { opacity: 1;    transform: translateY(0); }
}

.formsec--collapsed {
  animation: formsec-settle 120ms var(--ease-out-strong) both;
}
```

지켜야 할 값:

| 값 | 이유 |
|---|---|
| `120ms` | 버튼 `90ms` 보다 살짝 길다. 눌린 느낌 다음에 오는 반응이라 |
| `translateY(-2px)` | 2px 다. 더 크면 화면이 튀어 보인다 |
| `opacity: 0.55` 에서 시작 | `0` 에서 시작하면 글자가 나타나는 느낌이 되어 산만하다 |
| `var(--ease-out-strong)` | 저장소의 유일한 가속 곡선 |
| 높이(`height`) 를 애니메이션하지 않는다 | 아래 내용이 통째로 밀려 올라와 눈이 따라가지 못한다 |

🔴 **접기 자체는 즉시 일어나고, 애니메이션은 접힌 줄에만 붙인다.**
접히는 과정을 애니메이션하면 그 순간 아래 구획이 다 움직인다.

### 3-5. 움직임 줄이기 설정을 지킨다

`app.css` 의 `@media (prefers-reduced-motion: reduce)` 블록에
새 애니메이션도 넣는다.

```css
@media (prefers-reduced-motion: reduce) {
  .formsec--collapsed {
    animation-name: ui-fade-in;   /* 이미 있는 것을 쓴다 */
    transform: none;
  }

  :where(input, select, textarea):not(:disabled):active {
    transition: none;             /* 색 변화는 남기고 시간만 없앤다 */
  }
}
```

색이 바뀌는 것 자체는 남긴다. 움직임을 싫어하는 설정이지
**반응이 없기를 바라는 설정이 아니다.**

### 3-6. 마우스 효과를 추가하려면 감싼다

hover 효과를 새로 넣고 싶어지면 반드시 이 안에 넣는다.

```css
@media (hover: hover) and (pointer: fine) { /* ... */ }
```

터치 기기에서는 hover 가 탭 뒤에 남아붙어서, 누른 뒤에도 계속
반응한 것처럼 보인다. 테스트가 이걸 잡는다.

---

## 4. 함수·스타일 검증 절차 — 다음 작업자가 리뷰할 수 있게 남긴다

이 작업의 산출물은 함수 하나와 CSS 몇 줄이다. **왜 이 값인지**가
남지 않으면 다음 사람이 "그냥 예뻐서" 로 바꿔 버린다.

작업자는 이 절의 표를 **보고서에 실제 값으로 채워서** 낸다.
빈칸이 있으면 리뷰가 반려된다.

### 4-1. `FormSec` 근거 추적표

| 서명 요소 | 이렇게 만든 근거 | 다르게 만들면 생기는 일 |
|---|---|---|
| `collapsible?: boolean` — 물음표가 붙는다 | `FormSec` 을 쓰는 화면이 6개고, 이번에 고치는 건 2개다 | 필수로 만들면 나머지 4개 화면이 전부 깨진다 |
| `summary?: string` — 문자열이고 계산값이 아니다 | 판정 문구가 들어올 자리를 아예 안 만든다 | 객체로 받으면 `{level:"높음"}` 같은 판정이 들어온다 |
| 접힌 줄이 `<button>` 이다 | `app.css:442` 의 `:active` 가 `button` 에만 붙는다 | `<div onClick>` 으로 만들면 누른 느낌이 안 생긴다 |
| 접힌 높이가 44px 이상 | `touch-audit.ts` `meetsTouchContract` | e2e 터치 검사가 실패한다 |
| 값을 지우지 않고 감추기만 한다 | 오너 지침 "지우거나 하라는 게 아니야" | 접었다 펼치면 답이 사라진다 |
| 접는 시점이 "다음 구획을 건드렸을 때" | 방금 누른 값 확인을 먼저 보장한다 | 값이 들어오는 순간 접으면 화면이 튀어 사용자가 놓친다 |

### 4-2. 손으로 검산하는 표 — 코드 없이 먼저 채운다

**코드를 쓰기 전에** 이 표를 손으로 채운다. 그 다음 테스트를 이 표대로
쓴다. 순서를 바꾸면 코드가 낸 답을 정답으로 믿게 된다.

| # | 입력 | `summary` | 기대 렌더 | 기대 높이 | `children` |
|---|---|---|---|---|---|
| 1 | `collapsible` 없음 | `undefined` | 지금과 똑같이 펼쳐짐 | 기존 값 그대로 | 보인다 |
| 2 | `false` | `"7/10"` | 펼쳐짐 | 기존 값 그대로 | 보인다 |
| 3 | `true` | `"7/10"` | 접힌 한 줄 + `"7/10"` | **44px 이상** | 숨는다 |
| 4 | `true` | `undefined` | 접힌 한 줄, 값 칸은 비움 | 44px 이상 | 숨는다 |
| 5 | `true` → 접힌 줄 클릭 | `"7/10"` | 다시 펼쳐짐 | 기존 값 그대로 | 보인다 |
| 6 | 5번에서 다시 클릭 | `"7/10"` | 다시 접힘 | 44px 이상 | 숨는다 |

1번 줄이 **기존 4개 화면을 지키는 줄이다.** 이 줄이 깨지면
`RaceForm`·`RaceSelfChecks` 가 같이 깨진다.

5·6번 줄이 **값이 살아 있는지 보는 줄이다.** 접었다 펼쳤을 때
입력값이 그대로여야 한다.

### 4-3. 먼저 정해서 적어 둘 것 두 가지

스펙에 없어서 작업자가 정해야 한다. **정하고, 이유를 코드 주석과
보고서에 적고, 테스트로 잠근다.**

```tsx
// (가) 사용자가 답을 지워서 빈 값이 되면 다시 펼친다.
//      근거: 답이 없는 구획을 접어 두면 안 채운 걸 못 본다.
//      "빈 값" 판정은 각 화면이 하고 FormSec 은 done 만 받는다.
//
// (나) 사용자가 손으로 펼친 구획은, 답이 다시 들어와도 자동으로 접지 않는다.
//      근거: 사람이 연 것을 코드가 닫으면 조작을 빼앗긴 느낌이 된다.
//      FormSec 안에 "사람이 열었다" 상태를 따로 둔다.
```

(나)를 빼먹으면 **고치려고 펼친 구획이 값을 넣는 순간 다시 닫혀서
같은 동작을 반복하게 된다.** 이게 가장 흔한 실수다.

### 4-4. CSS 값 근거 추적표

| 값 | 이렇게 정한 근거 | 다르게 하면 |
|---|---|---|
| 입력칸 `90ms` | `app.css:444` 버튼과 같은 값 | 화면 안에 반응 속도가 두 개가 된다 |
| 입력칸에 `transform` 없음 | 글자 커서가 흔들린다 | 입력 중에 커서가 튄다 |
| 접힘 `120ms` | 버튼 90ms 다음에 오는 반응이라 살짝 길다 | 90ms 면 눌림과 겹쳐 안 보인다. 200ms 넘으면 느리다 |
| 접힘 `translateY(-2px)` | 2px | 6px 넘으면 화면이 튄다 |
| 접힘 시작 `opacity: 0.55` | 반쯤 보이는 상태에서 정착한다 | `0` 이면 새로 나타나는 느낌이라 산만하다 |
| `height` 를 애니메이션 안 함 | 아래 구획 전체가 밀려 올라온다 | 눈이 위치를 잃는다 |
| 색을 새로 안 만듦 | ADR A3 — 토큰 단일 소스 | `app.css` 와 루트 css 가 갈라진다 |

### 4-5. 리뷰하는 사람이 확인할 것 (체크리스트)

- [ ] §4-1 · §4-4 근거 추적표의 각 줄이 실제 파일·줄번호를 가리키는가
- [ ] §4-2 검산표 6줄이 **테스트 파일에 그대로** 들어갔는가
- [ ] §4-3 (가)(나)가 코드 주석과 테스트 양쪽에 적혀 있는가
- [ ] `Motion.contract.test.tsx` 에서 **기존 확인 줄을 지운 게 없는가**
      (`git diff` 에서 삭제 줄이 0이어야 한다)
- [ ] `app.css` 에 새 색·새 가속 곡선을 정의한 게 없는가
      — `grep -c "cubic-bezier" app/src/styles/app.css` → **1** (56번 줄 정의 하나)
      — `grep -cE "#[0-9a-fA-F]{3,6}" app/src/styles/app.css` → **3**
        (708·723·724번 줄, 인쇄용 블록. 작업 전과 같은 수여야 한다)
- [ ] `transition: all` 이 없는가
- [ ] 접힌 줄이 `<button>` 인가 (`<div onClick>` 아님)
- [ ] 안전 문구·저장 실패 문구·동의 문구가 그대로인가 (§3-1)
- [ ] 접었다 펼쳤을 때 입력값이 남아 있는가

### 4-6. 막히면 이렇게 남긴다

```tsx
// 판단보류: 여러 구획이 동시에 접힐 때 애니메이션을 겹칠지 순서대로 할지
// 정해진 게 없다. 임시로 각자 즉시 접히게 두고 테스트로 잠갔다.
// 오너 확인 필요. 바꿀 때 고칠 곳: FormSec + Motion 계약 테스트 1건.
```

**혼자 정하고 넘어가지 않는다.** 그게 근거 없는 임계값을 만들어 넣은
오판(`PRODUCT_NORTH_STAR.md` §5 사례 6)이 생긴 방식이다.

---

## 5. 테스트 (필수)

### 5-1. `app/src/screens/log-entry/FormSec.contract.test.tsx` (새 파일)

§4-2 검산표 6줄을 그대로 옮긴다. 더해서:

| 테스트 | 확인 내용 |
|---|---|
| `collapsible` 없으면 기존 동작 | `RaceForm` 처럼 접기 계약을 안 주는 화면이 안 깨짐 |
| 접힌 줄이 버튼 | `getByRole("button")` 으로 잡히는지 |
| `aria-expanded` | 접힘 `false` / 펼침 `true` |
| 값 보존 | 접기 → 펼치기 후 `input.value` 가 같은지 |
| 사람이 펼친 건 안 닫힘 | §4-3 (나) — 펼친 뒤 값 변경 시 그대로 펼쳐져 있는지 |
| `summary` 에 판정어 없음 | 화면 문자열에 `높음\|낮음\|좋음\|나쁨` 0건 |

### 5-2. `Motion.contract.test.tsx` **추가** (기존 줄 삭제 금지)

| 테스트 | 확인할 문자열 |
|---|---|
| 입력칸 반응이 있다 | `:where(input, select, textarea)` + `border-color 90ms` |
| 접힘 애니메이션이 있다 | `formsec-settle 120ms var(--ease-out-strong)` |
| 접힘이 2px 다 | `translateY(-2px)` |
| 높이를 움직이지 않는다 | `formsec-settle` 정의 안에 `height` 0건 |
| 움직임 줄이기에 새것도 들어갔다 | `reduce` 블록 안에 `.formsec--collapsed` |
| 새 가속 곡선 없음 | `cubic-bezier` 가 파일 전체에 **1개**뿐 |

### 5-3. `app/e2e/scroll-depth.spec.ts` (새 파일)

`touch-audit.ts` 의 `openEntry` 와 `TOUCH_PROJECTS` 를 **재사용**한다.

```ts
// 스크롤은 <main class="app-scroll-region"> 에서 일어난다. document 가 아니다.
const depth = await page.evaluate(() => {
  const region = document.querySelector("main.app-scroll-region")
  if (!region) throw new Error("scroll region not found")
  return region.scrollHeight / region.clientHeight
})
```

🔴 **아래 비율 표는 폐기됐다. §0-B 의 px 표를 쓴다.** (D1·D2)
분모가 기기마다 달라서 비율은 상한으로 쓸 수 없고, 다 채운 뒤 상한은
산술적으로 닿을 수 없다.

~~| 훈련 후 일지 (빈 상태) | `1.6` | 훈련 후 (다 채운 뒤) | `1.3` |~~
~~| 하루 마무리 (빈 상태) | `1.6` | 하루 마무리 (다 채운 뒤) | `1.4` |~~

**상한을 못 맞추면 상한을 올리지 말고 보고에 남긴다.**
숫자를 고쳐서 통과시키는 건 금지다. 단 **자가 틀렸으면 자를 고친다** —
그게 D1 이었다. 자를 고칠 때는 왜 고쳤는지 근거를 보고에 남긴다.

### 5-4. 기존 e2e 가 계속 통과해야 한다

```bash
cd app && CI=1 npx playwright test
```

`touch-targets.spec.ts` · `touch-surfaces.spec.ts` 가 **하나도 안 깨져야**
한다. 접기 때문에 버튼이 화면에서 사라지면 이 검사가 실패한다.
그때는 `scrollIntoViewIfNeeded` 를 넣는 게 아니라 **접기 조건을 고친다.**

### 5-5. grep 검사

```bash
# 새 가속 곡선을 만들었는지
grep -c "cubic-bezier" app/src/styles/app.css
#   → 1 (--ease-out-strong 정의 한 줄. 작업 전과 같다)

# 새 색을 박았는지
grep -cE "#[0-9a-fA-F]{3,6}" app/src/styles/app.css
#   → 3 (708·723·724번 줄, 인쇄용 블록. 늘어나면 안 된다)

# 금지된 전이
grep -n "transition: all" app/src/styles/app.css
#   → 0건

# 요약에 판정어가 들어갔는지
#   🔴 범위 수정 (D4). log-entry 전체로 재면 기존 코드가 이미 3건 위반한다
#   (EveningCheckin.tsx:14-15 MOOD_LABELS/SLEEP_QUALITY_LABELS, RaceSelfChecks.tsx:5).
#   그건 사용자가 고르는 선택지 이름이고 앱이 내리는 판정이 아니다.
#   검사할 것은 "FormSec 이 summary 로 그리는 문자열" 하나다 → 계약 테스트로 잠근다.
grep -rnE "높음|낮음|좋음|나쁨|양호|준비도|위험" app/src/screens/log-entry
#   → 3건 (작업 전과 같아야 한다. 늘어나면 안 된다)

# 안전 문구가 지워졌는지
grep -rn "지도자·보호자" app/src/screens app/src/components
#   → 작업 전과 같은 건수
```

---

## 6. 하지 말 것

- 안전 문구·저장 실패 문구·동의 문구를 줄이거나 지우기 — **절대 금지** (§3-1)
- 13×13 라디오 동그라미 크기 바꾸기 — **금지** (§1-6, 이미 44px 지킴)
- 몸 그림의 `aria-hidden="true"` 떼기 — **금지** (§2-3)
- 부위 버튼 10개의 위치·크기·순서 바꾸기 — **금지**
- `Motion.contract.test.tsx` 의 기존 확인 줄 삭제 — **금지**
- `app.css` 에 새 색·새 가속 곡선 정의 — **금지** (ADR A3)
- `transition: all` · 맨 `ease-in` · `scale(0)` — **금지** (테스트가 잡는다)
- `height` 애니메이션 — **금지** (§3-4)
- hover 효과를 `@media (hover: hover)` 밖에 넣기 — **금지**
- 시간·개수로 자동 접는 규칙 만들기 — **금지** (§2-1)
- `explicitOrMissing` 동작 바꾸기 — **금지** (§2-2)
- 접혔다고 값에 `0` 넣기 — **절대 금지**
- e2e 상한 숫자를 올려서 통과시키기 — **금지** (§5-3)
- `impl/` · `specs/` · `journal-schema.ts` 손대기 — **금지**
- 계산·처방·안전 판정 로직 손대기 — **금지** (이건 화면 작업이다)

---

## 7. 완료 기준

```bash
cd app && npm test && npx tsc --noEmit -p tsconfig.json && npm run build
cd impl && npm test                  # 98개 그대로 (안 건드렸으므로)
cd app && CI=1 npx playwright test   # 4개 프로젝트 전부
```

- [ ] `FormSec` 에 `collapsible`·`defaultOpen`·`autoOpenWhen`·`collapseWhenLeft`·`onTouch`·`summary` 추가, `collapsible` 없으면 기존 동작 유지
- [ ] `FormSec.contract.test.tsx`·`AutoCollapse.contract.test.tsx` 로 수동 재열기 보호와 다음 구획 접기 검증
- [ ] `Motion.contract.test.tsx` 에 6개 확인 **추가**, 기존 줄 삭제 0건
- [ ] `scroll-depth.spec.ts` 추가, `touch-audit.ts` 함수 재사용
- [ ] 객관 기록 구획이 닫힌 상태로 시작, `+ 추가` 로 펼쳐짐
- [ ] 몸 그림이 닫힌 상태로 시작, 통증 선택 시 자동 펼침
- [ ] 훈련 후 일지 빈 상태 **1300px 이하** (mobile) / **1350px 이하** (narrow) — §0-B
- [x] ~~훈련 후 일지 다 채운 뒤 1.3 화면 이하~~ — **폐기** (D2, 산술적으로 불가능)
- [ ] 하루 마무리 빈 상태 **1300px 이하** (mobile) / **1350px 이하** (narrow) — §0-B
- [x] ~~한 화면 동시 표시 줄 수 45줄 이하~~ — **폐기** (D7, 재는 방법이 정의돼 있지 않다)
- [ ] 입력칸 21개가 눌리면 테두리로 반응
- [ ] 접힘 애니메이션 120ms · 2px, 움직임 줄이기 설정에서 fade 로 대체
- [ ] 기존 e2e 통과, 회귀 0건 (실측 기준선을 보고에 적는다 — 143 은 어림수였다)
- [ ] §5-5 grep 전부 기대값 (판정어는 0건이 아니라 **작업 전과 같은 3건** — D4)
- [ ] **§4 검증 절차를 보고서에 실제 값으로 채워 냈음** (빈칸 0개)
- [ ] `impl/` · `specs/` · `journal-*` 변경 0건

---

## 8. 보고

`reports/review/WORK_ORDER_UX1_REPORT.md`

- 만든·고친 파일 목록
- **스크롤 깊이 before → after 실측표 — px 로** (§0-B, mobile·narrow 둘 다)
- **§0-A 리뷰 표 D1~D7 에 대해 실제로 어떻게 처리했는지**
- **구획별 높이 before → after** (§1-2 의 표를 다시 재서)
- **§4-1 · §4-4 근거 추적표** — 각 줄에 실제 파일·줄번호를 적어서
- **§4-2 검산표 6줄** — 손으로 정한 값과 실제 렌더 결과를 나란히
- **§4-3 (가)(나)** — 어떻게 정했고 왜 그렇게 정했는지
- **§4-5 체크리스트 9개** — grep 결과 붙여넣기
- `Motion.contract.test.tsx` 의 `git diff` — **삭제 줄이 0임을 보여준다**
- 목표 숫자를 못 맞춘 항목과 그 이유 (**상한을 올리지 말고 여기 적는다**)
- 판단이 필요했으나 하지 않고 남긴 것

**애매한 것을 임의로 정하지 말고 보고에 남긴다.** 그게 정답이다.
