# 오너 결정 브리핑 — 개인 목표 페이스

```yaml
목적: 승인 전에 맥락을 전부 이해하실 수 있게 설명한다
읽는사람: 오너
전제문서: PRODUCT_NORTH_STAR.md
관련기획: PLAN_A_FIRST_TEMPLATE_ACTIVATION.md
```

> 이 문서는 **결정을 재촉하지 않습니다.** 각 선택지가 무엇을 뜻하고,
> 잘못 고르면 무슨 일이 생기는지 먼저 설명합니다.

---

# 1부. 지금 무슨 일이 벌어지고 있나

## 1-1. 사용자가 지금 보는 것

훈련계획 베타에서 계획을 만들면 이런 게 나옵니다.

```
3일차 · 품질 세션
체감강도 5~6 · 30~40분
```

**끝입니다.** "얼마나 빨리 달려야 하는지"는 없습니다.
`plan-beta-store.ts`가 저장할 수 있는 처방 종류는 **`REST`(쉼)와
`RPE_TIME_RANGE`(체감강도+시간범위) 둘뿐**이기 때문입니다.
**거리도, 페이스도 담을 자리가 애초에 없습니다.**

> 이 말은 곧, 개인 페이스를 붙이려면 **저장 구조에 처방 종류를 하나
> 추가**해야 한다는 뜻입니다(예: `PACE_TARGET`). 화면만 고쳐서 되는
> 일이 아닙니다. 다만 기존 두 종류를 건드리지 않고 **추가**하는 것이라
> 기존 계획은 그대로 열립니다.

## 1-2. 오너께서 원하시는 것

```
3일차 · 품질 세션
1000m 5회 · 목표 3분 42초 · 사이 조깅 2분 30초
(내 5000m 기록 18분 30초 기준)
```

이게 코칭입니다. 그리고 **가능합니다.** 계산식은 이미 코드에 있습니다.

## 1-3. 계산식은 이미 있습니다 (실제 코드)

`impl/src/prescription/runtime.ts:177`

```
목표 반복 시간 = 내 기록(초) × (반복 거리 ÷ 내 기록의 거리)

예) 5000m를 18분 30초(1110초)에 뛰는 선수의 1000m 반복
    1110 × (1000 ÷ 5000) = 222초 = 3분 42초
```

**그런데 이게 앱에 연결돼 있지 않습니다.**
앱이 `impl`에서 가져다 쓰는 것을 전부 조사했습니다.

| 앱이 쓰는 impl 모듈 | 용도 |
|---|---|
| `@impl/d9/evaluator` | 몸 상태 확인 |
| `@impl/safety-gate/gate` | 안전 게이트 |
| `@impl/plan-generator/*` | 계획 생성 |
| `@impl/rve/signal` | 위험 신호 |
| `@impl/index` (표기 읽기 화면에서만) | 표기 파서 |

**`@impl/prescription`을 처방에 쓰는 곳은 한 군데도 없습니다.**
엔진은 만들어 뒀는데 배선이 안 된 상태입니다.

## 1-4. 그리고 선수 기록을 넣을 곳이 아예 없습니다

앱 화면을 전수 조사했습니다. 온보딩에도, 계획 입력(`PlanIntake`)에도
**최고기록·시즌기록·목표기록을 넣는 칸이 없습니다.**

계획을 만들 때 받는 건 이것뿐입니다:
종목 그룹 / 경험 수준 / 주당 가능 일수 / 몸 상태

**즉 지금은 계산식이 있어도 넣을 숫자가 없습니다.**
이것이 어느 길을 고르시든 1번 작업이어야 하는 이유입니다.

## 1-5. 참고 — 표기 읽기 화면은 이미 있습니다

`훈련표 표기 읽기` 화면(`NotationReader.tsx`)이 이미 배포돼 있습니다.
`2×(10×400m) @5000m RP · r60″ · R3′` 를 넣으면 세트·반복·거리를 풀어 줍니다.

그 화면에는 이런 안내가 붙어 있습니다:

> "개인 최고기록이나 목표기록으로 페이스를 계산하지 않아요."

**이번 작업은 정확히 저 문장을 지우는 일입니다.**
즉 완전히 새로운 기능이 아니라, 의도적으로 잠가 둔 문을 여는 일입니다.

---

# 2부. 제가 발견한 진짜 문제

여기가 가장 중요합니다. **결정 전에 반드시 이해하셔야 합니다.**

## 2-1. 카탈로그와 엔진이 서로 다른 사고방식으로 만들어졌습니다

훈련 강도를 표현하는 방법에는 **두 가지 철학**이 있습니다.

### 철학 A — "경기 페이스 기준" (엔진이 채택)

> "네 5000m 경기 페이스로 달려라"

- 필요한 것: **그 거리의 실제 기록**
- 장점: 계산이 단순하고 근거가 명확함. 선수 본인 기록이므로 반박 불가.
- 단점: 400m 반복을 하려면 400m 기록이 있어야 함

### 철학 B — "강도 구역 기준" (카탈로그가 채택)

> "역치(T) 강도로 달려라", "인터벌(I) 강도로 달려라"

- 필요한 것: **구역을 페이스로 바꾸는 환산 모델**
- 장점: 기록 하나로 모든 훈련 강도를 커버함
- 단점: 환산 모델의 근거가 필요함

**엔진은 A로, 카탈로그는 B로 만들어졌습니다.** 그래서 안 맞습니다.

## 2-2. 실측 — 카탈로그 30개를 파서에 전부 넣어봤습니다

| 표기 유형 | 개수 | 예시 | 엔진이 페이스를 낼 수 있나 |
|---|---|---|---|
| 강도 구역 (`@T`/`@I`/`@E`) | **13** | `5×3′ @I · r2′ jog` | ❌ 구역은 거리가 아님 |
| 강도 표기 없음 (스프린트·회복) | **13** | `3×30m · r3′`, `REST` | ❌ 해당 없음 |
| **경기 페이스 (`RP`)** | **2** | `5×1000m @5K RP` | ⭕ **가능** |
| 기타 (`@95% vVO2max`) | 2 | `4×3′ @95% vVO2max` | ❌ 모델 필요 |
| **합계** | **30** | | **통과 0개** |

**30개 전부 파서가 거부했습니다.** 통과한 건 0개입니다.

## 2-3. 왜 `RP` 2개도 그냥은 안 되나

`V2-SEED-05`의 표기는 `5×1000m @5K RP · r2′30″` 입니다.
파서가 원하는 형식은 `5×1000m @5000m RP · r150″` 입니다.

| 차이 | 사람은 이해함 | 기계는 못 읽음 |
|---|---|---|
| `5K` | 5000m | 숫자+`m` 형식이 아님 |
| `2′30″` | 150초 | 분·초 복합 표기 미지원 |

**이건 사람이 고쳐 주면 되는 사소한 차이입니다.**
실제로 제가 `5×1000m @5000m RP · r150″`로 바꿔 넣으니 **통과했습니다.**

## 2-4. 그런데 `@T`는 고쳐도 안 됩니다 — 이게 핵심입니다

`3×1600m @T · r1~2′` 를 아무리 다듬어도 페이스가 안 나옵니다.
**`T`(역치)는 거리가 아니라 강도 구역**이기 때문입니다.

"1600m를 역치 페이스로" → 역치 페이스가 몇 초인지 알려면
**내 기록에서 역치 페이스를 뽑아내는 공식**이 있어야 합니다.

**즉, 표기를 고치는 문제(2-3)와 환산 모델 문제(2-4)는 다른 문제입니다.**
전자는 작업이고, 후자는 **오너의 도메인 결정**입니다.

---

# 3부. 그래서 진짜 결정은 하나입니다

제가 앞서 6개 결정으로 나눠 드렸는데, **다시 보니 그게 러프했습니다.**
본질은 **하나의 큰 갈림길**이고 나머지는 그에 딸린 세부입니다.

## 🔴 큰 갈림길 — 강도를 어떻게 페이스로 바꿀 것인가

### 길 1 — 경기 페이스만 쓴다 (환산 모델 없음)

**작동 방식**: 선수가 5000m 기록을 넣으면 → 5000m 페이스 기반 반복만 처방

| 항목 | 내용 |
|---|---|
| 쓸 수 있는 템플릿 | **2개** (`V2-SEED-05`, `GL-SEED-01`) |
| 선수가 넣어야 할 기록 | 훈련하려는 거리마다 하나씩 |
| 근거 | 선수 본인 기록. 반박 불가. |
| 위험 | 낮음 |
| 오너 승인 부담 | 낮음 |
| **한계** | 역치·인터벌 훈련에 페이스를 못 붙임. **훈련의 대부분이 여기 해당** |

### 길 2 — 환산 모델을 도입한다 ⭐ 오너 말씀에 가까움

**작동 방식**: 기록 1개 → 환산 모델 → 모든 구역·거리의 페이스

오너께서 말씀하신
> "경기 목표, 최고기록, 시즌 기록등을 대입하면 모두 맞는 공식을 만들 수 있어"

**이게 정확히 길 2입니다.** 그리고 그런 공식은 실제로 존재합니다.

| 항목 | 내용 |
|---|---|
| 쓸 수 있는 템플릿 | **최대 17개** (구역 13 + RP 2 + 기타 2) |
| 선수가 넣어야 할 기록 | **1개면 됨** |
| 근거 | 확립된 공식이지만 **모집단 한계가 있음** |
| 위험 | 중간 — 모델을 잘못 적용하면 과부하 |
| 오너 승인 부담 | **높음. 이게 이 문서의 이유입니다.** |

## 3-1. 길 2를 택하면 어떤 공식을 쓰나

두 가지가 실제로 쓰입니다.

### ① 거리 간 기록 예측 — 리겔(Riegel) 공식

```
목표거리 기록 = 기준거리 기록 × (목표거리 ÷ 기준거리) ^ 1.06
```

5000m 18분 30초(1110초) → 1500m 약 **5분 10초**(309.8초) → 1500m 페이스 산출
*(직접 계산해 확인한 값입니다)*

- **장점**: 단순하고 검증이 많이 됨
- **한계**: 거리 차가 클수록 오차가 커짐(예: 5000m 기록으로 마라톤 예측)

### ② 구역 페이스 산출 — VDOT / 대니얼스 방식

기록 하나로 `VDOT` 점수를 내고, 그 점수에 대응하는
`E`(이지) · `M`(마라톤) · `T`(역치) · `I`(인터벌) · `R`(반복) 페이스를 표에서 읽습니다.

**중요 — 이건 이미 우리 카탈로그의 출처입니다.**
`SRC-VDOT-PACES` = `https://vdoto2.com/calculator/`

즉 카탈로그의 `@T`, `@I` 표기 자체가 **VDOT 체계에서 온 것**입니다.
카탈로그를 그 체계로 계산하는 건 이질적인 게 아니라 **원래 설계대로**입니다.

## 3-2. 그런데 여기 함정이 있습니다 — 반드시 아셔야 합니다

카탈로그의 출처 등록부에 **이렇게 적혀 있습니다.**

> `SRC-VDOT-PACES` … Use limit: **"Coach-facing examples, not TrainOracle dose authority."**
> (코치가 참고할 예시일 뿐, TrainOracle의 처방 권한이 아님)

**즉 "출처로 인용해도 된다"와 "이걸로 자동 계산해도 된다"는 다릅니다.**
지금 문서 상태로는 후자가 **명시적으로 금지**돼 있습니다.

그리고 카탈로그 §10에 활성화 요건이 못 박혀 있습니다.

```yaml
activation_requirements:
  all_numeric_templates:
    - coach_review_of_event_experience_and_current_context   # 코치 검토
    - sports_science_review_of_source_and_transfer_limitations # 스포츠과학 검토
    - explicit_lifecycle_change_from_DRAFT                    # 명시적 상태 변경
    - separate_active_template_approval_record                # 별도 승인 기록
```

**그래서 오너 승인이 필요한 겁니다.** 제가 임의로 켤 수 없게 설계돼 있고,
그 설계는 옳습니다.

## 3-3. 제 권고

**목적지는 길 2입니다.** 길 1은 코칭이 아니라 코칭의 일부입니다.
지난번에 제가 안전을 핑계로 목표를 축소했다가 반려당한 걸 반복하지 않겠습니다.

다만 **순서**는 이렇게 하시길 권합니다. 이건 목표 축소가 아니라 **공사 순서**입니다.

| 순서 | 할 일 | 오너 승인 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | **선수 기록 입력 화면** | 불필요 | **길 1이든 길 2든 무조건 필요.** 기록이 없으면 어떤 공식도 못 돌림 |
| 2 | 카탈로그에 기계용 표기 추가 | 가벼움 | `5K`→`5000m` 같은 표기 정리 |
| 3 | 길 1로 템플릿 2개 실제 배포 | 필요 | **환산 없이 되는 것부터 실제로 작동시켜 검증** |
| 4 | **환산 모델 도입 (길 2)** | **무거움** | 여기서 도메인 결정이 필요 |
| 5 | 구역 템플릿 13개 개방 | 필요 | |

**1번은 지금 바로 시작할 수 있습니다.** 어느 길을 고르셔도 필요하고,
안전 규칙을 하나도 건드리지 않습니다.

**3번을 4번보다 앞에 둔 이유**는 "안전하니까"가 아닙니다.
환산 모델을 붙이기 전에 **기록 입력 → 계산 → 화면 표시 → 폴백**의
배선이 실제로 도는지 확인해야, 4번에서 문제가 생겼을 때
원인이 모델인지 배선인지 구분할 수 있기 때문입니다.

---

# 4부. 딸린 세부 결정들

큰 갈림길을 정하시면 아래는 비교적 간단합니다.
**다만 각각 실제 위험이 있어서 짚고 넘어갑니다.**

## 세부 1 — 목표기록(GOAL)을 어떻게 다룰 것인가

오너께서 "경기 목표를 대입"을 말씀하셨는데, 여기 **미묘한 함정**이 있습니다.

**목표기록은 아직 달성하지 못한 기록입니다.**
5000m 최고기록이 18분 30초인데 목표가 17분인 선수에게
17분 기준 페이스로 훈련시키면 → **매 훈련이 과부하**입니다.

코드에도 규칙이 박혀 있습니다 (`anchor.ts:42`):

```
GOAL → "GOAL_ANCHOR_FORBIDDEN"
```

이건 "목표를 쓰지 마라"가 아니라 **"목표를 현재 능력으로 취급하지 마라"**입니다.
카탈로그에도 같은 취지의 정지 조건이 있습니다:
`STOP_IF_GOAL_MISREPRESENTED_AS_CURRENT_CAPABILITY`

### 제안 — 둘을 나란히 보여준다

```
오늘 목표:      3분 42초   ← PB/SB/최근기록 기준 (실제 훈련 지시)
목표 달성 시:   3분 30초   ← 경기 목표 기준 (참고용, 회색 표시)
```

이러면 오너께서 원하시는 "목표 대입"도 되고, 과부하도 막습니다.
**선수는 목표를 눈으로 보면서 오늘은 현재 실력대로 뜁니다.**

> ❓ 여쭙는 것: 이 2단 표시가 맞습니까? 아니면 목표는 아예 안 보여줄까요?

## 세부 2 — 오래된 기록(STALE)

**현재 코드는 오래된 기록이면 계산을 거부합니다** (`anchor.ts:23`).
제가 이전 문서에 "계산하되 안내한다"고 쓴 건 **제안이지 현재 동작이 아니었습니다.**
오해하실 수 있게 써서 바로잡았습니다.

| 선택지 | 결과 |
|---|---|
| ㉮ 현행 유지 (거부) | 안전하지만, 기록이 오래된 선수는 페이스를 아예 못 봄 |
| ㉯ 계산 + 경고 문구 | "8개월 전 기록 기준입니다" 표시. 대부분의 선수가 혜택 |
| ㉰ 기간별 차등 | 3개월 이내=정상 / 3~12개월=경고 / 그 이상=거부 |

**권고: ㉰.** 다만 **"몇 개월"을 정하는 건 훈련 도메인 판단**이라
제가 정할 수 없습니다. 시즌 주기를 아시는 오너께서 정하셔야 합니다.

> ❓ 여쭙는 것: 기록이 며칠/몇 개월까지 "현재 실력"으로 볼 수 있습니까?

## 세부 3 — 범위 표기를 누가 확정하나

카탈로그에 `3~4×500m`, `r2~3′` 처럼 **범위**로 적힌 게
**30개 중 12개**입니다(실측). 기계는 범위를 못 씁니다. 하나로 정해야 합니다.

범위는 원래 **코치가 선수 상태를 보고 그날 정하라는 뜻**입니다.
문서가 일부러 열어 둔 여백이지 빠뜨린 게 아닙니다. 그래서 이걸 코드가
자동으로 메우면 문서의 의도를 코드가 덮어쓰는 셈이 됩니다.

| 선택지 | 문제 |
|---|---|
| 코드가 자동으로 낮은 값 선택 | **근거 없는 처방.** 왜 3회인지 설명 못 함 |
| 오너/코치가 미리 확정 | 느리지만 근거가 남음 |
| 화면에서 선수가 선택 | 초보 선수가 판단할 수 없음 |

**권고: 오너/코치 확정.** 자동 선택은 "코드가 훈련량을 정하는" 것이라
`PRODUCT_NORTH_STAR.md` §7(도메인 판단은 작업자가 정하지 않는다)에 어긋납니다.

## 세부 4 — 미성년 선수

**현재 카탈로그 30개 전부 `minorAllowed: false`입니다.**
활성화 요건에 이렇게 적혀 있습니다:

```yaml
minor_related_use:
  - accepted_minor_policy       # 승인된 미성년 정책
  - guardian_consent_when_policy_requires  # 보호자 동의
  - named_human_reviewer        # 지정된 검토자 실명
```

**셋 다 지금 없습니다.** 그래서 현재는 미성년에게 숫자 처방이 안 나갑니다.

성장기 선수에게 성인 기반 환산 모델(VDOT 등)을 적용하는 건
스포츠과학적으로 **논쟁적**입니다. 제가 판단할 영역이 아닙니다.

> ❓ 여쭙는 것: 당분간 성인만 열까요? 아니면 미성년 정책을 지금 만들까요?
> (안 정하시면 현행 유지 = 성인만, 이 상태로도 진행 가능합니다)

## 세부 5 — 종목 표기 정리 방식

`5K` → `5000m` 같은 표기를 어디에 쓸 것인가.

| 선택지 | 설명 | 문제 |
|---|---|---|
| ㉮ 파서를 넓힌다 | 코드가 `5K`, `2′30″`를 이해하게 함 | 범위(`3~4`)는 여전히 코드가 임의 선택하게 됨 |
| ㉯ 카탈로그에 기계용 필드 **추가** | 원본은 그대로 두고 `machineNotation` 추가 | 필드가 하나 늘어남 |

**권고: ㉯.** 이유는 오너 지침 때문입니다.

> "문서들은 정리하라는게 구분하고 실제 활용하기위한 작업을 하는 거지
> 지우거나 하라는 게 아니야"

㉯는 **원본 코치 표기를 지우지 않습니다.** 옆에 기계용을 덧붙일 뿐입니다.
그리고 `3~4`를 `3`으로 정한 판단이 **문서에 눈에 보이게 남습니다.**
㉮는 그 판단이 코드 속에 숨습니다.

---

# 5부. 승인하실 때 이렇게만 답해 주셔도 됩니다

전부 답하실 필요 없습니다. **1번만 정해 주셔도 착수할 수 있습니다.**

```
1. 큰 갈림길:      길 1 / 길 2 / 길 2인데 순서는 권고대로
2. 목표기록 표시:  2단 표시 O / X
3. 기록 유효기간:  (   )개월까지 현재 실력으로 인정
4. 범위 확정:      오너·코치가 정함 / 다른 방식
5. 미성년:         당분간 성인만 / 정책 만들자
6. 표기 정리:      ㉯ 기계용 필드 추가 / ㉮ 파서 확장
```

## 아무것도 안 정하셔도 지금 할 수 있는 일

**선수 기록 입력 화면(경기 목표·PB·SB·최근 결과 + 종목 거리 + 날짜)** 은
어느 길을 고르셔도 반드시 필요합니다. 안전 규칙도 안 건드립니다.

> **"이것부터 만들어"** 라고만 하셔도 진행됩니다.

---

# 6부. 제가 확신하지 못하는 것 (숨기지 않겠습니다)

정직하게 적습니다. 이걸 알고 결정하셔야 합니다.

| 항목 | 제 상태 |
|---|---|
| 카탈로그 30개가 파서를 통과 못 한다 | **확실함.** 실제로 실행해서 확인 |
| 계산식이 이미 있다 | **확실함.** `runtime.ts:177` |
| 앱이 처방 엔진을 안 쓴다 | **확실함.** import 전수 조사 |
| 선수 기록 입력 칸이 없다 | **확실함.** 화면 전수 조사 |
| 저장 구조에 페이스 자리가 없다 | **확실함.** `REST`/`RPE_TIME_RANGE` 둘뿐 |
| 리겔·VDOT가 이 선수층에 적합한가 | **모릅니다.** 스포츠과학 판단 |
| 기록 유효기간 몇 개월이 맞나 | **모릅니다.** 도메인 판단 |
| 미성년에게 환산 모델이 안전한가 | **모릅니다.** 논쟁 영역 |
| 범위(`3~4`)의 적정값 | **모릅니다.** 코치 판단 |

**아래 4개는 제가 추측으로 정하면 안 되는 것들입니다.**
근거 없이 정한 숫자는 근거 없는 훈련 처방이 되고, 그건 선수에게 위험합니다.
